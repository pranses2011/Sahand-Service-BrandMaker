# 🔄 راهنمای بروزرسانی — به نسخه ۲.۱۹.۰

> ⚠️ **قبل از هر چیز**: از فایل‌ها و دیتابیس خود بکاپ کامل بگیرید.

---

## 🆕 نسخه ۲.۱۹.۰ — استخراج: مکان‌یابی + بازیابی خودکار + پارس دو-قالبی UAPI

### 📋 چه چیزی تغییر می‌کند؟

| بخش | شرح |
|-----|-----|
| 🚨 خطای «استخراج کامل نشد — index.php یافت نشد» | **ریشه (تأیید مستندات رسمی cPanel)**: پارامتر `destfiles` فقط برای عملیات copy/move/rename تعریف شده — برای `extract` مقصدی وجود ندارد و استخراج کنار خودِ آرشیو انجام می‌شود؛ cPanel قدیمی آرشیو را داخل **پوشه هم‌نام آرشیو** استخراج می‌کند (`samsung-site-….zip` → `پوشه‌مقصد/samsung-site-…/`) و index.php در ریشه پیدا نمی‌شد |
| 🧭 بازیابی خودکار | `settleExtractedFiles` جدید: بعد از استخراج، index.php در سه مکان جستجو می‌شود (مستقیم در مقصد / زیرپوشه هم‌نام آرشیو / تک‌پوشه ریشه ZIP بکاپ)؛ اگر در زیرپوشه بود، **کل محتویات به بالا منتقل و پوشه حذف می‌شود** — در همه حالات خروجی قطعی |
| 🛡 پارس دو-قالبی پاسخ UAPI | مستندات فعلی (cpanel.openapi 11.138) پاسخ UAPI را تو-در-تو تعریف می‌کنند (`result.status/data`)؛ کد حالا هر دو قالب (تخت قدیمی + تو-در-تو جدید) را می‌پارسد — **با ارتقای سرورِ هاست، کل سیستم نمی‌شکند** |
| 🧬 ادغام هوشمند تداخل‌ها | پوشه‌های هم‌نام (مثل css/ که مرحله ساخت پوشه‌ها از قبل ساخته) بازگشتی **ادغام** می‌شوند نه تودرتو (رفع `css/css/` احتمالی) |
| 🔍 دیاگنوستیک کامل | اگر خطایی بماند، پیام شکست حالا **«محتوای واقعی مسیر سایت»** (تا ۱۵ مدخل) + مرحله شکست را نشان می‌دهد — عیب‌یابی با یک نگاه |
| 🔄 ترتیب ایمن‌تر | راستی‌آمایی قبل از حذف ZIP انجام می‌شود (قبلاً اول حذف می‌شد) — اگر مشکلی باشد، تلاش مجدد ممکن است |

**بدون تغییر دیتابیس** — فقط فایل‌های PHP هسته استقرار.

### 🚀 روش بروزرسانی (پکیج update.zip)

1. **بکاپ بگیرید** (فایل‌ها + دیتابیس)
2. پکیج `update.zip` نسخه ۲.۱۹.۰ را دانلود کنید
3. محتویات آن را در **روت سایت ساز** استخراج کنید (بازنویسی فایل‌ها)
4. مرورگر را با `Ctrl+F5` رفرش کنید
5. از «تنظیمات» صفحه راستی‌آمایی سیستم، نسخه **2.19.0** را تأیید کنید

### 🧪 تست حیاتی بعد از بروزرسانی

**استقرار خودکار را دوباره اجرا کنید** — این بار نباید هیچ‌کدام از این خطاها را ببینید:

- ❌ «ساخت پوشه‌های حیاتی ناموفق — function mkdir not found» (رفع در ۲.۱۶)
- ❌ «استخراج ZIP ناموفق — function fileop not found» (رفع در ۲.۱۸)
- ❌ «استخراج کامل نشد — index.php در مسیر سایت یافت نشد» (رفع در ۲.۱۹ — حتی اگر سرورتان فایل‌ها را داخل پوشه هم‌نام آرشیو استخراج کند، خودکار به ریشه منتقل می‌شوند)

✅ انتظار: مراحل ۱۰گانه تا «تست نهایی» پیش برود و پیام «استقرار کامل شد — https://دامنه آنلاین است 🎉» را ببینید.

اگر باز هم خطایی ظاهر شد، **متن کامل آن را ارسال کنید** — پیام‌های خطای این نسخه «محتوای واقعی مسیر سایت» را هم نشان می‌دهند که عیب‌یابی را قطعی می‌کند.

### 📦 فایل‌های تغییر‌یافته در این نسخه

```
config.php              ← نسخه سیستم 2.19.0
core/CpanelAPI.php      ← call دو-قالبی + extractZip بازنویسی + settleExtractedFiles/mergeMoveUp/homeRelative
core/Deployer.php       ← مرحله استخراج با دیاگنوستیک کامل
core/BackupManager.php  ← بازیابی بکاپ با مسطح‌سازی خودکار
```

### 🆕 نصب از صفر

پکیج کامل `install.zip` را در روت هاست استخراج کنید و `install.php` را باز کنید — راهنمای کامل در `docs/installation.html`.

---

## 📜 سابقه نسخه‌های اخیر

- **۲.۱۸.۰** — ممیزی کامل cPanel: رفع ۸ خانواده باگ (fileop + پارامترها + chmod + SSL)
- **۲.۱۷.۰** — خطایاب ریشه‌ای (باگ تاریخی httpGet + پل jina + کدهای تلویزیون LG) + قالب‌ساز ۱۳۰ عنصر
- **۲.۱۶.۰** — رفع خطای mkdir استقرار (API2 رسمی + سه‌لایه)
- **۲.۱۵.۰** — رفع نهایی استقرار + خطایاب ۱۱ ارائه‌دهنده + ۷ سرویس تصویر + قالب‌ساز ۱۱۴ عنصر
- **۲.۱۴.۰** — کرون cPanel + سیستم بکاپ + خطایاب وب‌محور + منو آیکون‌دار
