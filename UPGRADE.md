# 🔄 راهنمای بروزرسانی — به نسخه ۲.۱۸.۰

> ⚠️ **قبل از هر چیز**: از فایل‌ها و دیتابیس خود بکاپ کامل بگیرید.

---

## 🆕 نسخه ۲.۱۸.۰ — استقرار خودکار: ممیزی کامل cPanel (رفع ۸ خانواده باگ)

### 📋 چه چیزی تغییر می‌کند؟

| بخش | شرح |
|-----|-----|
| 🚨 خطای استقرار «function fileop not found» | ریشه همان الگوی mkdir نسخه ۲.۱۶: تابع `Fileman::fileop` فقط در **API2** وجود دارد (طبق مستندات رسمی cPanel) اما کد قبلی آن را از UAPI صدا می‌زد. استخراج ZIP حالا با امضای رسمی API2 انجام می‌شود |
| 🛡 باگ‌های پنهان بعدی | ۷ باگ دیگر که به‌ترتیب بعد از استخراج می‌شکستند رفع شد: نوشتن config.php/.htaccess (پارامترها)، خواندن config در بروزرسانی، تنظیم مجوزها (chmod)، لیست پوشه‌ها، SSL (۳ تابع ناموجود) |
| 📦 استخراج قطعی | ZIP خارج از پوشه مقصد ابتدا کپی داخل مقصد می‌شود + راستی‌آمایی index.php پس از استخراج |
| 🗑 حذف فایل | سه‌لایه: UAPI delete_file → fileop unlink → سطل بازیافت |
| 💾 بکاپ | فشرده‌سازی با API2 رسمی + راستی‌آمایی وجود فایل ZIP پس از ساخت |

**بدون تغییر دیتابیس** — فقط فایل‌های PHP هسته استقرار.

### 🚀 روش بروزرسانی (پکیج update.zip)

1. **بکاپ بگیرید** (فایل‌ها + دیتابیس)
2. پکیج `update.zip` نسخه ۲.۱۸.۰ را دانلود کنید
3. محتویات آن را در **روت سایت ساز** استخراج کنید (بازنویسی فایل‌ها)
4. مرورگر را با `Ctrl+F5` رفرش کنید
5. از «تنظیمات» صفحه راستی‌آمایی سیستم، نسخه **2.18.0** را تأیید کنید

### 🧪 تست حیاتی بعد از بروزرسانی

**استقرار خودکار را دوباره اجرا کنید** — این بار نباید هیچ‌کدام از این خطاها را ببینید:

- ❌ «ساخت پوشه‌های حیاتی ناموفق — function mkdir not found» (رفع در ۲.۱۶)
- ❌ «استخراج ZIP ناموفق — function fileop not found» (رفع در ۲.۱۸)
- ❌ «نوشتن config.php ناموفق» (رفع پنهان در ۲.۱۸)

✅ انتظار: مراحل ۱۰گانه تا «تست نهایی» پیش برود و پیام «استقرار کامل شد — https://دامنه آنلاین است 🎉» را ببینید.

اگر خطای جدیدی ظاهر شد، **متن دقیق آن را از لاگ استقرار** (صفحه استقرار خودکار » جزئیات عملیات) ارسال کنید — پیام‌های خطای این نسخه دقیقاً می‌گویند کدام تابع/لایه شکست خورده.

### 📦 فایل‌های تغییر‌یافته در این نسخه

```
config.php              ← نسخه سیستم 2.18.0
core/CpanelAPI.php      ← بازنویسی بخش Fileman + SSL
core/Deployer.php       ← مرحله استخراج + راستی‌آمایی
core/BackupManager.php  ← فشرده‌سازی/بازیابی بکاپ
core/SSLManager.php     ← بررسی وضعیت SSL
```

### 🆕 نصب از صفر

پکیج کامل `install.zip` را در روت هاست استخراج کنید و `install.php` را باز کنید — راهنمای کامل در `docs/installation.html`.

---

## 📜 سابقه نسخه‌های اخیر

- **۲.۱۷.۰** — خطایاب ریشه‌ای (باگ تاریخی httpGet + پل jina + کدهای تلویزیون LG) + قالب‌ساز ۱۳۰ عنصر
- **۲.۱۶.۰** — رفع خطای mkdir استقرار (API2 رسمی + سه‌لایه)
- **۲.۱۵.۰** — رفع نهایی استقرار + خطایاب ۱۱ ارائه‌دهنده + ۷ سرویس تصویر + قالب‌ساز ۱۱۴ عنصر
