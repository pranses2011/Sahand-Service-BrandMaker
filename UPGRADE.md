# 🔄 راهنمای بروزرسانی — به نسخه ۲.۱.۰

> این راهنما نحوه ارتقای سایت ساز برند سهند سرویس به نسخه **۲.۱.۰ (موتور ۳.۱ — 🌐 جستجوی آنلاین وب + 🤖 ربات تلگرام)** را توضیح می‌دهد.
>
> - ارتقا از **۱.x** → دو مرحله‌ای: اول [راهنمای ارتقا به ۲.۰.۰](#--از-نسخه-1x-ابتدا-به-20) پایین همین فایل، سپس مراحل همین بخش
> - ارتقا از **۲.۰.۰** → فقط مراحل همین بخش

---

## ⚠️ قبل از شروع (الزامی)

| # | اقدام | توضیح |
|:-:|-------|-------|
| ۱ | 🗄️ **بکاپ کامل دیتابیس** | از phpMyAdmin → Export → SQL (کامل، با همه جدول‌ها) |
| ۲ | 📁 **بکاپ فایل‌ها** | کل پوشه سایت ساز را ZIP کنید (مخصوصاً `config.local.php` و `uploads/`) |
| ۳ | ✅ **ثبت نسخه فعلی** | پایین پنل مدیریت یا `config.php` → باید `1.0.0` یا `1.1.0` باشد |

> 💡 زمان موردنیاز: حدود ۵ دقیقه | ⏸️ قطعی سرویس: لازم نیست (بدون تغییر دیتابیس)

---

## 🎯 تغییرات مهم این نسخه

- 🌐 **جستجوی آنلاین وب**: موتور AI در صورت لزوم از اینترنت داده زنده می‌گیرد (محتوا/سئو/مقاله) — ۶ ارائه‌دهنده با جایگزینی خودکار، بدون نیاز به کلید
- 🤖 **ربات تلگرام متصل به دستیار فارسی**: گفتگوی کامل با موتور در تلگرام + پنل مدیریت + اعلان درخواست‌های جدید
- 🐛 **رفع باگ بحرانی نصب‌کننده** (خطای `Table 'users' doesn't exist` — ۳۱ جدول از ۳۳ ساخته نمی‌شد)
- 🔌 ۳ اندپوینت API جدید: `web-search`، `research`، `websearch-status` (مجموع ۳۷)
- 🧠 پارامتر `research` در `smart-generate` + ۲ فرمان جدید دستیار («جستجوی وب:» و «تحقیق درباره:»)

**💡 بدون تغییر ساختار جداول** — فقط ۲ ردیف تنظیمات جدید (websearch_settings و telegram_bot_settings) به جدول settings اضافه می‌شود که فایل بروزرسانی به‌صورت خودکار از طریق database.sql (INSERT IGNORE) انجام می‌دهد — برای نصب‌های موجود می‌توانید این دو ردیف را از phpMyAdmin هم اضافه کنید (اختیاری — بدون آن‌ها مقادیر پیش‌فرض استفاده می‌شود).

---

## 📦 روش ۱: بسته بروزرسانی (توصیه‌شده)

بسته `Sahand-Service-BrandMaker-v2.1.0-update.zip` از [صفحه رلیز](https://github.com/pranses2011/Sahand-Service-BrandMaker/releases) دانلود کنید. این بسته **فقط فایل‌های تغییرکرده** را شامل می‌شود.

### مراحل

```text
۱. محتویات ZIP بروزرسانی را استخراج کنید
۲. کل فایل‌ها را با FTP/File Manager روی هاست آپلود و جایگزین کنید
   (مسیرها داخل بسته از قبل درست است — فقط همه را روی روت سایت ساز کپی کنید)
۳. مطمئن شوید پوشه cache/engine قابل نوشتن است (مجوز 755 کافی است)
۴. پنل مدیریت را رفرش کنید — تمام! ✅
```

### فایل‌های جایگزین‌شده در بسته بروزرسانی

```
config.php                    ← نسخه 2.0.0
api/index.php                 ← ۷ اندپوینت جدید
engine/SahandAI.php           ← موتور v3.0.0
engine/CommandAssistant.php   ← 🆕 دستیار فرمان فارسی
engine/analyzers/BrandVoiceAnalyzer.php  ← 🆕
engine/generators/SmartPipeline.php      ← 🆕
engine/generators/ContentImprover.php    ← 🆕
engine/generators/TitleGenerator.php     ← 🆕
engine/utils/EngineCache.php             ← 🆕
admin/docs.php                ← ثبت راهنمای API جدید
docs/AI-API-GUIDE.md          ← 🆕 راهنمای کامل API خارجی
assets/fonts/README.md        ← لینک دانلود فونت‌ها
assets/icons/*/README.md      ← لینک دانلود آیکون‌ها (۱۰ پک)
README.md / CHANGELOG.md / CHECKLIST.md / WORKLOG.md / .gitignore
```

> ⚠️ توجه: `config.local.php` جایگزین **نمی‌شود** (تنظیمات شما دست‌نخورده می‌ماند) — این فایل در بسته نیست.

---

## 📦 روش ۲: بسته کامل نصب (اگر تغییرات دستی زیاد دارید)

اگر فایل‌های هسته را دستی تغییر داده‌اید، بسته کامل را نصب کنید و `config.local.php` + `uploads/` + فایل‌های سفارشی خود را از بکاپ برگردانید.

---

## ✅ راستی‌آزمایی پس از بروزرسانی

| # | بررسی | انتظار |
|:-:|-------|--------|
| ۱ | پایین پنل مدیریت | نسخه **2.1.0** |
| ۲ | `GET /api/ai/engine-info` | `engine_version: "3.1.0"` + قابلیت‌های `web_search` و `telegram_bot` |
| ۳ | پنل → مستندات | کارت «راهنمای کامل API هوش مصنوعی نسخه ۳.۱» |
| ۴ | تست دستی API جدید | `POST /api/ai/assistant` با فرمان «راهنما» |
| ۵ | 🌐 جستجوی وب | `POST /api/ai/web-search` با query دلخواه — نتایج واقعی اینترنت |
| ۶ | 🤖 پنل → ربات تلگرام | صفحه مدیریت ربات باز شود (حتی بدون توکن) |

### تست سریع با curl (جایگزین دامنه و کلید)

```bash
# سلامت موتور
curl "https://YOUR-DOMAIN/api/ai/engine-info"

# وضعیت جستجوی وب (عمومی — بدون کلید)
curl "https://YOUR-DOMAIN/api/ai/websearch-status"

# 🌐 جستجوی آنلاین (جدید)
curl -X POST "https://YOUR-DOMAIN/api/ai/web-search" \
  -H "Content-Type: application/json" -H "X-API-Key: YOUR_KEY" \
  -d '{"query": "تعمیر ماشین لباسشویی", "limit": 3}'

# دستیار فارسی
curl -X POST "https://YOUR-DOMAIN/api/ai/assistant" \
  -H "Content-Type: application/json" -H "X-API-Key: YOUR_KEY" \
  -d '{"command": "راهنما"}'

# خط تولید هوشمند + تحقیق وب ترکیبی
curl -X POST "https://YOUR-DOMAIN/api/ai/smart-generate" \
  -H "Content-Type: application/json" -H "X-API-Key: YOUR_KEY" \
  -d '{"brand_id": 1, "device_key": "washing_machine", "variants": 2, "research": true}'
```

### 🤖 راه‌اندازی ربات تلگرام پس از ارتقا (اختیاری — ۵ دقیقه)

۱. پنل → **🤖 ربات تلگرام** → توکن از [@BotFather](https://t.me/BotFather) را وارد و ذخیره کنید
۲. دکمه «🔌 تست اتصال» → سپس در تلگرام `/start` و `/id` به ربات بفرستید
۳. شناسه را در «شناسه‌های مجاز» پنل ثبت کنید → دکمه «🪝 راه‌اندازی وب‌هوک»

جزئیات کامل: بخش ۱۰ راهنمای API (`docs/AI-API-GUIDE.md`)

---

## 🆘 رفع اشکال

| مشکل | راه حل |
|------|--------|
| خطای نوشتن در `cache/engine` | مجوز پوشه `cache` را 755 (یا 775) کنید؛ پوشه `cache/engine` به‌صورت خودکار ساخته می‌شود |
| پنل هنوز نسخه قدیمی نشان می‌دهد | کش مرورگر (Ctrl+F5)؛ مطمئن شوید `config.php` جدید آپلود شده |
| خطای 404 در اندپوینت‌های جدید | `.htaccess` قدیم مانده — فایل `.htaccess` روت را از بسته کامل جایگزین کنید |
| کلید API کار نمی‌کند | کلیدهای API در دیتابیس هستند و بدون تغییر می‌مانند — اگر مشکل ادامه داشت، از پنل → تنظیمات → API کلید را بازبینی کنید |
| جستجوی وب خطا می‌دهد | مطمئن شوید cURL فعال است؛ در صورت تحریم ارائه‌دهنده، زنجیره جایگزین خودکار فعال می‌شود؛ در نهایت کلید SerpApi را در تنظیمات `websearch_settings` وارد کنید |
| وب‌هوک تلگرام set نمی‌شود | آدرس باید HTTPS باشد؛ برای تست محلی از `php telegram/poll.php` استفاده کنید |
| ربات پاسخ نمی‌دهد | شناسه چت را با `/id` بگیرید و در پنل مجاز کنید (لیست خالی = بسته) |

---

## 📚 ادامه مطالعه

- 🤖 [راهنمای کامل API هوش مصنوعی](docs/AI-API-GUIDE.md) — استفاده از موتور AI از بیرون سایت ساز
- 🔤 [دانلود فونت‌ها](assets/fonts/README.md) — ۳۰ فونت با لینک رسمی
- 🖼️ [دانلود پک‌های آیکون](assets/icons) — ۱۰ پک با لینک رسمی
- 📋 [سابقه تغییرات](CHANGELOG.md) — جزئیات کامل نسخه ۲.۰.۰
