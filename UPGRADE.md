# 🔄 راهنمای بروزرسانی — به نسخه ۲.۱۹.۱

> ⚠️ **قبل از هر چیز**: از فایل‌ها و دیتابیس خود بکاپ کامل بگیرید.

---

## 🆕 نسخه ۲.۱۹.۱ — راستی‌آمایی استقرار: پارس چندقالبی لیست + استعلام مستقیم + آپلود overwrite

### 📋 چه چیزی تغییر می‌کند؟

| بخش | شرح |
|-----|-----|
| 🚨 خطای «استخراج کامل نشد + محتوای مسیر سایت: خالی» | **ریشه**: `listFiles` فقط قالب جدید پاسخ UAPI (`data.files/dirs`) را می‌شناخت — سرورهای قدیمی cPanel قالب‌های دیگری (آرایه تخت مدخل‌ها / API2 listfiles) برمی‌گردانند؛ پاسخ ناشناخته **بی‌صدا** به «پوشه خالی» ترجمه می‌شد → راستی‌آمایی index.php شکست کاذب می‌خورد حتی وقتی استخراج کامل بود |
| 🧭 لیست چندقالبی با fallback سه‌لایه | UAPI list_files (مطلق → نسبی) → API2 listfiles (مطلق → نسبی) — اولین پاسخ «قالب‌شناسی‌شده» برنده است؛ «پوشه خالیِ قطعی» از «قالب ناشناخته» تفکیک می‌شود |
| 🎯 استعلام مستقیم وجود فایل | `entryExists` دو-لایه: استعلام مستقیم `get_file_information` (مستقل از لیست پوشه) + fallback لیست — حتی با لیست خراب، وجود index.php قطعی تشخیص داده می‌شود |
| 📤 آپلود overwrite | مستندات رسمی: پیش‌فرض cPanel فایل تکراری را با «already exists» رد می‌کند! چون ۲.۱۹ حذف ZIP را فقط بعد از استقرار موفق انجام می‌دهد، ZIP قدیمی از تلاش ناموفق قبلی آپلود جدید را بی‌صدا می‌شکست → حالا `overwrite=1` + راستی‌آزمایی succeeded/failed |
| 🔍 دیاگنوستیک سه‌موتوره | خطای استقرار حالا: محتوای مسیر + **وجود ZIP روی سرور** (تفکیک آپلود دروغگو از استخراج ناموفق) + وضعیت لیست + **نمونه پاسخ خام** — اگر خطایی بماند، گزارش بعدی قطعاً ریشه را نشان می‌دهد |
| ⚡ کش لیست هوشمند | خواندن‌های مکرر لیست در یک استقرار دیگر N بار API نمی‌زنند؛ هر عملیات فایل‌سیستم کش را باطل می‌کند |

**بدون تغییر دیتابیس** — فقط فایل‌های PHP هسته استقرار.

### 🚀 روش بروزرسانی (پکیج update.zip)

1. **بکاپ بگیرید** (فایل‌ها + دیتابیس)
2. پکیج `update.zip` نسخه ۲.۱۹.۱ را دانلود کنید
3. محتویات آن را در **روت سایت ساز** استخراج کنید (بازنویسی فایل‌ها)
4. مرورگر را با `Ctrl+F5` رفرش کنید
5. از «تنظیمات» صفحه راستی‌آمایی سیستم، نسخه **2.19.1** را تأیید کنید

### 🧪 تست حیاتی بعد از بروزرسانی

**استقرار خودکار را دوباره اجرا کنید** — این بار نباید هیچ‌کدام از این خطاها را ببینید:

- ❌ «ساخت پوشه‌های حیاتی ناموفق — function mkdir not found» (رفع در ۲.۱۶)
- ❌ «استخراج ZIP ناموفق — function fileop not found» (رفع در ۲.۱۸)
- ❌ «استخراج کامل نشد — index.php یافت نشد» (رفع در ۲.۱۹ — بازیابی از پوشه هم‌نام آرشیو)
- ❌ «محتوای مسیر سایت: خالی» (رفع در ۲.۱۹.۱ — پارس چندقالبی + استعلام مستقیم؛ حتی اگر لیست پوشه سرورتان قالب قدیمی برگرداند، راستی‌آمایی موفق می‌شود)

✅ انتظار: مراحل ۱۰گانه تا «تست نهایی» پیش برود و پیام «استقرار کامل شد — https://دامنه آنلاین است 🎉» را ببینید.

اگر باز هم خطایی ظاهر شد، **متن کامل آن را ارسال کنید** — پیام‌های این نسخه «وجود ZIP روی سرور» + «وضعیت لیست» + «نمونه پاسخ خام سرور» را هم نشان می‌دهند که عیب‌یابی را قطعی می‌کند.

### 📦 فایل‌های تغییر‌یافته در این نسخه

```
config.php              ← نسخه سیستم 2.19.1
core/CpanelAPI.php      ← listFiles چندقالبی + entryExists دو-لایه + uploadFile با overwrite/راستی‌آزمایی + کش با ابطال جهش
core/Deployer.php       ← مرحله استخراج با دیاگنوستیک سه‌موتوره
```

### 🆕 نصب از صفر

پکیج کامل `install.zip` را در روت هاست استخراج کنید و `install.php` را باز کنید — راهنمای کامل در `docs/installation.html`.

---

## 📜 سابقه نسخه‌های اخیر

- **۲.۱۹.۰** — استخراج: مکان‌یابی + بازیابی خودکار (پوشه هم‌نام آرشیو) + پارس دو-قالبی UAPI
- **۲.۱۸.۰** — ممیزی کامل cPanel: رفع ۸ خانواده باگ (fileop + پارامترها + chmod + SSL)
- **۲.۱۷.۰** — خطایاب ریشه‌ای (باگ تاریخی httpGet + پل jina + کدهای تلویزیون LG) + قالب‌ساز ۱۳۰ عنصر
- **۲.۱۶.۰** — رفع خطای mkdir استقرار (API2 رسمی + سه‌لایه)
- **۲.۱۵.۰** — رفع نهایی استقرار + خطایاب ۱۱ ارائه‌دهنده + ۷ سرویس تصویر + قالب‌ساز ۱۱۴ عنصر
- **۲.۱۴.۰** — کرون cPanel + سیستم بکاپ + خطایاب وب‌محور + منو آیکون‌دار
