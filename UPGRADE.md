# 🔄 راهنمای بروزرسانی — به نسخه ۲.۵.۰

> ⬆️ بالاترین بخش این فایل، راهنمای **نسخه ۲.۶.۰** است (خطایاب AI + یادگیری AI + تصاویر AI + ربات بله). بخش‌های پایین‌تر راهنمای ارتقاهای قبلی (۲.۵.۰، ۲.۴.۱، …) هستند.

---

## 🚀 نسخه ۲.۶.۰ — خطایاب AI + یادگیری AI + تصاویر AI + ربات بله

> **این نسخه برای شماست اگر:** می‌خواهید کدهای خطای واقعی هر برند/دستگاه با ۱۴ فیلد کامل تولید شود، ربات بله داشته باشید، تلگرام از واسط گوگل کار کند، تصاویر OG و مقاله خودکار با AI ساخته شوند، یا باگ‌های کپچا/تنظیمات/آیکون/دستگاه تکراری را رفع کنید.

### 🔍 چه چیزی تغییر کرد؟ (۱۸ اولویت اصلی)

| # | تغییر | نتیجه |
|---|-------|-------|
| ۱ | 🚨 **موتور خطایاب AI**: برند → دستگاه → تولید همه کدهای واقعی (۱۵۰ کد ۹ برند + جستجوی آنلاین) با ۱۴ فیلد + ویرایش + بهینه‌سازی تک‌فیلد AI | صفحه کدهای خطا کامل و واقعی |
| ۲ | 🧠 **موتور خودیادگیر** + پنل «یادگیری AI» | AI از بهبودهای موفق درس می‌گیرد؛ ارتقای اشتباه قابل غیرفعال‌سازی تک‌به‌تک |
| ۳ | 🖼️ **تصاویر AI یکتا**: OG هر مقاله + ۲ تصویر درون‌متن (PNG فارسی/SVG) + قابل تعویض | هر مقاله تصاویر اختصاصی مرتبط با موضوع خودش |
| ۴ | 💬 **ربات بله** (پنل + وب‌هوک + poll) | دستیار فارسی روی بله |
| ۵ | 🌉 **واسط Google Apps Script تلگرام** (relay دوطرفه + اسکریپت آماده + تست) | تلگرام در زمان تحریم از سرورهای گوگل |
| ۶ | 💬 **SahandDialog** — کادرهای تعاملی زیبا | جایگزین همه alert/confirm پیش‌فرض |
| ۷ | 🎯 دکمه «بهبود» کنار هر سنجه سئو | اصلاح انتخابی فقط همان سنجه |
| ۸ | 🚀 بهبود خودکار صفحه/کل برند تا ۱۰۰ | یک کلیک تا امتیاز کامل |
| ۹ | 🐛 کپچای لب‌تاپ (ادبلاکر) | اندپوینت خنثی + fallback — همیشه نمایش |
| ۱۰ | 🐛 سایدبار همیشه راست + تب‌های مقاوم تنظیمات | UI درست در همه دستگاه‌ها |
| ۱۱ | 🐛 دستگاه تکراری + «بهینه است با ۱ مورد» + نمایش آیکون‌ها | رفع کامل |
| ۱۲ | 🗑️ لغو نصب فونت/پک آیکون + 👁️ پیش‌نمایش فونت | مدیریت کامل دارایی‌ها |
| ۱۳ | 📅 سال شروع فعالیت نمایندگی | برای کارت‌های «... سال سابقه» |

### 📦 مراحل (۵ دقیقه)

```text
۱. Sahand-Service-BrandMaker-v2.6.0-update.zip را دانلود و استخراج کنید
۲. همه فایل‌ها را روی روت سایت‌ساز آپلود و جایگزین کنید
۳. پوشه bale/ جدید را کامل آپلود کنید
۴. یک‌بار پنل را باز کنید — مهاجرت خودکار دیتابیس (ستون‌های کد خطا + og_image) اجرا می‌شود
۵. Ctrl+F5 — تمام! ✅
```

> ⚠️ `config.local.php` و `uploads/` دست‌نخورده می‌مانند. بدون اجرای SQL دستی.

### ✅ بعد از آپدیت چک کنید

1. **کدهای خطا**: برند انتخاب کنید → فهرست دستگاه‌ها فیلتر می‌شود → «تولید همه کدها» → ویرایش یک کد → دکمه «✨ AI» کنار فیلدها
2. **یادگیری AI**: منوی جدید در سایدبار — پس از چند بهبود سئو، درس‌ها ثبت می‌شوند
3. **مقاله**: ویرایش یک مقاله → دکمه «🎨 تولید OG با AI» + دکمه‌های «🎯 بهبود» کنار سنجه‌ها
4. **ربات بله**: منوی جدید — توکن از @BotFather بله
5. **واسط گوگل**: ربات تلگرام ← کارت واسط → کپی اسکریپت → Deploy → تست
6. **تنظیمات**: تب‌ها — باز شدن صحیح و تمام‌عرض

 (کپچای تضمینی + اسکیل UI/UX Pro + بهینه PHP 8.3). بخش‌های پایین‌تر راهنمای ارتقاهای قبلی (۲.۴.۱، ۲.۴.۰، …) هستند.

---

## 🎨 نسخه ۲.۵.۰ — کپچای تضمینی + اسکیل UI/UX Pro + بهینه PHP 8.3

> **این نسخه برای شماست اگر:**
> - کد امنیتی صفحه ورود نمایش داده نمی‌شود (مخصوصاً بعد از تغییر نسخه PHP هاست)
> - می‌خواهید صفحات سایت‌ها با طراحی حرفه‌ای UI/UX ساخته شوند
> - PHP هاست را به ۸.۳ ارتقا داده‌اید و بهترین عملکرد را می‌خواهید

### 🔍 چه چیزی تغییر کرد؟

| # | تغییر | نتیجه |
|---|-------|-------|
| ۱ | کپچا با **رندر ۳ لایه‌ای تضمینی** بازنویسی شد (GD+فونت TTF → GD بزرگ‌نمایی نرم → SVG بدون وابستگی) | کد امنیتی **همیشه** نمایش داده می‌شود — حتی اگر GD خاموش باشد + حروف ~۲.۵ برابر درشت‌تر (بوم ۲۲۰×۷۲) |
| ۲ | **اسکیل UI/UX Pro** به سایت‌ساز اضافه شد: ۹ قانون UX + قواعد RTL فارسی + WCAG + بلوپرینت ۱۷ نوع صفحه | مرحله ۳ ساخت برند حالا چیدمان حرفه‌ای هر صفحه را تولید می‌کند + دکمه‌های طراحی/ممیزی/اصلاح خودکار در قالب‌ساز |
| ۳ | بهینه PHP 8.3: فایل `.user.ini` (OPcache + حافظه ۲۵۶M) + `json_validate` بومی + فشرده‌سازی zlib | سرعت ۲ تا ۵ برابر با OPcache + کارت «وضعیت سیستم» در داشبورد برای پایش |

### 📦 مراحل (۳ دقیقه)

```text
۱. فایل‌های نسخه جدید را از مخزن (یا بسته آپدیت رلیز) دانلود کنید
۲. همه فایل‌های تغییرکرده را روی روت سایت‌ساز آپلود و جایگزین کنید (فهرست پایین)
۳. ⚠️ فایل جدید .user.ini حتماً در روت آپلود شود (بهینه‌سازی PHP 8.3)
۴. پنل را یک‌بار Ctrl+F5 کنید — تمام! ✅
```

> 💡 بدون هیچ تغییر دیتابیسی — کاملاً file-level. `config.local.php` دست‌نخورده می‌ماند.

### 📁 فایل‌های تغییرکرده در نسخه ۲.۵.۰ (۱۳ فایل کد + ۶ فایل مستندات)

```
.user.ini                         ← 🆕 تنظیمات بهینه PHP 8.3 (OPcache/حافظه/زمان)
config.php                        ← نسخه 2.5.0 + json_validate + error_reporting هوشمند + zlib + autoloader اسکیل‌ها
core/Auth.php                     ← کپچای ۳ لایه‌ای تضمینی + حروف درشت
admin/login.php                   ← نمایش بهتر کپچا + دکمه رفرش مستقل
admin/index.php                   ← کارت «وضعیت سیستم و عملکرد» + ردیف اسکیل UI/UX Pro
assets/css/admin.css              ← استایل کپچای بزرگ‌تر + دکمه رفرش
engine/knowledge/uiux-patterns.json ← 🆕 پایگاه دانش اسکیل UI/UX Pro
engine/skills/UIUXPro.php         ← 🆕 کلاس اسکیل UI/UX Pro
engine/SahandAI.php               ← موتور ۳.۵.۰ + ۴ متد UI/UX
api/index.php                     ← ۴ اندپوینت جدید UI/UX
admin/brand-build.ajax.php        ← مرحله ۳: طراحی چیدمان صفحات با اسکیل
admin/brand-build.php             ← عنوان مرحله ۳ به‌روز
admin/template-builder.php        ← دکمه‌های طراحی/بررسی/اصلاح UX + پنل نتایج + requireLogin هندلرها
```

```
README.md / CHANGELOG.md / CHECKLIST.md / WORKLOG.md / UPGRADE.md / docs/AI-API-GUIDE.md
                                  ← 📖 مستندات: راهنمای API موتور ۳.۵ (۴۸ اندپوینت) + تاریخچه نسخه‌ها
                                  (آپلود این ۶ فایل اختیاری است — روی عملکرد اثری ندارند)
```

### ✅ بعد از آپدیت چک کنید

1. **صفحه ورود**: کد امنیتی باید با حروف درشت نمایش داده شود (اگر هنوز دیده نمی‌شود، یعنی کش مرورگر است — Ctrl+F5؛ رندر SVG حتی بدون GD کار می‌کند)
2. **داشبورد ← کارت «وضعیت سیستم و عملکرد»**: وضعیت OPcache را ببینید — اگر قرمز بود از cPanel (Select PHP Version ← Options ← opcache) فعالش کنید
3. **قالب‌ساز**: دکمه «✨ طراحی با UI/UX Pro» را بزنید و چیدمان پیشنهادی + امتیاز UX را ببینید
4. **ساخت برند جدید**: مرحله ۳ حالا «تولید محتوا + طراحی UI/UX Pro» است و چیدمان صفحات را تولید می‌کند

> 📌 سایت‌های برندِ قبلاً ساخته‌شده دست‌نخورده می‌مانند؛ برای بهره‌گیری از چیدمان حرفه‌ای، در قالب‌ساز دکمه «✨ طراحی با UI/UX Pro» را بزنید یا برند را بازتولید کنید.

---

## 🚑 هات‌فیکس ۲.۴.۱ — رفع باگ‌های حیاتی (الزامی برای همه کاربران ۲.۴.۰)

> **اگر بعد از بروزرسانی به ۲.۴.۰ این مشکلات را دارید، این هات‌فیکس برای شماست:**
> - داشبورد خالی است و منو (☰) باز نمی‌شود
> - دکمه «🎯 پیشنهاد بهترین عنوان سئو» خطای «Unexpected end of JSON input» می‌دهد
> - دکمه «🎯 بهبود سئو» خطای «Unexpected token '<'» می‌دهد

### 🔍 علت (خلاصه فنی)
1. **متد تکراری** `researchTopic()` در موتور AI باعث خطای مرگبار بارگذاری کلاس می‌شد → داشبورد خالی + منوی بی‌جان (از نسخه ۲.۳.۰)
2. تابع `trim()` روی متن فارسی **UTF-8 را خراب** می‌کرد → پاسخ خالی سرور در پیشنهاد عنوان
3. تابع `mb_strtok()` اصلاً در PHP وجود ندارد → خطای سرور در بهبود سئو

### 📦 مراحل (۲ دقیقه)

```text
۱. بسته Sahand-Service-BrandMaker-v2.4.1-update.zip را از [صفحه رلیز](https://github.com/pranses2011/Sahand-Service-BrandMaker/releases) دانلود کنید
۲. محتویات را استخراج و همه فایل‌ها را روی روت سایت ساز آپلود و جایگزین کنید
۳. پنل را یک‌بار Ctrl+F5 کنید — تمام! ✅
```

> 💡 بدون هیچ تغییر دیتابیسی — کاملاً file-level. `config.local.php` دست‌نخورده می‌ماند.

### 📁 فایل‌های جایگزین‌شده در بسته v2.4.1 (۱۴ فایل)

```
config.php                        ← ۲.۴.۱ + پلی‌فیل PHP ۷.۴ + هندلر AJAX-آگاه
engine/SahandAI.php               ← موتور ۳.۴.۱ — رفع متد تکراری researchTopic
engine/utils/PersianGrammar.php   ← رفع mb_strtok + نیم‌فاصله‌های واقعاً کارا + نقطه دوباره
engine/generators/TitleGenerator.php ← mb_trim — رفع پاسخ خالی پیشنهاد عنوان
engine/generators/SeoImprover.php    ← mb_trim + کلیدواژه‌های تمیز + Throwable
engine/generators/ArticleGenerator.php ← Throwable (تولید مقاله مقاوم‌تر)
engine/generators/ContentImprover.php  ← mb_rtrim
engine/analyzers/KeywordAnalyzer.php  ← mb_trim (۵ مورد)
includes/helpers.php              ← mb_trim/mb_rtrim جدید + json_response ضدخرابی
admin/articles.php                ← Throwable + هدر X-Requested-With
admin/index.php                   ← Throwable — داشبورد دیگر هرگز خالی نمی‌ماند
admin/includes/header.php         ← admin.js به هدر منتقل شد (منو همیشه کار می‌کند)
admin/includes/footer.php         ← admin.js از فوتر حذف شد
admin/brand-build.ajax.php        ← Throwable
api/index.php                     ← Throwable (۵ مورد)
api/middleware/rate-limit.php     ← Throwable (۲ مورد)
```

### ✅ راستی‌آزمایی پس از هات‌فیکس

| # | بررسی | انتظار |
|:-:|-------|--------|
| ۱ | پایین پنل مدیریت | نسخه **2.4.1** |
| ۲ | داشبورد | کارت‌های آمار + جدول وضعیت موتور AI + بازشدن منو با ☰ |
| ۳ | مقالات ← تولید جدید ← عنوان دلخواه با حرف اول «آ/ب/د/س/…» ← دکمه پیشنهاد | پنل تحلیل + ۸ پیشنهاد (بدون خطای JSON) |
| ۴ | ویرایش مقاله ← دکمه «🎯 بهبود سئو» | گزارش قبل/بعد + فهرست اصلاحات (بدون خطای '<') |
| ۵ | تولید مقاله | مقاله کامل با نگارش اصلاح‌شده (می‌کند → می‌کندِ درست با نیم‌فاصله) |

---

## ⚠️ قبل از شروع (الزامی)

| # | اقدام | توضیح |
|:-:|-------|-------|
| ۱ | 🗄️ **بکاپ کامل دیتابیس** | از phpMyAdmin → Export → SQL (کامل، با همه جدول‌ها) |
| ۲ | 📁 **بکاپ فایل‌ها** | کل پوشه سایت ساز را ZIP کنید (مخصوصاً `config.local.php` و `uploads/`) |
| ۳ | ✅ **ثبت نسخه فعلی** | پایین پنل مدیریت یا `config.php` → باید `2.3.0` باشد |

> 💡 زمان موردنیاز: حدود ۵ دقیقه | ⏸️ قطعی سرویس: لازم نیست (بدون تغییر دیتابیس)

---

## 🎯 تغییرات مهم این نسخه

### 📝 موتور نگارش فارسی — قلب جدید خط تولید
از این نسخه **همه محتوای تولیدی** (مقالات + صفحات برند) به‌صورت خودکار از یک نگارش‌گر فارسی عبور می‌کند: نیم‌فاصله‌ها اصلاح، علائم سجاوندی استاندارد، املای رایج درست، ارقام فارسی و **هم‌خوانی فعل و فاعل** («ما می‌کند» → «ما می‌کنیم»). کیفیت محتوا حالا **۸ بُعدی** است و بُعد جدید «نگارش فارسی» با وزن ۱۰ امتیاز دارد.

### 🎯 عنوان دلخواه + 📊 آمار سئو زیر هر مقاله
- موقع تولید مقاله می‌توانید **عنوان دلخواه** بنویسید و با دکمه «🎯 پیشنهاد بهترین عنوان سئو» بهترین نسخه (رتبه‌بندی‌شده با امتیاز) را جایگزین کنید — مقاله دقیقاً حول همان عنوان نوشته می‌شود
- زیر ویرایشگر هر مقاله، پنل **۱۲ سنجه سئو** + دکمه **«🎯 بهبود سئو»** که ۸ اصلاح خودکار را اعمال و گزارش قبل/بعد می‌دهد

### 🚀 سایر قابلیت‌های جدید
- 📰 **۲۵ نوع مقاله** (از ۹) — سلکت گروه‌بندی‌شده ۵ دسته
- 🔎 **جستجوی اینترنت هنگام نوشتن** (چک‌باکس «استفاده از جستجوی اینترنت») — داده‌های واقعی + پرسش‌های کاربران + منابع
- 🖼️ **تصاویر خودکار** در متن مقالات (پیش‌فرض روشن)
- ⬇️ **دانلود مستقیم فونت/آیکون روی سرور** — دکمه «⬇️ دانلود و نصب» در صفحات فونت/آیکون
- 🤖 **ربات تلگرام v2.2**: `/grammar` + `/suggest` + `/types` + معادل فارسی
- 🔌 **۳ اندپوینت جدید API** (مجموع ۴۴)

**💡 بدون هیچ تغییر دیتابیسی** — این نسخه کاملاً file-level است.

---

## 📦 روش ۱: بسته بروزرسانی (توصیه‌شده)

بسته `Sahand-Service-BrandMaker-v2.4.0-update.zip` از [صفحه رلیز](https://github.com/pranses2011/Sahand-Service-BrandMaker/releases) دانلود کنید. این بسته **فقط فایل‌های تغییرکرده** را شامل می‌شود.

### مراحل

```text
۱. محتویات ZIP بروزرسانی را استخراج کنید
۲. کل فایل‌ها را با FTP/File Manager روی هاست آپلود و جایگزین کنید
   (مسیرها داخل بسته از قبل درست است — فقط همه را روی روت سایت ساز کپی کنید)
۳. مطمئن شوید پوشه cache قابل نوشتن است (مجوز 755 کافی است)
۴. پنل مدیریت را رفرش کنید — تمام! ✅ (کش مرورگر خودکار می‌شکند)
```

### فایل‌های جایگزین‌شده در بسته بروزرسانی v2.4.0

```
config.php                        ← نسخه 2.4.0
core/AssetDownloader.php          ← 🆕 دانلود مستقیم فونت/آیکون روی سرور (بخش ۱ فاز Q)
engine/SahandAI.php               ← موتور v3.4.0 (+suggestArticleTitles/articleSeoStats/improveArticleSeo + custom_title/research/with_images)
engine/utils/PersianGrammar.php   ← 🆕 موتور قواعد نگارشی فارسی v1.1 (نیم‌فاصله/سجاوندی/املای رایج/هم‌خوانی فعل و فاعل)
engine/analyzers/QualityScorer.php ← v2.1 — بُعد هشتم «نگارش فارسی»
engine/generators/ArticleGenerator.php ← v2.3 (اصلاح نگارشی + ۱۶ outline جدید + custom_title + research + images)
engine/generators/ContentGenerator.php ← v1.1 (اصلاح نگارشی خروجی)
engine/generators/ContentImprover.php  ← استراتژی fixGrammar
engine/generators/TitleGenerator.php   ← v3.1 (suggestForCustom)
engine/generators/SeoImprover.php      ← 🆕 v1.0 (۱۲ سنجه سئو + ۸ اصلاح خودکار)
engine/CommandAssistant.php       ← ۳ intent جدید (نگارش/پیشنهاد عنوان سئو/انواع مقاله)
engine/knowledge/templates.json   ← ۲۵ نوع مقاله / ۱۳۶ قالب عنوان
api/index.php                     ← ۳ اندپوینت جدید (۴۴ مجموع)
telegram/TelegramBot.php          ← v2.2 — ۳ فرمان جدید + کیبورد و راهنمای جدید
admin/articles.php                ← عنوان دلخواه + پیشنهاد سئو + پنل آمار سئو + دکمه بهبود + چک‌باکس جستجو/تصویر
admin/brand-build.ajax.php        ← تولید اولیه ۸ مقاله با انواع متنوع
admin/fonts.php + admin/icons.php ← دکمه‌های دانلود مستقیم (بخش ۱ فاز Q)
assets/fonts/sources.json + assets/icons/sources.json ← 🆕 منابع رسمی دانلود
README.md / CHANGELOG.md / UPGRADE.md / CHECKLIST.md / WORKLOG.md
docs/AI-API-GUIDE.md              ← مستندات v3.4 (۱۹۳۲ خط)
```

> ⚠️ توجه: `config.local.php` جایگزین **نمی‌شود** (تنظیمات شما دست‌نخورده می‌ماند) — این فایل در بسته نیست.

---

## 📦 روش ۲: بسته کامل نصب (اگر تغییرات دستی زیاد دارید)

اگر فایل‌های هسته را دستی تغییر داده‌اید، بسته کامل `install.zip` را نصب کنید و `config.local.php` + `uploads/` + فایل‌های سفارشی خود را از بکاپ برگردانید.

---

## ✅ راستی‌آزمایی پس از بروزرسانی

| # | بررسی | انتظار |
|:-:|-------|--------|
| ۱ | پایین پنل مدیریت | نسخه **2.4.0** |
| ۲ | مقالات ← تولید جدید | فیلد «عنوان دلخواه» + دکمه «🎯 پیشنهاد بهترین عنوان سئو» + چک‌باکس جستجو/تصویر |
| ۳ | تولید با عنوان دلخواه + زدن دکمه پیشنهاد | پنل تحلیل با امتیاز + ۸ پیشنهاد؛ کلیک → جایگزینی عنوان |
| ۴ | ویرایش هر مقاله | پنل «📊 آمار کامل سئو» (۱۲ ردیف) + دکمه «🎯 بهبود سئو» |
| ۵ | زدن «بهبود سئو» روی مقاله با ایراد | گزارش قبل/بعد + فهرست اصلاحات + رفرش → محتوای اصلاح‌شده |
| ۶ | نوع مقاله | لیست گروه‌بندی‌شده ۲۵ نوع در ۵ دسته |
| ۷ | فونت‌ها/آیکون‌ها | دکمه «⬇️ دانلود و نصب» سبز کار می‌کند |
| ۸ | ربات تلگرام: `/status` | موتور AI نسخه 3.4.0 |
| ۹ | ربات تلگرام: `/نگارش ما این کار را انجام می دهیم` | امتیاز + ایرادها + نسخه اصلاح‌شده (می‌دهیم → می‌دهیم با نیم‌فاصله) |
| ۱۰ | ربات تلگرام: `/پیشنهاد یخچال سرد نمی‌کند` | بهترین عنوان سئو + سایر پیشنهادها |

---

---

<a name="upgrade-22-23"></a>
## ⬆️ ارتقای ۲.۲ → ۲.۳ (نسخه 2.3.0)

> این راهنما نحوه ارتقای سایت ساز برند سهند سرویس به نسخه **۲.۳.۰ (موتور ۳.۳ — 🏅 سئوی E-E-A-T + 📚 ویکی‌پدیا + ⚡ مسابقه موازی جستجو + 👁️ پیش‌نمایش زنده قالب‌ها + رفع ۵ باگ)** را توضیح می‌دهد.
>
> - ارتقا از **۲.۲.۰** → فقط مراحل همین بخش (پیشنهادی)
> - ارتقا از **۲.۱.۰ یا قدیمی‌تر** → اول [راهنمای ارتقای ۲.۱ → ۲.۲](#--ارتقای-21--22-نسخه-220) پایین همین فایل، سپس مراحل همین بخش

---

## ⚠️ قبل از شروع (الزامی)

| # | اقدام | توضیح |
|:-:|-------|-------|
| ۱ | 🗄️ **بکاپ کامل دیتابیس** | از phpMyAdmin → Export → SQL (کامل، با همه جدول‌ها) |
| ۲ | 📁 **بکاپ فایل‌ها** | کل پوشه سایت ساز را ZIP کنید (مخصوصاً `config.local.php` و `uploads/`) |
| ۳ | ✅ **ثبت نسخه فعلی** | پایین پنل مدیریت یا `config.php` → باید `2.2.0` باشد |

> 💡 زمان موردنیاز: حدود ۵ دقیقه | ⏸️ قطعی سرویس: لازم نیست (بدون تغییر دیتابیس)

---

## 🎯 تغییرات مهم این نسخه

### 🐛 رفع ۵ باگ گزارش‌شده کاربر
1. **صفحه ساخت برند** — متن‌ها به صورت خام بیرون می‌زدند (تگ PHP اضافی) → کاملاً سالم و ریسپانسیو
2. **خطای مرحله ۱ ساخت برند (تحلیل لوگو)** — حالا **SVG کامل پشتیبانی می‌شود** (استخراج رنگ از fill/stroke بدون Imagick) + باگ داخلی برای لوگوهای تک‌رنگ رفع شد + تحلیل ناموفق دیگر هرگز ساخت را متوقف نمی‌کند (پالت پیش‌فرض اعمال و ادامه)
3. **فیلتر بالای کارت‌ها نصفه دیده می‌شد** — فیلترها حالا در موبایل wrap و تمام‌عرض می‌شوند
4. **صفحه تنظیمات در موبایل** — تب‌ها اسکرول افقی نرم شدند + دکمه ذخیره تمام‌عرض زیر تب‌ها
5. **پیش‌نمایش قالب‌ها** — پیش‌نمایش زنده با محتوای نمونه + سوئیچ موبایل/تبلت/دسکتاپ + وایرفریم داخل بوم

### 🧊 مهم: کش مرورگر خودکار می‌شکند
از این نسخه، همه فایل‌های CSS/JS با پارامتر نسخه (زمان آخرین تغییر فایل) لود می‌شوند. یعنی بعد از بروزرسانی، **مرورگر به صورت خودکار فایل‌های جدید را می‌گیرد** و دیگر صفحه به‌هم‌ریختهِ قدیمی از کش نشان داده نمی‌شود. اگر باز هم چیدمان قدیمی دیدید، یک‌بار Ctrl+F5 بزنید.

### 🚀 قابلیت‌های جدید
- 🏅 **سئوی E-E-A-T**: چک‌لیست ۸ معیاری + موجودیت‌ها + خوشه کلیدواژه + People Also Ask + تحلیل شکاف محتوایی رقبا — ۲ اندپوینت جدید `seo-package` و `eeat-check` (مجموع ۴۱)
- 📰 **مقاله حرفه‌ای‌تر**: فهرست مطالب + زمان مطالعه + جعبه نکته/هشدار + مزایا-معایب + باکس آمار + حداقل ۱۰۰۰ کلمه
- 🌐 **جستجوی ۹ ارائه‌دهنده**: ویکی‌پدیا فارسی/انگلیسی + Startpage + مسابقه موازی (تا ۵۰٪ سریع‌تر) + تازگی و اعتماد دامنه
- 🤖 **ربات تلگرام v2.1**: ۴ فرمان جدید `/seo` `/eeat` `/plan` `/faq` + زبان طبیعی

**💡 بدون هیچ تغییر دیتابیسی** — این نسخه کاملاً file-level است.

---

## 📦 روش ۱: بسته بروزرسانی (توصیه‌شده)

بسته `Sahand-Service-BrandMaker-v2.3.0-update.zip` از [صفحه رلیز](https://github.com/pranses2011/Sahand-Service-BrandMaker/releases) دانلود کنید. این بسته **فقط فایل‌های تغییرکرده** را شامل می‌شود.

### مراحل

```text
۱. محتویات ZIP بروزرسانی را استخراج کنید
۲. کل فایل‌ها را با FTP/File Manager روی هاست آپلود و جایگزین کنید
   (مسیرها داخل بسته از قبل درست است — فقط همه را روی روت سایت ساز کپی کنید)
۳. مطمئن شوید پوشه cache/engine قابل نوشتن است (مجوز 755 کافی است)
۴. پنل مدیریت را رفرش کنید — تمام! ✅ (کش مرورگر خودکار می‌شکند)
```

### فایل‌های جایگزین‌شده در بسته بروزرسانی v2.3.0

```
config.php                        ← نسخه 2.3.0
core/ImageProcessor.php           ← 🆕 پشتیبانی کامل SVG/SVGZ (استخراج رنگ بدون Imagick)
engine/analyzers/ColorAnalyzer.php ← مسیر SVG + رفع باگ imagedestroy + analyzeFromPixels
engine/SahandAI.php               ← موتور v3.3.0 (+seoPackage/researchTopic/eeatCheck)
engine/CommandAssistant.php       ← ۴ intent جدید (سئو/اعتمادسنجی/برنامه محتوا/سوالات متداول)
engine/generators/ArticleGenerator.php ← v2.1 (TOC + زمان مطالعه + کال‌اوت + مزایا-معایب)
engine/generators/SeoGenerator.php     ← v1.2 (E-E-A-T + موجودیت + خوشه + شکاف محتوایی)
engine/services/WebSearchService.php   ← v1.2 (ویکی‌پدیا + Startpage + مسابقه موازی + trust/freshness)
api/index.php                     ← ۲ اندپوینت جدید (seo-package + eeat-check)
telegram/TelegramBot.php          ← v2.1 — ۴ فرمان جدید + فرمت‌دهی‌های جدید
admin/brand-new.php               ← 🐛 رفع باگ تگ PHP اضافی (صفحه ساخت برند)
admin/brand-build.ajax.php        ← 🐛 مرحله ۱ fallback نرم (هرگز متوقف نمی‌شود)
admin/settings.php                ← 🐛 نوار تب‌های جدید ریسپانسیو
admin/brands.php / articles.php / requests.php / error-codes.php / brand-edit.php ← 🐛 فیلترهای ریسپانسیو
admin/template-builder.php        ← 👁️ پیش‌نمایش زنده + وایرفریم + asset_ver
admin/template-preview.php        ← 🆕 رندر پیش‌نمایش زنده چیدمان
admin/includes/header.php + footer.php + login.php ← asset_ver (شکستن کش)
includes/helpers.php              ← 🆕 تابع asset_ver()
assets/css/admin.css              ← tabs-bar + tools ریسپانسیو + polish موبایل
assets/css/builder.css            ← استایل وایرفریم + مودال پیش‌نمایش
templates/brand-core/css/style.css ← استایل TOC/کال‌اوت/مزایا-معایب/آمار مقاله
README.md / CHANGELOG.md / UPGRADE.md / CHECKLIST.md / WORKLOG.md
docs/AI-API-GUIDE.md              ← مستندات v3.3 (۱۸۰۰+ خط)
```

> ⚠️ توجه: `config.local.php` جایگزین **نمی‌شود** (تنظیمات شما دست‌نخورده می‌ماند) — این فایل در بسته نیست.

---

## 📦 روش ۲: بسته کامل نصب (اگر تغییرات دستی زیاد دارید)

اگر فایل‌های هسته را دستی تغییر داده‌اید، بسته کامل `install.zip` را نصب کنید و `config.local.php` + `uploads/` + فایل‌های سفارشی خود را از بکاپ برگردانید.

---

## ✅ راستی‌آزمایی پس از بروزرسانی

| # | بررسی | انتظار |
|:-:|-------|--------|
| ۱ | پایین پنل مدیریت | نسخه **2.3.0** |
| ۲ | تنظیمات ← با موبایل (یا DevTools ← 390px) | تب‌ها اسکرول افقی + دکمه ذخیره تمام‌عرض |
| ۳ | مدیریت برندها ← موبایل | فیلتر جستجو/وضعیت کاملاً داخل کادر |
| ۴ | برند جدید ← افزودن | فرم تمیز بدون متن خام |
| ۵ | ساخت برند با **لوگو SVG** | مرحله ۱ با موفقیت (بدون خطا) + پالت رنگ |
| ۶ | قالب‌ها ← ویرایش قالب | دکمه «👁️ پیش‌نمایش زنده» کار می‌کند |
| ۷ | ربات تلگرام: `/status` | موتور AI نسخه 3.3.0 |
| ۸ | ربات تلگرام: `/seo تعمیر یخچال` | پکیج سئو با E-E-A-T و موجودیت‌ها |

---

---

<a name="upgrade-21-22"></a>
## ⬆️ ارتقای ۲.۱ → ۲.۲ (نسخه 2.2.0)

> کاربرانی که از ۲.۱ یا قدیمی‌تر ارتقا می‌دهند، اول این بخش را انجام دهند و سپس بخش اصلی بالای فایل را برای رسیدن به ۲.۳.۰.

### ⚠️ قبل از شروع (الزامی)

| # | اقدام | توضیح |
|:-:|-------|-------|
| ۱ | 🗄️ **بکاپ کامل دیتابیس** | از phpMyAdmin → Export → SQL (کامل، با همه جدول‌ها) |
| ۲ | 📁 **بکاپ فایل‌ها** | کل پوشه سایت ساز را ZIP کنید (مخصوصاً `config.local.php` و `uploads/`) |
| ۳ | ✅ **ثبت نسخه فعلی** | پایین پنل مدیریت یا `config.php` → باید `2.1.0` باشد |

> 💡 زمان موردنیاز: حدود ۵ دقیقه | ⏸️ قطعی سرویس: لازم نیست (بدون تغییر دیتابیس)

---

## 🎯 تغییرات مهم این نسخه

- 🐛 **رفع باگ «منو در وسط صفحه باز می‌شود»** در پنل موبایل + **ریسپانسیو کامل** پنل، سایت‌های برند و صفحه نصب
- 🖼️ **۳ تصویر مرتبط برای هر مقاله** (۹ تصویر فوتورئال جدید بسته‌بندی شد + درج خودکار در جایگاه‌های استراتژیک)
- 📰 **تحویل کامل مقاله در تلگرام**: «مقاله بنویس...» یا `/article موضوع` → فایل HTML آماده انتشار برای هر سایت دیگر + نسخه Markdown + آلبوم ۳ عکس — **بدون نیاز به ورود به سایت ساز**
- 🧠 **موتور ۳.۲**: غنی‌سازی ساختاری مقاله (فهرست مطالب + سایر پرسش‌های کاربران + داده‌های تازه وب با منبع + جعبه E-E-A-T) + تحقیق وب خودکار (auto) + امتیاز هدف ۸۸
- 🌐 **جستجوی وب قوی‌تر**: ارائه‌دهنده Mojeek (مجموع ۷) + فرصت‌های کلیدواژه long-tail + تلاش مجدد خودکار
- 📰 **اخبار زنده**: `POST /api/ai/news` + فرمان «اخبار: ...» در دستیار و ربات
- 🔍 **سئو عمیق‌تر**: HowTo Schema + Speakable + تراکم کلیدواژه + زمان مطالعه + og:image
- 🔌 ۲ اندپوینت API جدید: `news`، `article-images` (مجموع ۳۹)

**💡 بدون هیچ تغییر دیتابیسی** — این نسخه کاملاً file-level است؛ جداول، seedها و داده‌ها بدون تغییر می‌مانند.

---

## 📦 روش ۱: بسته بروزرسانی (توصیه‌شده)

بسته `Sahand-Service-BrandMaker-v2.2.0-update.zip` (از [رلیز v2.2.0](https://github.com/pranses2011/Sahand-Service-BrandMaker/releases/tag/v2.2.0)) از [صفحه رلیز](https://github.com/pranses2011/Sahand-Service-BrandMaker/releases) دانلود کنید. این بسته **فقط فایل‌های تغییرکرده** را شامل می‌شود.

### مراحل

```text
۱. محتویات ZIP بروزرسانی را استخراج کنید
۲. کل فایل‌ها را با FTP/File Manager روی هاست آپلود و جایگزین کنید
   (مسیرها داخل بسته از قبل درست است — فقط همه را روی روت سایت ساز کپی کنید)
۳. مطمئن شوید پوشه cache/engine قابل نوشتن است (مجوز 755 کافی است)
۴. پنل مدیریت را رفرش کنید — تمام! ✅
```

### فایل‌های جایگزین‌شده در بسته بروزرسانی v2.2.0

```
config.php                        ← نسخه 2.2.0
install.php                       ← ریسپانسیو مرحله‌بندی نصب
api/index.php                     ← ۲ اندپوینت جدید (news + article-images)
engine/SahandAI.php               ← موتور v3.2.0
engine/CommandAssistant.php       ← نیّت اخبار + موضوع دلخواه مقاله + no_cache
engine/generators/SmartPipeline.php        ← خط تولید ۱۱ مرحله‌ای v3.2
engine/generators/SeoGenerator.php         ← v1.1 (HowTo + تراکم + Speakable)
engine/services/WebSearchService.php       ← v1.1 (Mojeek + اخبار + opportunities)
engine/services/ArticleImageService.php    ← 🆕 سرویس ۳ تصویر مقاله
telegram/TelegramBot.php          ← 🆕 v2 — تحویل کامل مقاله + ۱۲ فرمان + callback
admin/includes/header.php         ← پس‌زمینه سایدبار + aria
assets/css/admin.css              ← 🐛 رفع باگ منو + ریسپانسیو کامل پنل
assets/js/admin.js                ← toggleSidebar هوشمند + بستن خودکار
templates/brand-core/css/style.css        ← منوی موبایل جدید + ریسپانسیو + عناصر v3.2
templates/brand-core/includes/header.php  ← منوی aria + toggleNav
templates/brand-core/js/app.js            ← toggleNav + بستن خودکار
assets/images/articles/*.jpg      ← 🆕 ۹ تصویر تخصصی مقاله (جديد)
README.md / CHANGELOG.md / UPGRADE.md / CHECKLIST.md / WORKLOG.md
docs/AI-API-GUIDE.md              ← مستندات v3.2 (اخبار + تصاویر + ربات v2)
```

> ⚠️ توجه: `config.local.php` جایگزین **نمی‌شود** (تنظیمات شما دست‌نخورده می‌ماند) — این فایل در بسته نیست.

---

## 📦 روش ۲: بسته کامل نصب (اگر تغییرات دستی زیاد دارید)

اگر فایل‌های هسته را دستی تغییر داده‌اید، بسته کامل را نصب کنید و `config.local.php` + `uploads/` + فایل‌های سفارشی خود را از بکاپ برگردانید.

---

## ✅ راستی‌آزمایی پس از بروزرسانی

| # | بررسی | انتظار |
|:-:|-------|--------|
| ۱ | پایین پنل مدیریت | نسخه **2.2.0** |
| ۲ | `GET /api/ai/engine-info` | `engine_version: "3.2.0"` |
| ۳ | 📱 پنل در موبایل | منو با ☰ از لبه صفحه باز می‌شود + پس‌زمینه تیره + با کلیک روی لینک بسته می‌شود |
| ۴ | 🖼️ تصاویر مقاله | `GET /api/ai/article-images` → ۹ تصویر با exists=true |
| ۵ | 📰 اخبار زنده | `POST /api/ai/news` با query «لوازم خانگی» |
| ۶ | 🧠 خط تولید کامل | `POST /api/ai/smart-generate` → خروجی شامل `images` (۳ عدد) و `article.toc` |
| ۷ | 🤖 ربات تلگرام | «مقاله بنویس درباره یخچال» → فایل HTML + MD + آلبوم ۳ عکس |

### تست سریع با curl (جایگزین دامنه و کلید)

```bash
# سلامت موتور
curl "https://YOUR-DOMAIN/api/ai/engine-info"

# 🖼️ فهرست تصاویر مقاله (عمومی — بدون کلید)
curl "https://YOUR-DOMAIN/api/ai/article-images"

# 📰 اخبار زنده (جدید v3.2)
curl -X POST "https://YOUR-DOMAIN/api/ai/news" \
  -H "Content-Type: application/json" -H "X-API-Key: YOUR_KEY" \
  -d '{"query": "لوازم خانگی", "limit": 3}'

# 🧠 خط تولید کامل با ۳ تصویر + تحقیق خودکار
curl -X POST "https://YOUR-DOMAIN/api/ai/smart-generate" \
  -H "Content-Type: application/json" -H "X-API-Key: YOUR_KEY" \
  -d '{"brand_id": 1, "device_key": "refrigerator", "variants": 2}'
```

### 🤖 تست تحویل کامل مقاله در تلگرام (پس از ارتقا)

۱. پنل → **🤖 ربات تلگرام** → مطمئن شوید ربات فعال و شناسه‌تان مجاز است
۲. در تلگرام به ربات بفرستید: `مقاله بنویس درباره تعمیر یخچال`
۳. انتظار: پیام «در حال ساخت...» → خلاصه → فایل `.html` → فایل `.md` → آلبوم ۳ عکس → دکمه‌های شیشه‌ای ✅

---

## 🆘 رفع اشکال

| مشکل | راه حل |
|------|--------|
| خطای نوشتن در `cache/engine` | مجوز پوشه `cache` را 755 (یا 775) کنید؛ پوشه `cache/engine` به‌صورت خودکار ساخته می‌شود |
| پنل هنوز نسخه قدیمی نشان می‌دهد | کش مرورگر (Ctrl+F5)؛ مطمئن شوید `config.php` جدید آپلود شده |
| خطای 404 در اندپوینت‌های جدید | `.htaccess` قدیم مانده — فایل `.htaccess` روت را از بسته کامل جایگزین کنید |
| تصاویر مقاله لود نمی‌شوند | پوشه `assets/images/articles/` را کامل آپلود کرده‌اید؟ (۹ فایل JPG) |
| ربات فایل نمی‌فرستد | `curl` فعال باشد + `sys_get_temp_dir()` قابل نوشتن؛ توکن ربات معتبر است |
| جستجوی وب خطا می‌دهد | مطمئن شوید cURL فعال است؛ در صورت تحریم ارائه‌دهنده، زنجیره جایگزین خودکار فعال می‌شود؛ در نهایت کلید SerpApi را در تنظیمات `websearch_settings` وارد کنید |
| وب‌هوک تلگرام set نمی‌شود | آدرس باید HTTPS باشد؛ برای تست محلی از `php telegram/poll.php` استفاده کنید |
| ربات پاسخ نمی‌دهد | شناسه چت را با `/id` بگیرید و در پنل مجاز کنید (لیست خالی = بسته) |

---

## 📚 ادامه مطالعه

- 🤖 [راهنمای کامل API هوش مصنوعی](docs/AI-API-GUIDE.md) — استفاده از موتور AI از بیرون سایت ساز (شامل راهنمای کامل ربات v2)
- 🔤 [دانلود فونت‌ها](assets/fonts/README.md) — ۳۰ فونت با لینک رسمی
- 🖼️ [دانلود پک‌های آیکون](assets/icons) — ۱۰ پک با لینک رسمی
- 📋 [سابقه تغییرات](CHANGELOG.md) — جزئیات کامل نسخه ۲.۲.۰

---

## ⬆️ از نسخه 1.x: ابتدا به 2.0

اگر هنوز نسخه ۱.x دارید، ابتدا طبق راهنمای داخل بسته `v2.0.0-update.zip` به نسخه ۲.۰.۰ ارتقا دهید (۲۷ فایل + فهرست راستی‌آزمایی همان بسته)، سپس مرحله بعدی و در پایان بخش اصلی همین فایل را اجرا کنید.

---

## ⬆️ ارتقای 2.0 → 2.1

ابتدا بسته `v2.1.0-update.zip` را نصب کنید (جستجوی آنلاین وب + ربات تلگرام + رفع باگ بحرانی نصب‌کننده دیتابیس)، سپس بخش اصلی همین فایل را اجرا کنید. جزئیات کامل تغییرات ۲.۱.۰ در [CHANGELOG](CHANGELOG.md) آمده است.
