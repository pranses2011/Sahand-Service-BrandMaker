# 🔄 راهنمای بروزرسانی — Sahand Service BrandMaker

نسخه هدف: **2.25.0** (موتور AI: 3.15.0) — [سابقه تغییرات](CHANGELOG.md)

---

## ⚠️ نکته مهم این نسخه (۲.۲۵.۰)

این نسخه **رفع ثبت درخواست سایت‌های برند** را دارد: ریشه خطای «ارتباط با سرور»،
نبود فایل `js/form-submit.php` در قالب بود (فرم به فایلی ارسال می‌شد که هرگز
ساخته نشده بود!). پس از بروزرسانی، برای هر برند مستقرشده یک‌بار
**«بروزرسانی استقرار»** را اجرا کنید تا فایل جدید پروکسی + استایل جدید
(کنتراست WCAG + تم آنی) به سایت برند منتقل شود. پالت‌های ویرایش‌شده دستی هم
با اولین ذخیره مجدد از فیلتر کنتراست عبور می‌کنند و رنگ‌های ناخوانا
به‌صورت خودکار اصلاح و گزارش می‌شوند.

## روش ۱ — پکیج بروزرسانی (پیشنهادی — بدون تغییر دیتابیس)

1. از پوشه فعلی سایت ساز خود **بکاپ کامل** بگیرید (فایل‌ها + دیتابیس).
2. فایل `update.zip` این رلیز را در **ریشه سایت ساز** (کنار `config.php`) آپلود کنید.
3. از فایل‌منیجر هاست (یا SSH با `unzip -o update.zip -d .`) آن را **استخراج** کنید —
   فایل‌های سیستمی جایگزین می‌شوند و `config.php` و `.htaccess` و `uploads/` و `database.sql` دست‌نخورده می‌مانند.
4. فایل `update.zip` را حذف کنید.
5. وارد پنل مدیریت شوید — همه چیز آماده است (**هیچ تغییر دیتابیسی لازم نیست**).

## روش ۲ — از سورس گیت‌هاب

```bash
git pull origin main
# اگر تغییرات محلی دارید: git stash && git pull && git stash pop
```

## 📋 پس از بروزرسانی — چک‌لیست تکمیلی

| # | اقدام | لازم/اختیاری |
|---|-------|---------------|
| ۱ | برای هر برند مستقرشده: **ویرایش برند ← استقرار ← بروزرسانی استقرار** | ⭐ لازم (انتقال فرم جدید + استایل کنتراست/تم) |
| ۲ | تب «پالت رنگ» ← دکمه «💾 ذخیره پالت» (عبور از فیلتر کنتراست WCAG) | پیشنهادی (تضمین خوانایی هر دو تم) |
| ۳ | تست ثبت درخواست روی سایت برند | ⭐ لازم (تأیید رفع) |
| ۴ | تست دکمه تغییر تم تاریک/روشن (آنی + آیکون) | پیشنهادی |
| ۵ | قالب‌ساز ← دکمه «⚙️ تنظیمات صفحه» | اختیاری (قابلیت جدید) |

## 📦 فایل‌های تغییر‌یافته این نسخه

```
config.php                                   (نسخه ۲.۲۵.۰)
templates/brand-core/js/form-submit.php      (🆕 پروکسی ثبت درخواست)
templates/brand-core/pages/request.php       (زنجیره سه‌مرحله‌ای فرم)
templates/brand-core/includes/header.php     (تم آنی + آیکون پویا + TTL)
templates/brand-core/css/style.css           (on-primary/on-gradient + تیره)
templates/brand-core/css/theme-dark.css      (هماهنگ‌سازی)
engine/analyzers/ColorAnalyzer.php           (موتور کنتراست WCAG)
admin/brand-edit.php                         (فیلتر پالت + پیش‌نمایش واقعی)
admin/brand-page-preview.php                 (رنگ‌های واقعی)
admin/template-builder.php                   (تنظیمات صفحه + ۲۰ عنصر + آیتم‌ها)
admin/template-preview.php                   (آینه رندرر + _page)
core/Deployer.php                            (ensureReadable روی ZIP)
```

## 🩺 راستی‌آزمایی

1. در پنل: «تنظیمات» ← سلامت سیستم ← نسخه باید **2.25.0** باشد.
2. روی سایت برند یک درخواست آزمایشی ثبت کنید — باید «کد پیگیری» بگیرید.
3. دکمه 🌙/☀️ هدر سایت برند — تغییر تم باید همان لحظه + با عوض شدن آیکون اتفاق بیفتد.
4. پنل ← ویرایش برند ← پالت رنگ ← پیش‌نمایش زنده: رنگ دکمه و گرادیانت باید
   دقیقاً همان چیزی باشد که در سایت برند می‌بینید (حداکثر تا ۲ دقیقه تأخیر کش).

## 🔙 بازیابی سایت‌های مستقرشده (در صورت مشکل)

اگر بعد از بروزرسانی مشکلی پیش آمد:
- سایت‌های برند مستقل از سایت ساز کار می‌کنند (فقط API داده مشترک است).
- «بروزرسانی استقرار» مجدد، فایل‌های سایت برند را از قالب سالم بازسازی می‌کند.
- بکاپ‌های پنل (پنل ← بکاپ‌ها) کل ساختار هر برند را نگه می‌دارند.
