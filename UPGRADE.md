# 🔄 راهنمای بروزرسانی — به نسخه ۲.۱۶.۰

> ⚠️ **قبل از هر چیز**: از فایل‌ها و دیتابیس خود بکاپ کامل بگیرید.

---

## 🆕 نسخه ۲.۱۶.۰ — ریشه‌یابی نهایی خطای mkdir استقرار

### 📋 چه چیزی تغییر می‌کند؟

| بخش | شرح |
|-----|-----|
| 🚀 رفع خطای mkdir | پیام «❌ ساخت پوشه‌های حیاتی ناموفق: css, js, pages, includes, cache — The system could not find the function "mkdir" in the module "Fileman"» ریشه‌ای رفع شد — تابع در UAPI هرگز وجود نداشته؛ حالا **API2 رسمی** با امضای صحیح (path + name + permissions) صدا زده می‌شود |
| 📂 ساخت پوشه مقاوم | ساخت بازگشتی تودرتو (والد→فرزند) + راستی‌آزمایی وجود — پوشه از قبل موجود دیگر خطا نمی‌دهد |
| 📝 نوشتن فایل مقاوم | writeFile دولایه: UAPI save_file_content → API2 savefile — فایل‌های config/htaccess سایت برند |
| 🛡️ سه لایه | API2 → FTP fallback → استخراج ZIP ساختار کامل را می‌سازد — استقرار دیگر در مرحله پوشه‌ها متوقف نمی‌شود |

### 🚀 روش بروزرسانی (پکیج update.zip)

1. **بکاپ بگیرید** (فایل‌ها + دیتابیس)
2. محتویات `update.zip` را در روت سایت‌ساز استخراج کنید (جایگزینی فایل‌ها)
3. پنل را با **Ctrl+F5** کامل رفرش کنید
4. **مهاجرت دیتابیس لازم نیست** — فقط فایل (بدون تغییر schema)

### 🔍 بعد از بروزرسانی چه بخواهید؟

- برای یک برند «بدون استقرار» روی «🚀 استقرار خودکار» کلیک و شروع کنید — دیگر نباید پیام «ساخت پوشه‌های حیاتی ناموفق» ببینید؛ لاگ استقرار باید مرحله پوشه‌ها را سبز نشان دهد
- اگر هاستتان API را محدود کرده باشد، نگران نباشید: FTP جایگزین می‌شود و در نهایت استخراج ZIP خودش ساختار پوشه‌ها را کامل می‌کند

---

## 🆕 نسخه ۲.۱۵.۰ — رفع نهایی استقرار + خطایاب ۱۱ ارائه‌دهنده + ۷ سرویس تصویر + قالب‌ساز ۱۱۴ عنصر

### 📋 چه چیزی تغییر می‌کند؟

| بخش | شرح |
|-----|-----|
| 📸 عکس واقعی AI مقاله | تصاویر مقاله حالا **عکس فوتورئال لحظه‌ای مرتبط با موضوع** هستند (سرویس رایگان Pollinations — اینترنت هنگام تولید لازم است) + تصویر شاخص هم عکس واقعی + واترمارک دوتایی درجا + در قطعی سرویس fallback خودکار به بسته آماده |
| 🔄 تولید مجدد تصاویر | تصاویر جدید **جایگزین** قبلی‌ها می‌شوند (نه افزودن به انتهای مقاله) — آپلودی‌های دستی محفوظ |
| 🚨 خطایاب v3.11 | ریشه‌یابی «هیچ کدی پیدا نشد» (کدهای خط‌تیره‌دار E-01/F-13) + استخراج ساختاریافته Causes/Troubleshooting + **ترجمه فارسی فیلدها** — مایکروویو/فر ال‌جی حالا ۱۴+ کد کامل |
| 📊 نوار پیشرفت | موقع تولید کدها، مودال زیبا با درصد و شرح مرحله‌به‌مرحله نمایش داده می‌شود |
| 🔤 فونت همه‌جا | باگ تاریخی «فونت لود نمی‌شد» رفع شد + فونت انتخابی در همه پیش‌نمایش‌ها + **سایت‌های مستقرشده جدید** هم با فونت انتخابی رندر می‌شوند (سایت‌های قبلاً مستقر: بروزرسانی/استقرار مجدد لازم است) |
| 🎭 قالب‌ساز v3.4 | ۱۲ عنصر جدید (۸۶) + دکمه‌های نوع نمایش واقعاً کار می‌کنند (ستون پهن می‌شود) + تنظیمات هر عنصر کامل و اثرگذار |
| 🔢 اعداد فارسی OG | همه اعداد تصاویر OG فارسی |
| 🪧 واترمارک نمایندگی | بزرگ‌تر از لوگوی برند (۳۳٪) + نسخه کش wm4 → خودکار بازتولید |
| ⏰ Cron cPanel | خطای «Can't locate Cpanel/API/Cron.pm» ریشه‌یابی شد — فراخوانی مستقیم API2 رسمی + ضدتکرار |
| 🚀 استقرار | باگ «این برند هنوز استقرار خودکار ندارد» رفع شد |
| 🖼 OG شفاف در regen | کش مرورگر با cache-buster شکسته شد |

### 🚀 روش بروزرسانی (پکیج update.zip)

1. **بکاپ بگیرید** (فایل‌ها + دیتابیس)
2. محتویات `update.zip` را در روت سایت‌ساز استخراج کنید (جایگزینی فایل‌ها)
3. پوشه `cache/` را خالی کنید (کش واترمارک wm4 و OG بازتولید می‌شود)
4. **مهاجرت دیتابیس لازم نیست** — فقط فایل (بدون تغییر schema)
5. برای فونت در سایت‌های مستقرشده: هر برند را یک‌بار «🔄 بروزرسانی» بگذارید تا theme-light/dark.css جدید با متغیرهای فونت مستقر شود
6. (اختیاری) برای فیلدهای فارسی‌شده کدهای قبلی: کدهای خطا ← حذف دستی کدهای انگلیسی‌دار و تولید مجدد، یا از دکمه ✨ AI تک‌فیلد استفاده کنید

### 🔍 بعد از بروزرسانی چه بخواهید؟

- مقاله جدید تولید کنید یا «تولید مجدد تصاویر» بزنید — عکس واقعی AI مرتبط با موضوع می‌بینید (اینترنت باید فعال باشد)
- کدهای خطای مایکروویو/فر ال‌جی را تولید کنید — ۱۴+ کد کامل با فیلدهای فارسی + نوار پیشرفت زنده
- قالب‌ساز ← «🖼️ کارتی» — ستون کتابخانه واقعاً پهن می‌شود و کارت‌ها بزرگ‌ترند
- دکمه ➕ افزودن Cron در تنظیمات cPanel — حالا واقعاً ثبت می‌شود

---

## 🆕 نسخه ۲.۱۳.۰ — خطایاب دو-منظوره + OG شفاف واقعی + قالب‌ساز ۷۴ عنصر + ۱۱ رفع باگ

### 📋 چه چیزی تغییر می‌کند؟

| بخش | شرح |
|-----|-----|
| 🚨 خطایاب AI v3.10 | کدهای معتبر پایگاه دانش رسمی (مستندات سازنده) حالا حتی وقتی جستجوی وب کند/قطع است ثبت می‌شوند + فیلدهای کدهای شناخته‌شده دقیق‌تر (داده curated مقدم) + ظرفشویی ال‌جی ۸→۱۴ کد |
| 🖼 OG شفاف واقعی | لوگوهای JPG هم پس‌زمینه سفیدشان هوشمندانه حذف می‌شود (flood-fill از لبه‌ها) — دیگر هیچ کادر سفیدی زیر لوگو نیست |
| 📐 تصاویر مقاله | ضدکشیدگی با ابعاد واقعی + استایل محافظ درون‌خطی (حتی روی سایت‌های مستقر قدیمی) + مرتبط‌تر با موضوع مقاله |
| 🪧 واترمارک‌ها | بسیار بزرگ‌تر (برند ۳۰٪ / نمایندگی ۲۵٪) + نسخه کش جدید wm3 → همه واترمارک‌های قبلی خودکار بازتولید می‌شوند |
| 🎭 قالب‌ساز | پیش‌نمایش واقعی همه ۷۴ عنصر + نمایش کارتی با پیش‌نمایش زنده + بلوک ترکیبی گروهی (چند بلوک باهم) + تنظیمات پیشرفته (اندازه عنوان/تراز/عرض/کلاس) |
| 🔌 cPanel | تست اتصال مقاوم (fallback چهار ماژول) + دکمه افزودن خودکار cron به cPanel |
| 🔤 فونت‌ها | فونت‌های تجاری حذف شدند؛ ۱۴ فونت آزاد جدید (ساحل، کتیبه، رقاص، گلزار و...) از داخل پنل نصب می‌شوند |
| 🎨 آیکون‌ها | کلیک روی آیکون = پیش‌نمایش بزرگ با اسلایدر اندازه |
| 🐛 ذخیره تنظیمات | «اطلاعات پایه ذخیره نمی‌شد» ریشه‌یابی و رفع شد (الگوی PRG + redirect ضدگلوله) |
| 🐛 استقرار | «❌ undefined» رفع شد + همه پیام‌ها با کادر زیبای SahandDialog |
| 📜 قانون کادر زیبا | alert() ساده در کل پنل خودکار به کادر زیبا تبدیل می‌شود (قانون دائمی BrandMaker.md بخش ۲۲) |

### 🚀 روش بروزرسانی (پکیج update.zip)

1. **بکاپ بگیرید** (فایل‌ها + دیتابیس)
2. محتویات `update.zip` را در روت سایت‌ساز استخراج کنید (جایگزینی فایل‌ها)
3. پوشه `cache/` را خالی کنید (کش تصاویر واترمارک/OG بازتولید می‌شود)
4. **مهاجرت دیتابیس لازم نیست** — این نسخه فقط فایل دارد (بدون تغییر schema)
5. به «فونت‌ها» بروید و فونت‌های جدید را با دکمه «⬇️ دانلود و نصب» نصب کنید (یا «دانلود گروهی همه فونت‌های آزاد»)
6. برای واترمارک‌های جدید: مقالات ← «تولید مجدد تصاویر مقاله» یا صبر کنید کش wm3 خودکار ساخته شود

### 🔍 بعد از بروزرسانی چه بخواهید؟

- تست اتصال cPanel (تنظیمات cPanel ← 🔍 تست اتصال) — دیگر خطای ماژول Version نمی‌گیرد
- افزودن دستورات cron با دکمه ➕ (تنظیمات cPanel ← بخش Cron Jobs)
- تولید کدهای خطا برای یک برند (مثلاً ظرفشویی ال‌جی) — حالا ۱۴+ کد با فیلدهای دقیق
- قالب‌ساز ← دکمه «🖼️ کارتی» — پیش‌نمایش واقعی همه عناصر را ببینید

---

## 🆕 نسخه ۲.۱۲.۰ — رفع دو باگ + فاوآیکون + OG شفاف + قالب‌ساز حرفه‌ای + کلیدهای API

### 📋 چه چیزی تغییر می‌کند؟

| بخش | شرح |
|-----|-----|
| 🐛 رفع باگ خطایاب | دکمه «تولید کدهای خطای دستگاه» حالا واقعاً جستجوی آنلاین را فعال می‌کند (قبلاً چک‌باکس مقدار on می‌فرستاد و همیشه غیرفعال می‌ماند) |
| 🐛 رفع حذف تکراری‌ها | دکمه «حذف دستگاه‌های تکراری» حالا واقعاً پاک‌سازی را اجرا می‌کند + دکمه «ذخیره دستگاه‌ها» هم درست شد |
| 🔖 فاوآیکون برند | ویرایش برند ← تب اطلاعات: آپلود فاوآیکون اختصاصی (پیش‌فرض = لوگو) |
| 🖼 OG شفاف | لوگو بدون کادر سفید + پس‌زمینه مطابق روشنایی لوگو کنتراست می‌گیرد |
| 🪧 واترمارک‌ها | بزرگ‌تر و پررنگ‌تر در تصاویر مقاله (نسخه‌های قبلی خودکار بازتولید می‌شوند) |
| 🖼 تصاویر مقاله | دیگر کشیده/له نمی‌شوند — نسبت اصلی حفظ می‌شود |
| 🔤 فونت محیط پنل | تنظیمات ← تب «فونت محیط»: فونت متن/عنوان/کد پنل |
| 🎭 قالب‌ساز | ۱۸ عنصر جدید (جمع ۷۳) + تنظیمات دقیق‌تر + بلوک ترکیبی قابل ذخیره |
| ☰ منوها | انتخابگر آیکون + nofollow + افزودن سریع + پیش‌نمایش + کپی منو |
| 🔑 کلیدهای API | منوی «سیستم ← کلیدهای API»: ساخت کلید can_deploy بدون SQL |
| 🌐 Addon Domain | دیالوگ استقرار: انتخاب زیردامنه یا دامنه الحاقی |
| 🗄️ دیتابیس | ستون favicon + گسترش enum domain_type + جدول builder_blocks — **مهاجرت کاملاً خودکار** |

### 🎯 گام‌های ارتقا (۳ دقیقه)

1. **بکاپ** — از فایل‌ها + دیتابیس فعلی خروجی بگیرید
2. **آپلود update.zip** — محتویات را در ریشه سایت‌ساز استخراج کنید (`config.local.php` و آپلودها دست‌نخورده می‌مانند)
3. **مهاجرت خودکار** — با اولین بازکردن پنل، تغییرات دیتابیس خودکار اعمال می‌شود (نشانگر `cache/.schema_v212`)
4. **تست سریع**:
   - مدیریت کدهای خطا ← دکمه تولید دستگاه: باید جستجوی آنلاین اجرا شود
   - ویرایش یک برند با دستگاه تکراری ← دکمه پاک‌سازی: باید دیالوگ تأیید و حذف واقعی انجام شود
   - تنظیمات ← فونت محیط: فونت پنل را تغییر دهید و ذخیره کنید
   - سیستم ← کلیدهای API: کلید can_deploy بسازید (اگر API خارجی دارید)

> 💡 واترمارک‌های مقالات قبلی: با نسخه کش جدید (wm2) دفعه بعد که تصاویر درخواست شوند با اندازه بزرگ‌تر بازتولید می‌شوند — نیازی به کار دستی نیست.

---

## 🚀 نسخه ۲.۱۱.۰ — افزونه استقرار خودکار (ساخت زیردامنه + آپلود + SSL بدون ورود به cPanel)

### 📋 چه چیزی اضافه می‌شود؟

| بخش | شرح |
|-----|-----|
| 🚀 استقرار خودکار | یک کلیک: زیردامنه cPanel + آپلود فایل‌ها + استخراج + config/htaccess + SSL + تست — بدون ورود به cPanel |
| 💾 بکاپ شمسی | ۵ بکاپ آخر هر برند با نام‌گذاری تاریخ شمسی + بازیابی + rollback خودکار |
| 🩺 مانیتورینگ | داشبورد سلامت + ۳ فایل cron (سلامت/SSL/بکاپ) |
| 📂 الگوی Document Root | ۶ الگوی آماده + متغیرهای پویا + پیش‌نمایش زنده |
| ✏️ ویرایش نام زیردامنه | دیالوگ پیش‌نمایش + اعتبارسنجی ۸ قانون DNS + ۳ پیشنهاد جایگزین |
| 🔌 API خارجی | ۱۳ اندپوینت جدید (deploy/health/backup) با کلید مجاز can_deploy |
| 🗄️ دیتابیس | ۶ جدول جدید + ۱۲ ستون — **مهاجرت کاملاً خودکار** |

### 🎯 گام‌های ارتقا (۵ دقیقه)

1. **بکاپ** — از فایل‌ها + دیتابیس فعلی خروجی بگیرید
2. **آپلود update.zip** — محتویات را در ریشه سایت‌ساز استخراج کنید (فایل‌های شما overwrite می‌شوند — `config.local.php` و آپلودها دست‌نخورده می‌مانند)
3. **مهاجرت خودکار** — با اولین بازکردن پنل، جداول و ستون‌های جدید خودکار ساخته می‌شوند (نشانگر `cache/.schema_v211`)
4. **تنظیمات cPanel** — منوی جدید «⚙️ اتصال cPanel»:
   - آدرس سرور + پورت (2083) + نام کاربری + **API Token** (راهنما: cPanel ▸ Security ▸ Manage API Tokens ▸ توکن با دسترسی Unrestricted)
   - دامنه اصلی (مثلاً `ea-fixer.ir`)
   - ✅ سوئیچ «استقرار خودکار فعال» را روشن کنید
   - 🔍 دکمه «تست اتصال» را بزنید
5. **Cron Jobs** — سه خط زیر را در cPanel ▸ Cron Jobs اضافه کنید (مسیر را با سایت خودتان تنظیم کنید):
   ```
   */15 * * * * php /home/USERNAME/public_html/brandmaker/cron/health-check.php
   0 3 * * * php /home/USERNAME/public_html/brandmaker/cron/ssl-check.php
   0 2 * * 6 php /home/USERNAME/public_html/brandmaker/cron/backup.php
   ```

### ✅ راستی‌آزمایی پس از ارتقا

1. منوی «🚀 استقرار سایت‌ها» در سایدبار دیده می‌شود
2. «⚙️ اتصال cPanel» ▸ تست اتصال: ✅ اتصال برقرار است
3. یک برند آزمایشی را استقرار کنید — نوار پیشرفت ۱۱ مرحله باید کامل شود
4. «💾 بکاپ‌ها» ▸ بکاپ فوری — نام فایل باید تاریخ شمسی داشته باشد
5. «📊 سلامت سایت‌ها» ▸ بررسی فوری همه

### ⚠️ نکات مهم

- **API Token**: فقط یکبار نمایش داده می‌شود — در جای امن نگه دارید
- **رمزنگاری**: توکن و رمز FTP با AES-256 در دیتابیس ذخیره می‌شوند؛ کلید در `cache/.deploy_secret` — **این فایل را حذف نکنید** (در غیر این صورت توکن‌ها باید مجدداً وارد شوند)
- **API خارجی**: برای استقرار از بیرون، باید کلیدی با مجوز `can_deploy` بسازید (کلیدهای معمولی برندها اجازه استقرار ندارند)
- **برندهای موجود**: مسیر استقرار قبلی حفظ می‌شود — الگوی Document Root فقط برای برندهای جدید است
- **حجم آپدیت**: حدود ۳ مگابایت (کدهای جدید + مستندات)

### 🔄 تغییرات فنی

- ۱۴ کلاس جدید `core/` + ۷ صفحه `admin/` + ۳ اندپوینت `api/endpoints/` + پوشه `cron/` (با .htaccess محافظ)
- مهاجرت: `cpanel_settings`، `deployments`، `deployment_logs`، `site_health`، `backups`، `ssl_certificates` + ستون‌های `brands` و `api_keys.can_deploy`
- بدون تغییر در جداول موجود — سازگار رو به عقب کامل

---

# 🔄 راهنمای بروزرسانی — به نسخه ۲.۱۰.۰

> ⬆️ بالاترین بخش این فایل، راهنمای **نسخه ۲.۱۰.۰** است (دارایی‌های کامل روی سرور + رفع 404 پلیس‌هولدرها + فرمت چاپی درخواست + گزارش PDF آمار). بخش‌های پایین‌تر راهنمای ارتقاهای قبلی هستند.

---

## 🚀 نسخه ۲.۱۰.۰ — فونت‌ها و آیکون‌های کامل + چاپ درخواست و گزارش آمار

> **این نسخه برای شماست اگر:** می‌خواهید همه فونت‌های آزاد و آیکون‌ها از همان نصب اول روی سرور باشند (بدون کلیک دانلود)، تصاویر جایگزین مقالات بدون تصویر شاخص به‌جای 404 نمایش داده شوند، برگ درخواست چاپی برای تکنسین داشته باشید، یا از آمار خروجی PDF/چاپی بگیرید.

### 🔍 چه چیزی تغییر کرد؟ (۵ اولویت اصلی)

| # | تغییر | نتیجه |
|---|-------|-------|
| ۱ | 🔤 **۱۹ فونت متن‌باز از پیش نصب (۸۶ فایل / ۳MB)** + دکمه «دانلود گروهی همه فونت‌های آزاد» | سایت‌های برند از ابتدا فونت واقعی از سرور شما می‌گیرند — فونت‌های تجاری همچنان آپلود دستی |
| ۲ | 🖼️ **۱۱ پک آیکون از ۸ به ~۱۳۰ آیکون هر پک (+۱۳۹۶، جمع ۱۵۷۱ با برچسب فارسی ۹۴٪)** + پک‌های جدید آیکونویر و بوت‌استرپ | انتخابگر آیکون قالب‌ساز از همان اول پر و کاربردی است |
| ۳ | 🐛 **رفع 404 تصاویر جایگزین**: default.svg و article.svg و favicon.png ارجاع‌شده در کد وجود نداشتند | مقاله بدون تصویر شاخص و سایت برند بدون لوگو، تصویر زیبا می‌گیرند نه خطا |
| ۴ | 🖨️ **فرمت چاپی درخواست** (request-print.php): برگ کامل با سربرگ برند + امضای سه‌طرفه | چاپ/ذخیره PDF برگ درخواست برای تکنسین در یک کلیک |
| ۵ | 📊 **گزارش PDF آمار** (analytics-report.php): KPI + روند روزانه + تفکیک‌ها + شهرها + سهم برندها | خروجی چاپی/PDF از داشبورد آمار با همان فیلترها |

### 📦 مراحل (۲ دقیقه)

```text
۱. محتوای update.zip را روی روت سایت‌ساز آپلود و جایگزین کنید
   ⚠️ حجم آپدیت بیشتر از همیشه است (~۸MB) چون فونت و آیکون پیش‌نصب شدهاند — طبیعی است
۲. پوشه assets/fonts/ و assets/icons/ حتماً کامل آپلود شوند (۸۶ فونت + ~۱۴۰۰ آیکون جدید)
۳. فایل‌های جدید admin/request-print.php و admin/analytics-report.php آپلود شوند
۴. هیچ تغییری در دیتابیس لازم نیست ✅
۵. تنظیمات خودتان دست نخورده است ✅
```

### ✅ راستی‌آزمایی پس از آپدیت

| گام | بررسی | نتیجه مورد انتظار |
|:---:|-------|-------------------|
| ۱ | پنل → آیکون‌ها | هر پک ~۱۳۰+ آیکون با برچسب فارسی |
| ۲ | پنل → فونت‌ها | همه فونت‌های آزاد نشان «نصب ✓» + دکمه دانلود گروهی |
| ۳ | پنل → درخواست‌ها → یک درخواست → دکمه 🖨️ چاپ | برگ چاپی با سربرگ برند باز می‌شود |
| ۴ | پنل → آمار → دکمه «🖨️ گزارش PDF» | گزارش چاپی با فیلتر فعلی باز می‌شود |
| ۵ | یک سایت برند با مقاله بدون تصویر | تصویر جایگزین آبی نمایش داده می‌شود (نه خطا) |

---

# 🔄 راهنمای بروزرسانی — به نسخه ۲.۹.۰ (پیشین)

> بخش راهنمای نسخه ۲.۹.۰ (فونت فارسی ریشه‌ای + OG پویا + تلگرام URL-اول + تصاویر واقعی مقاله + خطایاب کامل‌فیلد + پنل برند + قالب‌ساز المنتوری). بخش‌های پایین‌تر راهنمای ارتقاهای قبلی هستند.

---

## 🚀 نسخه ۲.۹.۰ — فونت ریشه‌ای + OG پویا + تلگرام URL-اول + تصاویر واقعی + قالب‌ساز المنتوری

> **این نسخه برای شماست اگر:** فونت تصویر OG هنوز به‌هم‌ریخته است و حروف کلمات جدا هستند، آخر عنوان ناقص می‌ماند، لوگوی برند در OG کوچک است، پس‌زمینه OG را در هر تولید متفاوت می‌خواهید، ربات تلگرام موقع تحویل مقاله خطای «there is no document in the request» می‌دهد، تصاویر مقاله به موضوع ربط ندارند (لباسشویی برای راهنمای خرید یخچال!)، خطایاب AI همه فیلدها را کامل پر نمی‌کند، دستگاه‌های تکراری در برند دارید، پیش‌نمایش صفحه‌های برند می‌خواهید، پالت رنگ و انتخابگر رنگ حرفه‌ای می‌خواهید، یا قالب‌ساز با ستون‌بندی و عناصر بیشتر و طراحی زنده می‌خواهید.

### 🔍 چه چیزی تغییر کرد؟ (۹ اولویت اصلی)

| # | تغییر | نتیجه |
|---|-------|-------|
| ۱ | ✍️ **رفع ریشه‌ای فونت فارسی OG**: دو تابع کمکی شکل‌دهی حروف جا‌به‌جا صدا زده می‌شدند + باگ زنجیره فرم + نگاشت غلط «ؤ» + افزودن «ئ» | حروف هر کلمه به‌هم چسبیده و سالم — ۱۸/۱۸ تست |
| ۲ | 🎨 **OG پویا + لوگو بزرگ + عنوان کامل**: ۱۰ سبک پس‌زمینه (هر تولید متفاوت) + نشان لوگو ۲۰۰px + شکست هوشمند ۳ خطی با جا شدن تضمینی | لوگوی بزرگ و خوانا؛ عنوان هرگز بریده نمی‌شود؛ هر تولید ظاهر جدید |
| ۳ | 🤖 **تلگرام URL-اول**: سند/آلبوم با آدرس عمومی ارسال می‌شود (فقط JSON) + fallback multipart | رفع قطعی «there is no document» — حتی با اسکریپت واسط قدیمی کار می‌کند |
| ۴ | 🖼️ **تصاویر مقاله واقعی**: ریشه «لباسشویی برای یخچال» (دستگاه از عنوان استنتاج می‌شود — قبلاً تصادفی!) + ۹ عکس واقعی جدید + واترمارک دوتایی شفاف روی همه | تصویر دقیقاً مطابق محتوای مقاله، بدون متن داخل تصویر |
| ۵ | 🚨 **خطایاب کامل‌فیلد**: قطعه استاندارد مقدم بر زمینه نویزی + تطبیق سیستم قطعه↔عنوان + مدل‌های سازگار از متن + بودجه ۳۴s | همه فیلدها کامل و بدون اشتباه — تست زنده ۶/۶ |
| ۶ | 🧹 **حذف تکراری‌ها**: دکمه حذف دستگاه‌های تکراری + ادغام خودکار کدهای خطای تکراری | برند تمیز با یک کلیک |
| ۷ | 👁️ **پیش‌نمایش صفحه‌ها + پالت حرفه‌ای**: مودال رندر واقعی هر صفحه + پالت گروه‌بندی‌شده + پیش‌نمایش زنده + انتخابگر HSV | بازبینی قبل از انتشار + کنترل کامل رنگ |
| ۸ | 🎭 **قالب‌ساز المنتوری**: ستون‌بندی ۲-۴ ستونه تودرتو + ۵۵+ عنصر + طراحی زنده WYSIWYG | ساخت هر صفحه‌ای بدون کد |
| ۹ | 🌉 **اسکریپت گوگل v3**: فایل مستقل ریپو با فرمت v1 قدیمی بود (ناسازگار با PHP!) | هر منبعی که کپی کنید، درست است |

### 📦 مراحل (۳ دقیقه)

```text
۱. محتوای update.zip را روی روت سایت‌ساز آپلود و جایگزین کنید
۲. پوشه assets/images/articles/ حتماً آپلود شود (۹ عکس جدید)
۳. 🔄 توصیه: اسکریپت واسط گوگل را از پنل → تلگرام → کارت واسط، کپی و مجدداً Deploy کنید
   (نسخه v3 — البته تحویل سند حالا حتی بدون این کار هم با «URL» انجام می‌شود)
۴. مرورگر را با Ctrl+F5 باز کنید
۵. تمام! بدون تغییر دیتابیس، بدون تغییر تنظیمات
```

### 🗂️ فهرست فایل‌های تغییرکرده (۲۲ فایل + ۹ عکس)

```text
engine/utils/PersianGlyphs.php            ← رفع ریشه‌ای شکل‌دهی حروف (v2)
engine/services/AiImageGenerator.php      ← OG v3 (پس‌زمینه پویا + لوگو بزرگ + خودجا‌شو)
engine/services/ArticleImageService.php   ← v3: نگاشت اختصاصی + فیلتر موجود
engine/generators/ArticleGenerator.php    ← استنتاج دستگاه از عنوان دلخواه
engine/generators/ErrorCodeEngine.php     ← v3.9: قطعه استاندارد + تطبیق سیستم + مدل‌ها
engine/CommandAssistant.php               ← واژه‌های محاوره‌ای
engine/SahandAI.php                       ← موتور 3.9.0
telegram/TelegramBot.php                  ← URL-اول + پاک‌سازی موقت
assets/scripts/google-apps-script.js      ← v3 (فرمت v1 قدیمی → سازگار)
admin/brand-edit.php                      ← تکراری‌ها + پیش‌نمایش + پالت + انتخابگر
admin/brand-page-preview.php              ← 🆕 رندر پیش‌نمایش صفحه
admin/template-builder.php               ← v3 المنتوری (ستون + ۵۵ عنصر + زنده)
admin/template-preview.php               ← رندر تودرتو + بلوک‌های جدید
admin/telegram.php                        ← GAS v3
assets/images/articles/*.jpg              ← 🆕 ۹ عکس واقعی جدید
config.php                                ← نسخه 2.9.0
```

### ✅ راستی‌آزمایی بعد از ارتقا

```text
۱. پنل → مقالات → تولید مقاله با عنوان «راهنمای خرید یخچال» → تصویر شاخص باید «یخچال» باشد
۲. مقاله → دکمه تولید OG → فونت فارسی سالم + لوگوی بزرگ + دو بار تولید = دو پس‌زمینه متفاوت
۳. ربات تلگرام → «مقاله بنویس...» → سند HTML باید تحویل شود (بدون خطای document)
۴. پنل → کدهای خطا → تولید کدها → فیلدها کامل (دلایل ≥۵، راه‌حل ≥۵، قطعه سازگار با عنوان)
۵. پنل → برند → تب دستگاه‌ها → دکمه حذف تکراری‌ها (اگر باشد)
۶. پنل → برند → تب صفحه‌ها → دکمه 👁 پیش‌نمایش
۷. پنل → قالب‌ساز → بخش چندستینی اضافه کنید و بلوک‌ها را داخل ستون‌ها بیندازید
```

---

## 📜 راهنمای نسخه ۲.۷.۲ (قدیمی‌تر)

> راهنمای ارتقای ۲.۷.۲ و نسخه‌های قبل‌تر در ادامه آمده است.

---

## 🚀 نسخه ۲.۷.۲ — رفع تحویل مقاله ربات + OG تضمینی + خطایاب سریع + جدول کارتی موبایل

> **این نسخه برای شماست اگر:** ربات تلگرام موقع تحویل مقاله خطای «Call to undefined method CURLFile::getMime()» می‌دهد، فونت تصویر OG خراب و بهم‌ریخته است، زمینه شفاف لوگوها در تصویر مشکی می‌شود، تصاویر مقاله به موضوع ربط ندارند (لباسشویی برای مقاله یخچال!)، ستون‌های جدول‌ها در موبایل له شده و متن‌ها چندسطری‌اند، خطایاب AI هیچ کدی اضافه نمی‌کند، حذف دسته‌جمعی کدهای خطا می‌خواهید، یا کادر کد امنیتی لاگین باید زیر تصویر و بزرگ‌تر باشد.

### 🔍 چه چیزی تغییر کرد؟ (۷ اولویت اصلی)

| # | تغییر | نتیجه |
|---|-------|-------|
| ۱ | 🤖 **رفع کرش تحویل مقاله ربات**: متد `getMime()` روی CURLFile وجود ندارد (صحیح: `getMimeType()`) | تحویل مقاله با عکس در تلگرام سالم |
| ۲ | 🖼 **OG تضمینی**: وزیرمتن Bold+Regular بسته‌بندی‌شده در پروژه + اعتبارسنجی پوشش فرم‌های حروف + رفع باگ زمینه مشکی واترمارک (`imagecopymerge` آلفا را نادیده می‌گرفت → کلاس ImageWatermark پیکسل‌به‌پیکسل) | فونت همیشه سالم + هر دو لوگو با شفافیت کامل |
| ۳ | 🖼 **تصاویر مقاله مطابق موضوع**: نگاشت هر ۴۲ دستگاه + هر ۲۵ نوع مقاله + استنتاج دستگاه از عنوان + واترمارک برند (پایین-راست) و نمایندگی (پایین-چپ) روی همه تصاویر | تصویر یخچال برای مقاله یخچال؛ هرگز لباسشویی! |
| ۴ | 🚨 **خطایاب سریع/دقیق**: بودجه ۲۴ ثانیه (قبلاً ۸۰s = مرگ روی هاست اشتراکی قبل از درج!) + نقشه ۴۰+ کد شناخته‌شده + زمینه کد-محور + رفع نشت انگلیسی عنوان‌ها | کدها واقعاً ثبت می‌شوند؛ IE=آبرسانی نه پمپ تخلیه |
| ۵ | 📋 **جدول‌ها در موبایل کارت می‌شوند**: هر ردیف = کارت با «برچسب: مقدار» + کادرهای راهنما بلوکی با نوار رنگی | متن‌های ۲۰ سطری در موبایل حذف شد |
| ۶ | 🗑 **حذف دسته‌جمعی کدهای خطا**: دکمه «حذف نتایج فیلتر (تعداد)» — دقیقاً رکوردهای فیلتر برند/دستگاه/جستجو | پاکسازی سریع انبوه |
| ۷ | 🔐 **لاگین ستونی**: کادر کد زیر تصویر + بزرگ‌تر در موبایل + رفع باگ `SAHEND_VERSION` | کد امنیتی کاملاً خوانا |

### 📦 مراحل (۳ دقیقه)

```text
۱. محتوای update.zip را روی روت سایت‌ساز آپلود و جایگزین کنید (۱۵ فایل + ۲ فونت جدید)
۲. پوشه assets/fonts/fa/vazirmatn/ حتماً آپلود شود (فونت تضمینی OG)
۳. مرورگر را با Ctrl+F5 باز کنید (نسخه CSS/JS خودکار عوض می‌شود)
۴. تمام! بدون تغییر دیتابیس، بدون تغییر تنظیمات
```

### 🗂️ فهرست فایل‌های تغییرکرده (۱۷ فایل)

```text
telegram/TelegramBot.php                 ← رفع getMime → getMimeType
engine/services/AiImageGenerator.php     ← زنجیره فونت تضمینی + پوشش فرم‌ها + شفافیت
engine/services/ImageWatermark.php       ← 🆕 کلاس واترمارک آلفا-درست
engine/services/ArticleImageService.php ← v2: استنتاج دستگاه + نگاشت کامل + واترمارک
engine/generators/ErrorCodeEngine.php   ← v3.1: بودجه زمانی + نقشه کدها + دقت
engine/generators/ArticleGenerator.php  ← ارسال برند به انتخاب تصویر
engine/generators/SmartPipeline.php     ← استنتاج دستگاه + واترمارک
engine/SahandAI.php                     ← موتور 3.7.2
admin/login.php                         ← کپچای ستونی + رفع SAHEND_VERSION
admin/error-codes.php                   ← حذف دسته‌جمعی با فیلتر
admin/articles.php                      ← device_key در تولید مجدد تصاویر
assets/css/admin.css                    ← جدول کارتی موبایل + کادر راهنمای جدید
assets/js/admin.js                      ← sahandLabelTables (برچسب خودکار کارت‌ها)
assets/fonts/fa/vazirmatn/*.ttf         ← 🆕 فونت تضمینی (۲ فایل)
config.php                              ← نسخه 2.7.2
```

### ✅ راستی‌آزمایی پس از آپلود

1. **ربات تلگرام**: «مقاله بنویس درباره تعمیر برد یخچال ال‌جی» — مقاله با تصاویر تحویل می‌شود بدون خطای getMime
2. **OG**: در ویرایش مقاله «تولید OG» — فونت فارسی متصل و درست + لوگوی برند بالا + واترمارک نمایندگی گوشه بدون جعبه مشکی
3. **تصاویر مقاله جدید**: تصویر شاخص همان دستگاه مقاله است + دو واترمارک لوگو در گوشه‌های پایین
4. **خطایاب**: تولید کدهای یک دستگاه — تا ~۲۵ ثانیه، کدها با عنوان فارسی درست (IE=ورود آب) ثبت می‌شوند
5. **موبایل**: فهرست مقالات/کدها — هر ردیف یک کارت خوانا؛ کادرهای راهنمای آبی/سبز مرتب
6. **حذف دسته‌جمعی**: فیلتر برند+دستگاه بزنید → دکمه قرمز «حذف نتایج فیلتر»
7. **لاگین موبایل**: تصویر کد بالا، کادر ورود بزرگ زیر آن

> ⚠️ **نکته تصاویر واترمارک‌شده**: در اولین تولید هر مقاله، نسخه واترمارک‌شده تصاویر ساخته و کش می‌شود (پوشه uploads/articles/wm باید قابل نوشتن باشد — پیش‌فرض هست).

---

## 🚀 نسخه ۲.۷.۱ — رفع کرش ربات + OG برنددار واقعی + خطایاب دوفازی + مقاله موضوع‌محور

> **این نسخه برای شماست اگر:** ربات تلگرام روی همه پیام‌ها خطای «Return value must be of type array, true returned» می‌دهد، تصویر OG هنوز فونت/لوگو ندارد، ستون‌های جدول‌ها خراب و متن‌ها چندسطری‌اند، خطایاب فیلدهای خالی/غلط می‌گذارد، مقالات ربات بی‌ربط به موضوع درخواستی‌اند، یا کادر کد امنیتی لاگین کوچک است.

### 🔍 چه چیزی تغییر کرد؟ (۶ اولویت اصلی)

| # | تغییر | نتیجه |
|---|-------|-------|
| ۱ | 🤖 **رفع کرش ربات تلگرام**: تلگرام برای sendChatAction/answerCallbackQuery مقدار `true` برمی‌گرداند و متدهای API با TypeError می‌مردند — نرمال‌سازی نتیجه در هر ۴ مسیر | ربات روی همه پیام‌ها سالم |
| ۲ | 🖼 **OG واقعاً برنددار**: رفع باگ نام→اسلاگ فونت (ریشه عدم استفاده از فونت پیش‌فرض) + مبدل WOFF→TTF + @font-face در SVG + رستر لوگوهای SVG | فونت پیش‌فرض سایت + لوگوی برند + واترمارک نمایندگی |
| ۳ | 🚨 **خطایاب دوفازی**: جستجوی اختصاصی هر کد + خواندن متن کامل صفحه + رد کدهای بی‌شاهد + ادغام داده سازنده + استخراج مشخصات/محل واقعی | همه ۱۴ فیلد دقیق و کامل — بدون فیلد خالی |
| ۴ | ✍️ **مقاله موضوع‌محور**: موضوع درخواستی برای اولین بار به ژنراتور می‌رسد؛ outline ۷بخشی حول موضوع؛ فکت‌های وب در نثر؛ FAQ پرسش‌های واقعی همان موضوع | مقاله دقیقاً درباره همان چیزی که خواستید |
| ۵ | 📋 **جدول‌های سالم**: ریشه CSS (`overflow-wrap: anywhere` عرض ذاتی سلول را صفر می‌کرد) رفع شد | ستون‌های با عرض مناسب؛ متن تک‌سطری |
| ۶ | 🔐 **لاگین دوپنلی + کپچای بزرگ**: پنل برند کنار فرم + کادر کد امنیتی ۲۶px/۸۴px | کد امنیتی کاملاً خوانا |

### 📦 مراحل (۳ دقیقه)

```text
۱. فایل‌های تغییرکرده را روی روت سایت‌ساز آپلود و جایگزین کنید (۱۴ فایل — فهرست پایین)
۲. مرورگر را با Ctrl+F5 باز کنید (نسخه CSS خودکار عوض می‌شود)
۳. تمام! بدون تغییر دیتابیس، بدون تغییر تنظیمات
```

### 🗂️ فهرست فایل‌های تغییرکرده (۱۴ فایل)

```text
assets/css/admin.css            ← رفع عرض ستون‌ها + کادرهای راهنما
admin/login.php                 ← دوپنلی + کپچای بزرگ
config.php                      ← نسخه 2.7.1
core/Auth.php                   ← کپچا ۲۵۰×۸۴ مقیاس‌پذیر
telegram/TelegramBot.php        ← نرمال‌سازی نتیجه true (رفع کرش)
engine/SahandAI.php             ← research_context + موتور 3.7.1
engine/CommandAssistant.php     ← موضوع→عنوان + research=auto
engine/generators/SmartPipeline.php        ← custom_title + حفظ عنوان موضوعی
engine/generators/ArticleGenerator.php     ← outline موضوع‌محور ۷بخشی + فکت در نثر
engine/generators/ErrorCodeEngine.php      ← خطایاب دوفازی v3.0
engine/services/AiImageGenerator.php       ← زنجیره فونت/لوگو v2.7.1
engine/knowledge/error-codes-brands.json   ← رفع املای KB
templates/brand-core/css/style.css         ← رفع عرض ستون جدول‌های سایت
docs/AI-API-GUIDE.md + README/CHANGELOG/WORKLOG/CHECKLIST  ← مستندات
```

### ✅ راستی‌آزمایی پس از آپلود

1. **ربات تلگرام**: پیام بدهید — باید پاسخ بگیرید بدون «خطا در پردازش پیام»
2. **جدول‌ها**: فهرست مقالات/برندها — ستون‌ها عریض و متن‌ها تک‌سطری
3. **خطایاب**: تولید کدهای یک دستگاه — رکوردها باید منبع URL و مشخصات فنی واقعی داشته باشند
4. **مقاله ربات**: «مقاله بنویس درباره تعمیر برد یخچال ال‌جی» — مقاله باید درباره همان موضوع باشد نه موضوع عمومی
5. **لاگین**: کادر کد امنیتی بزرگ و کد تایپ‌شده کاملاً خوانا
6. **OG**: در ویرایش مقاله «تولید OG» — فونت همان فونت عنوان سایت‌ها + لوگوها

> ⚠️ **نکته فونت OG**: اگر فونت پیش‌فرض سایت فقط woff2 است، دفعه اول ممکن است چند ثانیه بیشتر طول بکشد (تلاش تهیه TTF) — بعدش کش می‌شود.

---

## 🚀 نسخه ۲.۷.۰ — سئو موضوع‌محور + خطایاب وب‌محور + نگارش v1.2

> **این نسخه برای شماست اگر:** می‌خواهید سئوی هر صفحه برند «مطابق عنوان و کاربرد خودش» بهینه شود (نه تبلیغ عمومی نمایندگی)، خطایاب کدهای خطا را از اینترنت استخراج کند با ۵ دلیل/۵ راه‌حل، متن‌ها خواناتر و روان‌تر باشند، تصویر OG با فونت و لوگوی خودتان ساخته شود، مقاله را قبل از انتشار پیش‌نمایش کنید، یا باگ‌های ربات بله/تنظیمات تلگرام/کپچا لپ‌تاپ/بهم‌ریختگی تنظیمات را رفع کنید.

### 🔍 چه چیزی تغییر کرد؟ (۱۴ اولویت اصلی)

| # | تغییر | نتیجه |
|---|-------|-------|
| ۱ | 🎯 **سئو موضوع‌محور صفحات**: هر نوع صفحه (درباره/خدمات/تماس/ضمانت/سؤالات/کدهای خطا/تعرفه/مقالات/خانه/اختصاصی) محتوای مکمل «مربوط به خودش» می‌گیرد؛ کلیدواژه از عنوان همان صفحه | دیگر همه صفحات تبلیغ نمایندگی نیستند؛ امتیاز تا ۱۰۰ |
| ۲ | 🚨 **خطایاب وب‌محور**: جستجوی اینترنتی منبع اصلی؛ اعتبارسنجی زمینه؛ استخراج دلایل/راه‌حل از منابع واقعی | کدهای واقعی‌تر با منبع + حداقل ۵ دلیل و ۵ راه‌حل |
| ۳ | ✒️ **نگارش فارسی v1.2**: «می‌باشد»→«است»، فعل‌های ساده، حذف حشو، ۲۵ واژه تخصصی تعمیرات | متن‌های روان‌تر و استاندارد |
| ۴ | 🛡 **بهبوددهنده HTML-امن**: ساختار محتوا (هدینگ/لیست/بولد) بعد از بهبود دیگر نابود نمی‌شود | کیفیت بصری مقالات حفظ می‌شود |
| ۵ | 🖼 **تصاویر OG برنددار**: فونت پیش‌فرض سایت‌ها + لوگوی برند + واترمارک نمایندگی | OG منحصربه‌فرد هر برند |
| ۶ | 👁 **پیش‌نمایش مقاله**: دکمه 👁 در فهرست و ویرایش — نمایش کامل شبیه سایت عمومی | بررسی قبل از انتشار |
| ۷ | 🐛 **ربات بله**: رفع خطای تحویل مقاله (آپلود به tapi.bale.ai به‌جای api.telegram.org) | تحویل کامل مقاله در بله |
| ۸ | 🐛 **تنظیمات تلگرام**: رفع پاک‌شدن متقابل تنظیمات ربات/واسط گوگل + آپلود فایل از واسط (base64) | هر دو فرم مستقل کار می‌کنند |
| ۹ | 🐛 **کپچای لپ‌تاپ**: رندر درون‌خطی داخل HTML — صفر درخواست شبکه | نمایش تضمینی در همه مرورگرها/ادبلاکرها |
| ۱۰ | 🐛 **بهم‌ریختگی تنظیمات**: ریشه (کوتیشن تودرتو در onclick) رفع شد؛ عرض محتوا تمام‌صفحه | صفحه تنظیمات تمیز |
| ۱۱ | 🎨 **لاگین جدید**: طراحی حرفه‌ای UI/UX Pro + نمایش رمز + حالت لودینگ | تجربه ورود مدرن |
| ۱۲ | 🔎 **جستجوی آیکون**: داخل همان کادر پک + ۲ پک جدید (آیکونویر ۱۶۷۱ / بوت‌استرپ ۲۰۵۰) | پیدا کردن سریع آیکون |
| ۱۳ | ⚙️ موتور 3.5→3.7 · سیستم 2.6→2.7 — بدون تغییر دیتابیس، بدون اندپوینت جدید | ارتقای عمیق کیفیت |

### 📦 مراحل (۳ دقیقه)

```text
۱. Sahand-Service-BrandMaker-v2.7.0-update.zip را دانلود و استخراج کنید
۲. همه فایل‌ها را روی روت سایت‌ساز آپلود و جایگزین کنید (۲۰ فایل — فهرست پایین)
۳. مرورگر را با Ctrl+F5 باز کنید (نسخه CSS/JS خودکار عوض می‌شود)
۴. تمام! بدون تغییر دیتابیس
```

### 🗂️ فهرست فایل‌های بسته آپدیت (۲۰ فایل)

```text
admin/articles.php            ← پیش‌نمایش مقاله + دکمه 👁 + لوگو در OG
admin/brand-edit.php          ← سئو موضوع‌محور (کلیدواژه خودکار از صفحه)
admin/captcha.php             ← حالت AJAX کپچا
admin/icons.php               ← جستجوی داخل پک
admin/login.php               ← بازطراحی کامل
admin/settings.php            ← رفع کوتیشن تودرتو
admin/telegram.php            ← scope فرم‌ها + GAS جدید
assets/icons/sources.json     ← ۲ پک جدید
assets/js/admin.js            ← addRepeatHTML کلون‌محور
bale/BaleBot.php              ← آپلود به tapi.bale.ai
config.php                    ← نسخه 2.7.0
core/Auth.php                 ← کپچای درون‌خطی
docs/AI-API-GUIDE.md          ← موتور ۳.۷
engine/SahandAI.php           ← ENGINE_VERSION 3.7.0
engine/generators/ContentImprover.php   ← HTML-امن
engine/generators/ErrorCodeEngine.php   ← وب‌محور + ۵ دلیل/راه‌حل
engine/generators/PageSeoImprover.php   ← موضوع‌محور
engine/services/AiImageGenerator.php    ← فونت/لوگو/واترمارک
engine/utils/PersianGrammar.php         ← v1.2
telegram/TelegramBot.php      ← آپلود از واسط + apiBaseUrl
```

### ✅ راستی‌آزمایی پس از آپلود

1. **لاگین**: کپچا باید بدون هیچ درخواست شبکه‌ای نمایش داده شود (DevTools → Network خالی از captcha)
2. **تنظیمات**: عرض تب‌ها تمام‌صفحه؛ ذخیره «ربات تلگرام» نباید تنظیمات «واسط گوگل» را پاک کند و بالعکس
3. **سئوی صفحه برند**: دکمه «بهبود خودکار» روی صفحه «تماس» باید محتوای «راه‌های ارتباطی» بگذارد نه تبلیغ
4. **خطایاب**: تولید کدهای یک دستگاه — گزارش باید «از جستجوی اینترنتی» را ذکر کند و هر رکورد ≥۵ دلیل/راه‌حل داشته باشد
5. **پیش‌نمایش**: دکمه 👁 کنار هر مقاله در فهرست

> ⚠️ **نکته ربات بله**: اگر قبلاً ربات بله را راه‌اندازی کرده‌اید، نیازی به تغییر نیست — فقط فایل‌ها جایگزین شوند.
> ⚠️ **نکته اسکریپت گوگل**: اگر واسط گوگل فعال است و می‌خواهید «آپلود فایل» (تحویل مقاله) هم از واسط عبور کند، اسکریپت داخل پنل (ربات تلگرام ← بخش واسط گوگل) را دوباره کپی و Deploy کنید — نسخه جدید files_b64 دارد.

---

# 🔄 راهنمای بروزرسانی — به نسخه ۲.۵.۰ (قدیمی)

> بخش‌های پایین‌تر راهنمای ارتقاهای قبلی (۲.۶.۰ قدیمی، ۲.۵.۰، …) هستند؛ راهنمای فعال ۲.۷.۰ در بالای فایل است. برای نسخه ۲.۶.۰ ببینید (خطایاب AI + یادگیری AI + تصاویر AI + ربات بله). بخش‌های پایین‌تر راهنمای ارتقاهای قبلی (۲.۵.۰، ۲.۴.۱، …) هستند.

---

## 🚀 نسخه ۲.۶.۰ — خطایاب AI + یادگیری AI + تصاویر AI + ربات بله

> **این نسخه برای شماست اگر:** می‌خواهید کدهای خطای واقعی هر برند/دستگاه با ۱۴ فیلد کامل تولید شود، ربات بله داشته باشید، تلگرام از واسط گوگل کار کند، تصاویر OG و مقاله خودکار با AI ساخته شوند، یا باگ‌های کپچا/تنظیمات/آیکون/دستگاه تکراری را رفع کنید.

### 🔍 چه چیزی تغییر کرد؟ (۱۸ اولویت اصلی)

| # | تغییر | نتیجه |
|---|-------|-------|
| ۱ | 🚨 **موتور خطایاب AI**: برند → دستگاه → تولید همه کدهای واقعی (۱۵۰ کد ۹ برند + جستجوی آنلاین) با ۱۴ فیلد + ویرایش + بهینه‌سازی تک‌فیلد AI | صفحه کدهای خطا کامل و واقعی |
| ۲ | 🧠 **موتور خودیادگیر** + پنل «یادگیری AI» | AI از بهبودهای موفق درس می‌گیرد؛ ارتقای اشتباه قابل غیرفعال‌سازی تک‌به‌تک |
| ۳ | 🖼️ **تصاویر AI یکتا**: OG هر مقاله + ۲ تصویر درون‌متن (PNG فارسی/SVG) + قابل تعویض | هر مقاله تصاویر اختصاصی مرتبط با موضوع خودش |
| ۴ | 💬 **ربات بله** (پنل + وب‌هوک + poll) | دستیار فارسی روی بله |
| ۵ | 🌉 **واسط Google Apps Script تلگرام** (relay دوطرفه + اسکریپت آماده + تست) | تلگرام در زمان تحریم از سرورهای گوگل |
| ۶ | 💬 **SahandDialog** — کادرهای تعاملی زیبا | جایگزین همه alert/confirm پیش‌فرض |
| ۷ | 🎯 دکمه «بهبود» کنار هر سنجه سئو | اصلاح انتخابی فقط همان سنجه |
| ۸ | 🚀 بهبود خودکار صفحه/کل برند تا ۱۰۰ | یک کلیک تا امتیاز کامل |
| ۹ | 🐛 کپچای لب‌تاپ (ادبلاکر) | اندپوینت خنثی + fallback — همیشه نمایش |
| ۱۰ | 🐛 سایدبار همیشه راست + تب‌های مقاوم تنظیمات | UI درست در همه دستگاه‌ها |
| ۱۱ | 🐛 دستگاه تکراری + «بهینه است با ۱ مورد» + نمایش آیکون‌ها | رفع کامل |
| ۱۲ | 🗑️ لغو نصب فونت/پک آیکون + 👁️ پیش‌نمایش فونت | مدیریت کامل دارایی‌ها |
| ۱۳ | 📅 سال شروع فعالیت نمایندگی | برای کارت‌های «... سال سابقه» |

### 📦 مراحل (۵ دقیقه)

```text
۱. Sahand-Service-BrandMaker-v2.6.0-update.zip را دانلود و استخراج کنید
۲. همه فایل‌ها را روی روت سایت‌ساز آپلود و جایگزین کنید
۳. پوشه bale/ جدید را کامل آپلود کنید
۴. یک‌بار پنل را باز کنید — مهاجرت خودکار دیتابیس (ستون‌های کد خطا + og_image) اجرا می‌شود
۵. Ctrl+F5 — تمام! ✅
```

> ⚠️ `config.local.php` و `uploads/` دست‌نخورده می‌مانند. بدون اجرای SQL دستی.

### ✅ بعد از آپدیت چک کنید

1. **کدهای خطا**: برند انتخاب کنید → فهرست دستگاه‌ها فیلتر می‌شود → «تولید همه کدها» → ویرایش یک کد → دکمه «✨ AI» کنار فیلدها
2. **یادگیری AI**: منوی جدید در سایدبار — پس از چند بهبود سئو، درس‌ها ثبت می‌شوند
3. **مقاله**: ویرایش یک مقاله → دکمه «🎨 تولید OG با AI» + دکمه‌های «🎯 بهبود» کنار سنجه‌ها
4. **ربات بله**: منوی جدید — توکن از @BotFather بله
5. **واسط گوگل**: ربات تلگرام ← کارت واسط → کپی اسکریپت → Deploy → تست
6. **تنظیمات**: تب‌ها — باز شدن صحیح و تمام‌عرض

 (کپچای تضمینی + اسکیل UI/UX Pro + بهینه PHP 8.3). بخش‌های پایین‌تر راهنمای ارتقاهای قبلی (۲.۴.۱، ۲.۴.۰، …) هستند.

---

## 🎨 نسخه ۲.۵.۰ — کپچای تضمینی + اسکیل UI/UX Pro + بهینه PHP 8.3

> **این نسخه برای شماست اگر:**
> - کد امنیتی صفحه ورود نمایش داده نمی‌شود (مخصوصاً بعد از تغییر نسخه PHP هاست)
> - می‌خواهید صفحات سایت‌ها با طراحی حرفه‌ای UI/UX ساخته شوند
> - PHP هاست را به ۸.۳ ارتقا داده‌اید و بهترین عملکرد را می‌خواهید

### 🔍 چه چیزی تغییر کرد؟

| # | تغییر | نتیجه |
|---|-------|-------|
| ۱ | کپچا با **رندر ۳ لایه‌ای تضمینی** بازنویسی شد (GD+فونت TTF → GD بزرگ‌نمایی نرم → SVG بدون وابستگی) | کد امنیتی **همیشه** نمایش داده می‌شود — حتی اگر GD خاموش باشد + حروف ~۲.۵ برابر درشت‌تر (بوم ۲۲۰×۷۲) |
| ۲ | **اسکیل UI/UX Pro** به سایت‌ساز اضافه شد: ۹ قانون UX + قواعد RTL فارسی + WCAG + بلوپرینت ۱۷ نوع صفحه | مرحله ۳ ساخت برند حالا چیدمان حرفه‌ای هر صفحه را تولید می‌کند + دکمه‌های طراحی/ممیزی/اصلاح خودکار در قالب‌ساز |
| ۳ | بهینه PHP 8.3: فایل `.user.ini` (OPcache + حافظه ۲۵۶M) + `json_validate` بومی + فشرده‌سازی zlib | سرعت ۲ تا ۵ برابر با OPcache + کارت «وضعیت سیستم» در داشبورد برای پایش |

### 📦 مراحل (۳ دقیقه)

```text
۱. فایل‌های نسخه جدید را از مخزن (یا بسته آپدیت رلیز) دانلود کنید
۲. همه فایل‌های تغییرکرده را روی روت سایت‌ساز آپلود و جایگزین کنید (فهرست پایین)
۳. ⚠️ فایل جدید .user.ini حتماً در روت آپلود شود (بهینه‌سازی PHP 8.3)
۴. پنل را یک‌بار Ctrl+F5 کنید — تمام! ✅
```

> 💡 بدون هیچ تغییر دیتابیسی — کاملاً file-level. `config.local.php` دست‌نخورده می‌ماند.

### 📁 فایل‌های تغییرکرده در نسخه ۲.۵.۰ (۱۳ فایل کد + ۶ فایل مستندات)

```
.user.ini                         ← 🆕 تنظیمات بهینه PHP 8.3 (OPcache/حافظه/زمان)
config.php                        ← نسخه 2.5.0 + json_validate + error_reporting هوشمند + zlib + autoloader اسکیل‌ها
core/Auth.php                     ← کپچای ۳ لایه‌ای تضمینی + حروف درشت
admin/login.php                   ← نمایش بهتر کپچا + دکمه رفرش مستقل
admin/index.php                   ← کارت «وضعیت سیستم و عملکرد» + ردیف اسکیل UI/UX Pro
assets/css/admin.css              ← استایل کپچای بزرگ‌تر + دکمه رفرش
engine/knowledge/uiux-patterns.json ← 🆕 پایگاه دانش اسکیل UI/UX Pro
engine/skills/UIUXPro.php         ← 🆕 کلاس اسکیل UI/UX Pro
engine/SahandAI.php               ← موتور ۳.۵.۰ + ۴ متد UI/UX
api/index.php                     ← ۴ اندپوینت جدید UI/UX
admin/brand-build.ajax.php        ← مرحله ۳: طراحی چیدمان صفحات با اسکیل
admin/brand-build.php             ← عنوان مرحله ۳ به‌روز
admin/template-builder.php        ← دکمه‌های طراحی/بررسی/اصلاح UX + پنل نتایج + requireLogin هندلرها
```

```
README.md / CHANGELOG.md / CHECKLIST.md / WORKLOG.md / UPGRADE.md / docs/AI-API-GUIDE.md
                                  ← 📖 مستندات: راهنمای API موتور ۳.۵ (۴۸ اندپوینت) + تاریخچه نسخه‌ها
                                  (آپلود این ۶ فایل اختیاری است — روی عملکرد اثری ندارند)
```

### ✅ بعد از آپدیت چک کنید

1. **صفحه ورود**: کد امنیتی باید با حروف درشت نمایش داده شود (اگر هنوز دیده نمی‌شود، یعنی کش مرورگر است — Ctrl+F5؛ رندر SVG حتی بدون GD کار می‌کند)
2. **داشبورد ← کارت «وضعیت سیستم و عملکرد»**: وضعیت OPcache را ببینید — اگر قرمز بود از cPanel (Select PHP Version ← Options ← opcache) فعالش کنید
3. **قالب‌ساز**: دکمه «✨ طراحی با UI/UX Pro» را بزنید و چیدمان پیشنهادی + امتیاز UX را ببینید
4. **ساخت برند جدید**: مرحله ۳ حالا «تولید محتوا + طراحی UI/UX Pro» است و چیدمان صفحات را تولید می‌کند

> 📌 سایت‌های برندِ قبلاً ساخته‌شده دست‌نخورده می‌مانند؛ برای بهره‌گیری از چیدمان حرفه‌ای، در قالب‌ساز دکمه «✨ طراحی با UI/UX Pro» را بزنید یا برند را بازتولید کنید.

---

## 🚑 هات‌فیکس ۲.۴.۱ — رفع باگ‌های حیاتی (الزامی برای همه کاربران ۲.۴.۰)

> **اگر بعد از بروزرسانی به ۲.۴.۰ این مشکلات را دارید، این هات‌فیکس برای شماست:**
> - داشبورد خالی است و منو (☰) باز نمی‌شود
> - دکمه «🎯 پیشنهاد بهترین عنوان سئو» خطای «Unexpected end of JSON input» می‌دهد
> - دکمه «🎯 بهبود سئو» خطای «Unexpected token '<'» می‌دهد

### 🔍 علت (خلاصه فنی)
1. **متد تکراری** `researchTopic()` در موتور AI باعث خطای مرگبار بارگذاری کلاس می‌شد → داشبورد خالی + منوی بی‌جان (از نسخه ۲.۳.۰)
2. تابع `trim()` روی متن فارسی **UTF-8 را خراب** می‌کرد → پاسخ خالی سرور در پیشنهاد عنوان
3. تابع `mb_strtok()` اصلاً در PHP وجود ندارد → خطای سرور در بهبود سئو

### 📦 مراحل (۲ دقیقه)

```text
۱. بسته Sahand-Service-BrandMaker-v2.4.1-update.zip را از [صفحه رلیز](https://github.com/pranses2011/Sahand-Service-BrandMaker/releases) دانلود کنید
۲. محتویات را استخراج و همه فایل‌ها را روی روت سایت ساز آپلود و جایگزین کنید
۳. پنل را یک‌بار Ctrl+F5 کنید — تمام! ✅
```

> 💡 بدون هیچ تغییر دیتابیسی — کاملاً file-level. `config.local.php` دست‌نخورده می‌ماند.

### 📁 فایل‌های جایگزین‌شده در بسته v2.4.1 (۱۴ فایل)

```
config.php                        ← ۲.۴.۱ + پلی‌فیل PHP ۷.۴ + هندلر AJAX-آگاه
engine/SahandAI.php               ← موتور ۳.۴.۱ — رفع متد تکراری researchTopic
engine/utils/PersianGrammar.php   ← رفع mb_strtok + نیم‌فاصله‌های واقعاً کارا + نقطه دوباره
engine/generators/TitleGenerator.php ← mb_trim — رفع پاسخ خالی پیشنهاد عنوان
engine/generators/SeoImprover.php    ← mb_trim + کلیدواژه‌های تمیز + Throwable
engine/generators/ArticleGenerator.php ← Throwable (تولید مقاله مقاوم‌تر)
engine/generators/ContentImprover.php  ← mb_rtrim
engine/analyzers/KeywordAnalyzer.php  ← mb_trim (۵ مورد)
includes/helpers.php              ← mb_trim/mb_rtrim جدید + json_response ضدخرابی
admin/articles.php                ← Throwable + هدر X-Requested-With
admin/index.php                   ← Throwable — داشبورد دیگر هرگز خالی نمی‌ماند
admin/includes/header.php         ← admin.js به هدر منتقل شد (منو همیشه کار می‌کند)
admin/includes/footer.php         ← admin.js از فوتر حذف شد
admin/brand-build.ajax.php        ← Throwable
api/index.php                     ← Throwable (۵ مورد)
api/middleware/rate-limit.php     ← Throwable (۲ مورد)
```

### ✅ راستی‌آزمایی پس از هات‌فیکس

| # | بررسی | انتظار |
|:-:|-------|--------|
| ۱ | پایین پنل مدیریت | نسخه **2.4.1** |
| ۲ | داشبورد | کارت‌های آمار + جدول وضعیت موتور AI + بازشدن منو با ☰ |
| ۳ | مقالات ← تولید جدید ← عنوان دلخواه با حرف اول «آ/ب/د/س/…» ← دکمه پیشنهاد | پنل تحلیل + ۸ پیشنهاد (بدون خطای JSON) |
| ۴ | ویرایش مقاله ← دکمه «🎯 بهبود سئو» | گزارش قبل/بعد + فهرست اصلاحات (بدون خطای '<') |
| ۵ | تولید مقاله | مقاله کامل با نگارش اصلاح‌شده (می‌کند → می‌کندِ درست با نیم‌فاصله) |

---

## ⚠️ قبل از شروع (الزامی)

| # | اقدام | توضیح |
|:-:|-------|-------|
| ۱ | 🗄️ **بکاپ کامل دیتابیس** | از phpMyAdmin → Export → SQL (کامل، با همه جدول‌ها) |
| ۲ | 📁 **بکاپ فایل‌ها** | کل پوشه سایت ساز را ZIP کنید (مخصوصاً `config.local.php` و `uploads/`) |
| ۳ | ✅ **ثبت نسخه فعلی** | پایین پنل مدیریت یا `config.php` → باید `2.3.0` باشد |

> 💡 زمان موردنیاز: حدود ۵ دقیقه | ⏸️ قطعی سرویس: لازم نیست (بدون تغییر دیتابیس)

---

## 🎯 تغییرات مهم این نسخه

### 📝 موتور نگارش فارسی — قلب جدید خط تولید
از این نسخه **همه محتوای تولیدی** (مقالات + صفحات برند) به‌صورت خودکار از یک نگارش‌گر فارسی عبور می‌کند: نیم‌فاصله‌ها اصلاح، علائم سجاوندی استاندارد، املای رایج درست، ارقام فارسی و **هم‌خوانی فعل و فاعل** («ما می‌کند» → «ما می‌کنیم»). کیفیت محتوا حالا **۸ بُعدی** است و بُعد جدید «نگارش فارسی» با وزن ۱۰ امتیاز دارد.

### 🎯 عنوان دلخواه + 📊 آمار سئو زیر هر مقاله
- موقع تولید مقاله می‌توانید **عنوان دلخواه** بنویسید و با دکمه «🎯 پیشنهاد بهترین عنوان سئو» بهترین نسخه (رتبه‌بندی‌شده با امتیاز) را جایگزین کنید — مقاله دقیقاً حول همان عنوان نوشته می‌شود
- زیر ویرایشگر هر مقاله، پنل **۱۲ سنجه سئو** + دکمه **«🎯 بهبود سئو»** که ۸ اصلاح خودکار را اعمال و گزارش قبل/بعد می‌دهد

### 🚀 سایر قابلیت‌های جدید
- 📰 **۲۵ نوع مقاله** (از ۹) — سلکت گروه‌بندی‌شده ۵ دسته
- 🔎 **جستجوی اینترنت هنگام نوشتن** (چک‌باکس «استفاده از جستجوی اینترنت») — داده‌های واقعی + پرسش‌های کاربران + منابع
- 🖼️ **تصاویر خودکار** در متن مقالات (پیش‌فرض روشن)
- ⬇️ **دانلود مستقیم فونت/آیکون روی سرور** — دکمه «⬇️ دانلود و نصب» در صفحات فونت/آیکون
- 🤖 **ربات تلگرام v2.2**: `/grammar` + `/suggest` + `/types` + معادل فارسی
- 🔌 **۳ اندپوینت جدید API** (مجموع ۴۴)

**💡 بدون هیچ تغییر دیتابیسی** — این نسخه کاملاً file-level است.

---

## 📦 روش ۱: بسته بروزرسانی (توصیه‌شده)

بسته `Sahand-Service-BrandMaker-v2.4.0-update.zip` از [صفحه رلیز](https://github.com/pranses2011/Sahand-Service-BrandMaker/releases) دانلود کنید. این بسته **فقط فایل‌های تغییرکرده** را شامل می‌شود.

### مراحل

```text
۱. محتویات ZIP بروزرسانی را استخراج کنید
۲. کل فایل‌ها را با FTP/File Manager روی هاست آپلود و جایگزین کنید
   (مسیرها داخل بسته از قبل درست است — فقط همه را روی روت سایت ساز کپی کنید)
۳. مطمئن شوید پوشه cache قابل نوشتن است (مجوز 755 کافی است)
۴. پنل مدیریت را رفرش کنید — تمام! ✅ (کش مرورگر خودکار می‌شکند)
```

### فایل‌های جایگزین‌شده در بسته بروزرسانی v2.4.0

```
config.php                        ← نسخه 2.4.0
core/AssetDownloader.php          ← 🆕 دانلود مستقیم فونت/آیکون روی سرور (بخش ۱ فاز Q)
engine/SahandAI.php               ← موتور v3.4.0 (+suggestArticleTitles/articleSeoStats/improveArticleSeo + custom_title/research/with_images)
engine/utils/PersianGrammar.php   ← 🆕 موتور قواعد نگارشی فارسی v1.1 (نیم‌فاصله/سجاوندی/املای رایج/هم‌خوانی فعل و فاعل)
engine/analyzers/QualityScorer.php ← v2.1 — بُعد هشتم «نگارش فارسی»
engine/generators/ArticleGenerator.php ← v2.3 (اصلاح نگارشی + ۱۶ outline جدید + custom_title + research + images)
engine/generators/ContentGenerator.php ← v1.1 (اصلاح نگارشی خروجی)
engine/generators/ContentImprover.php  ← استراتژی fixGrammar
engine/generators/TitleGenerator.php   ← v3.1 (suggestForCustom)
engine/generators/SeoImprover.php      ← 🆕 v1.0 (۱۲ سنجه سئو + ۸ اصلاح خودکار)
engine/CommandAssistant.php       ← ۳ intent جدید (نگارش/پیشنهاد عنوان سئو/انواع مقاله)
engine/knowledge/templates.json   ← ۲۵ نوع مقاله / ۱۳۶ قالب عنوان
api/index.php                     ← ۳ اندپوینت جدید (۴۴ مجموع)
telegram/TelegramBot.php          ← v2.2 — ۳ فرمان جدید + کیبورد و راهنمای جدید
admin/articles.php                ← عنوان دلخواه + پیشنهاد سئو + پنل آمار سئو + دکمه بهبود + چک‌باکس جستجو/تصویر
admin/brand-build.ajax.php        ← تولید اولیه ۸ مقاله با انواع متنوع
admin/fonts.php + admin/icons.php ← دکمه‌های دانلود مستقیم (بخش ۱ فاز Q)
assets/fonts/sources.json + assets/icons/sources.json ← 🆕 منابع رسمی دانلود
README.md / CHANGELOG.md / UPGRADE.md / CHECKLIST.md / WORKLOG.md
docs/AI-API-GUIDE.md              ← مستندات v3.4 (۱۹۳۲ خط)
```

> ⚠️ توجه: `config.local.php` جایگزین **نمی‌شود** (تنظیمات شما دست‌نخورده می‌ماند) — این فایل در بسته نیست.

---

## 📦 روش ۲: بسته کامل نصب (اگر تغییرات دستی زیاد دارید)

اگر فایل‌های هسته را دستی تغییر داده‌اید، بسته کامل `install.zip` را نصب کنید و `config.local.php` + `uploads/` + فایل‌های سفارشی خود را از بکاپ برگردانید.

---

## ✅ راستی‌آزمایی پس از بروزرسانی

| # | بررسی | انتظار |
|:-:|-------|--------|
| ۱ | پایین پنل مدیریت | نسخه **2.4.0** |
| ۲ | مقالات ← تولید جدید | فیلد «عنوان دلخواه» + دکمه «🎯 پیشنهاد بهترین عنوان سئو» + چک‌باکس جستجو/تصویر |
| ۳ | تولید با عنوان دلخواه + زدن دکمه پیشنهاد | پنل تحلیل با امتیاز + ۸ پیشنهاد؛ کلیک → جایگزینی عنوان |
| ۴ | ویرایش هر مقاله | پنل «📊 آمار کامل سئو» (۱۲ ردیف) + دکمه «🎯 بهبود سئو» |
| ۵ | زدن «بهبود سئو» روی مقاله با ایراد | گزارش قبل/بعد + فهرست اصلاحات + رفرش → محتوای اصلاح‌شده |
| ۶ | نوع مقاله | لیست گروه‌بندی‌شده ۲۵ نوع در ۵ دسته |
| ۷ | فونت‌ها/آیکون‌ها | دکمه «⬇️ دانلود و نصب» سبز کار می‌کند |
| ۸ | ربات تلگرام: `/status` | موتور AI نسخه 3.4.0 |
| ۹ | ربات تلگرام: `/نگارش ما این کار را انجام می دهیم` | امتیاز + ایرادها + نسخه اصلاح‌شده (می‌دهیم → می‌دهیم با نیم‌فاصله) |
| ۱۰ | ربات تلگرام: `/پیشنهاد یخچال سرد نمی‌کند` | بهترین عنوان سئو + سایر پیشنهادها |

---

---

<a name="upgrade-22-23"></a>
## ⬆️ ارتقای ۲.۲ → ۲.۳ (نسخه 2.3.0)

> این راهنما نحوه ارتقای سایت ساز برند سهند سرویس به نسخه **۲.۳.۰ (موتور ۳.۳ — 🏅 سئوی E-E-A-T + 📚 ویکی‌پدیا + ⚡ مسابقه موازی جستجو + 👁️ پیش‌نمایش زنده قالب‌ها + رفع ۵ باگ)** را توضیح می‌دهد.
>
> - ارتقا از **۲.۲.۰** → فقط مراحل همین بخش (پیشنهادی)
> - ارتقا از **۲.۱.۰ یا قدیمی‌تر** → اول [راهنمای ارتقای ۲.۱ → ۲.۲](#--ارتقای-21--22-نسخه-220) پایین همین فایل، سپس مراحل همین بخش

---

## ⚠️ قبل از شروع (الزامی)

| # | اقدام | توضیح |
|:-:|-------|-------|
| ۱ | 🗄️ **بکاپ کامل دیتابیس** | از phpMyAdmin → Export → SQL (کامل، با همه جدول‌ها) |
| ۲ | 📁 **بکاپ فایل‌ها** | کل پوشه سایت ساز را ZIP کنید (مخصوصاً `config.local.php` و `uploads/`) |
| ۳ | ✅ **ثبت نسخه فعلی** | پایین پنل مدیریت یا `config.php` → باید `2.2.0` باشد |

> 💡 زمان موردنیاز: حدود ۵ دقیقه | ⏸️ قطعی سرویس: لازم نیست (بدون تغییر دیتابیس)

---

## 🎯 تغییرات مهم این نسخه

### 🐛 رفع ۵ باگ گزارش‌شده کاربر
1. **صفحه ساخت برند** — متن‌ها به صورت خام بیرون می‌زدند (تگ PHP اضافی) → کاملاً سالم و ریسپانسیو
2. **خطای مرحله ۱ ساخت برند (تحلیل لوگو)** — حالا **SVG کامل پشتیبانی می‌شود** (استخراج رنگ از fill/stroke بدون Imagick) + باگ داخلی برای لوگوهای تک‌رنگ رفع شد + تحلیل ناموفق دیگر هرگز ساخت را متوقف نمی‌کند (پالت پیش‌فرض اعمال و ادامه)
3. **فیلتر بالای کارت‌ها نصفه دیده می‌شد** — فیلترها حالا در موبایل wrap و تمام‌عرض می‌شوند
4. **صفحه تنظیمات در موبایل** — تب‌ها اسکرول افقی نرم شدند + دکمه ذخیره تمام‌عرض زیر تب‌ها
5. **پیش‌نمایش قالب‌ها** — پیش‌نمایش زنده با محتوای نمونه + سوئیچ موبایل/تبلت/دسکتاپ + وایرفریم داخل بوم

### 🧊 مهم: کش مرورگر خودکار می‌شکند
از این نسخه، همه فایل‌های CSS/JS با پارامتر نسخه (زمان آخرین تغییر فایل) لود می‌شوند. یعنی بعد از بروزرسانی، **مرورگر به صورت خودکار فایل‌های جدید را می‌گیرد** و دیگر صفحه به‌هم‌ریختهِ قدیمی از کش نشان داده نمی‌شود. اگر باز هم چیدمان قدیمی دیدید، یک‌بار Ctrl+F5 بزنید.

### 🚀 قابلیت‌های جدید
- 🏅 **سئوی E-E-A-T**: چک‌لیست ۸ معیاری + موجودیت‌ها + خوشه کلیدواژه + People Also Ask + تحلیل شکاف محتوایی رقبا — ۲ اندپوینت جدید `seo-package` و `eeat-check` (مجموع ۴۱)
- 📰 **مقاله حرفه‌ای‌تر**: فهرست مطالب + زمان مطالعه + جعبه نکته/هشدار + مزایا-معایب + باکس آمار + حداقل ۱۰۰۰ کلمه
- 🌐 **جستجوی ۹ ارائه‌دهنده**: ویکی‌پدیا فارسی/انگلیسی + Startpage + مسابقه موازی (تا ۵۰٪ سریع‌تر) + تازگی و اعتماد دامنه
- 🤖 **ربات تلگرام v2.1**: ۴ فرمان جدید `/seo` `/eeat` `/plan` `/faq` + زبان طبیعی

**💡 بدون هیچ تغییر دیتابیسی** — این نسخه کاملاً file-level است.

---

## 📦 روش ۱: بسته بروزرسانی (توصیه‌شده)

بسته `Sahand-Service-BrandMaker-v2.3.0-update.zip` از [صفحه رلیز](https://github.com/pranses2011/Sahand-Service-BrandMaker/releases) دانلود کنید. این بسته **فقط فایل‌های تغییرکرده** را شامل می‌شود.

### مراحل

```text
۱. محتویات ZIP بروزرسانی را استخراج کنید
۲. کل فایل‌ها را با FTP/File Manager روی هاست آپلود و جایگزین کنید
   (مسیرها داخل بسته از قبل درست است — فقط همه را روی روت سایت ساز کپی کنید)
۳. مطمئن شوید پوشه cache/engine قابل نوشتن است (مجوز 755 کافی است)
۴. پنل مدیریت را رفرش کنید — تمام! ✅ (کش مرورگر خودکار می‌شکند)
```

### فایل‌های جایگزین‌شده در بسته بروزرسانی v2.3.0

```
config.php                        ← نسخه 2.3.0
core/ImageProcessor.php           ← 🆕 پشتیبانی کامل SVG/SVGZ (استخراج رنگ بدون Imagick)
engine/analyzers/ColorAnalyzer.php ← مسیر SVG + رفع باگ imagedestroy + analyzeFromPixels
engine/SahandAI.php               ← موتور v3.3.0 (+seoPackage/researchTopic/eeatCheck)
engine/CommandAssistant.php       ← ۴ intent جدید (سئو/اعتمادسنجی/برنامه محتوا/سوالات متداول)
engine/generators/ArticleGenerator.php ← v2.1 (TOC + زمان مطالعه + کال‌اوت + مزایا-معایب)
engine/generators/SeoGenerator.php     ← v1.2 (E-E-A-T + موجودیت + خوشه + شکاف محتوایی)
engine/services/WebSearchService.php   ← v1.2 (ویکی‌پدیا + Startpage + مسابقه موازی + trust/freshness)
api/index.php                     ← ۲ اندپوینت جدید (seo-package + eeat-check)
telegram/TelegramBot.php          ← v2.1 — ۴ فرمان جدید + فرمت‌دهی‌های جدید
admin/brand-new.php               ← 🐛 رفع باگ تگ PHP اضافی (صفحه ساخت برند)
admin/brand-build.ajax.php        ← 🐛 مرحله ۱ fallback نرم (هرگز متوقف نمی‌شود)
admin/settings.php                ← 🐛 نوار تب‌های جدید ریسپانسیو
admin/brands.php / articles.php / requests.php / error-codes.php / brand-edit.php ← 🐛 فیلترهای ریسپانسیو
admin/template-builder.php        ← 👁️ پیش‌نمایش زنده + وایرفریم + asset_ver
admin/template-preview.php        ← 🆕 رندر پیش‌نمایش زنده چیدمان
admin/includes/header.php + footer.php + login.php ← asset_ver (شکستن کش)
includes/helpers.php              ← 🆕 تابع asset_ver()
assets/css/admin.css              ← tabs-bar + tools ریسپانسیو + polish موبایل
assets/css/builder.css            ← استایل وایرفریم + مودال پیش‌نمایش
templates/brand-core/css/style.css ← استایل TOC/کال‌اوت/مزایا-معایب/آمار مقاله
README.md / CHANGELOG.md / UPGRADE.md / CHECKLIST.md / WORKLOG.md
docs/AI-API-GUIDE.md              ← مستندات v3.3 (۱۸۰۰+ خط)
```

> ⚠️ توجه: `config.local.php` جایگزین **نمی‌شود** (تنظیمات شما دست‌نخورده می‌ماند) — این فایل در بسته نیست.

---

## 📦 روش ۲: بسته کامل نصب (اگر تغییرات دستی زیاد دارید)

اگر فایل‌های هسته را دستی تغییر داده‌اید، بسته کامل `install.zip` را نصب کنید و `config.local.php` + `uploads/` + فایل‌های سفارشی خود را از بکاپ برگردانید.

---

## ✅ راستی‌آزمایی پس از بروزرسانی

| # | بررسی | انتظار |
|:-:|-------|--------|
| ۱ | پایین پنل مدیریت | نسخه **2.3.0** |
| ۲ | تنظیمات ← با موبایل (یا DevTools ← 390px) | تب‌ها اسکرول افقی + دکمه ذخیره تمام‌عرض |
| ۳ | مدیریت برندها ← موبایل | فیلتر جستجو/وضعیت کاملاً داخل کادر |
| ۴ | برند جدید ← افزودن | فرم تمیز بدون متن خام |
| ۵ | ساخت برند با **لوگو SVG** | مرحله ۱ با موفقیت (بدون خطا) + پالت رنگ |
| ۶ | قالب‌ها ← ویرایش قالب | دکمه «👁️ پیش‌نمایش زنده» کار می‌کند |
| ۷ | ربات تلگرام: `/status` | موتور AI نسخه 3.3.0 |
| ۸ | ربات تلگرام: `/seo تعمیر یخچال` | پکیج سئو با E-E-A-T و موجودیت‌ها |

---

---

<a name="upgrade-21-22"></a>
## ⬆️ ارتقای ۲.۱ → ۲.۲ (نسخه 2.2.0)

> کاربرانی که از ۲.۱ یا قدیمی‌تر ارتقا می‌دهند، اول این بخش را انجام دهند و سپس بخش اصلی بالای فایل را برای رسیدن به ۲.۳.۰.

### ⚠️ قبل از شروع (الزامی)

| # | اقدام | توضیح |
|:-:|-------|-------|
| ۱ | 🗄️ **بکاپ کامل دیتابیس** | از phpMyAdmin → Export → SQL (کامل، با همه جدول‌ها) |
| ۲ | 📁 **بکاپ فایل‌ها** | کل پوشه سایت ساز را ZIP کنید (مخصوصاً `config.local.php` و `uploads/`) |
| ۳ | ✅ **ثبت نسخه فعلی** | پایین پنل مدیریت یا `config.php` → باید `2.1.0` باشد |

> 💡 زمان موردنیاز: حدود ۵ دقیقه | ⏸️ قطعی سرویس: لازم نیست (بدون تغییر دیتابیس)

---

## 🎯 تغییرات مهم این نسخه

- 🐛 **رفع باگ «منو در وسط صفحه باز می‌شود»** در پنل موبایل + **ریسپانسیو کامل** پنل، سایت‌های برند و صفحه نصب
- 🖼️ **۳ تصویر مرتبط برای هر مقاله** (۹ تصویر فوتورئال جدید بسته‌بندی شد + درج خودکار در جایگاه‌های استراتژیک)
- 📰 **تحویل کامل مقاله در تلگرام**: «مقاله بنویس...» یا `/article موضوع` → فایل HTML آماده انتشار برای هر سایت دیگر + نسخه Markdown + آلبوم ۳ عکس — **بدون نیاز به ورود به سایت ساز**
- 🧠 **موتور ۳.۲**: غنی‌سازی ساختاری مقاله (فهرست مطالب + سایر پرسش‌های کاربران + داده‌های تازه وب با منبع + جعبه E-E-A-T) + تحقیق وب خودکار (auto) + امتیاز هدف ۸۸
- 🌐 **جستجوی وب قوی‌تر**: ارائه‌دهنده Mojeek (مجموع ۷) + فرصت‌های کلیدواژه long-tail + تلاش مجدد خودکار
- 📰 **اخبار زنده**: `POST /api/ai/news` + فرمان «اخبار: ...» در دستیار و ربات
- 🔍 **سئو عمیق‌تر**: HowTo Schema + Speakable + تراکم کلیدواژه + زمان مطالعه + og:image
- 🔌 ۲ اندپوینت API جدید: `news`، `article-images` (مجموع ۳۹)

**💡 بدون هیچ تغییر دیتابیسی** — این نسخه کاملاً file-level است؛ جداول، seedها و داده‌ها بدون تغییر می‌مانند.

---

## 📦 روش ۱: بسته بروزرسانی (توصیه‌شده)

بسته `Sahand-Service-BrandMaker-v2.2.0-update.zip` (از [رلیز v2.2.0](https://github.com/pranses2011/Sahand-Service-BrandMaker/releases/tag/v2.2.0)) از [صفحه رلیز](https://github.com/pranses2011/Sahand-Service-BrandMaker/releases) دانلود کنید. این بسته **فقط فایل‌های تغییرکرده** را شامل می‌شود.

### مراحل

```text
۱. محتویات ZIP بروزرسانی را استخراج کنید
۲. کل فایل‌ها را با FTP/File Manager روی هاست آپلود و جایگزین کنید
   (مسیرها داخل بسته از قبل درست است — فقط همه را روی روت سایت ساز کپی کنید)
۳. مطمئن شوید پوشه cache/engine قابل نوشتن است (مجوز 755 کافی است)
۴. پنل مدیریت را رفرش کنید — تمام! ✅
```

### فایل‌های جایگزین‌شده در بسته بروزرسانی v2.2.0

```
config.php                        ← نسخه 2.2.0
install.php                       ← ریسپانسیو مرحله‌بندی نصب
api/index.php                     ← ۲ اندپوینت جدید (news + article-images)
engine/SahandAI.php               ← موتور v3.2.0
engine/CommandAssistant.php       ← نیّت اخبار + موضوع دلخواه مقاله + no_cache
engine/generators/SmartPipeline.php        ← خط تولید ۱۱ مرحله‌ای v3.2
engine/generators/SeoGenerator.php         ← v1.1 (HowTo + تراکم + Speakable)
engine/services/WebSearchService.php       ← v1.1 (Mojeek + اخبار + opportunities)
engine/services/ArticleImageService.php    ← 🆕 سرویس ۳ تصویر مقاله
telegram/TelegramBot.php          ← 🆕 v2 — تحویل کامل مقاله + ۱۲ فرمان + callback
admin/includes/header.php         ← پس‌زمینه سایدبار + aria
assets/css/admin.css              ← 🐛 رفع باگ منو + ریسپانسیو کامل پنل
assets/js/admin.js                ← toggleSidebar هوشمند + بستن خودکار
templates/brand-core/css/style.css        ← منوی موبایل جدید + ریسپانسیو + عناصر v3.2
templates/brand-core/includes/header.php  ← منوی aria + toggleNav
templates/brand-core/js/app.js            ← toggleNav + بستن خودکار
assets/images/articles/*.jpg      ← 🆕 ۹ تصویر تخصصی مقاله (جديد)
README.md / CHANGELOG.md / UPGRADE.md / CHECKLIST.md / WORKLOG.md
docs/AI-API-GUIDE.md              ← مستندات v3.2 (اخبار + تصاویر + ربات v2)
```

> ⚠️ توجه: `config.local.php` جایگزین **نمی‌شود** (تنظیمات شما دست‌نخورده می‌ماند) — این فایل در بسته نیست.

---

## 📦 روش ۲: بسته کامل نصب (اگر تغییرات دستی زیاد دارید)

اگر فایل‌های هسته را دستی تغییر داده‌اید، بسته کامل را نصب کنید و `config.local.php` + `uploads/` + فایل‌های سفارشی خود را از بکاپ برگردانید.

---

## ✅ راستی‌آزمایی پس از بروزرسانی

| # | بررسی | انتظار |
|:-:|-------|--------|
| ۱ | پایین پنل مدیریت | نسخه **2.2.0** |
| ۲ | `GET /api/ai/engine-info` | `engine_version: "3.2.0"` |
| ۳ | 📱 پنل در موبایل | منو با ☰ از لبه صفحه باز می‌شود + پس‌زمینه تیره + با کلیک روی لینک بسته می‌شود |
| ۴ | 🖼️ تصاویر مقاله | `GET /api/ai/article-images` → ۹ تصویر با exists=true |
| ۵ | 📰 اخبار زنده | `POST /api/ai/news` با query «لوازم خانگی» |
| ۶ | 🧠 خط تولید کامل | `POST /api/ai/smart-generate` → خروجی شامل `images` (۳ عدد) و `article.toc` |
| ۷ | 🤖 ربات تلگرام | «مقاله بنویس درباره یخچال» → فایل HTML + MD + آلبوم ۳ عکس |

### تست سریع با curl (جایگزین دامنه و کلید)

```bash
# سلامت موتور
curl "https://YOUR-DOMAIN/api/ai/engine-info"

# 🖼️ فهرست تصاویر مقاله (عمومی — بدون کلید)
curl "https://YOUR-DOMAIN/api/ai/article-images"

# 📰 اخبار زنده (جدید v3.2)
curl -X POST "https://YOUR-DOMAIN/api/ai/news" \
  -H "Content-Type: application/json" -H "X-API-Key: YOUR_KEY" \
  -d '{"query": "لوازم خانگی", "limit": 3}'

# 🧠 خط تولید کامل با ۳ تصویر + تحقیق خودکار
curl -X POST "https://YOUR-DOMAIN/api/ai/smart-generate" \
  -H "Content-Type: application/json" -H "X-API-Key: YOUR_KEY" \
  -d '{"brand_id": 1, "device_key": "refrigerator", "variants": 2}'
```

### 🤖 تست تحویل کامل مقاله در تلگرام (پس از ارتقا)

۱. پنل → **🤖 ربات تلگرام** → مطمئن شوید ربات فعال و شناسه‌تان مجاز است
۲. در تلگرام به ربات بفرستید: `مقاله بنویس درباره تعمیر یخچال`
۳. انتظار: پیام «در حال ساخت...» → خلاصه → فایل `.html` → فایل `.md` → آلبوم ۳ عکس → دکمه‌های شیشه‌ای ✅

---

## 🆘 رفع اشکال

| مشکل | راه حل |
|------|--------|
| خطای نوشتن در `cache/engine` | مجوز پوشه `cache` را 755 (یا 775) کنید؛ پوشه `cache/engine` به‌صورت خودکار ساخته می‌شود |
| پنل هنوز نسخه قدیمی نشان می‌دهد | کش مرورگر (Ctrl+F5)؛ مطمئن شوید `config.php` جدید آپلود شده |
| خطای 404 در اندپوینت‌های جدید | `.htaccess` قدیم مانده — فایل `.htaccess` روت را از بسته کامل جایگزین کنید |
| تصاویر مقاله لود نمی‌شوند | پوشه `assets/images/articles/` را کامل آپلود کرده‌اید؟ (۹ فایل JPG) |
| ربات فایل نمی‌فرستد | `curl` فعال باشد + `sys_get_temp_dir()` قابل نوشتن؛ توکن ربات معتبر است |
| جستجوی وب خطا می‌دهد | مطمئن شوید cURL فعال است؛ در صورت تحریم ارائه‌دهنده، زنجیره جایگزین خودکار فعال می‌شود؛ در نهایت کلید SerpApi را در تنظیمات `websearch_settings` وارد کنید |
| وب‌هوک تلگرام set نمی‌شود | آدرس باید HTTPS باشد؛ برای تست محلی از `php telegram/poll.php` استفاده کنید |
| ربات پاسخ نمی‌دهد | شناسه چت را با `/id` بگیرید و در پنل مجاز کنید (لیست خالی = بسته) |

---

## 📚 ادامه مطالعه

- 🤖 [راهنمای کامل API هوش مصنوعی](docs/AI-API-GUIDE.md) — استفاده از موتور AI از بیرون سایت ساز (شامل راهنمای کامل ربات v2)
- 🔤 [دانلود فونت‌ها](assets/fonts/README.md) — ۳۰ فونت با لینک رسمی
- 🖼️ [دانلود پک‌های آیکون](assets/icons) — ۱۰ پک با لینک رسمی
- 📋 [سابقه تغییرات](CHANGELOG.md) — جزئیات کامل نسخه ۲.۲.۰

---

## ⬆️ از نسخه 1.x: ابتدا به 2.0

اگر هنوز نسخه ۱.x دارید، ابتدا طبق راهنمای داخل بسته `v2.0.0-update.zip` به نسخه ۲.۰.۰ ارتقا دهید (۲۷ فایل + فهرست راستی‌آزمایی همان بسته)، سپس مرحله بعدی و در پایان بخش اصلی همین فایل را اجرا کنید.

---

## ⬆️ ارتقای 2.0 → 2.1

ابتدا بسته `v2.1.0-update.zip` را نصب کنید (جستجوی آنلاین وب + ربات تلگرام + رفع باگ بحرانی نصب‌کننده دیتابیس)، سپس بخش اصلی همین فایل را اجرا کنید. جزئیات کامل تغییرات ۲.۱.۰ در [CHANGELOG](CHANGELOG.md) آمده است.
