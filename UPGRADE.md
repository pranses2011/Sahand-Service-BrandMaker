# 🔄 راهنمای بروزرسانی — Sahand Service BrandMaker

نسخه هدف: **2.26.0** (موتور AI: 3.15.0) — [سابقه تغییرات](CHANGELOG.md)

---

## ⚠️ نکته مهم این نسخه (۲.۲۶.۰)

این نسخه **فعال‌سازی آمار بازدید** را دارد: ریشه «آمار خالی»، سه ایراد زنجیره‌ای در
ردیاب داخلی بود (متغیرهای جاوااسکریپتی هرگز به مرورگر تزریق نشده بودند + مسیر اندپوینت
ناقص + درخواست با preflight). پس از بروزرسانی، برای هر برند مستقرشده یک‌بار
**«بروزرسانی استقرار»** را اجرا کنید تا ردیاب جدید (`js/tracker.js` + `footer.php` +
`config.php`) و فایل‌های مقاله (تصویر شاخص + صفحه مقاله) و `style.css` جدید
(**هیدر رنگی از رنگ تاکیدی لوگو**) منتقل شود؛ سپس یک‌بار صفحه اصلی سایت برند را باز
کنید — از همان لحظه آمار ثبت می‌شود و در «آمار و گزارش‌گیری» نوار سبز «ردیاب فعال»
را می‌بینید. **قابلیت‌های جدید قالب‌ساز (ظواهر متعدد + استخراج عناصر از سایت) فقط در
پنل ادمین است و نیازی به استقرار ندارد.**

## روش ۱ — پکیج بروزرسانی (پیشنهادی — تغییر دیتابیس خودکار)

1. از پوشه فعلی سایت ساز خود **بکاپ کامل** بگیرید (فایل‌ها + دیتابیس).
2. فایل `update.zip` این رلیز را در **ریشه سایت ساز** (کنار `config.php`) آپلود کنید.
3. از فایل‌منیجر هاست (یا SSH با `unzip -o update.zip -d .`) آن را **استخراج** کنید —
   فایل‌های سیستمی جایگزین می‌شوند و `config.php` و `.htaccess` و `uploads/` و `database.sql` دست‌نخورده می‌مانند.
4. فایل `update.zip` را حذف کنید.
5. وارد پنل مدیریت شوید — جدول جدید `personal_elements` (عناصر شخصی قالب‌ساز) در اولین
   بازدید **خودکار** ساخته می‌شود (مهاجرت marker — بدون دستکاری دستی دیتابیس).

## روش ۲ — از سورس گیت‌هاب

```bash
git pull origin main
# اگر تغییرات محلی دارید: git stash && git pull && git stash pop
```

## 📋 پس از بروزرسانی — چک‌لیست تکمیلی

| # | اقدام | لازم/اختیاری |
|---|-------|---------------|
| ۱ | برای هر برند مستقرشده: **ویرایش برند ← استقرار ← بروزرسانی استقرار** | ⭐ لازم (انتقال ردیاب + فایل‌های مقاله + هیدر رنگی) |
| ۲ | بازدید یک‌بار از صفحه اصلی سایت برند + رفرش «آمار و گزارش‌گیری» | ⭐ لازم (تأیید نوار سبز «ردیاب فعال») |
| ۳ | تست صفحه مقاله (نمایش متن + تصویر شاخص) | ⭐ لازم (تأیید رفع) |
| ۴ | مشاهده هدر رنگی سایت برند (از رنگ تاکیدی لوگو — تا ۲ دقیقه بعد از کش) | پیشنهادی |
| ۵ | قالب‌ساز ← انتخاب یک بلوک ← بخش «🎭 ظاهر عنصر» (۱۲ ظاهر + استایل دکمه + هاور) | اختیاری (قابلیت جدید) |
| ۶ | قالب‌ساز ← دکمه «🌐 استخراج از سایت» ← آزمایش با یک آدرس | اختیاری (قابلیت جدید) |

## 📦 فایل‌های تغییر‌یافته این نسخه

```
config.php                                   (نسخه ۲.۲۶.۰ + مهاجرت جدول personal_elements)
core/ElementExtractor.php                    (🆕 استخراج عناصر از سایت خارجی)
admin/template-builder.php                   (ظواهر متعدد + پنل استخراج + عناصر شخصی)
admin/template-preview.php                   (آینه ظواهر + رندر pelement)
admin/analytics.php                          (نوار وضعیت زنده ردیاب)
api/endpoints/analytics.php                 (بروزرسانی ردیف خروج — نرخ پرش واقعی)
core/ConfigGenerator.php                     (TRACKER_URL کامل + BRANDMAKER_URL + cdn_asset + seo.php)
templates/brand-core/config.php              (هم‌ارز تولیدی: ثابت‌ها + توابع)
templates/brand-core/includes/footer.php     (تزریق TRACKER_URL/BRAND_ID به مرورگر)
templates/brand-core/includes/header.php     (متغیرهای هیدر از رنگ تاکیدی لوگو)
templates/brand-core/includes/functions.php  (brand_header_vars_css + article_image)
templates/brand-core/includes/seo.php        (گارد + og:image مطلق)
templates/brand-core/pages/article.php       (بارگذاری seo.php)
templates/brand-core/css/style.css           (هیدر متغیرمحور + منوی موبایل)
templates/brand-core/js/tracker.js           (بدون preflight + گارد دفاعی)
database.sql                                 (جدول personal_elements)
docs/AI-API-GUIDE.md                         (نسخه ۲.۲۶.۰ + نگاشت + /api/track)
```

## 🩺 راستی‌آزمایی

1. در پنل: «تنظیمات» ← سلامت سیستم ← نسخه باید **2.26.0** باشد.
2. صفحه اصلی یک سایت برند را باز کنید ← پنل ← «آمار و گزارش‌گیری»: نوار
   **🟢 ردیاب بازدید فعال است** + شمارنده «بازدید امروز» ≥ ۱.
3. صفحه مقاله سایت برند: متن کامل مقاله + تصویر شاخص باید نمایش داده شوند.
4. هدر سایت برند: گرادیانت رنگی از رنگ تاکیدی لوگو (نه سفید همیشگی) + متن کاملاً خوانا.
5. قالب‌ساز: یک بلوک انتخاب کنید ← «🎭 ظاهر عنصر» ← «شیشه‌ای» — بوم همان لحظه تغییر می‌کند.
6. قالب‌ساز ← «🌐 استخراج از سایت» ← یک آدرس واقعی بدهید ← روی نام عنصر کلیک کنید —
   پیش‌نمایش با استایل واقعی سایت مبدأ کنار فهرست نمایش داده می‌شود.

## 🔙 بازیابی سایت‌های مستقرشده (در صورت مشکل)

اگر بعد از بروزرسانی مشکلی پیش آمد:
- سایت‌های برند مستقل از سایت ساز کار می‌کنند (فقط API داده مشترک است).
- «بروزرسانی استقرار» مجدد، فایل‌های سایت برند را از قالب سالم بازسازی می‌کند.
- بکاپ‌های پنل (پنل ← بکاپ‌ها) کل ساختار هر برند را نگه می‌دارند.
