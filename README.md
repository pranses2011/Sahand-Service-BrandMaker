# 🏗️ سایت ساز برند سهند سرویس — Sahand BrandMaker

> سیستم اختصاصی تولید خودکار سایت برای برندهای لوازم خانگی — **PHP خالص، بدون هیچ وابستگی خارجی، سازگار با هاست اشتراکی cPanel**

[![نسخه](https://img.shields.io/badge/نسخه-2.9.0-blue)]() [![موتور AI](https://img.shields.io/badge/موتور_AI-v3.9.0_فونت_ریشه‌ای+OG_پویا+تصاویر_واقعی+خطایاب_کامل-ff69b4)]() [![PHP](https://img.shields.io/badge/PHP-7.4%2B_بهینه_برای_8.3-purple)]() [![MySQL](https://img.shields.io/badge/MySQL-5.7%2B-orange)]() [![بدون وابستگی](https://img.shields.io/badge/وابستگی-صفر-green)]() [![تست](https://img.shields.io/badge/تست-450_موفق-brightgreen)]()

## 🎯 سایت ساز چه می‌کند؟

با دادن **لوگو + نام برند (فارسی/انگلیسی) + دامنه**، یک سایت کامل و حرفه‌ای تولید می‌شود:

| قابلیت | توضیح |
|--------|-------|
| 🎨 استخراج رنگ از لوگو | الگوریتم K-Means → پالت تم روشن + تاریک (۱۷ متغیر CSS) — **پشتیبانی کامل SVG** (استخراج رنگ از fill/stroke بدون Imagick) + fallback نرم |
| ✍️ محتوای یکتا | موتور AI داخلی (بدون API خارجی) — هیچ دو سایتی متن یکسان ندارند |
| 📰 ۵ مقاله اولیه | هر مقاله ۱۰۰۰-۱۵۰۰ کلمه + **۳ تصویر مرتبط** + فهرست مطالب + زمان مطالعه + جعبه نکته/هشدار + مزایا-معایب |
| 🔍 سئوی خودکار | عنوان، متا، Schema (Article/FAQ/HowTo/Speakable)، OG، تراکم کلیدواژه، sitemap، robots + **چک‌لیست E-E-A-T + موجودیت‌ها + خوشه معنایی + شکاف محتوایی** |
| 📦 خروجی ZIP | هسته PHP آماده آپلود + کلید API + پالت رنگ |
| 📨 درخواست خدمات | ارسال ۵ کاناله: ایمیل، تلگرام (مستقیم + واسط گوگل)، بله، 🤖 ربات تلگرام |
| 📊 آمار کامل | ردیاب داخلی + GeoIP محلی ایران + نمودار + نقشه شهرها |
| 🎭 قالب‌ساز | درگ‌اند‌دراپ **المنتور-گونه: ستون‌بندی ۲-۴ ستونه تودرتو + ۵۵+ عنصر در ۱۲ دسته + طراحی زنده WYSIWYG** + اسکیل UI/UX Pro (طراحی خودکار، ممیزی UX، اصلاح خودکار) |
| 🌐 جستجوی آنلاین وب + 📰 اخبار | موتور AI در صورت لزوم از اینترنت داده زنده و اخبار تازه می‌گیرد — **۹ ارائه‌دهنده (ویکی‌پدیا fa/en + Startpage + ...)** با **مسابقه موازی curl_multi** (تأخیر تا ۵۰٪ کمتر) + تشخیص تازگی و اعتماد دامنه |
| 🤖 ربات تلگرام v2.1 | دستیار فارسی کامل در تلگرام + **تحویل مقاله کامل با فایل HTML/Markdown و آلبوم ۳ عکس** + ۱۶ فرمان (جدید: /seo پکیج سئو، /eeat اعتمادسنجی، /plan برنامه محتوا، /faq سوالات متداول) |
| 📱 ریسپانسیو کامل | پنل + سایت‌های برند + نصب — منو، گریدها، جدول‌ها و فرم‌ها در همه ابعاد |

## 🆕 تازه‌های نسخه ۲.۱۰.۰ — دارایی‌های کامل روی سرور + نمای چاپی

| حوزه | تغییرات |
|------|---------|
| 🔤 **۱۹ فونت متن‌باز روی سرور** | ۸۶ فایل (۳MB) از پیش نصب — فارسی: وزیرمتن/وزیر/صمیم/شبنم/گندم/تنها/پرستو/میخک/لاله‌زار + ۱۰ فونت انگلیسی + دکمه «دانلود گروهی فونت‌های آزاد» + رفع 404 دانلود وزیر |
| 🖼️ **آیکون‌های غنی از ابتدا** | ۱۱ پک عمومی هر یک ~۱۳۰ آیکون گزینشی برتر (جمع ۱۵۷۱ با برچسب فارسی ۹۴٪) + دو پک جدید: آیکونویر و بوت‌استرپ — دکمه «نصب کامل» برای کل پک باقی است |
| 🖼️ **رفع 404 تصاویر جایگزین** | تصاویر placeholder (مقاله/عمومی/فاویکون/لوگو/OG) که در کد ارجاع بودند اما وجود نداشتند — ساخته و در مسیر استاندارد قرار گرفتند |
| 🖨️ **فرمت چاپی درخواست** | برگ درخواست حرفه‌ای قابل چاپ/ذخیره PDF — سربرگ لوگوی برند + تمام فیلدها + پیوست‌ها + محل امضای سه‌طرفه |
| 📊 **گزارش PDF آمار** | گزارش چاپی کامل با همان فیلتر داشبورد — KPI + روند روزانه + تفکیک دستگاه/مرورگر/OS + شهرها + صفحات پربازدید + سهم برندها |

## 🆕 تازه‌های نسخه ۲.۹.۰ (موتور AI ۳.۹) — فونت فارسی ریشه‌ای + OG پویا + تلگرام URL-اول + تصاویر واقعی + قالب‌ساز المنتوری

| حوزه | تغییرات |
|------|---------|
| ✍️ **رفع ریشه‌ای فونت OG** | «حروف یک کلمه به هم چسبیده نیستند» حل شد: دو تابع کمکی شکل‌دهی جا‌به‌جا صدا زده می‌شدند + باگ زنجیره فرم + نگاشت غلط «ؤ» + افزودن «ئ» — ۱۸/۱۸ تست واحد + تأیید VLM |
| 🎨 **OG پویا** | ۱۰ سبک پس‌زمینه متفاوت — **هر بار تولید، طرح جدید** (mesh/موج/پرتو/حباب/گرید/راه‌راه/حلقه/نقطه‌موج/شفق/چندضلعی) + لوگوی برند بزرگ (نشان ۲۰۰px) + عنوان هرگز بریده نمی‌شود (۳ خط خودجا‌شو) |
| 🤖 **تلگرام URL-اول** | رفع قطعی «there is no document»: سند/آلبوم با آدرس عمومی ارسال می‌شود (فقط JSON — با هر نسخه واسط کار می‌کند) + اسکریپت گوگل v3 + پاک‌سازی خودکار موقت‌ها |
| 🖼️ **تصاویر مقاله واقعی** | ریشه «تصویر لباسشویی برای مقاله یخچال» حل شد (دستگاه از عنوان استنتاج می‌شود — قبلاً تصادفی!) + بسته ۱۸ عکس واقعی اختصاصی (۹ تای جدید AI-generated بدون متن) + واترمارک دوتایی برند/نمایندگی شفاف روی همه تصاویر |
| 🚨 **خطایاب کامل‌فیلد** | قطعه استاندارد مقدم بر زمینه نویزی (رفع «LE → پمپ تخلیه») + تطبیق سیستم قطعه↔عنوان + بودجه ۳۴s + مدل‌های سازگار از متن — تست زنده ۶/۶ کد کامل |
| 🧹 **پاک‌سازی تکراری‌ها** | دکمه «حذف دستگاه‌های تکراری» + ادغام خودکار کدهای خطای تکراری (پر کردن فیلدهای خالی + ادغام دلایل/راه‌حل‌ها) |
| 👁️ **پیش‌نمایش صفحه‌ها** | دکمه پیش‌نمایش هر صفحه برند در مودال — رندر واقعی با لوگو + پالت رنگ + سوییچ دسکتاپ/موبایل |
| 🎨 **پالت رنگ حرفه‌ای** | نمایش قالب‌بندی‌شده پالت غالب + تم روشن/تاریک گروه‌بندی‌شده + پیش‌نمایش زنده مینی‌سایت + انتخابگر رنگ HSV با قطره‌چشم و پیش‌فرض‌های لوگو |
| 🎭 **قالب‌ساز المنتوری** | 🏛 **ستون‌بندی ۲-۴ ستونه تودرتو** + **۵۵+ عنصر در ۱۲ دسته** + ⚡ **طراحی زنده** (بوم واقعی WYSIWYG — ویرایش متن با به‌روزرسانی آنی) |

## 🆕 تازه‌های نسخه ۲.۸.۰ (موتور AI ۳.۸) — خطایاب صددرصد وب‌محور + تحویل سند تلگرام + bidi OG

| حوزه | تغییرات |
|------|---------|
| 🚨 **خطایاب فقط-وب** | فقط کدهای راستی‌آزمایی‌شدهٔ وب ثبت می‌شوند — دانش داخلی دیگر کد اضافه نمی‌کند (فقط فیلد خالی را پر می‌کند)؛ چیزی نیافت → گزارش صادقانه به‌جای کد ساختگی |
| 📊 **پارس جدول کدها** | صفحات فهرست سازنده (دقیق‌ترین منبع) خوانده می‌شوند: `OE: خطای تخلیه آب`، `UE \| Unbalance Error`، «نمایش IE می‌دهد: خطای آبرسانی» — رفع ۲ باگ رجکس (بلعیده‌شدن کد خط بعد + کدهای دوحرفی فارسی) |
| 🎯 **قطعه سازگار با عنوان** | «خطای قفل درب با قطعه پمپ تخلیه» غیرممکن شد — قطعه از عنوان معنادار استخراج می‌شود، نه از پنجره نویزی متن |
| 🤖 **تحویل سند تلگرام** | ریشه‌یابی «there is no document» (اسکریپت واسط قدیمی) + تلاش مجدد آپلود مستقیم + تحویل مرحله‌مقاوم + نجات نهایی متن مقاله |
| ✍️ **PersianGlyphs v2** | بازنویسی bidi: متن ترکیبی فارسی/عدد/انگلیسی در تصاویر OG سالم + آینه پرانتزها + رفع چسبیدن حروف دو کلمه |
| 🔤 **فونت تضمینی** | فونت بدون حروف فارسی (پ چ ژ ک گ ی) خودکار رد می‌شود — مستطیل‌های خالی محو |

## 🆕 تازه‌های نسخه ۲.۷.۱ (موتور AI ۳.۷.۱) — رفع کرش ربات + OG برنددار + خطایاب دوفازی + مقاله موضوع‌محور

| حوزه | تغییرات |
|------|---------|
| 🤖 **رفع کرش ربات تلگرام** | تلگرام برای برخی متدها `true` برمی‌گرداند و ربات روی «همه» پیام‌ها با TypeError می‌مرد — متد نرمال‌سازی به هر ۴ مسیر API (مستقیم/واسط گوگل/آپلود) اضافه شد |
| 🖼️ **تصویر OG برنددار** | فونت = **فونت پیش‌فرض عنوان سایت‌ها** (رفع باگ نام→اسلاگ + مبدل WOFF→TTF در PHP خالص + جاسازی @font-face در SVG) + **لوگوی برند** بالای تصویر + **لوگوی نمایندگی واترمارک گوشه** — حتی لوگوهای SVG با Imagick رستر می‌شوند |
| 🚨 **خطایاب دوفازی** | برای هر کد کاندید: جستجوی اختصاصی حول خود کد + **خواندن متن کامل صفحه** → تکمیل دقیق همه ۱۴ فیلد از منابع واقعی؛ کدهای بی‌شاهد رد می‌شوند؛ مشخصات فنی و محل قطعه از متن استخراج می‌شوند؛ ادغام داده دقیق سازنده |
| ✍️ **مقالات موضوع‌محور** | مقاله دقیقاً حول **عنوان درخواستی** نوشته می‌شود (رفع ریشه «مقاله بی‌ربط»): outline ۷بخشی موضوع‌محور + فکت‌های تحقیق وب بافته‌شده در نثر + پاسخ تحلیلی پرسش‌های واقعی کاربران + FAQ همان موضوع |
| 📋 **جدول‌های سالم** | رفع خرابی عرض ستون‌ها (متن چندسطری تا ۲۰ خط) در همه لیست‌ها و کادرهای راهنمای آبی/سبز — باگ CSS `overflow-wrap: anywhere` |
| 🔐 **لاگین دوپنلی** | پنل برند گرادیانی کنار فرم (دسکتاپ) + **کادر کد امنیتی چند برابر بزرگ‌تر** (فونت ۲۶px، تصویر ۲۵۰×۸۴) |

## 🆕 تازه‌های نسخه ۲.۶.۰ (موتور AI ۳.۶) — خطایاب AI + یادگیری AI + تصاویر AI + ربات بله

| حوزه | تغییرات |
|------|---------|
| 🚨 **موتور خطایاب AI (۱۴ فیلدی)** | تولید همه کدهای خطای «واقعی» هر برند+دستگاه: پایگاه دانش ۱۵۰ کدیِ ۹ برند پرتقاضا (ال‌جی، سامسونگ، بوش، الکترولوکس، آریستون، بکو، ویرپول، پاناسونیک، اسنوا) + 🌐 جستجوی آنلاین فارسی/انگلیسی با منبع‌یابی URL — ساختار کامل ۱۴ فیلدی (برند/دستگاه/زیرنوع/مدل‌ها/کد/عنوان/شدت ۵ سطحی/توضیح یکتا/دلایل/راه‌حل‌های [کاربر]-[تکنسین]/نوع خطا/قطعه/مشخصات فنی/محل قطعه) + ویرایش کامل + ✨ بهینه‌سازی تک‌فیلد با AI |
| 🧠 **موتور خودیادگیر** | هر بهبود موفق (سئو/محتوا/UX/کد خطا) «درس» ثبت می‌کند و در کارهای بعدی ترجیح داده می‌شود — لاگ حسابرسی `logs/ai-upgrades.log` + پنل «یادگیری AI» برای فعال/غیرفعال کردن تک‌تک درس‌ها (ارتقای اشتباه = فقط همان غیرفعال) |
| 🖼️ **تصاویر AI یکتا** | هر مقاله: تصویر OG مرتبط با موضوع + ۲ تصویر درون‌متن یکتا (هیچ دو مقاله‌ای یکی نیست) — PNG فارسی با شکل‌دهنده حروف خالص PHP (PersianGlyphs) / ImageMagick / SVG — قابل ویرایش و تولید مجدد |
| 💬 **ربات بله** | دستیار فارسی کامل روی پیام‌رسان بله (BaleBot + poll + وب‌هوک + پنل اختصاصی) |
| 🌉 **واسط گوگل تلگرام** | عبور از تحریم: کل ارتباط ربات تلگرام از سرورهای گوگل (relay دوطرفه) — اسکریپت آماده با جای‌گذاری خودکار + تست |
| 💬 **SahandDialog** | API کادرهای تعاملی زیبا (آیکون/رنگ/ورودی متن/چک‌باکس/رادیو/توست) — جایگزین کامل همه alert/confirm پیش‌فرض |
| 🎯 **بهبود تک‌سنجه سئو** | کنار هر سنجه در کادر آمار مقاله دکمه «🎯 بهبود» — فقط همان سنجه اصلاح می‌شود |
| 🚀 **بهبود برند تا ۱۰۰** | دکمه «بهبود خودکار این صفحه تا ۱۰۰» + «بهبود همه بخش‌ها و صفحات برند» (حلقه تحلیل→اصلاح→سنجش) |
| 🐛 **رفع ۷ باگ گزارش‌شده** | کپچا روی لب‌تاپ (ادبلاکر — اندپوینت خنثی + fallback) · سایدبار همیشه راست · دستگاه‌های تکراری برند · «بهینه است» با وجود ۱ مورد (اصلاح‌کننده تراکم کلیدواژه) · نمایش همه آیکون‌ها با دسته‌بندی پوشه + صفحه‌بندی · تب‌های مقاوم تنظیمات (تمام‌عرض + بازگشت به بالا) |
| 🗑️ **لغو نصب فونت/آیکون** | حذف کامل فونت‌ها (همه وزن‌ها) و پک‌های آیکون از سرور با یک کلیک + 👁️ پیش‌نمایش زنده فونت با اسلایدر اندازه |
| 📅 **فعالیت از سال** | تنظیم «سال شروع فعالیت نمایندگی» برای کارت‌های سایت («... سال سابقه») |
| 🔌 **API: ۵۴ اندپوینت** | ۶ اندپوینت جدید: خطایاب ×۲ + تصویر OG + خودیادگیر ×۲ + وب‌هوک بله |
| 🗃️ **مهاجرت خودکار** | ستون‌های جدید دیتابیس (og_image + ۸ ستون کد خطا + severity پنج‌سطحی) بدون اجرای SQL دستی |

## 🆕 تازه‌های نسخه ۲.۵.۰ (موتور AI ۳.۵) — کپچای مقاوم + اسکیل UI/UX Pro + بهینه PHP 8.3

| حوزه | تغییرات |
|------|---------|
| 🔒 **کپچای تضمینی + حروف درشت** | رفع مشکل نمایش کد امنیتی صفحه لاگین با **رندر ۳ لایه‌ای**: ۱) GD + فونت TTF (فونت‌های سایت‌ساز یا سیستم) با حروف ۳۰px و چرخش واقعی ۲) GD فونت داخلی با بزرگ‌نمایی نرم ۲.۶× (Bicubic) ۳) **SVG خالص PHP — حتی بدون اکستنشن GD نمایش داده می‌شود** — بوم ۲۲۰×۷۲ (قبلاً ۱۷۰×۵۲)، دکمه رفرش مستقل 🔄، پاک‌سازی بافر خروجی و سقوط نرم در هر خطا |
| 🎨 **اسکیل UI/UX Pro (جدید)** | مغز طراحی حرفه‌ای صفحات: ۹ قانون کلاسیک UX (هیک، فیتس، یاکوب، فون رستورف...) + توکن‌های طراحی (فاصله ۴/۸، تایپ، رنگ ۶۰-۳۰-۱۰) + قواعد RTL فارسی + WCAG + الگوهای نرخ تبدیل + **بلوپرینت ۱۷ نوع صفحه + قواعد ۳۴ بلوک + ممیزی امتیازی ۰-۱۰۰** |
| 🪄 **طراحی خودکار صفحات** | مرحله ۳ ساخت برند حالا چیدمان حرفه‌ای هر ۱۵ صفحه را با اسکیل UI/UX Pro تولید و در layout_json ذخیره می‌کند (زمینه‌محور: نام برند، دستگاه‌ها، مقالات) — میانگین امتیاز UX بلوپرینت‌ها **۹۲/۱۰۰** |
| 🔍 **ممیزی و اصلاح UX در قالب‌ساز** | دکمه‌های «✨ طراحی با UI/UX Pro» + «🔍 بررسی UX» + «🛠️ اصلاح خودکار» با پنل نتایج کامل: امتیاز رنگی، نقاط قوت، مشکلات اولویت‌بندی‌شده (بحرانی/مهم/متوسط/جزئی) و منطق طراحی هر بخش |
| ⚡ **بهینه PHP 8.3** | فایل `.user.ini` (OPcache + حافظه ۲۵۶M + زمان ۱۲۰s + کش مسیر + سخت‌سازی نشست) + `json_validate` بومی PHP 8.3 (پلی‌فیل برای قدیمی‌تر) + error_reporting بدون نویز E_DEPRECATED + فشرده‌سازی zlib خروجی |
| 🖥️ **کارت وضعیت سیستم در داشبورد** | نمایش زنده: نسخه PHP، GD + FreeType، OPcache، mbstring، cURL، حافظه، زمان اجرا، سقف آپلود — با راهنمای فعال‌سازی OPcache (۲ تا ۵ برابر سرعت) |
| 🔌 **API: ۴۸ اندپوینت** | ۴ اندپوینت جدید: `uiux-design` + `uiux-review` + `uiux-improve` + `uiux-skill-info` |

## 🚑 هات‌فیکس ۲.۴.۱ (موتور ۳.۴.۱) — رفع ۵ باگ حیاتی گزارش‌شده کاربر

> ⚠️ **برای همه کاربران ۲.۴.۰ (و ۲.۳.۰) الزامی است.** جزئیات کامل در [CHANGELOG](CHANGELOG.md) و مراحل ارتقا در [UPGRADE](UPGRADE.md).

| # | باگ رفع‌شده | علت ریشه‌ای |
|---|--------------|--------------|
| ۱ | داشبورد خالی + منوی بازنشدنی + شکست تولید مقاله و API موتور | متد تکراری `researchTopic()` در SahandAI (از v2.3.0) — خطای غیرقابل‌گرفت بارگذاری کلاس |
| ۲ | خطای «Unexpected end of JSON input» در دکمه پیشنهاد بهترین عنوان سئو | `trim()` بایت‌محور PHP فهرست‌های دارای «،؛» بایت نخست حروف فارسی (ا، ب، د، س…) را می‌بُرید → UTF-8 خراب → `json_encode=false` → پاسخ خالی |
| ۳ | خطای «Unexpected token '<'» در دکمه بهبود سئو | `mb_strtok()` در PHP وجود ندارد → Error بی‌گرفت → پاسخ HTML به‌جای JSON |
| ۴ | نیم‌فاصله‌گذاری پسوندها بی‌اثر بود (کتاب ها اصلاح نمی‌شد) | ۶ الگوی regex با lookbehind طول-متغیر — PCRE پشتیبانی نمی‌کند |
| ۵ | نقطه دوباره بعد اصلاح فعل («می‌دهیم..») + کلیدواژه‌های HTML («</p>») + کاراکتر � در متا | باگ‌های خرد همراه — همه رفع شدند |

**سخت‌سازی‌های همراه:** پلی‌فیل توابع PHP ۸ روی سرورهای ۷.۴ + همه catchها به `Throwable` ارتقا یافتند + هندلر استثنای سراسری AJAX-آگاه (پاسخ JSON به‌جای HTML) + `json_response()` ضدخرابی UTF-8 + `mb_trim()/mb_rtrim()` چندبایتی-امن + انتقال admin.js به هدر (منو حتی هنگام خطای رندر کار می‌کند) — راستی‌آزمایی کامل با شبیه‌سازی PHP 8.3: داشبورد ۱۱.۸KB رندر کامل، پیشنهاد عنوان JSON معتبر، بهبود سئو ۳۶→۸۴.

## 🆕 تازه‌های نسخه ۲.۴.۰ (موتور AI ۳.۴ — فاز Q)

| حوزه | تغییرات |
|------|---------|
| 📝 **موتور نگارش فارسی (PersianGrammar)** | اصلاح خودکار نیم‌فاصله، علائم سجاوندی، املای رایج، ارقام فارسی و **هم‌خوانی فعل و فاعل** (جدول ۶ ضمیر × ۱۰ فعل) در همه محتوای تولیدی + امتیاز نگارش ۰-۱۰۰ + بُعد هشتم کیفیت «نگارش» در QualityScorer |
| ⬇️ **دانلود مستقیم فونت/آیکون روی سرور** | AssetDownloader: نصب با یک کلیک از منابع رسمی (jsDelivr/npm/GitHub) — بدون دانلود روی کامپیوتر و آپلود مجدد — ۹ فونت فارسی + ۱۰ انگلیسی + ۹ پک آیکون |
| 📰 **۲۵ نوع مقاله** | از ۹ به ۲۵ نوع: ایمنی، نصب و راه‌اندازی، اشتباهات رایج، گارانتی، فناوری‌شناسی، باورغلط/واقعیت، چک‌لیست، مطالعه موردی، واژه‌نامه، تاریخچه، نکات خبرگان، علامت‌محور، آمار، محیط زیست، فرآیند نمایندگی + ۶ نوع دیگر |
| 🎯 **عنوان دلخواه + پیشنهاد بهترین عنوان سئو** | عنوان خود را بنویسید؛ موتور آن را تحلیل کرده ۸+ پیشنهاد سئو رتبه‌بندی‌شده (با میزان بهبود امتیاز) می‌دهد و مقاله دقیقاً حول عنوان انتخابی نوشته می‌شود |
| 📊 **آمار کامل سئو زیر هر مقاله + دکمه «بهبود سئو»** | پنل ۱۲ سنجه وزنی (عنوان/متا/تراکم/مقدمه/حجم/هدینگ/لینک/alt/FAQ/TOC/نگارش/خوانایی) + اصلاح خودکار ۸ موردی با گزارش قبل/بعد و ذخیره |
| 🔎 **جستجوی اینترنت هنگام نوشتن** | مقاله با تحقیق آنلاین نوشته می‌شود: داده‌های واقعی + پرسش‌های کاربران + بخش «منابع» (E-E-A-T) + کلیدواژه‌های ترند در تگ‌ها |
| 🖼️ **تصاویر خودکار در متن مقالات** | ۳ تصویر (شاخص/میان‌متن/پایانی) با alt و figcaption درج + ذخیره تصویر شاخص در دیتابیس |
| 🤖 **ربات تلگرام v2.2** | ۳ فرمان جدید: `/grammar` (اصلاح نگارش + متن اصلاح‌شده)، `/suggest` (بهترین عنوان سئو)، `/types` (۲۵ نوع) + دستیار فارسی: «نگارش: ...»، «پیشنهاد عنوان سئو: ...»، «انواع مقاله» |
| 🔌 **API: ۴۴ اندپوینت** | ۳ اندپوینت جدید: `suggest-article-titles` + `article-seo-stats` + `improve-article-seo` |

## 🆕 تازه‌های نسخه ۲.۳.۰ (موتور AI ۳.۳)

| حوزه | تغییرات |
|------|---------|
| 🐛 رفع باگ‌های گزارش‌شده | **صفحه ساخت برند** (تگ PHP اضافی — متن خام چاپ می‌شد) + **خطای مرحله ۱ تحلیل لوگو** (پشتیبانی SVG + باگ imagedestroy برای لوگوهای تک‌رنگ) + **فیلترهای نصفه‌دیده بالای کارت‌ها** + **صفحه تنظیمات در موبایل** (تب‌های اسکرولی + دکمه تمام‌عرض) + **پیش‌نمایش قالب‌ها** (وایرفریم + پیش‌نمایش زنده) |
| 🧊 شکستن کش مرورگر | CSS/JS حالا نسخه‌دارند (?v=filemtime) — بعد از بروزرسانی دیگر چیدمان قدیمی از کش نشان داده نمی‌شود |
| 🏅 سئو (v3.3) | چک‌لیست E-E-A-T هشت‌معیاری + استخراج موجودیت‌ها + خوشه کلیدواژه ثانویه + People Also Ask + تحلیل شکاف محتوایی رقبا — ۲ اندپوینت جدید `seo-package` و `eeat-check` |
| 📰 مقاله (v2.1) | فهرست مطالب با لنگر + زمان مطالعه + جعبه نکته حرفه‌ای/هشدار ایمنی + مزایا-معایب + باکس آمار + حداقل ۱۰۰۰ کلمه |
| 🌐 جستجو (v1.2) | ویکی‌پدیا فارسی/انگلیسی + Startpage (مجموع ۹) + **مسابقه موازی curl_multi** + تازگی و اعتماد دامنه نتایج |
| 🤖 ربات تلگرام (v2.1) | ۴ فرمان جدید: `/seo`، `/eeat`، `/plan`، `/faq` + معادل فارسی + زبان طبیعی («سئو بررسی کن: ...») |

## ⚡ نصب سریع (۵ دقیقه)

```bash
# ۱. بسته نصب را از صفحه Releases دانلود کنید (یا کل ریپو را آپلود کنید)
# ۲. روی هاست cPanel در زیردامنه brandmaker آپلود و استخراج کنید
# ۳. یک دیتابیس MySQL بسازید
# ۴. در مرورگر باز کنید:
https://brandmaker.your-domain.ir/install.php
# ۵. اطلاعات دیتابیس + حساب مدیر → نصب خودکار ✅
```

> ❌ بدون Composer | ❌ بدون Node.js | ❌ بدون SSH | ✅ فقط آپلود + دیتابیس

📥 **[صفحه دانلود نسخه‌ها (Releases)](https://github.com/pranses2011/Sahand-Service-BrandMaker/releases)** — شامل بسته نصب کامل + بسته بروزرسانی + فایل‌های راهنما + چک‌سام SHA256

> 🆕 **آخرین نسخه: v2.8.0** — 🚨 خطایاب صددرصد وب‌محور (فقط کدهای واقعی راستی‌آزمایی‌شده + پارس جدول کدهای سازنده + قطعه همیشه سازگار با عنوان) + 🤖 تحویل سند تلگرام مقاوم (تشخیص خودکار خطای واسط + نجات نهایی مقاله) + ✍️ PersianGlyphs v2 (متن ترکیبی فارسی/عدد/انگلیسی سالم در OG) + اعتبارسنجی کامل فونت فارسی — *[تازه‌های کامل در CHANGELOG](CHANGELOG.md)* | *[⬇️ دانلود رلیز v2.8.0](https://github.com/pranses2011/Sahand-Service-BrandMaker/releases/tag/v2.8.0)* (install.zip + update.zip)

## 📁 ساختار پروژه

```
├── admin/               ← پنل مدیریت (۲۰+ صفحه فارسی RTL)
│   ├── brand-build.php  ← فرآیند ساخت ۸ مرحله‌ای با نوار پیشرفت
│   ├── telegram.php     ← 🤖 مدیریت ربات تلگرام (تست اتصال/وب‌هوک/پیام آزمایشی)
│   ├── template-builder.php ← قالب‌ساز درگ‌اند‌دراپ + 👁️ پیش‌نمایش زنده (موبایل/تبلت/دسکتاپ) + وایرفریم بلوک‌ها
│   ├── template-preview.php ← 👁️ رندر پیش‌نمایش زنده چیدمان با محتوای نمونه
│   └── ...
├── api/                 ← API داخلی (۴۱ اندپوینت AI + ۱۱ اندپوینت برند + وب‌هوک تلگرام)
├── engine/              ← موتور هوش مصنوعی داخلی v3.4 (SahandAI + PersianGrammar + SeoImprover)
│   ├── generators/      ← ۱۰ ژنراتور (مقاله/سئو/FAQ/خط تولید هوشمند/بهبوددهنده/عنوان‌ساز/...)
│   ├── analyzers/       ← ۷ آنالایزر (رنگ، یکتایی، کلیدواژه، سئو، کیفیت، نیت، صدای برند)
│   ├── services/        ← 🌐 WebSearchService v1.2 (۹ ارائه‌دهنده + مسابقه موازی + اخبار) + 🖼️ ArticleImageService (۳ تصویر هر مقاله)
│   ├── CommandAssistant.php ← 💬 دستیار فرمان زبان طبیعی فارسی
│   ├── utils/           ← KnowledgeBase، TextProcessor، EngineCache، ...
│   └── knowledge/       ← ۱۳ فایل دانش: ۷۷ برند + ۴۲ دستگاه + ۱۲۵ کد خطا + ...
├── telegram/            ← 🤖 ربات تلگرام v2.1 (۱۶ فرمان + تحویل کامل مقاله + وب‌هوک + long-polling)
├── core/                ← ۱۲ کلاس هسته (DB، Auth، Cache، ...)
├── templates/brand-core/ ← هسته سایت برند (۳۳ فایل) — قالب ZIP
├── assets/              ← ۱۰ پک آیکون + ۳۰ فونت + 🖼️ ۹ تصویر مقاله (articles/) + CSS/JS
├── geoip/               ← دیتابیس محلی IP ایران (بدون API خارجی)
├── docs/                ← ۸ سند راهنمای فارسی + AI-API-GUIDE.md (مرجع کامل API)
└── database.sql         ← اسکیمای ۳۳ جدول
```

## 🤖 موتور AI داخلی نسخه ۳.۴ «بی‌رقیب + نگارش‌گر فارسی + بهبودگر سئو + متصل به اینترنت»

موتور با ترکیب ۶ روش محتوای یکتا تولید می‌کند: انتخاب قالب seeded، جایگذاری دانش واقعی برند، جایگزینی مترادف (۴۷۵ واژه)، بازنویسی عبارت و ساختار، بررسی یکتایی (شباهت زیر ۳۵٪)، بهینه‌سازی سئو.

### 🌟 قابلیت‌های جدید نسخه ۳.۲ (v3.2)

| قابلیت | توضیح |
|--------|-------|
| 🖼️ **۳ تصویر برای هر مقاله** | ۹ تصویر فوتورئال تخصصی + انتخاب هوشمند بر اساس دستگاه/موضوع + درج استراتژیک (بنر + ۳۵٪ + ۷۲٪) + alt/کپشن سئو |
| 🏗️ **غنی‌سازی ساختاری مقاله** | فهرست مطالب + «سایر پرسش‌های کاربران» (PAA) + بخش «داده‌های تازه از وب» با ذکر منبع + جعبه اعتبار E-E-A-T |
| 📰 **اخبار زنده** | جستجوی اخبار تازه فارسی (Google News RSS) — روند‌ها، قیمت روز، رویداد صنعت |
| 🌐 **جستجوی وب قوی‌تر** | ارائه‌دهنده جدید Mojeek (مجموع ۷) + فرصت‌های کلیدواژه long-tail + تلاش مجدد خودکار |
| 🔍 **سئوی عمیق‌تر** | اسکیمای Article غنی + HowTo Schema + Speakable + تراکم کلیدواژه + زمان مطالعه + og:image |
| 📱 **تحویل کامل در تلگرام** | مقاله با فایل HTML آماده انتشار برای هر سایت + Markdown + آلبوم ۳ عکس — بدون ورود به سایت ساز |
| 🧠 **خط تولید ۱۱ مرحله‌ای** | تحقیق وب خودکار (auto) + امتیاز هدف پیش‌فرض ۸۸ |

### 🚀 قابلیت‌های نسخه ۳ (v3.0 + v3.1 — پایه نسخه فعلی)

| قابلیت | توضیح |
|--------|-------|
| 🧠 **خط تولید هوشمند** | یک فراخوانی = ۱۱ مرحله خودکار: نیت ← کلیدواژه ← تحقیق وب ← مقاله بهترین-از-N ← بهبود خودکار ← غنی‌سازی ← تصاویر ← عنوان بهینه ← سئو ← FAQ/اسکیما ← یکتایی |
| 🔎 **جستجوی آنلاین وب** | ۷ ارائه‌دهنده با جایگزینی خودکار — ۴ رایگان بدون کلید (DuckDuckGo×۲/Mojeek/Bing) + ۳ کلیددار اختیاری (SerpApi/Google CSE/Bing API) |
| 🧪 **تحقیق ساختاریافته** | یک فراخوانی = کلیدواژه ترند + سؤالات واقعی کاربران + داده‌های عددی تازه + فرصت‌های long-tail + منابع |
| 🔧 **بهبود خودکار محتوا** | چرخه خود-درمانی: شناسایی ابعاد ضعیف و اعمال ۷ استراتژی هدفمند تا رسیدن به امتیاز هدف — با ضمانت عدم تخریب |
| 🎙️ **تحلیل صدای برند** | پروفایل لحن (رسمیت/فنی/محاوره) + ۴ تیپ صدا + سنجش انسجام متن جدید با صدای برند |
| 🏷️ **عنوان‌ساز CTR-محور** | ۸ الگوی عنوان + امتیازدهی طول/عدد/سؤال/واژه قدرت/موقعیت کلیدواژه |
| 💬 **دستیار فرمان فارسی** | رابط زبان طبیعی: «مقاله بنویس برای اسنوا درباره لباسشویی» → اجرای خودکار |
| ⚡ **کش موتور** | پاسخ‌های پرتکرار آنی — TTL + آمار hit/miss + پاکسازی |
| 🛡️ **امنیت وب** | محافظت SSRF + محدودیت نرخ ساعتی + کش TTL-دار + توکن مخفی وب‌هوک (hash_equals) |

### 🧠 قابلیت‌های نسخه ۲ (همچنان فعال)

🏆 امتیاز کیفیت ۷ بُعدی | 🎰 بهترین-از-N | 🧭 تشخیص نیت جستجو | 🗓️ تقویم محتوای هوشمند | 📰 ۹ نوع مقاله | 📊 سئوی ۲۶ بررسی | 🧮 TF-IDF و خوشه‌بندی | 🌱 ریشه‌یاب فارسی

### 📚 پایگاه دانش

۷۷ برند (۳۱۶ فکت تاریخی) + ۴۲ دستگاه (۴۸۳ ایراد رایج) + ۱۲۵ کد خطا + ۲۱ سناریوی عیب‌یابی وزن‌دار + ۲۰ قطعه یدکی + ۴۷۵ واژه مترادف + ۶۰ عبارت بازنویسی + تقویم فصلی ۱۲ ماه + مناطق ۱۰ شهر

## 🔌 API موتور AI — استفاده از بیرون سایت ساز

موتور AI یک سرویس مستقل با **۳۹ اندپوینت JSON** است که از هر برنامه خارجی (PHP / Python / JavaScript / ربات تلگرام / n8n / Postman) قابل فراخوانی است:

```bash
# 🚀 خط تولید هوشمند — یک فراخوانی = مقاله کامل + سئو + FAQ + تضمین کیفیت
# 🌐 پارامتر research: مقاله با داده‌های زنده وب غنی می‌شود
curl -X POST "https://YOUR-DOMAIN/api/ai/smart-generate" \
  -H "Content-Type: application/json" -H "X-API-Key: YOUR_KEY" \
  -d '{"brand_id": 1, "device_key": "washing_machine", "target_score": 85, "research": true}'

# 💬 دستیار فارسی — بدون نیاز به یادگیری اندپوینت‌ها
curl -X POST "https://YOUR-DOMAIN/api/ai/assistant" \
  -H "Content-Type: application/json" -H "X-API-Key: YOUR_KEY" \
  -d '{"command": "کلمات کلیدی ماشین لباسشویی"}'

# 🌐 جستجوی آنلاین اینترنت (جدید v3.1 — بدون نیاز به کلید ارائه‌دهنده)
curl -X POST "https://YOUR-DOMAIN/api/ai/web-search" \
  -H "Content-Type: application/json" -H "X-API-Key: YOUR_KEY" \
  -d '{"query": "قیمت موتور ماشین لباسشویی", "limit": 5}'

# 🧪 تحقیق ساختاریافته آنلاین — کلیدواژه ترند + سؤالات واقعی + داده تازه
curl -X POST "https://YOUR-DOMAIN/api/ai/research" \
  -H "Content-Type: application/json" -H "X-API-Key: YOUR_KEY" \
  -d '{"topic": "تعمیر یخچال ساید بای ساید"}'
```

📖 **مرجع کامل:** [`docs/AI-API-GUIDE.md`](docs/AI-API-GUIDE.md) — ۱۵۰۰+ خط مستندات فارسی با مثال‌های واقعی به ۴ زبان + راهنمای کامل راه‌اندازی ربات تلگرام

## 🔐 امنیت

- Prepared Statements (ضد SQL Injection)
- CSRF توکن در تمام فرم‌ها + هدر AJAX
- پاکسازی XSS (ورودی و خروجی + SVG)
- CAPTCHA + قفل ۵ تلاش + محدودیت نرخ (۱۲۰/دقیقه)
- آپلود امن (MIME واقعی + امضای باینری + ضد traversal)
- رمزنگاری BCRYPT + نشست امن (httponly, samesite, regeneration)

## 📚 مستندات

پس از ورود به پنل: **منوی مستندات** — شامل راهنمای کاربری، نصب، API، ویرایش دستی، واسط گوگل، کدهای خطا و آیکون‌ها + راهنمای کامل API هوش مصنوعی نسخه ۳.

| سند | مسیر |
|-----|------|
| 🤖 راهنمای کامل API هوش مصنوعی (برای استفاده خارجی) | [`docs/AI-API-GUIDE.md`](docs/AI-API-GUIDE.md) |
| 🔤 دانلود فونت‌ها (۳۰ فونت با لینک رسمی) | [`assets/fonts/README.md`](assets/fonts/README.md) |
| 🖼️ دانلود پک‌های آیکون (۱۰ پک با لینک رسمی) | [`assets/icons/*/README.md`](assets/icons) |
| 📋 چک‌لیست پیشرفت زنده | [`CHECKLIST.md`](CHECKLIST.md) |
| 📒 ورک‌لاگ مرحله‌ای | [`WORKLOG.md`](WORKLOG.md) |
| 📋 سابقه تغییرات | [`CHANGELOG.md`](CHANGELOG.md) |

## 🧪 کیفیت و تست

- **۳۷۸ تست خودکار موفق** در ۷ مجموعه (موتور، رگرسیون v3/v2، پایگاه دانش ×۲، موتور پایه، تجزیزگر SQL نصب‌کننده) — شامل تست‌های زنده اینترنت 🌐 و راستی‌آزمایی مرورگر هدلس ریسپانسیو 📱
- تست‌ها قابل اجرای مجدد: `scripts/test-*.php` (با PHP CLI + MariaDB)

## 📋 پیشرفت پروژه

- ✅ **چک‌لیست کامل:** [`CHECKLIST.md`](CHECKLIST.md) — ۲۷۰+ مورد با وضعیت زنده
- 📒 **ورک‌لاگ:** [`WORKLOG.md`](WORKLOG.md) — گزارش مرحله‌به‌مرحله
- 📄 **سند مرجع:** [`BrandMaker.md`](BrandMaker.md) — پرامپت جامع اولیه درخواست
- 📋 **سابقه تغییرات:** [`CHANGELOG.md`](CHANGELOG.md) — تغییرات هر نسخه

## 📜 لایسنس

اختصاصی — نمایندگی سهند سرویس (ea-fixer.ir)
