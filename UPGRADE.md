# 🔄 راهنمای بروزرسانی — به نسخه ۲.۲۱.۰

> ⚠️ **قبل از هر چیز**: از فایل‌ها و دیتابیس خود بکاپ کامل بگیرید.

---

## 🆕 نسخه ۲.۲۱.۰ — قالب‌سازی همه‌جانبه: دکمه قالب‌ساز و UI/UX Pro روی هر صفحه + کتابخانه ۱۷۰ قالب + رد کردن مرحله

### 📋 چه چیزی تغییر می‌کند؟

| بخش | شرح |
|-----|-----|
| 🎭 دکمه‌های جدید در تب «صفحه‌ها» | کارت هر صفحه دو دکمه دارد: «قالب‌ساز» (ویرایش چیدمان همان صفحه در بوم درگ‌اند‌دراپ) و «✨ طراحی با UI/UX Pro» (طراحی یک‌کلیکی چیدمان حرفه‌ای) |
| 📚 کتابخانه ۱۷۰ قالب آماده | ۱۰ سبک متفاوت × ۱۷ نوع صفحه — با اولین باز کردن صفحه «قالب‌ها» به‌صورت خودکار و **بدون تغییر دیتابیس** اضافه می‌شوند |
| 🧩 قالب‌ساز | دراپ‌داون نوع صفحه: همه انواع صفحات سایت برند (۱۷ گزینه) + حالت ویرایش مستقیم صفحه برند |
| ⏭️ ویزارد ساخت برند | دکمه «رد کردن مرحله و ادامه» هنگام خطای هر مرحله |
| 🐛 رفع باگ | بلوک مرده pagination در طراحی وبلاگ + فعال‌سازی شخصی‌سازی نام برند در هیرو |

### 🔧 روش بروزرسانی (پکیج update.zip)

1. **بکاپ** از فایل‌ها و دیتابیس بگیرید.
2. محتوای `update.zip` را روی فایل‌های فعلی **استخراج/جایگزین** کنید (فهرست فایل‌ها در ادامه).
3. **بدون هیچ تغییر دیتابیس** — جدول‌های جدید یا ستون‌های جدید وجود ندارد.
4. وارد پنل شوید و صفحه **«قالب‌ها»** را باز کنید — پیام «📚 ۱۷۰ قالب کتابلایه جدید اضافه شد» را می‌بینید (فقط در اولین بازدید؛ قالب‌های دستی و پیش‌فرض‌های فعلی شما دست نمی‌خورند).
5. برای اطمینان از تکمیل، دکمه **«↻ بازسازی کتابخانه»** را هم بزنید (بی‌ضرر و idempotent است).

### ✅ راستی‌آزمایی بعد از بروزرسانی

- در **ویرایش یک برند ← تب «صفحه‌ها»** روی کارت هر صفحه دکمه‌های «🎭 قالب‌ساز» و «✨ طراحی با UI/UX Pro» را ببینید.
- روی «قالب‌ساز» کلیک کنید — نوار آبی «حالت ویرایش صفحه برند» بالای بوم نمایش داده شود؛ چند بلوک جابجا کنید و «💾 ذخیره در صفحه برند» را بزنید — به تب صفحه‌ها برگردید.
- روی «✨ طراحی با UI/UX Pro» کلیک کنید — پنل سبز با امتیاز UX و منطق طراحی در کارت همان صفحه ظاهر شود.
- صفحه «قالب‌ها» — هر نوع صفحه ۱۰ کارت با نام سبک‌های مختلف (مدرن/کلاسیک/مینیمال/…) داشته باشد.
- در ساخت برند جدید، اگر مرحله‌ای خطا داد — دکمه «⏭️ رد کردن مرحله … و ادامه مراحل بعد» نمایش داده شود.

### 📁 فایل‌های تغییر‌یافته در این نسخه

| فایل | نوع تغییر |
|------|-----------|
| `engine/generators/TemplateLibrary.php` | 🆕 جدید — کتابخانه ۱۷۰ قالب (۱۰ سبک × ۱۷ نوع) |
| `admin/brand-edit.php` | ✏️ دکمه‌های قالب‌ساز/UI/UX Pro + هندلر AJAX + JS |
| `admin/template-builder.php` | ✏️ ۱۷ نوع صفحه + حالت ?brand_page= + UI |
| `admin/templates.php` | ♻️ بازنویسی — سید کتابلایه + بازسازی + قفل حذف |
| `admin/brand-build.php` | ✏️ دکمه رد کردن مرحله + حالت skipped |
| `engine/skills/UIUXPro.php` | 🐛 رفع کد مرده شخصی‌سازی نام برند در هیرو |
| `engine/knowledge/uiux-patterns.json` | 🐛 بلوک pagination → related-links |
| `config.php` | 🔖 نسخه 2.21.0 |

---

## 📜 سابقه نسخه‌های اخیر

- **2.20.0** — تست نهایی هوشمند استقرار: خوددرمانی .htaccess سه‌سطحی + تست دوسکیم HTTP + دیاگنوستیک error_log
- **2.19.1** — راستی‌آمایی استقرار: پارس چندقالبی لیست cPanel + استعلام مستقیم + آپلود overwrite
- **2.19.0** — استخراج با مکان‌یابی هوشمند + انتقال خودکار به ریشه
- **2.18.0** — ممیزی کامل cPanel: رفع ۸ خانواده باگ (fileop رسمی API2 و…)
- **2.17.0** — خطایاب ریشه‌ای (پل jina) + قالب‌ساز ۱۳۰ عنصر
