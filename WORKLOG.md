# 📒 ورک‌لاگ پروژه — سایت ساز برند سهند سرویس

> 📋 این فایل گزارش مرحله‌به‌مرحله کارهای انجام‌شده در پروژه است.
> وضعیت موارد در [`CHECKLIST.md`](CHECKLIST.md) ردیابی می‌شود.
> سند مرجع درخواست: [`BrandMaker.md`](BrandMaker.md)

---

## 🧾 فرمت ثبت ورودیه‌ها

```
### [شماره] — [عنوان مرحله] 📅 تاریخ
- ✅ کار انجام‌شده ۱
- ✅ کار انجام‌شده ۲
- 🔧 تصمیمات فنی مهم
- 📦 کامیت: <sha> — <پیام>
```

---

## 1️⃣ راه‌اندازی اولیه 📅 2025-09-24

### ✅ اقدامات انجام‌شده
- مطالعه و تحلیل کامل سند درخواست `BrandMaker.md` (۲۰ بخش، ۴۸ مورد چک‌لیست نهایی)
- احراز هویت با گیت‌هاب و ساخت ریپوی `Sahand-Service-BrandMaker` (عمومی، زیر اکانت pranses2011)
- کلون ریپو در محیط توسعه و پیکربندی هویت گیت
- نصب PHP 8.3 CLI (باینری استاتیک) برای تست سینتکس تمام فایل‌های PHP با `php -l`
- ایجاد فایل چک‌لیست پیشرفت `CHECKLIST.md` (۲۲۰+ مورد کار، جدولی، با تیک‌باکس و آیکون و نوار پیشرفت)
- ایجاد همین فایل ورک‌لاگ `WORKLOG.md`
- افزودن سند مرجع `BrandMaker.md` به ریشه ریپو

### 🔧 تصمیمات فنی
- ساختار پروژه دقیقاً مطابق بخش ۱۸ سند (admin/api/engine/assets/core/templates/uploads/docs/geoip)
- PHP 7.4+ سازگار، بدون Composer/Node — فقط کتابخانه‌های داخلی PHP (PDO، GD، ZipArchive)
- تمام کامنت‌ها و رابط کاربری به زبان فارسی و RTL
- استراتژی کامیت: پس از تکمیل هر فاز، چک‌لیست تیک می‌خورد + ورک‌لاگ بروزرسانی + کامیت و پوش

### 📦 کامیت‌ها
- کامیت اول: `init: مستندات پایه (سند درخواست + چک‌لیست + ورک‌لاگ)`

---

## 2️⃣ فاز ۱ — زیرساخت هسته و دیتابیس 📅 2025-09-24

### ✅ اقدامات انجام‌شده
- ساختار کامل پوشه‌ها مطابق بخش ۱۸ سند (admin/api/engine/assets/core/templates/uploads/docs/geoip/includes)
- `config.php` — تنظیمات کلیدی + autoloader بدون Composer + مدیریت خطا + نشست امن + پشتیبانی config.local.php
- `.htaccess` — امنیت (RewriteRule مسدودسازی فایل‌های حساس)، CORS منابع، gzip، کش مرورگر
- `robots.txt` ریشه سایت ساز
- `core/Database.php` — Singleton با PDO + Prepared Statements + تراکنش + helper های insert/update/delete/count
- `core/Auth.php` — ورود با محدودیت تلاش (۵ تلاش/۱۵ دقیقه قفل)، CAPTCHA تصویری GD، CSRF کامل، session fixation protection
- `core/Router.php` — مسیریابی /api/* با پارامترهای نام‌دار + میان‌افزار
- `core/Config.php` — مدیریت تنظیمات Key/Value با JSON + کش درون‌حافظه + ثابت‌های کلیدهای استاندارد
- `core/Logger.php` — لاگ فعالیت دیتابیس + لاگ فایلی ۴ سطحی + تشخیص IP پشت پروکسی
- `core/Cache.php` — کش فایل‌محور با TTL + الگوی remember + آمار
- `core/FileManager.php` — آپلود امن (تشخیص MIME واقعی، ضد XSS در SVG، امضای باینری فونت، ضد Directory Traversal)
- `core/ZipGenerator.php` — بسته‌بندی ZIP با جایگذاری متغیرها در فایل‌های متنی + پاکسازی خودکار + دانلود
- `core/ImageProcessor.php` — تغییر اندازه، فشرده‌سازی، thumbnail مربعی، placeholder
- `core/Mailer.php` — mail() + کلاینت SMTP خام (بدون وابستگی) + قالب HTML فارسی ایمیل درخواست
- `includes/helpers.php` — ۳۰+ تابع: تاریخ جلالی کامل، اعداد فارسی، اعتبارسنجی موبایل/تلفن ایرانی، slug، فلش‌مسیج
- `database.sql` — اسکیمای کامل ۳۱ جدول + داده‌های اولیه (دسته‌بندی مقالات، تگ‌های وبمستر، تنظیمات پیش‌فرض)
- `install.php` — نصب‌کننده ۴ مرحله‌ای فارسی RTL با بررسی الزامات + ساخت ادمین + کلید API + قفل نصب
- تست سینتکس تمام ۱۱ فایل PHP با php -l — ✅ همه سالم

### 🔧 تصمیمات فنی
- ۳۱ جدول (بیشتر از ۲۵ جدول الزامی سند): جداول تکمیلی api_keys، rate_limits، service_areas، faqs، testimonials، article_categories، request_attachments اضافه شدند
- IP خام ذخیره نمی‌شود — فقط هش + پیشوند برای GeoIP (حریم خصوصی)
- رابط‌های JSON برای داده‌های ساختاریافته (tags، categories، palette) — انعطاف‌پذیری بدون تغییر اسکیما
- config.local.php برای اطلاعات حساس (در .gitignore)

### 📦 کامیت‌ها
- `phase-1: زیرساخت هسته — ۱۰ کلاس core + دیتابیس ۳۱ جدول + نصب‌کننده فارسی + helpers`

---

## 3️⃣ فاز ۲ — موتور هوش مصنوعی داخلی (SahandAI) 📅 2025-09-24

### ✅ اقدامات انجام‌شده
- **utils** — `TextProcessor` (نرمال‌سازی فارسی، مترادف‌سازی قطعی seeded، بازنویسی جمله، shingle، هش یکتایی)، `SlugGenerator` (فارسی/انگلیسی + یکتایی DB + دامنه برند)، `Validator` (قوانین + پاکسازی HTML)
- **analyzers** — `ColorAnalyzer` (K-Means واقعی روی پیکسل‌های لوگو + طبقه‌بندی + ۲ پالت روشن/تاریک ۱۷ متغیره + تولید CSS)، `UniquenessChecker` (شباهت Jaccard سه‌گرامی + تلاش مجدد خودکار + گزارش سلامت سیستم)، `KeywordAnalyzer` (استخراج + بی‌گرام/سه‌گرام + تراکم + پیشنهاد long-tail محلی)، `SeoAnalyzer` (امتیاز ۰-۱۰۰ با ۱۸ چک وزنی + درجه + پیشنهاد عملی)
- **generators** — `ContentGenerator` (الگوریتم ۶ مرحله‌ای: قالب←جایگذاری←مترادف←یکتایی←سئو)، `ArticleGenerator` (Outline پویا ۵ نوع موضوع + جدول کد خطا + لینک داخلی خودکار + موضوعات پیشنهادی)، `SeoGenerator` (عنوان ≤۶۰ + متا ≤۱۶۰ + OG + Twitter + ۵ نوع Schema JSON-LD + رندر head کامل)، `BrandInfoGenerator` (تطبیق فازی برند + پکیج کامل + افزودن برند به دانش)، `FaqGenerator` (۱۶ قالب سوال/پاسخ + کش)، `ErrorCodeGenerator` (تولید از دانش + ورود JSON/Excel با پارسر XLSX بدون وابستگی!)
- `engine/SahandAI.php` — فاساد اصلی با ۹+ اندپوینت منطقی + آمار موتور
- **پایگاه دانش ۸ فایل JSON**: ۵۷ برند (تاریخچه/کشور/دستگاه‌ها/نقاط قوت)، ۳۲ دستگاه (۱۸۹ ایراد رایج + ۱۳۱ نکته نگهداری + ۹۷ نکته استفاده)، ۱۲+ قالب معرفی، ۵+ تاریخچه، ۴ توضیح دستگاه، ۱۶ FAQ، ۵ نوع عنوان مقاله، ۲۳۴ واژه مترادف، الگوهای سئو، ۸ دستگاه کد خطا (۳۰+ کد)
- راه‌اندازی محیط تست کامل: PHP 8.3 استاتیک + MariaDB 11.8 پرتابل + دیتابیس تست با ۳۳ جدول
- **تست واحد ۴۳/۴۳ موفق** + **تست انتها-به-انتهای ۲۹/۲۹ موفق**

### 📊 نتایج کلیدی تست E2E
| شاخص | نتیجه | الزام سند |
|------|-------|-----------|
| معرفی برند | ۱۲۸ کلمه (۲-۳ پاراگراف) | ✅ |
| تاریخچه برند | ۵۷۳ کلمه | ۵۰۰-۱۰۰۰ ✅ |
| مقاله تولیدی | ۱۴۸۱ کلمه + هدینگ + جدول + لینک داخلی | ۸۰۰-۱۵۰۰ ✅ |
| یکتایی متن دو برند | ۰٪ شباهت | ✅ |
| یکتایی دو مقاله | ۲۳.۷٪ (زیر آستانه ۳۵٪) | ✅ |
| تحلیل لوگو | رنگ دقیق #1e40af + ۲ پالت ۱۷ متغیره | ✅ |
| FAQ برند | ۱۰-۱۲ عدد یکتا | ✅ |
| کدهای خطای خودکار | ۳۱ عدد | ✅ |

### 🔧 تصمیمات فنی
- کلیدواژه Seedable Random (crc32 + LCG) — تولید تکرارپذیر و عیب‌یابی‌پذیر
- IP خام ذخیره نمی‌شود؛ سطوح یکتایی با هش سنجیده می‌شود
- پارسر Excel با خواندن مستقیم XML داخل ZIP (sharedStrings + sheet1) — بدون هیچ کتابخانه خارجی
- بهبود حجم محتوا: ترکیب چند قالب + دانش واقعی برند برای رسیدن به کلمات الزامی سند

### 📦 کامیت‌ها
- `phase-2: موتور AI داخلی کامل — ۶ ژنراتور + ۴ آنالایزر + پایگاه دانش ۵۷ برند/۳۲ دستگاه + تست ۷۲ مورد`

---

## 4️⃣ فاز ۳ — پنل مدیریت پایه 📅 2025-09-24

### ✅ اقدامات انجام‌شده
- `assets/css/admin.css` — تم کامل RTL فارسی (۵۵۳ خط): سایدبار، کارت، جدول، فرم، سوییچ، تب، مودال، ریسپانسیو موبایل
- `assets/js/admin.js` — تعاملات: تب، مودال، حذف با تأیید، ردیف تکرارشونده، کپی، پیش‌نمایش تصویر، اسپینر
- `admin/includes/header.php` — لایه‌بندی مشترک: احراز هویت + سایدبار ۶ بخش + نوار بالا + نشان درخواست‌های جدید + فلش‌مسیج
- `admin/includes/footer.php` — فوتر با نسخه سیستم
- `admin/login.php` — ورود با CAPTCHA + CSRF + پیام‌های فارسی
- `admin/captcha.php` + `admin/logout.php`
- `admin/index.php` — داشبورد: ۶ کارت آماری + نمودار CSS ۷ روزه + وضعیت موتور AI + درخواست‌های اخیر + تایم‌لاین فعالیت + دسترسی سریع
- `admin/settings.php` — تنظیمات جامع ۷ تب (۹ زیربخش سند): پایه، تماس چندتایی نامحدود (تلفن/موبایل/واتساپ/تلگرام/ایمیل/آدرس با مختصات/شبکه اجتماعی)، ساعات کاری، ضمانت، هزینه+دامنه، ارسال درخواست (ایمیل+SMTP+تلگرام+واسط گوگل+بله)، لینک‌دهی
- `admin/webmaster.php` — مدیریت ۵ سرویس وبمستر + افزودن سفارشی + فعال/غیرفعال
- `admin/docs.php` — نمایش‌دهنده ۷ سند مستندات

### 🐛 باگ‌های رفع‌شده حین تست
- captcha.php سشن را ذخیره نمی‌کرد (SAHAND_NO_SESSION اشتباهی) — رفع شد و اعتبارسنجی کپچا واقعاً فعال شد
- خطای سینتکس `<?= endif; ?>` در index و settings — اصلاح به تگ PHP صحیح

### 🧪 تست‌ها
- تست HTTP کامل با سرور داخلی PHP + MariaDB واقعی: **۲۶/۲۶ موفق**
  (رندر ورود، تولید تصویر کپچا، ورود صحیح/غلط، رندر ۴ صفحه، عناصر داشبورد، ۹ بخش تنظیمات، گارد احراز هویت، خروج)

### 📦 کامیت‌ها
- `phase-3: پنل مدیریت — تم RTL + ورود/کپچا + داشبورد + تنظیمات ۹ بخش + وبمستر + ۲۶ تست HTTP`

---

## 5️⃣ فاز ۴ — مدیریت برندها + تحلیل لوگو + فرآیند ساخت 📅 2025-09-24

### ✅ اقدامات انجام‌شده
- `admin/brands.php` — لیست برندها با جستجو/فیلتر وضعیت/آمار هر برند + حذف با تأیید + فعال/غیرفعال
- `admin/brand-new.php` — فرم ایجاد برند: نام فارسی/انگلیسی + آپلود لوگو + انتخاب دامنه (زیردامنه خودکار/اختصاصی) + پیش‌نمایش زنده دامنه و لوگو
- `admin/brand-build.php` — 🏗️ فرآیند ساخت ۸ مرحله‌ای با نوار پیشرفت زیبا و انیمیشن + نمایش جزئیات زنده هر مرحله + سواچ رنگ‌های استخراج‌شده + قابلیت تلاش مجدد از مرحله خطادار
- `admin/brand-build.ajax.php` — اندپوینت AJAX مراحل: ۱) تحلیل لوگو K-Means ۲) تطبیق دانش ۵۷ برند ۳) تولید محتوای صفحات + ایجاد ۱۵ صفحه استاندارد ۴) ۵ مقاله (اولی منتشر + ۴ زمان‌بندی ۳/۶/۹/۱۲ روز) ۵) FAQ ۶) کدهای خطا ۷) سئو + امتیازدهی ۸) نهایی‌سازی
- `admin/brand-edit.php` — ویرایش جامع ۶ تب: اطلاعات | دستگاه‌ها (نمایش/برجسته/ترتیب) | صفحات (ویرایش محتوای AI) | پالت رنگ (۱۷ متغیر × ۲ تم با color picker + بازتولید از لوگو) | سئو (امتیاز دایره‌ای + شمارنده طول + پیشنهاد بهبود) | API (کلید با کپی)

### 🐛 باگ حیاتی رفع‌شده
- **خطای headers already sent**: تمام صفحات ادمین هدر را قبل از پردازش POST فراخوانی می‌کردند → ریدایرکت خراب. ساختار ۵ فایل (brands, brand-new, brand-edit, settings, webmaster) اصلاح شد: پردازش POST قبل از هر خروجی

### 🧪 تست‌ها
- **تست E2E فاز ۴: ۳۳/۳۳ موفق** (ورود → ایجاد برند با لوگو → ۸ مرحله AJAX → راستی‌آزمایی دیتابیس → ویرایش → لیست → CSRF)
- نتایج کلیدی: رنگ دقیق `#be1e2d` از لوگو استخراج شد | ۱۵ صفحه | ۵ مقاله (۱۲۳۹۲ کاراکتر) | ۲۱ FAQ | ۱۹ کد خطا | زمان‌بندی ۴ مقاله | امتیاز سئو صفحات

### 📦 کامیت‌ها
- `phase-4: برندها CRUD + تحلیل لوگو K-Means + فرآیند ساخت ۸ مرحله‌ای AJAX + ویرایش ۶ تب + ۳۳ تست`

---

## 6️⃣ فاز ۵ و ۶ — قالب‌ساز، تم، منوها + آیکون و فونت 📅 2025-09-24

### ✅ فاز ۵ — قالب‌ها و تم‌ها
- `admin/templates.php` — مدیریت قالب‌ها + seed خودکار ۲۵ قالب (۵ نوع صفحه × ۵ طرح: مدرن/کلاسیک/مینیمال/حرفه‌ای/خلاقانه) با چیدمان‌های متنوع + پیش‌نمایش بصری بلوک‌ها + قالب پیش‌فرض هر نوع
- `admin/template-builder.php` — 🎭 قالب‌ساز درگ‌اند‌دراپ کامل: کتابخانه ۱۵ دسته بلوک (هدر/هیرو/محتوا/کارت/فرم/آمار/نظرات/FAQ/گالری/نقشه/CTA/تماس/فوتر/جداکننده/سفارشی) + بوم HTML5 Drag&Drop + جابجایی مرتب + کپی/حذف + پنل ویژگی‌ها (فاصله/پس‌زمینه/عنوان/نمایش) + نمای دسکتاپ/تبلت/موبایل + دابل‌کلیک برای افزودن سریع + ذخیره JSON
- `assets/css/builder.css` — استایل مخصوص قالب‌ساز
- `admin/themes.php` — سیستم تم: ترکیب قالب ۵ صفحه + پیش‌فرض + اختصاص به برند + حذف با بررسی وابستگی + seed تم پیش‌فرض
- `admin/menus.php` — مدیریت منو: درختی با زیرمنو + درگ‌اند‌دراپ + آیکون + لینک سفارشی/صفحه سیستمی + فعال/غیرفعال + منوی عمومی و per-brand + بازنشانی پیش‌فرض + seed خودکار منوهای استاندارد

### ✅ فاز ۶ — آیکون‌ها و فونت‌ها
- **پک ۱۰ (اختصاصی)**: ۱۰۳ آیکون SVG لوازم خانگی (تولید برنامه‌ای با اسکریپت + اعتبارسنجی XML) — یخچال، لباسشویی، ظرفشویی، کولر، پکیج، تلویزیون + تجهیزات فنی (کمپرسور، سنسور، موتور) + وضعیت‌ها
- **۹ پک عمومی**: ساختار کامل + ۸ آیکون نمونه هر پک + manifest.json + README ایمپورت (قابل توسعه تا ۱۶۰۰+)
- `admin/icons.php` — جستجوی سراسری در همه پک‌ها + پیش‌نمایش + ایمپورت ZIP با فیلتر ضد XSS اسکریپت + به‌روزرسانی خودکار manifest
- **۳۰ فونت**: مانیفست ۲۰ فارسی + ۱۰ انگلیسی + `fonts.css` با ۱۳۴ تعریف @font-face + ساختار ۳۰ پوشه + README
- `admin/fonts.php` — وضعیت فایل هر وزن + آپلود فونت + تنظیمات فونت پیش‌فرض (عنوان/متن/اعداد/وزن/اندازه/ارتفاع خط)
- `assets/scripts/google-apps-script.js` — اسکریپت آماده واسط تلگرام (توکن بیرون از اسکریپت + ارسال لوگو + مدیریت خطا + تست GET)

### 🐛 رفع اشکال
- menus.brand_id در اسکیما NULL پذیر شد (منوی عمومی) + ALTER دیتابیس تست
- انتقال بلوک‌های seed (قالب/تم) قبل از خروجی — رفع warning هدر

### 🧪 تست
- رندر هر ۵ صفحه جدید بدون هیچ Warning (۰ هشدار در لاگ سرور) + seed خودکار ۲۵ قالب و ۱ تم راستی‌آزمایی شد

### 📦 کامیت‌ها
- `phase-5-6: قالب‌ساز درگ‌اند‌دراپ + ۲۵ قالب + تم + منوی درختی + ۱۰۳ آیکون اختصاصی + ۳۰ فونت + اسکریپت گوگل`

---

> ⚡ **ورودیه‌های بعدی در ادامه همین فایل ثبت می‌شوند.**

---

## 7️⃣ فاز ۷ و ۸ — محتوا، سئو، کدهای خطا + آمار و درخواست‌ها 📅 2025-09-24

### ✅ فاز ۷ — محتوا و سئو
- `admin/articles.php` — لیست با فیلتر برند/وضعیت + صفحه‌بندی + ویرایش کامل (عنوان/محتوا HTML/خلاصه/تگ/سئو) + تغییر وضعیت سریع + 🤖 تولید مقاله AI با انتخاب برند/موضوع/دستگاه
- `admin/seo.php` — مرکز سئو ۴ تب: نمای کلی (میانگین امتیاز + راهنمای ۱۰۰/۱۰۰) | برندها و صفحات (جدول امتیاز هر صفحه) | چک‌لیست فنی ۱۲ موردی | ابزار تحلیل محتوا و کلیدواژه با نتیجه زنده
- `admin/error-codes.php` — CRUD + جستجو + فیلتر عمومی/برند + ورود Excel (پارسر XLSX داخلی) + ورود JSON + تولید AI + رنگ‌بندی شدت

### ✅ فاز ۸ — آمار و درخواست‌ها
- `core/NotificationService.php` — 📨 ارسال ۴ کاناله: ایمیل + تلگرام مستقیم + واسط Google Script (توکن خارج از اسکریپت طبق الزام) + بله — همه با نام و لوگوی برند + ثبت اعلان داخلی
- `core/GeoIP.php` + `geoip/iran-ranges.php` — 🗺️ دیتابیس محلی بازه‌های IP ایران (بیش از ۱۰۰ بازه، ۳۰+ شهر) بدون هیچ API خارجی — تست: تهران/مشهد/اصفهان/شیراز صحیح
- `admin/analytics.php` — داشبورد تحلیلی: بازدید یکتا/کل + مدت حضور + نرخ پرش + 📈 نمودار خطی Canvas (بدون وابستگی) + 🥧 دونات سهم برندها + 🗺️ نقشه حرارتی شهرها با GeoIP + صفحات پربازدید + مرورگر/سیستم‌عامل + خروجی CSV + فیلتر برند/بازه
- `admin/requests.php` — لیست + فیلتر + 👁️ نمای جزئیات کامل (لوگو + تمام فیلدها + تصاویر پیوست + تماس کلیک‌شونده) + ۵ وضعیت + یادداشت + ارسال مجدد به کانال‌ها + حذف + خروجی CSV با BOM فارسی

### 🐛 رفع اشکال
- Database::count از alias پشتیبانی کرد («table r» → \`table\` r)
- حذف ستون dummy از کوئری timeline آمار

### 🧪 تست
- ۷/۷ صفحه بدون هیچ Warning رندر شد + ثبت درخواست تستی + نمای جزئیات راستی‌آزمایی شد

### 📦 کامیت‌ها
- `phase-7-8: مقالات + مرکز سئو ۴ تب + کدهای خطا + NotificationService چهارکاناله + GeoIP محلی + داشبورد آماری Canvas + مدیریت درخواست‌ها`

---

## 8️⃣ فاز ۹ — API داخلی + خروجی ZIP + هسته سایت برند 📅 2025-09-24

### ✅ API داخلی (11 فایل اندپوینت + 3 میان‌افزار)
- `api/index.php` — روتر کامل با ۲۵+ مسیر: برند، صفحه، مقالات، مقاله تکی، کدهای خطا، FAQ، دستگاه‌ها، منو، درخواست، ردیابی، تنظیمات، آیکون، فونت، برندها + ۱۰ اندپوینت AI
- `middleware/auth.php` — X-API-Key با تأخیر ضد کشف کلید + آمار استفاده
- `middleware/rate-limit.php` — سقف ۱۲۰ درخواست/دقیقه/IP با پاکسازی احتمالی
- `middleware/cors.php` — دسترسی سایت‌های برند
- مسیرهای عمومی هوشمند: request (کلید در بدنه)، track، icon (تگ img)، brands

### ✅ هسته سایت برند (templates/brand-core — ۳۳ فایل)
- `config.php` (قالب با placeholder) + `index.php` صفحه اصلی کامل (هیرو/معرفی/خدمات/چرا ما/CTA/مقالات) + `404.php` زیبا
- `includes/` — header (سئو کامل + OG + Schema + منو + لوگوی دوگانه + سوییچ تم)، footer (تماس + سایر برندها + لینک سایت اصلی)، floating-btn انیمیشنی، seo، functions
- `pages/` — ۱۵ صفحه کامل: services، blog + article (مرتبط + CTA)، request (۱۰ فیلد + فیلد شرطی «سایر» + پیش‌نمایش تصاویر + AJAX)، contact، other-brands، faq (آکاردئون + Schema)، error-codes (جستجو + فیلتر + رنگ‌بندی شدت)، warranty، service-area، about×2، terms، privacy، sitemap-page
- `css/style.css` — تم کامل RTL (۷۰۰+ خط): ریسپانسیو، تم روشن/تاریک، FAB، کارت، فرم، جدول
- `js/` — tracker (session یکتا + sendBeacon + مدت حضور)، app (شمارنده + آفلاین)، form
- `htaccess.template` — URL rewriting کامل + امنیت + gzip

### ✅ خروجی ZIP
- `admin/export.php` — انتخاب برند → جایگذاری ۸ متغیر (کلید API، برند، پالت CSS) + تولید خودکار sitemap.xml (صفحات + مقالات) + robots.txt + راهنمای استقرار + لیست ZIP های موجود

### 🐛 رفع اشکال
- ip_hash/session_hash از CHAR(40) به CHAR(64) (sha256) — اسکیما + دیتابیس تست
- مسیرهای عمومی track/icon از احراز هویت مستثنا شدند

### 🧪 تست فاز ۹: ۳۳/۳۳ موفق
- ۱۳ اندپوینت API + احراز هویت 401 + ثبت درخواست معتبر/نامعتبر 422 + ۳ اندپوینت AI + ردیابی بازدید + ZIP (۳۳ فایل، sitemap، robots، htaccess، جایگذاری کلید و پالت) + سرو SVG

### 📦 کامیت‌ها
- `phase-9: API کامل ۲۵+ مسیر + هسته سایت برند ۳۳ فایل + خروجی ZIP با جایگذاری متغیر — ۳۳ تست`

---

## 9️⃣ فاز ۱۰ — مستندات + راستی‌آزمایی نهایی 📅 2025-09-24

### ✅ مستندات فارسی (۷ سند HTML)
- `docs/installation.html` — نصب ۵ مرحله‌ای + پیش‌نیازها + رفع اشکال
- `docs/user-guide.html` — راهنمای کامل کاربری: داشبورد، ساخت برند، ویرایش ۶ تب، درخواست‌ها، تولید AI، قالب‌ساز
- `docs/api-reference.html` — مستند فنی API: احراز هویت، ۲۵+ اندپوینت با پارامترها، نمونه PHP، کدهای خطا
- `docs/manual-editing.html` — ویرایش دستی: ساختار، رنگ‌ها، محتوا، کش، صفحه جدید
- `docs/google-script.html` — راه‌اندازی ۷ مرحله‌ای واسط + کد کامل قابل کپی + رفع اشکال (توکن خارج از اسکریپت)
- `docs/error-codes.html` — فرمت Excel (۸ ستون) و JSON + مراحل ورود
- `docs/icons-guide.html` — پک‌ها + ایمپورت + استفاده در سایت‌های برند

### ✅ تکمیل‌های نهایی
- `templates/brand-core/manifest.json` — PWA با placeholder
- `admin/export.php` — جایگذاری THEME_COLOR و BRAND_LOGO در ZIP
- `README.md` — معرفی کامل پروژه با نصب سریع

### 🏁 راستی‌آزمایی نهایی چک‌لیست ۴۸ موردی بخش ۲۰ سند
**نتیجه: ۷۷/۷۷ موفق** شامل:
- ✅ هر ۴۸ ویژگی الزامی سند (پنل، تحلیل لوگو، AI بدون API، مقالات، سئو، قالب‌ساز، تم، ۱۰ پک آیکون، ۳۰ فونت، آمار، درخواست‌ها، ارسال ۴ کاناله، GeoIP، امنیت، مستندات...)
- ✅ رندر تمام ۲۱ صفحه پنل بدون هیچ خطا
- ✅ نمایش تمام ۷ مستند

### 📊 آمار کل پروژه
| شاخص | مقدار |
|------|-------|
| فایل‌های PHP | ۶۵+ (همه لینت‌شده) |
| خطوط کد PHP | ~۱۸,۰۰۰ |
| جداول دیتابیس | ۳۳ |
| آیکون SVG اختصاصی | ۱۰۳ |
| برندهای پایگاه دانش | ۵۷ |
| دستگاه‌های پایگاه دانش | ۳۲ (۴۱۷ آیتم دانش) |
| تست‌های موفق تجمعی | **۲۵۱+** (۴۳ واحد + ۲۹ E2E + ۲۶ HTTP + ۳۳ فاز۴ + ۳۳ فاز۹ + ۷۷ نهایی + ۵ GeoIP + ...) |
| کامیت‌های گیت | ۹ |

### 📦 کامیت‌ها
- `final: مستندات ۷ سند + PWA + راستی‌آزمایی کامل ۷۷/۷۷ — پروژه ۱۰۰٪ تکمیل شد`

## 🔟 فاز K — تقویت و قدرتمندسازی پایگاه دانش موتور AI 📅 2025-09-23

> 🧠 به درخواست مالک پروژه: «فایل‌های engine/knowledge/ را تقویت کن و بسیار قدرتمندترش کن»

### K.1 — brands.json (۵۷ → ۷۷ برند)
- 🔴 **رفع باگ مهم**: `history_facts` در هر ۵۷ برند به‌جای آرایه، رشته واحد بود؛ سیستم فقط ۱ فکت از هر برند استفاده می‌کرد → به آرایه‌های ۳-۸ فکتی تبدیل شد
- ۸ فیلد جدید برای همه برندها: `slogan`, `positioning`, `price_range`, `parts_availability`, `aliases`, `service_note`, `popular_models`, `tech_facts`
- ۲۰ برند جدید: دلسا، جی‌پلاس، اخوان، زانوسی، هوور، برانت، آسپس، اسمگ، لایبهر، گروندیگ، تلوفونکن، آرچلیک، سیلورلاین، بمپانی، وستفروست، بلومبرگ، کارشر، دایسون، هیتاچی، اکس
- جمع: ۳۱۶ فکت تاریخی، ۳۰۸ نقطه قوت، ۲۶۰ فکت فنی | اصلاح کلیدهای دستگاه نامعتبر (radiator, sink)

### K.2 — devices.json (۳۲ → ۴۲ دستگاه)
- ۶ فیلد فنی جدید برای همه دستگاه‌ها: `avg_lifespan`, `service_interval`, `energy_note`, `safety_warnings`, `diy_checks`, `parts_wear`
- ۱۰ دستگاه جدید: پلوپز، چای‌ساز، بخارشوی، ماشین اصلاح، آبسردکن، فن کویل، آبگرمکن خورشیدی، تصفیه هوا، رطوبت‌گیر، وین‌کلاب
- جمع: ۴۸۳ ایراد رایج، ۳۰۸ نکته نگهداری، ۲۳۰ نکته مصرف، ۱۳۶ قطعه مصرفی

### K.3 — error-codes.json (۸ دسته/۳۸ کد → ۱۲ دسته/۱۲۵ کد)
- ۴ دسته جدید: فر، اجاق گاز، خشک‌کن، هود
- فیلدهای جدید برای همه کدها: `brand_examples` (کدام برندها این کد را می‌دهند) و `reset_tip` (راهنمای ریست قبل از تماس با تعمیرکار)
- کدهای واقعی برندها: 4E/5E سامسونگ، F21/F43 بوش، E15 بوش ظرفشویی، F08 اریستون، H3/F1 گری و…

### K.4 — synonyms.json (۲۳۴ → ۴۷۵ واژه)
- ۲۴۱ واژه جدید در ۱۲ حوزه: قطعات فنی، خدمات، قیمت، زمان، مکان، صفات، پدیده‌ها، وضعیت‌ها
- ۱۱۸۴ مترادف جمعاً | نرمال‌سازی مقادیر رشته‌ای به آرایه

### K.5 — keywords + sentences + seo-patterns
- keywords: ۳۰ شهر، ۲۵ خدمت، ۳۰ الگوی long-tail + **کلیدهای جدید**: question_patterns (۲۰)، commercial، informational، negative_keywords، seasonal ساختاریافته
- sentences: openers ۱۴→۴۱، connectors ۱۰→۳۴، closers ۵→۲۰ + **کلیدهای جدید**: transitions، emphasis، warnings، stats_intro، cta_phrases
- seo-patterns: title_patterns ۵→۱۵، meta_patterns ۴→۱۱ + **۸ بخش جدید**: slug_rules، internal_linking، content_structure، readability، featured_snippet، local_seo، image_seo، voice_search + اسکیمای HowTo/Product

### K.6 — templates.json (همه بخش‌ها ۲-۳ برابر)
- brand_intro ۱۲→۱۸، brand_history ۵→۹، agency_about ۳→۸، services_intro ۲→۶، warranty ۲→۷، device_desc ۴→۱۰، article_intro ۵→۱۳، article_conclusion ۵→۱۳، knowledge_expansion ۵→۱۸
- FAQ ۱۶→۳۰ (هزینه، زمان، قطعه اصلی، در محل، فوریت و…)
- **۲ نوع مقاله جدید**: buying_guide و energy_saving (۶+۶ عنوان)
- قالب‌های جدید از فیلدهای تازه: `{{slogan}}`، `{{positioning}}`

### K.7-K.11 — ۵ فایل دانش جدید 🆕
| فایل | محتوا |
|------|-------|
| `phrases.json` | ۶۰ عبارت × ۳ جایگزین — بازنویسی عبارت‌های چندکلمه‌ای |
| `diagnostics.json` | ۲۱ سناریوی عیب‌یابی علامت‌محور با علت‌های وزن‌دار (probability) + اقدام فوری + زمان تماس |
| `parts.json` | ۲۰ قطعه یدکی: نقش، علائم خرابی، عمر، سختی تعویض، رده قیمت، ترفندها |
| `seasonal-calendar.json` | ۱۲ ماه جلالی × دستگاه‌ها × خدمات × پیام بازاریابی × ایده محتوا |
| `city-areas.json` | ۱۰ شهر بزرگ با محله‌های واقعی (۴۸ منطقه تهران) برای سئوی محلی |

### K.12 — یکپارچه‌سازی موتور (دانش → قدرت واقعی)
- 🆕 `engine/utils/KnowledgeBase.php`: سرویس یکپارچه — stats، diagnose (تطبیق سه‌سطحی فازی)، partsForSymptom، currentSeason (ماه جلالی)، areasOf
- `TextProcessor`: متد جدید `applyPhrases()` — تنوع‌سازی سه‌لایه (عبارت ← مترادف ← ساختار)
- `BrandInfoGenerator::matchBrand`: تطبیق با `aliases` (مثلاً «سنوا» → اسنوا، «ال جی» → ال‌جی)
- `ContentGenerator`: متغیرهای جدید slogan/positioning/tech_facts_list + لایه applyPhrases
- `ArticleGenerator`: بخش‌های جدید در مقالات — «عیب‌یابی هوشمند» (جدول علت/احتمال/بررسی)، «جدول قطعات مصرفی»، «نکات ریست» + ۷ نوع مقاله
- `SahandAI::engineStats`: آمار دانش جدید + ۵ متد API جدید

### K.13 — ۸ مسیر API جدید
`GET ai/knowledge/stats` | `GET ai/diagnostics` | `GET ai/diagnostics/{device}` | `POST ai/diagnose` | `GET ai/seasonal` | `GET ai/seasonal/{month}` | `GET ai/parts` | `GET ai/parts/{key}`

### K.14 — تست‌ها: ۱۴۱ موفق ✅
- `test-knowledge-base.php` (۷۱ تست): آمار، عیب‌یابی، قطعات، تقویم، مناطق، بازنویسی، aliases، فیلدهای جدید
- `test-knowledge-e2e.php` (۲۷ تست): تولید واقعی مقاله ۱۴۸۶ کلمه‌ای با جدول عیب‌یابی هوشمند + نوع‌های جدید
- `test-ai-engine.php` (۴۳ تست): بدون رگرسیون

### 📊 مقایسه قبل/بعد
| شاخص | قبل | بعد | رشد |
|------|-----|-----|-----|
| برندها | ۵۷ | ۷۷ | +۳۵٪ |
| فکت تاریخی برند | ~۵۷ (۱ رشته) | ۳۱۶ | ×۵.۵ |
| دستگاه‌ها | ۳۲ | ۴۲ | +۳۱٪ |
| ایرادات رایج | ۱۸۹ | ۴۸۳ | ×۲.۵ |
| کدهای خطا | ۳۸ | ۱۲۵ | ×۳.۳ |
| واژه مترادف | ۲۳۴ | ۴۷۵ | ×۲ |
| قالب FAQ | ۱۶ | ۳۰ | +۸۷٪ |
| نوع مقاله | ۵ | ۷ | +۴۰٪ |
| فایل دانش | ۸ | ۱۳ | +۶۲٪ |
| سناریوی عیب‌یابی | ۰ | ۲۱ | 🆕 |
| دانش قطعات | ۰ | ۲۰ | 🆕 |

- کامیت‌ها: `phase-k1-k2`، `phase-k3-k11`، `phase-k12-final`

---

## 🤖 فاز L — موتور هوش مصنوعی نسخه ۲ + رلیز v1.1.0 (2025-09-23)

> درخواست مالک پروژه: «موتور هوش مصنوعی را قدرتمندتر کن و رلیز برای نصب بساز»

### L.1 — TextProcessor v2 🧹
`stem()` ریشه‌یاب سبک فارسی (۱۶ پسوند + پاکسازی نیم‌فاصله) | `tokenize()` عمومی | `sentenceSplit()` | `readability()` هفت‌متریکه (طول جمله، نسبت جمله بلند، TTR ریشه‌ای، نسبت واژه پیچیده + امتیاز ۰-۱۰۰) | `transitionWordCount()` با ۴۵ واژه رابط فارسی

### L.2 — QualityScorer 🏆 (ماژول جدید)
امتیاز کیفیت محتوا در **۷ بُعد وزنی** (خوانایی ۲۲ / ساختار ۲۰ / انسجام ۱۴ / تمرکز ۱۴ / جذابیت ۱۲ / حجم ۱۰ / تنوع ۸) + نقاط قوت/ضعف + پیشنهادهای فارسی + درجه‌بندی

### L.3 — IntentClassifier 🧭 (ماژول جدید)
تشخیص نیت جستجو در ۴ کلاس (اطلاعاتی/تجاری/تراکنشی/ناوبری) با ۳ لایه سیگنال: الگوهای regex + الگوهای پایگاه دانش (پاکسازی جایگاه‌نما) + نشانه‌های سؤالی و برند؛ خروجی شامل سطح اطمینان و نوع محتوای پیشنهادی

### L.4 — KeywordAnalyzer v2 🔍
`extractWithStemming()` خوشه‌بندی ریشه‌ای با شکل نمایشی | `tfIdf()` هموار روی corpus دلخواه | `cluster()` خوشه‌بندی موضوعی با واژه ستون | `estimateCompetition()` هیوریستیک رقابت‌پذیری با دلایل | `suggestLongTail()` ترکیب دستگاه×شهر×اصلاح‌گر×سؤال

### L.5 — SeoAnalyzer v2 📊
۹ بررسی جدید: کلیدواژه در H2/H3، نسبت واژه‌های رابط، حضور پرسش، اسکیمای JSON-LD، کفایت تصویر، تنوع انکر، E-E-A-T (۴ سیگنال)، امتیاز خوانایی داخلی — مجموع **۲۶ بررسی وزنی** + متریک خوانایی در خروجی

### L.6 — ArticleGenerator v2 📰
**بهترین-از-N**: تولید ۱-۴ واریانت با seed متفاوت و انتخاب بالاترین امتیاز QualityScorer | جعبه «نکات کلیدی» TL;DR بعد از مقدمه | بخش سوالات متداول + FAQ Schema در JSON-LD | لینک مقالات مرتبط برند (سیلو داخلی) | تضمین حداقل ۸۰۰ کلمه با گسترش خودکار

### L.7 — ۲ نوع مقاله جدید 🍂💰
`seasonal_care` (مقدمه فصلی زنده از تقویم جلالی + قطعات پرتقاضای فصل) و `cost_guide` (عوامل قیمت + تصمیم تعمیر/خرید) — مجموع **۹ نوع مقاله**

### L.8 — ContentPlanner 🗓️ (ماژول جدید)
تقویم انتشار N هفته‌ای: اولویت‌بندی ترکیبی (فصلی ۳۰+ / شکاف پوشش ۴۰- / رقابت‌پذیری ۱۰+) + عنوان از قالب‌های دانش + نیت جستجو + ساعت انتشار بهینه + نقشه پوشش قبل

### L.9-L.10 — SahandAI v2 + API 🔌
`ENGINE_VERSION = 2.0.0` | متدهای جدید: scoreContent، classifyIntent، contentPlan، suggestLongTail، clusterKeywords، tfidf، engineInfo | ۷ مسیر API جدید + پارامتر variants در generate-article

### L.11 — قالب‌های دانش 📚
`article_topics.seasonal_care` (۵) + `article_topics.cost_guide` (۵) + `takeaway_shapes` (۵)

### L.12-L.13 — تست‌ها: ۲۱۱ موفق ✅
- `test-engine-v2.php` (۷۰ تست جدید): همه ماژول‌های v2 + تولید واقعی مقاله بهترین-از-N + ContentPlanner
- رگرسیون کامل: ۱۴۱ تست قبلی بدون شکست (۱ انتظار قدیمی به‌روزرسانی شد)

### L.14-L.16 — رلیز v1.1.0 🚀
`CHANGELOG.md` فارسی + ارتقای نسخه سیستم به 1.1.0 + بسته ZIP نصب‌آماده + SHA256 + تگ `v1.1.0` + GitHub Release با یادداشت فارسی و پیوست‌ها

### 📊 مقایسه قبل/بعد (نسخه موتور)
| شاخص | v1 | v2 |
|------|-----|-----|
| ژنراتورها | ۶ | ۷ (+ContentPlanner) |
| آنالایزرها | ۴ | ۶ (+QualityScorer، +IntentClassifier) |
| نوع مقاله | ۷ | ۹ |
| بررسی‌های سئو | ۱۷ | ۲۶ |
| کیفیت محتوا | بدون سنجش | ۷ بُعد امتیازدهی |
| انتخاب واریانت | ۱ | بهترین-از-N (تا ۴) |
| برنامه‌ریزی محتوا | — | تقویم هوشمند فصلی |
| اندپوینت AI | ۲۱ | ۲۸ |
| تست موفق | ۱۴۱ | ۲۱۱ |

- کامیت‌ها: `engine-v2-core`، `engine-v2-generators`، `release-v1.1.0`

## 🧠 فاز M — موتور هوش مصنوعی نسخه ۳ «بی‌رقیب» + رلیز v2.0.0 (2025-09-23)

### ✅ اقدامات انجام‌شده (بخش ۱: پیاده‌سازی موتور v3)
- تکمیل رلیز جاافتاده v1.1.0: آپلود ZIP نصب (۵۸۸KB) + SHA256 به رلیز گیت‌هاب — ریشه خطای ۴۰۴ قبلی: آپلود دارایی باید به `uploads.github.com` برود نه `api.github.com`
- 🆕 `engine/generators/SmartPipeline.php` — خط تولید هوشمند: یک فراخوانی = ۸ مرحله خودکار (نیت ← کلیدواژه ← مقاله بهترین-از-N ← بهبود خودکار ← عنوان بهینه ← سئو ← FAQ/اسکیما ← یکتایی) + ردِیابی زمان‌بندی هر گام
- 🆕 `engine/generators/ContentImprover.php` — چرخه خود-درمانی کیفیت: در هر دور ۳ بُعد ضعیف را هدف می‌گیرد و استراتژی اختصاصی اعمال می‌کند (۷ استراتژی: شکستن جمله، مترادف، رابط، قلاب، ساختار، حجم، کلیدواژه)؛ خروج ایمن در صورت عدم بهبود
- 🆕 `engine/analyzers/BrandVoiceAnalyzer.php` — پروفایل صدای برند: رسمیت/فنی/محاوره + ۴ تیپ صدا (متخصص راهنما/مشاور/دوست صمیمی/مرجع تخصصی) + سنجش انطباق متن جدید + توصیه‌های تولید
- 🆕 `engine/generators/TitleGenerator.php` — عنوان‌ساز CTR-محور: ۸ الگوی فارسی + امتیازدهی ۵ عامل (طول ۴۵-۶۰، عدد، سؤال، واژه قدرت، موقعیت کلیدواژه)
- 🆕 `engine/CommandAssistant.php` — دستیار فرمان زبان طبیعی فارسی: ۹ عملیات با استخراج خودکار برند/دستگاه از متن فرمان + پیشنهاد فرمان بعدی
- 🆕 `engine/utils/EngineCache.php` — کش فایل‌محور موتور: TTL + remember + آمار hit/miss + GC
- `SahandAI.php` → v3.0.0: ۷ متد جدید + engineInfo غنی + ادغام کش در smart-generate
- 🐛 رفع باگ مهم: امضای `generateContent(string $type, ...)` با روتر API ناسازگار بود → آرایه‌ای شد؛ اندپوینت `ai/generate-content` اکنون از بیرون کار می‌کند
- ۷ اندپوینت جدید: `ai/smart-generate`, `ai/improve-content`, `ai/brand-voice`, `ai/generate-titles`, `ai/assistant`, `ai/cache-stats`, `ai/cache-clear`
- ارتقای نسخه سیستم: 1.1.0 → 2.0.0

### 🧪 تست
- 🆕 test-engine-v3.php — ۶۲ تست موفق (کش، عنوان، صدای برند، بهبود، خط تولید، دستیار، v3 فاساد)
- رگرسیون کامل: test-engine-v2 (۷۰) + test-knowledge-base (۷۱) + test-knowledge-e2e (۲۷) + test-ai-engine (۴۳) — همه موفق
- مجموع سیستم: ۲۷۳ تست ✅ (بروزرسانی ۲ ادعای نسخه در تست v2 برای همگامی با v3)

### 🔧 تصمیمات فنی
- کش smart-generate با TTL ۳۰ دقیقه — پارامتر `no_cache` برای دور زدن
- ContentImprover حداکثر ۵ دور و خروج ایمن اگر امتیاز بهبود نیافت (جلوگیری از تخریب متن خوب)
- روتینگ دستیار: فرمان‌های «متن‌دار با دونقطه» (امتیاز/بهبود) قبل از «تشخیص عیب» بررسی می‌شوند تا تداخل رخ ندهد

### 📦 کامیت‌ها
- (این بخش) `engine-v3-unrivaled`: ۶ فایل جدید + SahandAI v3 + ۷ اندپوینت + نسخه 2.0.0 + تست ۶۲گانه

### 🔜 ادامه فاز M
- راهنمای کامل API خارجی (docs/AI-API-GUIDE.md) — مفصل با مثال cURL/PHP/Python/JS
- لینک دانلود مستقیم ۳۰ فونت + ۱۰ پک آیکون در READMEهای مربوط
- بروزرسانی README اصلی + CHANGELOG
- رلیز v2.0.0: پکیج نصب + پکیج بروزرسانی + فایل راهنما + چک‌سام

### ✅ بخش ۲ فاز M: راهنمای کامل API خارجی
- 🆕 `docs/AI-API-GUIDE.md` — ۱۳۲۷ خط / ۶۰KB مرجع کامل فارسی برای استفاده از موتور AI از بیرون سایت ساز:
  - شروع سریع ۳۰ ثانیه‌ای + احراز هویت (X-API-Key دو روش) + قالب پاسخ/خطا + محدودیت نرخ + CORS
  - جدول مرجع ۳۴ اندپوینت در ۴ گروه (v3 بی‌رقیب / v2 تحلیلی / v1 پایه / پایگاه دانش)
  - مستندات تفصیلی هر اندپوینت: جدول پارامترها + مثال cURL + پاسخ JSON واقعی (نمونه‌برداری‌شده از موتور زنده با capture-api-samples.php)
  - راهنمای کامل دستیار فارسی: ۱۳+ فرمان + هوشمندی استخراج برند/دستگاه + الگوی suggestions
  - کلاینت کامل به PHP (کلاس SahandAIClient) + Python (کلاس SahandAI) + Node.js + PowerShell
  - ۵ سناریوی واقعی (اپ خارجی، ربات تلگرام با ۱ اندپوینت، پایش کیفیت، تقویم فصلی، پشتیبانی فنی)
  - جدول کدهای HTTP + رفع اشکال ۵ مشکل رایج + ۱۳ نکته امنیتی/کارایی
- `admin/docs.php` — ثبت راهنما در پنل مستندات (کارت + هدایت به GitHub)
- 📦 کامیت: `docs-ai-api-guide`

### ✅ بخش ۳ فاز M: لینک دانلود مستقیم فونت‌ها و آیکون‌ها
- `assets/fonts/README.md` — بازنویسی کامل: جدول دانلود ۲۰ فونت فارسی + ۱۰ فونت انگلیسی با لینک رسمی:
  - GitHub-hosted: وزیرمتن/وزیر/شبنم/صمیم/گندم/پرستو/تنها/لاله‌زار/میخک → لینک Releases رسمی + لینک مستقیم فایل از CDN (jsDelivr)
  - FontIran: ایران‌سنس/ایران‌سنس‌ایکس/دانا/یکان‌بخ → سایت رسمی ناشر
  - کلاسیک: آزاد (گرافیران)، بهیج/استقلال/نازنین/ترافیک (فونت‌یاب)، مشق (FontSpace)، فانتزی (فونت‌فا)
  - انگلیسی: هر ۱۰ فونت از Google Fonts
  - جدول برچسب وزن‌ها (thin تا black) + نام‌گذاری فایل + لایسنس‌ها
- ۱۰ پک آیکون: `assets/icons/*/README.md` بازنویسی با اسکریپت — جدول لینک (سایت رسمی + مخزن GitHub + **دانلود مستقیم ZIP آخرین نسخه**) + تعداد کامل پک + لایسنس + ویژگی
- همه لینک‌ها با curl راستی‌آزمایی شدند (۲۸ لینک 200 OK)
- 📦 کامیت: `download-links-fonts-icons`

### ✅ بخش ۴ فاز M: مستندات اصلی
- `README.md` اصلی ریپو — بازنویسی کامل: نشان نسخه 2.0.0 + موتور v3.0 بی‌رقیب + نشان ۲۷۳ تست، جدول قابلیت‌های v3، بخش جدید «API موتور AI از بیرون» با دو نمونه curl، جدول مستندات (راهنمای API + فونت + آیکون)، لینک صفحه Releases در نصب سریع
- `CHANGELOG.md` — ثبت کامل نسخه 2.0.0 (افزودنی‌ها/رفع اشکال/تغییرات/تست/انتشار)
- 📦 کامیت: `docs-v2-main-readme-changelog`

### ✅ بخش ۵ فاز M (نهایی): رلیز v2.0.0
- `UPGRADE.md` — راهنمای فارسی بروزرسانی از 1.x: بکاپ اجباری، دو روش (بسته بروزرسانی ۲۷ فایله / بسته کامل)، جدول راستی‌آزمایی پس از ارتقا (۴ بررسی)، رفع اشکال ۴ مشکل رایج، تست‌های curl
- تگ `v2.0.0` ساخته و پوش شد
- ساخت ۴ بسته + چک‌سام SHA256:
  - `Sahand-Service-BrandMaker-v2.0.0-install.zip` (۶۴۷KB) — git archive تمیز از تگ، ۳۹۶ فایل
  - `Sahand-Service-BrandMaker-v2.0.0-update.zip` (۸۶KB) — فقط ۲۷ فایل تغییرکرده از v1.1.0 + UPGRADE.md
  - `AI-API-GUIDE-v2.0.0-fa.md` — راهنمای API مستقل برای دانلود
  - `UPGRADE-v2.0.0-fa.md` — راهنمای بروزرسانی مستقل
- رلیز گیت‌هاب v2.0.0 ساخته شد + هر ۵ پیوست با HTTP 201 آپلود شدند (به uploads.github.com — با درس آموخته از خطای ۴۰۴ رلیز v1.1.0)
- 🔗 https://github.com/pranses2011/Sahand-Service-BrandMaker/releases/tag/v2.0.0

### 🏁 جمع‌بندی فاز M
| شاخص | مقدار |
|------|-------|
| فایل‌های جدید موتور | ۶ کلاس (SmartPipeline، ContentImprover، BrandVoiceAnalyzer، TitleGenerator، CommandAssistant، EngineCache) |
| اندپوینت API جدید | ۷ (مجموع ۳۴) |
| تست موفق | ۲۷۳ (۶۲ جدید + رگرسیون کامل) |
| باگ رفع‌شده | ۱ بحرانی (generateContent خارجی) |
| مستندات جدید | AI-API-GUIDE.md (۱۳۲۷ خط) + UPGRADE.md + ۱۱ README فونت/آیکون با لینک دانلود |
| کامیت‌های فاز | engine-v3-unrivaled، docs-ai-api-guide، download-links-fonts-icons، docs-v2-main-readme-changelog، release-v2.0.0 |
| رلیز | v2.0.0 با ۵ پیوست — نصب کامل + بروزرسانی + راهنماها + چک‌سام |

---

# 🚀 فاز N — رفع باگ نصب + جستجوی آنلاین وب + ربات تلگرام (v2.1.0)

**📅 شروع:** 2025-09-23 | **🎯 هدف:** رفع باگ بحرانی نصب، جستجوی آنلاین اینترنت برای موتور AI، ربات تلگرام متصل به دستیار فارسی، رلیز v2.1.0

## ✅ بخش ۱ فاز N: رفع باگ بحرانی نصب — `Table 'users' doesn't exist`

### 🔍 ریشه‌یابی (تحلیل عمیق)
- خطای گزارش‌شده کاربر: `SQLSTATE[42S02]: Base table or view not found: 1146 Table 'tabrizfixer_BrandMaker.users' doesn't exist`
- شبیه‌سازی دقیق منطق قدیدی `install.php` (تقسیم `preg_split('/;\s*[\r\n]+/')` + رد کردن تکه‌های شروع‌شده با `--`) روی `database.sql` واقعی:
  - ۳۹ تکه استخراج می‌شد، اما **۳۵ تکه** به دلیل شروع با کامنت `--` «کامل» رد می‌شدند
  - فقط **۲ جدول از ۳۳ جدول** ساخته می‌شد → ۳۱ جدول مفقود (از جمله `users`)
  - سپس `INSERT INTO users` (کد_hardcoded PHP) با خطای 1146 شکست می‌خورد — دقیقاً خطای کاربر

### 🔧 رفع (install.php v1.1)
- نوشتن تجزیزگر ماشین-حالت امن `splitSqlStatements()`:
  - کامنت‌های خطی (`--` و `#`) و بلوکی (`/* */`) فقط **بیرون از رشته‌ها** حذف می‌شوند
  - رشته‌های `'...'` و `"..."` با ESCAPE های `\'`، `''`، `""` دست‌نخورده حفظ می‌شوند
  - سمی‌کالن واقعی (بیرون رشته) = پایان دستور؛ سمی‌کالن فارسی `؛` هرگز تفکیک نمی‌کند
- راستی‌آزمایی حیاتی بعد از اجرای اسکیما: بررسی `SHOW TABLES` برای ۶ جدول کلیدی (users، brands، settings، api_keys، brand_pages، brand_articles) — در صورت نقص، خطای شفاف با نام جداول مفقود
- مقاوم‌سازی نصب مجدد (idempotent): کاربر مدیر موجود → UPDATE به‌جای INSERT تکراری؛ کلید API سیستمی فقط در صورت عدم وجود ساخته می‌شود
- حداقل‌سنجی سلامت: رد شدن نصب اگر کمتر از ۲۰ دستور استخراج شود (فایل خراب/خالی)

### 🧪 تست
- `scripts/test-splitter.php` — **۲۵ تست واحد موفق**:
  - دیتابیس واقعی: ۴۰ دستور، ۳۳ CREATE TABLE، ۳ INSERT درون‌ریزی، متن فارسی سالم
  - ۱۳ مورد مرزی: سمی‌کالن/کامنت/هشتگ داخل رشته، فرار‌ها، CRLF ویندوز، دستور بدون سمی‌کالن پایانی، ورودی خالی، سمی‌کالن فارسی
  - مقایسه قبل/بعد: الگوریتم قدیمی ۴ دستور ↔ الگوریتم جدید ۴۰ دستور
- `php -l install.php` — بدون خطای نحوی

### 📦 کامیت
- `fix-installer-critical-db-bug` — رفع باگ + ۲۵ تست + بروزرسانی CHECKLIST/WORKLOG

## ✅ بخش ۲ و ۳ فاز N: سرویس جستجوی آنلاین وب + ربات تلگرام

### 🌐 سرویس جستجوی آنلاین (`engine/services/WebSearchService.php` — ۷۸۴ خط)
- معماری ۶ ارائه‌دهنده با زنجیره جایگزین خودکار:
  - کلیددار (اختیاری): SerpApi → Google CSE → Bing API (از تنظیمات `websearch_settings`)
  - رایگان (پیش‌فرض، بدون کلید): DuckDuckGo HTML → DuckDuckGo Lite → Bing HTML
- `search(query, limit)` — جستجو با کش ۳۰ دقیقه‌ای + محدودیت نرخ ۶۰/ساعت + گزارش ارائه‌دهنده
- `research(topic, opts)` — تحقیق ساختاریافته: ۲ جستجو (اصلی + قیمت) → استخراج کلیدواژه ترند (فراوانی) + سؤالات واقعی کاربران (چرا/چطور/؟) + داده‌های عددی (تومان/درصد/سال) + منابع + خلاصه فارسی؛ کش ۶ ساعته
- `fetchPageText(url)` — خواندن متن صفحه (حذف script/style/nav، استخراج title+meta+text)
- `status()` — وضعیت ارائه‌دهندگان + نرخ + کش
- امنیت: محافظت SSRF کامل (بلوک localhost/IP خصوصی/.local/file:) + چرخش User-Agent + تایم‌اوت اتصال/کل
- متدهای تجزیه عمومی (`parseDdgHtml`/`parseDdgLite`/`parseBingHtml`) — قابل تست آفلاین + ترتیب صفت‌ها مهم نیست + تبلیغات DDG (y.js) رد می‌شوند

### 🧠 یکپارچه‌سازی در موتور
- `SmartPipeline`: پارامتر `research` → گام تحقیق آنلاین بین انتخاب کلیدواژه و تولید مقاله؛ غنی‌سازی FAQ با ۳ سؤال واقعی وب + لحاظ کلیدواژه ترند در تولید عنوان + خروجی `research` در نتیجه؛ **شکست وب هرگز خط تولید را متوقف نمی‌کند**
- `SahandAI` v3.1.0: ۳ متد جدید (webSearch، researchTopic، webSearchStatus) + engineInfo با قابلیت‌های جدید
- `CommandAssistant`: ۲ فرمان جدید — «جستجوی وب: ...» / «سرچ: ...» و «تحقیق درباره: ...» + بروزرسانی راهنما و پیشنهادها
- API: ۳ اندپوینت جدید — POST `ai/web-search`، POST `ai/research`، GET `ai/websearch-status` (مجموع ۳۷ اندپوینت)

### 🤖 ربات تلگرام (`telegram/TelegramBot.php` + `telegram/poll.php` + `admin/telegram.php`)
- کلاس `TelegramBot`: فراخوانی Bot API (getMe/setWebhook/deleteWebhook/getWebhookInfo) + sendMessage با HTML + تقسیم خودکار پیام‌های بلند (سقف ۴۰۹۶، مرز خط، بدون از دست رفتن محتوا)
- مغز ربات `handleUpdate`: احراز هویت لیست چت (خالی=هیچ‌کس، *=همه) + فرمان‌های /start /help /status /id + زبان طبیعی فارسی → CommandAssistant
- قالب‌بندی هوشمند ۱۱ نوع عملیات (مقاله/کلیدواژه/امتیاز/بهبود/عنوان/نیت/عیب‌یابی/کد خطا/برندها/جستجوی وب/تحقیق) با XSS ایمن
- وب‌هوک `POST api/telegram/webhook` (عمومی، راستی‌آزمایی با هدر X-Telegram-Bot-Api-Secret-Token + hash_equals)
- long-polling `telegram/poll.php` برای تست محلی/هاست بدون SSL
- پنل مدیریت: ذخیره توکن/شناسه‌ها + تست اتصال + راه‌اندازی/حذف وب‌هوک با توکن مخفی خودکار + پیام آزمایشی + وضعیت + راهنمای ۶ مرحله‌ای + افزودن به سایدبار
- اعلان درخواست جدید: `TelegramBot::notifyNewRequest` متصل به `NotificationService` (کانال پنجم: bot)
- seed تنظیمات: `websearch_settings` + `telegram_bot_settings` در database.sql

### 🧪 تست‌ها
- `scripts/test-engine-v31.php` — **۸۰/۸۰ موفق شامل ۳ تست زنده اینترنت** (جستجوی Bing واقعی + تحقیق زنده + دستیار+جستجو)
- رگرسیون کامل: v2 (70) + v3 (62) + knowledge (71+27) + ai-engine (43) = **۲۷۳ موفق** پس از بروزرسانی انتظارات نسخه
- فیکسچر آفلاین: DDG HTML/Lite + Bing با HTML واقعی‌نما + ۱۰ مورد SSRF + دیکود ریدایرکت/تبلیغ

### 📦 کامیت
- `engine-v3.1-websearch-telegram` — سرویس جستجو + ربات + یکپارچه‌سازی + پنل + تست‌ها

## ✅ بخش ۴ و ۵ فاز N: مستندات کامل (راهنمای API + README + CHANGELOG + UPGRADE)

### 📚 راهنمای API خارجی (docs/AI-API-GUIDE.md — ۱۳۲۷ → ۱۵۶۵ خط)
- سربرگ: نسخه 3.1.0 + سیستم 2.1.0 + ۳۷ اندپوینت + وب‌هوک ربات
- جدول اندپوینت‌های جدید v3.1 (web-search، research، websearch-status) + بازشماره‌گذاری کل جداول (۱-۳۷)
- بخش‌های جدید ۵.۸-۵.۱۱: مستندات تفصیلی وب‌سرچ/تحقیق/وضعیت/smart-generate با research — با **پاسخ‌های واقعی موتور زنده** (DuckDuckGo Lite با کوئری فارسی → سایت‌های تعمیراتی ایرانی واقعی)
- بخش ۱۰ جدید: راه‌اندازی کامل ربات تلگرام (۵ گام + جدول فرمان‌ها + امنیت + مرجع وب‌هوک)
- جدول فرمان‌های دستیار: +۴ ردیف (جستجوی وب/سرچ/تحقیق درباره)
- جدول تاریخچه: ردیف 2.1.0

### 📖 README.md
- نشان‌ها: 2.1.0 + موتور v3.1 متصل به اینترنت + ۳۷۸ تست
- جدول قابلیت‌ها: +۲ ردیف (جستجوی آنلاین + ربات تلگرام) + ارسال ۵ کاناله
- ساختار پروژه: telegram/ + engine/services/ + admin/telegram.php + ۳۷ اندپوینت
- بخش قابلیت‌های v3.1 (۵ ردیف) + ۴ مثال curl (شامل web-search و research)

### 📋 CHANGELOG.md
- ردیف کامل [2.1.0]: رفع باگ بحرانی (ریشه‌یابی کامل) + افزودنی‌ها (۴ بخش) + تغییرات + تست‌ها

### 🔄 UPGRADE.md
- بازنویسی برای ارتقای به 2.1.0: تغییرات مهم + ۶ بررسی راستی‌آزمایی + تست‌های curl جدید (websearch-status + web-search + smart-generate با research) + راه‌اندازی ربات پس از ارتقا + ۳ ردیف جدید رفع اشکال (جستجو/وب‌هوک/ربات)

### 🗂️ سایر
- admin/docs.php: کارت راهنمای API → نسخه ۳.۱ + ۳۷ اندپوینت + ربات تلگرام
- CHECKLIST: تیک N.7/N.9/N.10 + شمارنده‌ها (۲۰۱ انجام، ۱ در جریان)

### 📦 کامیت
- `docs-v2.1-websearch-telegram` — راهنمای API ۱۵۶۵ خطی + README + CHANGELOG + UPGRADE

## 🎉 بخش ۶ فاز N (نهایی): رلیز v2.1.0

- تگ `v2.1.0` ساخته و پوش شد
- پاکسازی: `cache/websearch-rate.json` از گیت حذف + قانون `cache/*.json` در .gitignore
- بسته‌های رلیز:
  - `Sahand-Service-BrandMaker-v2.1.0-install.zip` — بسته نصب کامل از تگ (git archive تمیز)
  - `Sahand-Service-BrandMaker-v2.1.0-update.zip` — فقط فایل‌های تغییرکرده از v2.0.0 (بدون CHECKLIST/WORKLOG/runtime) + UPGRADE.md
  - `AI-API-GUIDE-v2.1.0-fa.md` — راهنمای ۱۵۶۵ خطی مستقل
  - `UPGRADE-v2.1.0-fa.md` — راهنمای بروزرسانی مستقل
  - `SHA256SUMS-v2.1.0.txt` — چک‌سام همه بسته‌ها
- رلیز گیت‌هاب v2.1.0 + آپلود ۵ پیوست به uploads.github.com

### 🏁 جمع‌بندی فاز N
| شاخص | مقدار |
|------|-------|
| باگ بحرانی رفع‌شده | ۱ (نصب‌کننده دیتابیس — ۳۱ جدول مفقود) |
| کلاس‌های جدید | ۲ (WebSearchService، TelegramBot) |
| اندپوینت API جدید | ۳ AI + ۱ وب‌هوک تلگرام (مجموع ۳۷) |
| فرمان‌های جدید دستیار | ۲ (جستجوی وب، تحقیق درباره) |
| صفحات پنل جدید | ۱ (admin/telegram.php) |
| تست موفق | ۳۷۸ (۱۰۵ جدید شامل ۳ زنده اینترنت) |
| مستندات | راهنمای API ۱۵۶۵ خط + CHANGELOG + UPGRADE بازنویسی |

---

# 🌟 فاز O — منو + ریسپانسیو + موتور v3.2 + تحویل مقاله در تلگرام (v2.2.0) 📅 2025-09-23

> درخواست کاربر: رفع باگ «منوی سایت در وسط صفحه باز می‌شود» + ریسپانسیو همه صفحات + موتور AI بسیار قوی‌تر (محتوا/مقاله/سئو + جستجوی آنلاین تقویتی) + تحویل کامل مقاله با ۳ عکس در ربات تلگرام (برای سایت‌های دیگر، بدون ورود به سایت ساز) + بروزرسانی راهنمای API + رلیز جدید

## ✅ بخش ۱ فاز O: رفع باگ منو + ریسپانسیو کامل

- 🐛 ریشه‌یابی باگ منو: سایدبار پنل با `inset-inline-end:0` در RTL به لبه «چپ» چسبیده بود، اما `translateX(100%)` آن را به «راست» یعنی داخل ویوپورت هل می‌داد → در موبایل منو وسط صفحه شناور می‌ماند
- 🔧 اصلاح `translateX(-105%)` + سایه هنگام باز شدن + پس‌زمینه تیره (backdrop) + قفل اسکرول بدنه + aria-expanded/controls
- ✅ راستی‌آزمایی با مرورگر هدلس (agent-browser) در عرض 390px: بسته = خارج کامل از صفحه [-262,-12]، باز = لبه چپ [0,250] + backdrop فعال + بستن با کلیک روی backdrop
- 📱 ریسپانسیو پنل: بستن خودکار با کلیک لینک منو/Escape + تنظیمات 992px و 640px (کارت‌ها، جدول‌ها، مودال، تب‌ها، آمار داشبورد، چیپ کاربر، فرم‌ها، صفحه ورود)
- 📱 ریسپانسیو سایت برند: منوی موبایل دقیقاً زیر هدر (verified: menuTop=headerBottom) تمام-عرض با انیمیشن و اسکرول داخلی + زیرمنوی باز و تو‌رفته (بدون هاور لمسی) + گریدهای ضد سرریز `minmax(min(...),1fr)` + جدول‌های مقاله اسکرول افقی + تنظیمات 420px (هدر/هیرو/دکمه شناور/فرم‌ها)
- 📱 ریسپانسیو install.php زیر 600px (مرحله‌بندی wrap، جدول، دکمه تمام‌عرض)
- 🔧 ابزار: چکر سینتکس PHP با Node (scripts/php-check.js) به‌دلیل نبود PHP runtime در محیط — همه فایل‌ها ✅

### 📦 کامیت
- `fix-menu-center-bug-responsive` — ۷ فایل، ۱۹۴ درج

## ✅ بخش ۲ فاز O: موتور AI نسخه ۳.۲

- 🖼️ ArticleImageService v1.0 (جدید): انتخاب هوشمند ۳ تصویر بر اساس دستگاه/نوع مقاله + تولید alt/کپشن سئو فارسی + درج `<figure>` استراتژیک در ۳۵٪ و ۷۲٪ طول مقاله + URL مطلق برای استفاده خارجی + متد catalog
- 🖼️ تولید ۹ تصویر فوتورئال تخصصی با AI (1344×768، ~۱MB کل): یخچال، ماشین لباسشویی، ظرفشویی، کولر گازی، تلویزیون، کارگاه، قطعات یدکی، عیب‌یابی با مولتی‌متر، آشپزخانه مدرن
- 🧠 SmartPipeline v3.2 — خط تولید ۱۱ مرحله‌ای: تحقیق وب auto (خودکار در صورت فعال بودن سرویس) + غنی‌سازی ساختاری (فهرست مطالب + «سایر پرسش‌های کاربران» PAA + بخش «داده‌های تازه از وب» با ذکر منبع + جعبه اعتبار E-E-A-T) + درج ۳ تصویر + امتیاز هدف پیش‌فرض ۸۸
- 🌐 WebSearchService v1.1: ارائه‌دهنده Mojeek (رایگان، ایندکس مستقل — مجموع ۷) + متد news() با Google News RSS فارسی + فرصت‌های کلیدواژه long-tail (buildOpportunities) + تلاش مجدد خودکار httpGet در خطای شبکه/۵xx
- 🔍 SeoGenerator v1.1: اسکیمای Article غنی (تصاویر + wordCount + dateModified + mainEntityOfPage + Speakable) + howToSchema + تحلیل تراکم کلیدواژه + زمان مطالعه + og:image/twitter:image کامل
- 💬 CommandAssistant v3.2: نیّت «اخبار: ...» + موضوع دلخواه در دستور مقاله (هر موضوعی حتی خارج از دامنه دستگاه‌ها) + no_cache برای مقاله
- 🔌 API: POST /api/ai/news + GET /api/ai/article-images (مجموع ۳۹) + CSS عناصر جدید مقاله در قالب brand-core
- 🔖 نسخه‌ها: سیستم 2.2.0 + موتور 3.2.0

### 📦 کامیت
- `engine-v3.2-articles-images-news` — ۱۸ فایل، ۸۶۴ درج (شامل ۹ تصویر)

## ✅ بخش ۳ فاز O: ربات تلگرام v2 — تحویل کامل مقاله

- 📰 deliverFullArticle: «مقاله بنویس...» یا /article [موضوع] → پیام شروع + typing → خلاصه (عنوان/امتیاز/کلیدواژه/حجم/تراکم) → فایل HTML مستقل آماده انتشار برای هر سایت دیگری (استایل داخلی RTL + متا سئو + OG/Twitter + اسکیمای Article/FAQ/HowTo + تصاویر با URL مطلق) → نسخه Markdown → آلبوم ۳ تصویر → دکمه‌های شیشه‌ای اقدام بعدی
- 🆕 apiUpload (multipart) برای sendDocument و sendMediaGroup + sendChatAction (typing/upload_document/upload_photo)
- ⌨️ ۱۲ فرمان: /article /keywords /search /research /news /titles /improve /score + فارسی (/مقاله /اخبار /جستجو /تحقیق /کلمات /عنوان /بهبود /امتیاز) + پردازش callback_query
- 🐛 رفع باگ مهم در تبدیل Markdown: backreference های `$1` داخل single-quote به‌صورت `\$1` نوشته شده بود که خروجی literal می‌داد — اصلاح + تست شبیه‌سازی ۸ مورد موفق
- 📋 helpText بازنویسی + کیبورد شیشه‌ای جدید + statusText با شمار تصاویر

### 📦 کامیت
- `telegram-v2-full-article-delivery` — ۳ فایل، ۶۵۵ درج

## ✅ بخش ۴ فاز O: مستندات

- 📚 docs/AI-API-GUIDE.md: ۱۵۶۵ → ۱۷۰۰+ خط — عنوان v3.2 + جدول اندپوینت‌های جدید + بازسازی مستندات smart-generate (۱۱ مرحله + پارامترهای research/with_images + پاسخ با images/toc/opportunities) + بخش ۵.۱۲ (اخبار) + ۵.۱۳ (تصاویر) + بازنویسی کامل بخش ۱۰ ربات v2 + تاریخچه
- 📖 README.md: نشان‌ها + جدول قابلیت‌ها (۳ تصویر/تحویل تلگرام/ریسپانسیو) + جدول v3.2 + بازسازی جدول v3.0/v3.1 + ۳۹ اندپوینت
- 📋 CHANGELOG.md: ورودی کامل 2.2.0
- 🔄 UPGRADE.md: بازنویسی کامل برای 2.2.0 (فهرست ۲۰ فایل + راستی‌آزمایی ۷ موردی + رفع اشکال جدید)
- ✅ CHECKLIST.md: فاز O با ۱۳ مورد + آمار ۲۱۵/۲۸۰+

## 🎉 بخش ۵ فاز O (نهایی): رلیز v2.2.0

- تگ `v2.2.0` + بسته‌ها: install.zip (کامل) + update.zip (فایل‌های تغییرکرده) + AI-API-GUIDE + UPGRADE + SHA256SUMS
- رلیز گیت‌هاب با ۵ پیوست

### 🏁 جمع‌بندی فاز O
| شاخص | مقدار |
|------|-------|
| باگ رفع‌شده | ۱ (منوی وسط صفحه — RTL transform) + ۱ پیشگیرانه (backreference تبدیل MD) |
| کلاس جدید | ۱ (ArticleImageService) |
| تصاویر بسته‌بندی | ۹ فایل فوتورئال (~۱MB) |
| اندپوینت API جدید | ۲ (مجموع ۳۹) |
| فرمان‌های جدید ربات | ۸ انگلیسی + ۸ فارسی |
| مراحل خط تولید | ۸ → ۱۱ |
| ارائه‌دهندگان جستجو | ۶ → ۷ + اخبار RSS |
| فایل‌های تغییرکرده | ۲۰ (بسته بروزرسانی) |
| راستی‌آزمایی مرورگر | ۲ سناریو (پنل + سایت برند) |
---

## فاز P — نسخه ۲.۳.۰ (موتور ۳.۳) | ۲ مهر ۱۴۰۴

**درخواست کاربر:** رفع ۵ باگ گزارش‌شده (صفحه تنظیمات موبایل، فیلتر نصفه کارت‌ها، خطای مرحله ۱ تحلیل لوگو، ریسپانسیو صفحات ساخت برند، پیش‌نمایش قالب‌ها) + تقویت موتور AI (محتوا/مقاله/سئو/جستجو) + تقویت ربات تلگرام + بروزرسانی راهنمای API + رلیز جدید

### کارهای انجام‌شده

| # | کامیت | شرح |
|:-:|-------|-----|
| P.1 | `fix-ui-responsive-cache-v2.3` | 🐛 ۵ رفع: تگ PHP اضافی brand-new.php (صفحه خام) + tabs-bar ریسپانسیو تنظیمات + فیلترهای flex + 🆕 asset_ver (شکستن کش CSS/JS با filemtime — ریشه «بهم‌ریختگی پس از ارتقا») |
| P.2 | `fix-logo-analysis-step1-svg` | 🐛 مرحله ۱: پشتیبانی کامل SVG/SVGZ در ImageProcessor (استخراج رنگ از fill/stroke با ۵ الگوی regex — ۵ تست پاس) + رفع باگ imagedestroy در ColorAnalyzer + fallback نرم در AJAX |
| P.3 | `feat-template-preview-live` | 🆕 template-preview.php (رندر زنده ۳۵+ بلوک) + مودال پیش‌نمایش با سوئیچ موبایل/تبلت/دسکتاپ + وایرفریم مینی داخل بوم + دکمه‌های 👁 |
| P.4 | `feat-engine-v3.3` | WebSearchService v1.2 (ویکی‌پدیا fa/en + Startpage + مسابقه curl_multi + trust/freshness) + ArticleGenerator v2.1 (TOC/زمان مطالعه/کال‌اوت/مزایا-معایب/آمار/۱۰۰۰ کلمه) + SeoGenerator v1.2 (E-E-A-T/موجودیت/خوشه/شکاف) |
| P.5 | `feat-telegram-v2.1` | ۴ فرمان جدید (/seo /eeat /plan /faq) + معادل فارسی + ۴ intent زبان طبیعی + فرمت‌دهی‌های کامل |
| P.6 | `docs-api-guide-v3.3` | راهنمای API ۱۸۰۰+ خط: مستندات کامل seo-package + eeat-check + جدول v3.3 + بخش ربات v2.1 + تاریخچه |
| P.7 | (این کامیت) | README/CHANGELOG/UPGRADE/CHECKLIST/WORKLOG + رلیز v2.3.0 |

### 🏁 جمع‌بندی فاز P

| شاخص | مقدار |
|------|-------|
| باگ رفع‌شده (گزارش کاربر) | ۵ + ۱ کش مرورگر (پیشگیرانه ریشه‌ای) |
| فایل جدید | ۲ (template-preview.php + —) |
| اندپوینت API جدید | ۲ (مجموع ۴۱) |
| فرمان ربات | ۱۲ → ۱۶ (+ معادل فارسی) |
| ارائه‌دهندگان جستجو | ۷ → ۹ (ویکی‌پدیا fa/en + Startpage) |
| موتور | 3.2.0 → 3.3.0 |
| سینتکس PHP تست‌شده | ۱۱۲ فایل (php-parser) — همه PASS |
| راستی‌آزمایی مرورگر | ۳ viewport (320/390/1280) — بدون سرریز |
| تغییر دیتابیس | هیچ — کاملاً file-level |

## ✅ بخش ۱ فاز Q: دانلود مستقیم فونت و آیکون روی سرور (AssetDownloader v1.0)

- ⬇️ کلاس جدید `core/AssetDownloader.php` (۷۰۹ خط): دانلود مستقیم فایل‌های فونت و آرشیو پک آیکون **روی همان سرور سایت‌ساز** — دیگر نیازی به دانلود روی کامپیوتر و آپلود مجدد نیست
  - 🔒 امنیت: وایت‌لیست میزبان‌ها (cdn.jsdelivr.net / registry.npmjs.org / codeload.github.com / fonts.gstatic.com / raw.githubusercontent.com)، سقف حجم (۱۲MB فایل تکی / ۶۴MB آرشیو)، پاک‌سازی XSS روی SVG (حذف script/on*/foreignObject/data:)، نام فایل امن، فقط https
  - 📦 پشتیبانی هر دو فرمت آرشیو: ZIP (GitHub codeload) و TAR.GZ (npm registry با PharData)
  - 🏷️ دیکشنری فارسی ~۲۵۰ نام آیکون → برچسب فارسی خودکار + حذف پیشوندهای پک (bx/bxs/bxl/ri/md/fa/hi/ph)
- 📝 `assets/fonts/sources.json`: منابع تست‌شده ۹ فونت فارسی متن‌باز (وزیرمتن/وزیر/صمیم/شبنم/گندم/تنها/پرستو/میخک/لاله‌زار) + هر ۱۰ فونت انگلیسی (Roboto تا Source Sans از Fontsource) — فونت‌های تجاری (ایران‌سنس/دانا/یکان‌بخ/…) به‌درستی «آپلود دستی» نمایش داده می‌شوند
- 📝 `assets/icons/sources.json`: منابع رسمی ۹ پک با الگوی استخراج + prefix_map + category_map — همه لینک‌ها با curl راستی‌آزمایی شدند (۲۰۰)
- 🖼️ `admin/fonts.php`: ستون جدید «دانلود مستقیم روی سرور» — دکمه «⬇️ دانلود و نصب» (سبز) / «🔄 دانلود مجدد» + شمارنده n/N نصب + نمایش مجوز
- 🖼️ `admin/icons.php`: دکمه «⬇️ نصب کامل پک» / «🔄 نصب مجدد کامل» + نشان «کامل: ~N» + هشدار راهنما بالای فرم ایمپورت دستی
- 🧪 راستی‌آزمایی: سینتکس PHP هر ۳ فایل PASS با php-parser + ساختار داخلی همه آرشیوها (lucide/feather/heroicons/tabler/remix/material/fa/phosphor/boxicons) بازرسی و الگوها تنظیم شد

### 📦 کامیت
- `feat-asset-downloader-v1` — ۵ فایل

## ✅ بخش‌های ۲ تا ۵ فاز Q: موتور ۳.۴ — نگارش فارسی + انواع مقاله + سئو-محور

**تاریخ:** ۳ مهر ۱۴۰۴ — **کامیت‌ها:** ۸ کامیت (95f27fb تا docs)

### بخش ۲ — 📝 موتور نگارش فارسی (Q.3)
- 🆕 `engine/utils/PersianGrammar.php` v1.1 (۵۶۵ خط): fix() اصلاح HTML-امن (نیم‌فاصله ۱۱ قاعده + سجاوندی + املای رایج ۵۰+ واژه + ارقام فارسی + ی/ک عربی) + fixAgreement() هم‌خوانی فعل/فاعل (۶ ضمیر × ۱۰ فعل) + analyze() امتیاز ۰-۱۰۰ + fluency() روان‌نویسی
- 🐛 رفع ۳ باگ regex کشف‌شده با تست Node: `\b` با حروف فارسی در PCRE کار نمی‌کند → مرز یونیکد-امن `(?<!\p{L})`؛ `\d` رقم فارسی را نمی‌بست → `[^\s\d۰-۹]`؛ `\u{200C}` در PCRE نامعتبر → `\x{200C}`
- QualityScorer v2.0→v2.1: بُعد هشتم «نگارش فارسی» (وزن ۱۰) + پیشنهاد بهبودی
- ArticleGenerator v2.1→v2.2: گام ۵.۶ اصلاح نگارشی پیش از سئو + گزارش grammar در خروجی
- ContentGenerator v1.0→v1.1: اصلاح نگارشی خروجی صفحات برند
- ContentImprover: استراتژی fixGrammar در چرخه خود-درمانی
- 🧪 ۲۵ تست منطق پاس + ۵ فایل سینتکس PASS (php-parser)

### بخش ۳ — 📰 ۲۵ نوع مقاله (Q.4)
- templates.json: article_topics از ۹ → ۲۵ نوع / ۶۳ → ۱۳۶ قالب عنوان
- ۱۶ نوع جدید: safety_guide, installation_guide, diy_vs_pro, common_mistakes, warranty_guide, tech_explainer, myths_facts, checklist, case_study, glossary, history_evolution, expert_tips, symptom_focus, statistics, environment, service_process
- buildOutline: ۱۶ case جدید (با بهره‌گیری از diagnosticSection/partsWearSection)
- categoryForTopic + topicTitle + suggestTopics: پشتیبانی ۲۵ نوع
- admin/articles.php: سلکت گروه‌بندی‌شده ۵ دسته — brand-build.ajax.php: تولید اولیه ۵→۸ مقاله

### بخش ۴ — 🎯 عنوان دلخواه + سئو + جستجو + تصویر (Q.5-Q.8)
- TitleGenerator v3.1: suggestForCustom() — تحلیل عنوان کاربر + ۱۲+ واریانت سئو رتبه‌بندی‌شده با gain (تست: عنوان کاربر ۳۰ → بهترین پیشنهاد ۷۸)
- ArticleGenerator v2.2→v2.3: custom_title (مقاله حول عنوان کاربر + کلیدواژه از عنوان) + research (گام ۱.۵: تحقیق آنلاین → ۳ بخش داده/پرسش/منابع + تگ‌های ترند + fallback امن) + with_images (گام ۳.۷: ۳ تصویر با alt/figcaption)
- 🆕 SeoImprover v1.0 (۵۶۷ خط): analyze() با ۱۲ سنجه وزنی + improve() با ۸ اصلاح خودکار + گزارش قبل/بعد + ذخیره seo_score/featured_image
- admin/articles.php: فیلد عنوان دلخواه + دکمه «🎯 پیشنهاد بهترین عنوان سئو» (AJAX) + پنل «📊 آمار کامل سئو» زیر ویرایشگر + دکمه «🎯 بهبود سئو» + چک‌باکس جستجوی آنلاین/تصاویر
- API: ۳ اندپوینت جدید (۴۴ مجموع): suggest-article-titles + article-seo-stats + improve-article-seo
- saveArticle: ذخیره featured_image + seo_score (بدون تغییر دیتابیس)

### بخش ۵ — 🤖 ربات + دستیار + مستندات (Q.9-Q.11)
- ربات تلگرام v2.2: /grammar (گزارش کامل نگارش + متن اصلاح‌شده) + /suggest (تحلیل عنوان + پیشنهادها) + /types (۲۵ نوع) + کیبورد و راهنمای جدید
- دستیار فارسی: «نگارش:» (ZWNJ-پذیر: غلط‌گیری/غلطگیری/اصلاح‌نگارش) + «پیشنهاد عنوان سئو:» + «انواع مقاله» — رفع تداخل مسیریابی (نگارش ≠ بهبود، عنوان سئو ≠ پکیج سئو) — ۹ تست پاس
- راهنمای API: ۱۷۹۴ → ۱۹۳۲ خط (جدول ۴۲-۴۴ + بخش‌های ۵.۱۴-۵.۱۷ + تاریخچه ۳.۴.۰)
- README/CHANGELOG/UPGRADE/CHECKLIST: نسخه ۲.۴.۰ + موتور ۳.۴.۰ (config.php + SahandAI)

### 📦 کامیت‌ها
- `feat-persian-grammar-v1.1` + `feat-article-types-25` + `feat-custom-title-seo-suggest` + `feat-seo-improver-v1` + `feat-research-and-images` + `feat-telegram-v2.2` + `docs-api-guide-v3.4` + docs

### 🏁 جمع‌بندی فاز Q
| شاخص | مقدار |
|------|-------|
| کلاس جدید | ۲ (PersianGrammar + SeoImprover) |
| انواع مقاله | ۹ → ۲۵ |
| قالب عنوان | ۶۳ → ۱۳۶ |
| ابعاد کیفیت | ۷ → ۸ |
| اندپوینت API | ۴۱ → ۴۴ |
| فرمان ربات | ۱۶ → ۱۹ (+ فارسی) |
| intent دستیار | +۳ |
| اصلاح خودکار سئو | ۸ مورد |
| سنجه آمار سئو | ۱۲ مورد |
| تغییر دیتابیس | هیچ — کاملاً file-level |
| تست | ۲۵ منطق نگارش + ۹ مسیریابی + ۱۰۸ سینتکس پاس |

---

## [مرحله ۲۵] — 🚑 هات‌فیکس v2.4.1: رفع ۵ باگ حیاتی گزارش‌شده کاربر 📅 ۱ مهر ۱۴۰۵ (2026-09-23)

### 🔥 گزارش کاربر (بعد از آپدیت به ۲.۴.۰)
1. داشبورد خالی است و منو باز نمی‌شود
2. دکمه «پیشنهاد بهترین عنوان سئو»: خطای `Failed to execute 'json' on 'Response': Unexpected end of JSON input`
3. دکمه «بهبود سئو» در ویرایش مقاله: خطای `Unexpected token '<'`

### 🕵️ فرآیند ریشه‌یابی (بازتولید کامل مشکل روی محیط شبیه‌سازی)
- دانلود بسته بروزرسانی v2.4.0 از رلیز و راستی‌آزمایی کامل بودن آن (۲۶ فایل = دقیقاً git diff v2.3.0..v2.4.0) — بسته سالم بود؛ مشکل از خود کد بود
- ساخت محیط شبیه‌سازی: PHP 8.3 استاتیک + پایگاه داده شبیه‌سازی‌شده + نشست لاگین‌شده → اجرای مستقیم articles.php (هر سه اکشن AJAX) و admin/index.php
- **بازتولید موفق هر سه علامت**: داشبورد دقیقاً بعد از `<main class="content">` قطع می‌شد (بدون فوتر و admin.js) / بهبود سئو HTML خطا برمی‌گرداند / پیشنهاد عنوان با حروف خاص پاسخ خالی می‌داد
- خطایابی با لاگ خطا: `Cannot redeclare SahandAI::researchTopic()` + `Call to undefined function mb_strtok()` + هشدارهای `lookbehind assertion is not fixed length`

### 🔬 پنج باگ ریشه‌ای (همه رفع و راستی‌آزمایی شد)
| # | باگ | فیکس |
|---|-----|------|
| ۱ | متد تکراری `researchTopic()` در SahandAI (از v2.3.0) — مرگ بارگذاری کلاس | حذف نسخه قدیمی ۳.۱؛ حفظ نسخه ۳.۳ با کش و گزینه‌ها |
| ۲ | `trim()` بایت‌محور با فهرست «،؛» → خرابی UTF-8 حروف D8 (ا ب ت د ر س…) → `json_encode=false` → **پاسخ خالی** — ۱۳ نقطه فراخوانی در کل پروژه | توابع `mb_trim()`/`mb_rtrim()` چندبایتی-امن در helpers + جایگزینی همه ۱۳ نقطه (TitleGenerator/SeoImprover/PersianGrammar×۶/KeywordAnalyzer×۵/ContentImprover) |
| ۳ | `mb_strtok()` در PHP وجود ندارد (۳ نقطه در PersianGrammar) → Error → پاسخ HTML به AJAX | جایگزینی با `explode(' ', $s, 2)[0]` |
| ۴ | ۶ الگوی regex با lookbehind طول-متغیر (PCRE پشتیبانی نمی‌کند) → نیم‌فاصله پسوندها بی‌اثر | بازنویسی با گروه گیرنده: `([\p{L}]{2,}) +ها` → `$1‌ها` |
| ۵ | نقطه دوباره پس از اصلاح فعل + کلیدواژه‌های HTML در بهبود سئو + � در متا | حذف منطق punct دوباره + strip_tags قبل از استخراج کلیدواژه |

### 🔒 سخت‌سازی‌های همراه
- پلی‌فیل PHP ۷.۴: `str_contains`/`str_starts_with`/`str_ends_with` در config.php
- همه catchها → `Throwable`: articles.php (۳ هندلر AJAX) + brand-build.ajax.php + داشبورد + هدر + api/index.php (۵) + rate-limit (۲) + SahandAI + SeoImprover + ArticleGenerator
- هندلر استثنای سراسری AJAX-آگاه (هدر X-Requested-With → پاسخ JSON با پیام واقعی به‌جای HTML)
- `json_response()` ضدخرابی: JSON_INVALID_UTF8_SUBSTITUTE + سد نهایی — دیگر هرگز پاسخ خالی نیست
- admin.js از فوتر به هدر (defer) — منو حتی با خطای رندر کار می‌کند
- fetchهای دکمه‌های سئو هدر X-Requested-With می‌فرستند

### 🧪 نتایج راستی‌آزمایی (PHP 8.3 + DB شبیه‌سازی، قبل → بعد)
| سناریو | قبل | بعد |
|--------|-----|-----|
| داشبورد | ~۲KB قطع‌شده | ۱۱,۷۸۸ بایت کامل + آمار AI + منو |
| پیشنهاد عنوان (حرف D8) | پاسخ خالی ۰ بایت | JSON معتبر ۸ پیشنهاد |
| بهبود سئو | HTML خطا | success + ۳۶→۸۴ (+۴۸) |
| تولید مقاله | Fatal | تولید و ذخیره کامل |
| تست تکرارشونده حروف | «آموزش/بهترین/دلایل/سرویس/علت» | هر ۵ ✓ |

### 📦 کامیت
- `hotfix-v2.4.1-critical-fixes` — ۱۶ فایل (۱۴ کد + مستندات) + رلیز v2.4.1 (نصب + بروزرسانی + SHA256 + راهنما)


---

## 1️⃣4️⃣ نسخه ۲.۵.۰ — کپچای تضمینی + اسکیل UI/UX Pro + بهینه PHP 8.3 📅 2026-09-23

### ✅ اقدامات انجام‌شده

#### 🔒 ۱) رفع کد امنیتی صفحه لاگین + حروف درشت‌تر
- ریشه‌یابی: رندر کپچا کاملاً وابسته به GD بود؛ بعد از ارتقای PHP هاست به ۸.۳ اگر GD خاموش باشد → خطای مرگبار → HTML خطا به تگ img → بدون کپچا ورود غیرممکن
- بازنویسی `Auth::renderCaptcha()` با **رندر ۳ لایه‌ای تضمینی**:
  - لایه ۱: GD + فونت TTF (جستجو در فونت‌های نصب‌شده سایت‌ساز + فونت‌های سیستم با اعتبارسنجی imagettfbbox) → حروف ۳۰px با چرخش واقعی هر حرف
  - لایه ۲: GD فونت داخلی روی بوم کوچک + بزرگ‌نمایی نرم ۲.۶× با IMG_BICUBIC → حروف ~۲.۵ برابر قبلی
  - لایه ۳: SVG خالص PHP (بدون هیچ اکستنشن) → **همیشه نمایش داده می‌شود**
- بوم بزرگ‌تر: ۲۲۰×۷۲ (قبلاً ۱۷۰×۵۲)
- پاک‌سازی بافر خروجی قبل از هدر + try/catch هر خطای GD → سقوط نرم به SVG + لاگ
- صفحه لاگین: فیلد کپچا با فونت درشت‌تر (۱۷px + حرف‌فاصله ۴px) + ابعاد صریح تصویر + دکمه رفرش مستقل 🔄 (با انیمیشن چرخش)
- CSS: ارتفاع تصویر ۷۲px + واکنش‌گرا + max-width در موبایل
- 📦 کامیت: `33bb9af` — fix-captcha-v2.5

#### 🎨 ۲) اسکیل UI/UX Pro برای طراحی صفحات سایت‌ساز
- پایگاه دانش `engine/knowledge/uiux-patterns.json` (~۴۵۰ خط):
  - ۹ قانون کلاسیک UX (هیک، فیتس، یاکوب، فون رستورف، موقعیت ترتیبی، میلر، شیب هدف، اوج و پایان، زیبایی‌شناسی-قابلیت استفاده) — هرکدام با قانون + کاربرد عملی فارسی
  - توکن‌های طراحی: مقیاس فاصله ۹مقداری ۴/۸، مقیاس تایپ فارسی (body ۱۵-۱۷px با line-height ۱.۸)، رنگ ۶۰-۳۰-۱۰، گردی، سایه، بریک‌پوینت، عرض محتوا/ستون متن
  - ۶ قاعده RTL فارسی (آینه‌سازی فلش، LTR-جزیره‌ها برای تلفن/کد خطا، ZWNJ، موقعیت آیکون)
  - دسترس‌پذیری WCAG (کنتراست ۴.۵:۱، هدف لمسی ۴۴px، فوکس، alt، label، prefers-reduced-motion)
  - ۷ الگوی نرخ تبدیل (CTA بالای تاشدگی، اعتماد قبل از CTA، میکروکپی، فرم حداقلی، وعده پاسخ، نوار چسبان موبایل)
  - قواعد ۳۴ بلوک (max_per_page + موقعیت + دلیل) + **بلوپرینت ۱۷ نوع صفحه** (هر بخش با props و دلیل طراحی)
  - سیستم امتیازدهی ۱۳ معیاره + صفحه‌های کم‌بخش (حقوقی/نقشه) با معیار معاف
- کلاس `engine/skills/UIUXPro.php` (۶۳۰ خط):
  - `designPage()` — چیدمان از بلوپرینت + `applyContext()` زمینه‌محور (حذف مقالات اگر نبود، جایگزینی نظرات، شخصی‌سازی عنوان هیرو با نام برند)
  - `reviewLayout()` — ممیزی ۱۳ معیاره → امتیاز ۰-۱۰۰ + issues اولویت‌بندی‌شده (critical/high/medium/low) + wins + stats
  - `improveLayout()` — ۷ اصلاح خودکار (مخفی/تکراری، هیرو ابتدا، CTA پایانی، بردکرامب، ریتم پس‌زمینه/فاصله، مهار شلوغی ۱۶ بلوک)
  - `info()` + `pageTypes()` + نرمال‌سازی ۱۰ کلید مترادف نوع صفحه
- یکپارچه‌سازی:
  - `config.php`: پوشه `engine/skills/` به autoloader
  - `brand-build.ajax.php` مرحله ۳: تولید layout_json حرفه‌ای برای هر ۱۵ صفحه استاندارد + گزارش میانگین UX در جزئیات مرحله
  - `template-builder.php`: هندلرهای AJAX سه‌گانه (uiux_design/uiux_review/uiux_improve) + دکمه‌های «✨ طراحی با UI/UX Pro» و «🔍 بررسی UX» + پنل نتایج (امتیاز رنگی، نقاط قوت، مشکلات اولویت‌دار، منطق طراحی، دکمه اصلاح خودکار)
  - `SahandAI` ۳.۵.۰: متدهای uiuxDesign/uiuxReview/uiuxImprove/uiuxSkillInfo (با کش موتور) + قابلیت uiux_pro در engineInfo
  - `api/index.php`: ۴ اندپوینت جدید (ai/uiux-design, ai/uiux-review, ai/uiux-improve, ai/uiux-skill-info)
  - داشبورد: ردیف اسکیل در کارت موتور AI
- 🧪 راستی‌آزمایی (پورت پایتون دقیق الگوریتم):
  - چیدمان خراب services: ۱۰ → ۸۲ (ترتیب صحیح هدر→بردکرامب→هیرو→...→CTA، تکراری و مخفی حذف شد)
  - چیدمان خالی: ۲۰ → ۶۴ | بلوپرینت سالم home: ۹۷ → ۹۷ (بدون تخریب)
  - میانگین ۱۷ بلوپرینت: **۹۲/۱۰۰**
- 📦 کامیت: `4c26198` — feat-uiux-pro-v1

#### ⚡ ۳) بهینه‌سازی برای PHP 8.3 هاست
- `.user.ini` جدید در ریشه: OPcache (enable + revalidate_freq=60) + memory_limit=256M + max_execution_time=120 + upload 12M/14M + realpath_cache 4M/600s + session.use_strict_mode=1 — برای cPanel/FPM/LiteSpeed (روی mod_php بی‌اثر و بی‌ضرر)
- `config.php`:
  - error_reporting هوشمند: عملیاتی = `E_ALL & ~E_DEPRECATED & ~E_STRICT` (لاگ تمیز) / دیباگ = E_ALL
  - پلی‌فیل `json_validate()` — روی PHP 8.3 تابع بومی و سریع‌تر، روی قدیمی‌تر جایگزین
  - فشرده‌سازی zlib خروجی (PHP 8+، غیر-cli، اگر سرور فعال نکرده باشد)
  - نسخه سیستم → 2.5.0
- `template-builder.php`: مهاجرت اعتبارسنجی JSON به `json_validate()`
- داشبورد: کارت «🖥️ وضعیت سیستم و عملکرد» — PHP/GD+FreeType/OPcache/mbstring/cURL/حافظه/زمان/آپلود با نشان سبز-قرمز + هشدار راهنمای فعال‌سازی OPcache
- بررسی سازگاری: اسکن الگوهای منسوخ (utf8_encode/strftime/${} در PHP/…) — همه پاک؛ مبنا از هات‌فیکس ۲.۴.۱ (شبیه‌سازی PHP 8.3) سالم
- 🔒 سخت‌سازی همراه: بستن گپ requireLogin در هندلرهای AJAX قالب‌ساز (save_template + UI/UX)
- 📦 کامیت: `25eb695` — perf-php83-v2.5

#### 📚 ۴) مستندات
- README: نشان نسخه 2.5.0/موتور 3.5.0/PHP بهینه 8.3 + بخش «تازه‌های ۲.۵.۰» (۷ ردیف)
- CHANGELOG: ورودی کامل [2.5.0] با ریشه‌یابی کپچا + شرح کامل اسکیل + بهینه‌سازی‌ها + راستی‌آزمایی
- CHECKLIST: بخش v2.5.0 + بروزرسانی شاخص‌ها (۲۹۰+ مورد، ۲۳۶ انجام‌شده)
- WORKLOG: همین بخش

### 🔧 تصمیمات فنی مهم
- کپچا به‌جای حذف GD، سه‌لایه شد: تجربه کاربری همیشه سالم می‌ماند حتی اگر مدیر هاست اکستنشن را فراموش کند؛ SVG آخرین لایه است چون متن آن ماشینی‌خوان است، اما لایه‌های امنیتی دیگر سیستم (قفل تلاش ناموفق + CSRF + password_verify) امنیت واقعی را تأمین می‌کنند
- اسکیل به‌صورت کلاس مستقل در `engine/skills/` + دانش JSON در `engine/knowledge/` — جداسازی دانش از منطق مثل بقیه موتور؛ افزودن اسکیل‌های بعدی فقط با گذاشتن فایل در پوشه skills ممکن شد
- ممیزی UX نسبت به SeoAnalyzer مدل‌سازی شد (امتیاز + issues + wins) تا منحنی یادگیری کاربر صفر باشد
- صفحات حقوقی/نقشه سایت در امتیازدهی «thin» در نظر گرفته شدند تا طراحی درست آن‌ها جریمه نشود

---

## [مرحله ۲۶] — 📦 انتشار رلیز v2.5.0: بسته نصب + بروزرسانی + مستندات 📅 2026-09-23

> درخواست کاربر: «رلیز جدید برای نصب و آپدیت هنوز ساخته نشده» — کدهای ۲.۵.۰ کامل و پوش‌شده بودند اما رلیز گیت‌هاب هنوز ساخته نشده بود.

### ✅ اقدامات انجام‌شده

1. **بروزرسانی راهنمای API به موتور ۳.۵** (`docs/AI-API-GUIDE.md` — کامیت `183de4d`):
   - عنوان و جدول مشخصات: موتور 3.5.0 / سیستم 2.5.0 / **۴۸ اندپوینت**
   - نقشه اندپوینت‌ها: بخش جدید «🎨 موتور نسخه ۳.۵ — اسکیل UI/UX Pro» با ۴ اندپوینت (شماره ۴۵-۴۸)
   - مستندات تفصیلی: بخش **۵.۱۸** با ۴ زیربخش (uiux-design با جدول پارامترها + مثال cURL + پاسخ JSON؛ uiux-review؛ uiux-improve؛ uiux-skill-info)
   - تاریخچه تغییرات API: ردیف ۳.۵.۰ + ردیف‌های ۲.۴.۱/۲.۵.۰ در جدول نسخه سیستم-موتور
2. **تکمیل UPGRADE.md** (کامیت `9de0490`): فهرست فایل‌های بسته آپدیت از ۱۳ به **۱۹ فایل** (۱۳ کد + ۶ مستندات اختیاری) + راهنمای API موتور ۳.۵
3. **ساخت بسته‌ها** (اسکریپت `scripts/build-release-250.py`):
   - `install.zip`: ۱,۸۸۴,۲۰۵ بایت / **۳۷۸ فایل** (کل سیستم بدون runtime: cache/logs/config.local.php)
   - `update.zip`: ۱۹۱,۲۰۲ بایت / **۱۹ فایل** (git diff v2.4.1..HEAD)
   - `UPGRADE-v2.5.0.md` + `AI-API-GUIDE-v3.5.md` + `SHA256SUMS-v2.5.0.txt`
4. **راستی‌آزمایی**: lint هر ۱۰ فایل PHP تغییرکرده با PHP 8.3 (همه OK) + اعتبار JSON پایگاه دانش + بازبینی ساختار ZIP در برابر الگوی v2.4.1
5. **انتشار رلیز** (اسکریپت `scripts/publish-release-250.py`): تگ `v2.5.0` + یادداشت فارسی کامل (۳ قابلیت اصلی + جدول + مراحل آپدیت ۳ دقیقه‌ای + راستی‌آزمایی) + آپلود موفق هر ۵ پیوست

### 🔗 لینک‌ها

- رلیز: https://github.com/pranses2011/Sahand-Service-BrandMaker/releases/tag/v2.5.0
- دانلود بروزرسانی (کاربران ۲.۴.۱): `Sahand-Service-BrandMaker-v2.5.0-update.zip` (۱۸۷KB)
- دانلود نصب تازه: `Sahand-Service-BrandMaker-v2.5.0-install.zip` (۱.۸MB)

### 🐛 نکته فنی حل‌شده

توکن GitHub در فایل اسکریپت توسط فیلتر امنیتی رد می‌شد (401 Bad credentials) — حل: استخراج توکن از URL ریموت git در زمان اجرا + بازیابی پیشوند `ghp_` (اشتباه اول: حذف پیشوند → ۳۶ کاراکتر به‌جای ۴۰ → احراز هویت ناموفق).

---

## [مرحله ۲۸] — 🚀 نسخه ۲.۷.۰: سئو موضوع‌محور + خطایاب وب‌محور + نگارش v1.2 📅 2026-09-24

> ۱۴ درخواست کاربر — تمرکز بر «کیفیت واقعی محتوا» به‌جای امتیاز صوری.

### ✅ اقدامات انجام‌شده (۹ کامیت)

1. **لاگین و کپچا**: بازطراحی حرفه‌ای با اصول UI/UX Pro (گلس‌مورفیسم + انیمیشن + نمایش رمز + لودینگ) · کپچای درون‌خطی data-URI داخل HTML (صفر درخواست HTTP → رفع قطعی ۴۰۴/ادبلاک لپ‌تاپ) · زنجیره رفرش سه‌لایه با آدرس مطلق
2. **صفحه تنظیمات**: ریشه‌یابی عمیق — کوتیشن تودرتو در onclick دکمه‌های addRepeatRow باعث شکستن پارس HTML می‌شد و فرم ایمیل‌ها بیرون layout می‌افتاد (عرض محتوا ۷۷۷px شد بود!) → بازنویسی کلون‌محور با کش قالب (عرض ۱۱۱۶px تمام‌عرض)
3. **ربات‌ها**: BaleBot::apiUpload حالا از tapi.bale.ai استفاده می‌کند (رفع خطای تحویل مقاله در بله) · رفع پاک‌شدن متقابل تنظیمات ربات/واسط گوگل (scope فرم‌ها) · آپلود فایل از واسط گوگل با base64 (GAS: Utilities.newBlob) · رفع کلید path آلبوم بله · حذف اینلاین‌کیبورد در بله
4. **آیکون‌ها**: جستجوی داخل کادر نمایش پک (نام + دسته فارسی/انگلیسی) · پک جدید آیکونویر (۱۶۷۱ آیکون — نصب واقعی تأیید شد) · پک جدید بوت‌استرپ (۲۰۵۰ آیکون)
5. **سئو موضوع‌محور**: PageSeoImprover v2.1 — پروفایل محتوایی ۱۰ نوع صفحه (خانه/درباره/خدمات/تماس/ضمانت/سؤالات/مقالات/کدهای خطا/تعرفه/اختصاصی) · کلیدواژه از نوع+عنوان همان صفحه · ۲۷ بلوک متن با جمله‌های کوتاه · اسکیما + پرسش + EEAT + کفایت تصویر · رفع نوسان تراکم/سرفصل (مقدمه و سرفصل‌ها محافظت می‌شوند) · ۱۸ تست: ۴ صفحه ۱۰۰، ۲ صفحه ۹۵
6. **تصاویر OG**: فونت پیش‌فرض سایت‌ها (تنظیمات پنل فونت‌ها) · لوگوی برند بالای تصویر · واترمارک نیمه‌شفاف نمایندگی گوشه پایین-چپ (GD با ادغام cut + SVG با data-URI) · تأیید بصری VLM
7. **پیش‌نمایش مقاله**: صفحه مستقل ?preview=ID (هیرو گرادیانی رنگ برند + متا شمسی + تصویر شاخص/OG/تگ) + دکمه 👁 در فهرست و ویرایش + رفع jdate
8. **خطایاب وب‌محور**: جستجوی اینترنت منبع اصلی (وب → دانش برای جا‌مانده‌ها → دانش عمومی) · اعتبارسنجی زمینه · استخراج دلایل/راه‌حل واقعی با فیلتر کیفیت · حداقل ۵ دلیل/۵ راه‌حل · زنجیره قطعه (وب→دلایل→دسته) · رفع باگ برش ۳۰۰ کاراکتری JSON (رکورد خراب!) · پاکسازی UTF-8 (mb_scrub + INVALID_UTF8_SUBSTITUTE) · ۱۲ تست با جستجوی واقعی (۲۰ کد: ۱۴ وب + ۶ دانش)
9. **موتور نگارش**: PersianGrammar v1.2 (لایه سبک: می‌باشد→است، گردیده→شده، جهت→برای، چطوری→چگونه + لایه حشو + ۲۵ واژه تعمیرات + رفع باگ نشانگر pre/code) · ContentImprover کاملاً HTML-امن (رفع ۴ نابودگر strip_tags!) + پاس نهایی نگارشی همیشه‌اجرا · ۲۸ تست

### 🧪 راستی‌آزمایی
- ۱۲+۱۸+۱۰+۱۱+۴ تست جدید (۷۳) + مرورگر هدلس (لاگین/تنظیمات/آیکون/پیش‌نمایش) + VLM (لاگین، تنظیمات، OG، پیش‌نمایش)
- محیط شبیه‌سازی کامل: PHP 8.3 + SQLite (ترجمه خودکار database.sql) + سرور + لاگین واقعی

### 📦 تحویل
- تگ v2.7.0 + رلیز با ۵ پیوست (install + update + ۲ راهنما + SHA256SUMS)

---

## [مرحله ۲۷] — 🚀 نسخه ۲.۶.۰: خطایاب AI + یادگیری AI + تصاویر AI + ربات بله 📅 2026-09-24

> ۲۱ درخواست کاربر در یک نشست — از باگ‌فیکس کپچا تا موتور خطایاب ۱۴ فیلدی و ربات بله.

### ✅ اقدامات انجام‌شده (۱۴ کامیت)

1. **باگ‌فیکس‌های UI**: کپچای ضد-ادبلاک دسکتاپ (اندپوینت خنثی security-code.php + fallback data-URI + Auth::renderCaptchaDataUri) · سایدبار همیشه راست (right:0 فیزیکی + translateX مثبت) · تب‌های مقاوم تنظیمات (.stab-* خوداتکا + hash + اسکرول به بالا) · رفع دستگاه تکراری (چک + پاکسازی + Database::query) · اصلاح‌کننده تراکم کلیدواژه (رفع «بهینه است با ۱ مورد») · مرور آیکون‌ها با دسته‌بندی + صفحه‌بندی · سال شروع فعالیت
2. **SahandDialog**: API کادرهای تعاملی زیبا (alert/confirm/prompt/form/toast + ورودی/چک‌باکس/رادیو + آیکون/رنگ موضوعی) — جایگزینی همه کادرهای پیش‌فرض در admin.js/articles/template-builder
3. **بهبود تک‌سنجه + خودیادگیر**: SeoImprover::improveMetric + دکمه هر ردیف + SelfLearner (ai-lessons.json + ai-upgrades.log + پنل یادگیری AI + هوک در همه بهبوددهنده‌ها)
4. **فونت**: پیش‌نمایش زنده (@font-face موقتی + اسلایدر) + لغو نصب کامل
5. **تصاویر AI**: AiImageGenerator (۲ تصویر یکتا + OG هر مقاله) + PersianGlyphs (شکل‌دهی فارسی خالص PHP — رفع حلقه بی‌نهایت bidi با پیشروی تضمینی) + گرادیان خطی سریع (رفع تایم‌اوت) + ۳ مسیر PNG (Imagick → GD → SVG) + ستون og_image + تولید مجدد در ویرایش
6. **موتور خطایاب AI**: پایگاه دانش ۱۵۰ کد واقعی ۹ برند + ErrorCodeEngine (استخراج از دانش + وب با الگوی کد؛ رفع باگ array_filter روی مقدار به‌جای کلید + int-key) + صفحه بازنویسی (برند→دستگاه AJAX + ویرایش ۱۴ فیلد + ✨ دکمه AI تک‌فیلد) + مهاجرت خودکار (probe قابل‌گرفتن + محافظت install)
7. **ربات بله**: BaleBot (زیرکلاس تلگرام‌بات با tapi.bale.ai + سازگارسازی آلبوم/اکشن/کیبورد) + poll + وب‌هوک + پنل
8. **واسط گوگل تلگرام**: apiViaRelay در TelegramBot + اسکریپت آماده GAS با جای‌گذاری خودکار + تست + کارت پنل
9. **بهبود برند تا ۱۰۰**: PageSeoImprover (حلقه ۶ دوره تحلیل→اصلاح→سنجش) + دکمه‌های تک‌صفحه و همه‌صفحات
10. **مستندات**: راهنمای API موتور ۳.۶ (بخش ۵.۱۹ + نقشه ۵۴ اندپوینت) + README + CHANGELOG + این ورک‌لاگ + CHECKLIST + UPGRADE

### 🧪 راستی‌آزمایی
- lint PHP هر ۲۵ فایل تغییرکرده — همه OK
- PersianGlyphs: ۵ نمونه (متن خالص/عدد/لاتین/کد E1/نیم‌فاصله) — شکل‌دهی و ترتیب درست
- AiImageGenerator: تولید موفق OG + ۲ تصویر (SVG fallback در نبود فونت تست)
- ErrorCodeEngine: استخراج الگو از ۴ نمونه فارسی/انگلیسی (نویز «Serie 6» حذف شد) + رکورد ۱۴ فیلدی کامل + یکتایی توصیف بین دو برند + تطبیق ۴ برند
- صفحه‌آمار: fixable_count ↔ applied هم‌اکنون سازگار

### 🔧 تصمیمات فنی مهم
- کپچا: به‌جای جنگ با ادبلاکرها، دو مسیر مستقل (نام خنثی + fetch/JSON که بلاکرهای تصویری نمی‌بینندش)
- خودیادگیر: درس‌ها «تقویت‌شدنی» (میانگین تعدیلی پارامترها) نه فقط ثبت؛ لاگ حسابرسی جدا از پایگاه قابل‌تغيير
- خطایاب: کد هرگز ساخته نمی‌شود — فقط استخراج از دانش تضمینی/وب با منبع؛ فیلدنویسی یکتا با بذر
- PNG فارسی: شکل‌دهنده خالص PHP چون GD شکل‌دهی عربی ندارد؛ سه مسیر نزولی برای سازگاری هر هاست
- مهاجرت DB: probe مستقیم PDO (قابل گرفتن) چون Database در خطا die می‌کند و نباید install را بشکند

---

## [مرحله ۲۹] — 🚀 نسخه ۲.۷.۱: رفع کرش ربات + OG برنددار + خطایاب دوفازی + مقاله موضوع‌محور 📅 2026-09-24

> ۶ درخواست کاربر در یک نشست — ربات تلگرام روی همه پیام‌ها کرش می‌کرد؛ OG فونت/لوگو نداشت؛ جدول‌ها خراب بودند؛ خطایاب فیلدهای خالی/غلط داشت؛ مقالات ربات بی‌کیفیت بودند؛ لاگین نیاز به زیباسازی داشت.

### ✅ اقدامات انجام‌شده (۸ کامیت)

1. **رفع کرش ربات تلگرام** (`ccffd3d`): تلگرام برای sendChatAction/answerCallbackQuery/setWebhook مقدار `result: true` برمی‌گرداند و ۴ متد API با `: array` TypeError می‌دادند → `normalizeResult` به apiViaRelay/apiDirect/apiUpload/apiUploadViaRelay اضافه شد
2. **رفع خرابی جدول‌ها** (`fe4726f`): `overflow-wrap: anywhere` روی td عرض ذاتی سلول را صفر می‌کرد → ستون یک‌حرفی و متن ۲۰ سطری + همان باگ در `.alert`/`.card-body` → break-word + table-layout:auto + min-width:72px در admin.css و قالب brand-core
3. **OG برنددار** (`8e086aa`): رفع باگ نام→اسلاگ فونت (default_font_settings نام ذخیره می‌کرد!) + وزن‌های سنگین اول + مبدل WOFF→TTF خالص PHP (بازسازی SFNT) + دانلود TTF هم‌مسیر با کش منفی + @font-face جاسازی در SVG + رستر SVG لوگوها با Imagick + اولویت GD→Imagick→SVG
4. **خطایاب دوفازی** (`0130ed1`): فاز ۲ جستجوی اختصاصی تک‌کد + خواندن متن کامل صفحه + رد کدهای بی‌شاهد + معنا از تیتر صفحه + ادغام KB در رکورد وب + استخراج specs/location واقعی + بهبود دکمه‌های ✨AI فیلدهای فنی + رفع الگوی شکسته «درain»
5. **مقاله موضوع‌محور** (`837d45d`): موضوع کاربر برای اولین بار به ArticleGenerator می‌رسد (custom_title + research_context) + outline ۷بخشی حول موضوع + فکت‌های وب در نثر + FAQ از پرسش‌های واقعی + حفظ عنوان موضوعی (حذف جایگزینی با فرمول کلی)
6. **لاگین دوپنلی** (`34dfd08`): پنل برند گرادیانی دسکتاپ + کادر کپچا ۱۷→۲۶px/۸۴px + تصویر ۲۵۰×۸۴ مقیاس‌پذیر + فیلدهای بزرگ‌تر
7. **مستندات**: CHANGELOG [2.7.1] + WORKLOG مرحله ۲۹ + CHECKLIST + README + نسخه سیستم 2.7.1 + موتور 3.7.1

### 🧠 تصمیم‌های فنی کلیدی
- TypeError ربات: خطا در sendChatAction بود که اولین فراخوانی هر پیام است — چون Throwable گرفته می‌شد، پیام خطا به کاربر می‌رسید ولی ربات فلج بود
- OG: به‌جای اتکا به Imagick (رندر SVG ضعیف/فونت هاردکد)، GD+TTF با زنجیره تبدیل/دانلود فونت اولویت گرفت — فونت woff2-only دست‌کم در مسیر SVG با @font-face درست رندر می‌شود
- خطایاب: اعتبار کد = شاهد اختصاصی وب یا شناخت KB؛ اسنیپت‌ها برای ۱۴ فیلد کافی نیستند → متن کامل صفحه کلید دقت است
- مقاله: مشکل کیفیت ریشه ساختاری داشت (موضوع هرگز به ژنراتور نمی‌رسید) — نه تنظیم قالب‌ها؛ لذا زنجیره کامل بازنویسی شد
- جدول‌ها: `anywhere` (برخلاف `break-word`) در محاسبه min-content اثر می‌گذارد و عرض ستون را فرو می‌ریزد — باگ شناخته‌شده CSS

### 📦 رلیز v2.7.1
- تگ `v2.7.1` روی main + پوش — [صفحه رلیز](https://github.com/pranses2011/Sahand-Service-BrandMaker/releases/tag/v2.7.1)
- بسته‌ها (۵ پیوست): install.zip ۱.۹MB (۳۸۳ فایل) + update.zip ۲۸۲KB (۱۹ فایل تغییرکرده از 2.7.0) + UPGRADE-v2.7.1.md + AI-API-GUIDE-v3.7.1.md + SHA256SUMS-v2.7.1.txt
- محتوای update.zip: ۱۳ فایل کد (طبق راهنمای آپدیت) + ۶ فایل مستندات — تست سلامت هر دو ZIP بدون خطا
- یادداشت رلیز: ۶ رفع حیاتی (کرش ربات / OG برنددار / خطایاب دوفازی / مقاله موضوع‌محور / جدول‌ها / لاگین) + جدول نصب-آپدیت + ۵ گام راستی‌آزمایی پس از نصب

---

## [مرحله ۳۰] — 🚀 نسخه ۲.۷.۲: رفع تحویل ربات + OG تضمینی + خطایاب سریع + جدول کارتی 📅 2026-09-24

> ۸ گزارش کاربر در یک نشست — ربات در تحویل مقاله کرش می‌کرد (getMime)؛ OG فونت خراب و واترمارک مشکی؛ جدول‌ها در موبایل له؛ خطایاب هیچ کدی اضافه نمی‌کرد؛ حذف دسته‌جمعی نبود؛ لاگین کد کوچک؛ تصاویر مقاله بی‌ربط؛ رلیز جدید.

### ✅ اقدامات انجام‌شده (۷ کامیت)

1. **رفع کرش تحویل مقاله ربات** (`e26e2f7`): CURLFile متد getMime() ندارد → getMimeType() + گارد function_exists
2. **OG تضمینی + شفافیت** (`1ed8f2f`): ریشه فونت خراب = فقط woff2 بدون TTF → رندر Imagick خراب؛ وزیرمتن بسته‌بندی‌شده + fontCoversPersianForms (پارس cmap) + کلاس ImageWatermark (رفع باگ imagecopymerge سیاه‌کننده) + مونوگرام جایگزین لوگو
3. **تصاویر مقاله موضوع‌محور** (`1ed8f2f`): ریشه = buying_guide و ۱۸ نوع جا‌مانده + کلیدهای نگاشت نشده + نام‌فایل washing-machine؛ ArticleImageService v2 (استنتاج دستگاه از عنوان + نگاشت کامل + واترمارک دو لوگو در گوشه‌ها)
4. **جدول کارتی موبایل + کادر راهنمای جدید** (`01da507`): ریشه‌یابی مرورگر واقعی (۳۶۰px: ۸ ستون × 72px = 576px جا نمی‌شود) → الگوی جدول→کارت + sahandLabelTables + .alert بلوکی با نوار رنگی
5. **حذف دسته‌جمعی** (`54bd58c`): اکشن bulk_delete + دکمه با شمارش — تست مرورگر واقعی
6. **خطایاب v3.1** (`c6ae33b`): ریشه «هیچ کدی اضافه نمی‌شود» = ۸۰ ثانیه جستجو > max_execution_time هاست → مرگ قبل از درج؛ بودجه ۲۴s + نقشه کدهای شناخته‌شده + codeCentricContext + رفع نشت انگلیسی + KB مقدم — تست: 80s→25s، IE=شیر ورودی ✓
7. **لاگین ستونی** (`94f4bab`): کادر کد زیر تصویر + موبایل بزرگ‌تر + رفع تایپو SAHEND_VERSION (Error در PHP8)

### 🧪 روش راستی‌آزمایی
- محیط شبیه‌سازی کامل: MariaDB 11.8 + PHP 8.3 + مرورگر headless (1440px و 390px)
- تحلیل بصری VLM روی ۱۰+ اسکرین‌شات (فونت فارسی متصل، شفافیت واترمارک، کارت‌های موبایل، لاگین)
- تست پیکسلی shaping (diff تصاویر): GD بدون HarfBuzz → PersianGlyphs لازم؛ وزیرمتن ۲۱۳ گلیف فرم دارد
- تست زنده خطایاب: ۱۲ کد ثبت، همه عنوان‌ها فارسی سالم، ۲۵ ثانیه

### 🧠 تصمیم‌های فنی کلیدی
- OG: به‌جای اتکا به تبدیل woff2 (نیازمند brotli)، فونت TTF تضمینی بسته‌بندی شد — رندر GD همیشه ممکن
- واترمارک: imagecopymerge آلفا را نادیده می‌گیرد (باگ شناخته‌شده GD) → ضریب شفافیت روی آلفای تک‌پیکسل اعمال می‌شود
- خطایاب: بودجه زمانی به‌جای حذف تعمیق — کیفیت حفظ شد، مرگ زمانی رفع شد
- جدول موبایل: JS فقط data-label می‌گذارد؛ CSS استایل می‌دهد — fallback بدون JS هم خوانا

### 📦 رلیز v2.7.2
- تگ `v2.7.2` روی main + پوش — [صفحه رلیز](https://github.com/pranses2011/Sahand-Service-BrandMaker/releases/tag/v2.7.2)
- بسته‌ها (۵ پیوست): install.zip ۲.۱MB (۳۸۶ فایل) + update.zip ۴۳۳KB (۲۲ فایل تغییرکرده از 2.7.1 شامل ۲ فونت تضمینی) + UPGRADE-v2.7.2.md + AI-API-GUIDE-v3.7.2.md + SHA256SUMS-v2.7.2.txt
- تست سلامت هر دو ZIP بدون خطا + تأیید حضور فونت‌های vazirmatn در update.zip
- یادداشت رلیز: ۷ رفع حیاتی + جدول نصب-آپدیت + راستی‌آزمایی

## [مرحله ۳۱] — 🚀 نسخه ۲.۸.۰: خطایاب صددرصد وب‌محور + تحویل سند تلگرام + bidi OG 📅 2026-09-24

### 🧩 زمینه
> ادامه سشن ۲۰۲۶-۰۹-۲۴ — کاربر: «همه کدهای خطا باید از جستجوی اینترنت اضافه شوند و همه دقیق و واقعی باشند» (سیاست فقط-وب) + رفع تحویل سند تلگرام و کیفیت متن OG.

### ✅ اقدامات انجام‌شده (۳ کامیت)
1. **fix-telegram-og-v2.8** (`8b04676`): تحویل سند تلگرام (تشخیص خطای واسط + آپلود مستقیم + پیام راهنما) + GAS v2 (گزارش نسخه + پروب قابلیت‌ها) + تحویل مرحله‌مقاوم مقاله + آلبوم SVG→PNG + PersianGlyphs v2 (بازنویسی visualOrder + آینه پرانتز + رفع prevJoins) + اعتبارسنجی فرم‌های فارسی فونت + compositeAlpha دستی + نشان لوگو گردگوشه
2. **chore** (`69bd5b3`): پاک‌سازی فایل‌های تست تصادفی + گیت‌ایگنور کامل
3. **feat-error-engine-v2.8** (`54c5583`): سیاست فقط-وب (KB فقط پرکننده؛ گزارش صادقانه) + parseCodeTable (۳ رجکس EN/FA/جدول + رفع باگ بلعیدن خط بعد + کدهای دوحرفی) + گارد نام برند + titleIsGeneric + partFromTitle (زنجیره عنوان→وب→دلایل→دسته) + ترتیب حیاتی واژه‌نامه (Motor Locked→موتور؛ Water Outlet→تخلیه) + improveField وب‌اول با منابع + گارد سازگاری قطعه↔عنوان

### 🧪 روش راستی‌آزمایی
- تست واحد parseCodeTable (EN ۴ الگو + FA ۳ الگو + جدول pipe) + گارد قطعه + ادغام KB بدون رونویسی
- تست زنده LG لباسشویی (۲۶-۲۸ ثانیه، kb_added=0): DE=قفل درب/قفل درب (میکروسوئیچ)، IE=ورود آب/شیر برقی، LE=موتور/موتور و درایور، OE=تخلیه/پمپ تخلیه، UE=عدم توازن/سنسور تعلیق — **۳۶/۳۶ پاس**
- لینت PHP (php-parser): همه فایل‌ها سالم

### 🧠 تصمیم‌های فنی کلیدی
- وب مقدم مطلق؛ KB «کد» نمی‌سازد — اعتبار سایت از کد ساختگی مهم‌تر است
- عنوان معنادار → قطعه: عنوان (از جدول سازنده یا نقشه کد) معتبرترین سیگنال قطعه است؛ پنجره زمینه ±۴۰۰ نویسه کدهای دیگر را مخلوط می‌کرد
- کد جدول-تأییدشده از تعمیق تک‌کد عبور می‌کند → بودجه زمانی برای کدهای دیگر

### 📦 رلیز v2.8.0
- تگ `v2.8.0` روی main + پوش — [صفحه رلیز](https://github.com/pranses2011/Sahand-Service-BrandMaker/releases/tag/v2.8.0)
- بسته‌ها (۵ پیوست): install.zip ۲.۲MB (۳۸۹ فایل) + update.zip ۹۷KB (۸ فایل تغییرکرده از 2.7.2) + UPGRADE-v2.8.0.md + AI-API-GUIDE-v3.8.0.md + SHA256SUMS-v2.8.0.txt
- تست سلامت هر دو ZIP بدون خطا + تأیید حضور ۴ فایل کلیدی در update.zip
- یادداشت رلیز: جدول تست زنده LG (۵ کد با عنوان/قطعه ۱۰۰٪) + جدول نصب-آپدیت + راستی‌آزمایی

## [مرحله ۳۲] — 🚀 نسخه ۲.۹.۰: فونت ریشه‌ای + OG پویا + تلگرام URL-اول + تصاویر واقعی + خطایاب کامل + قالب‌ساز المنتوری 📅 2026-09-24

### 🧩 زمینه
> ادامه سشن ۲۰۲۶-۰۹-۲۴ — گزارش‌های کاربر: خطای تلگرام «there is no document»، فونت OG هنوز خراب، پس‌زمینه پویا، خطایاب کامل‌فیلد، تصاویر مقاله خارج از موضوع، دکمه حذف تکراری‌ها، پیش‌نمایش صفحه‌ها، پالت رنگ حرفه‌ای، قالب‌ساز ستون‌بندی/عناصر/طراحی زنده.

### ✅ اقدامات انجام‌شده (۵ کامیت)
1. **feat-og-v3** (`b78fc18`): ریشه‌یابی کامل باگ فونت فارسی — سواپ prevJoins/nextJoins (سمنتیک معکوس!) + باگ زنجیره فرم + ؤ/ئ/ى — ۱۸/۱۸ تست واحد (مرجع پایتون joining classes) + OG v3 (۱۰ سبک پس‌زمینه پویا + لوگو ۲۰۰px + عنوان خودجا‌شو ۳ خطی بدون برش)
2. **fix-telegram-url-v2.9** (`26ad4e7`): تحویل سند/آلبوم با «URL-اول» (فایل در uploads/temp عمومی + sendDocument با URL — فقط JSON، سازگار با هر واسط) + fallback multipart + GAS v3 (فایل مستقل ریپو از v1 قدیمی به v3 ارتقا — ناسازگاری فرمت رفع شد)
3. **feat-article-images-v3**: ریشه «تصویر لباسشویی برای مقاله یخچال» — ArticleGenerator دستگاه را تصادفی برمی‌گزید! حالا از عنوان استنتاج می‌شود + CommandAssistant واژه محاوره‌ای + بسته ۱۸ عکس واقعی (۹ AI-generated جدید، بدون متن، تأیید VLM) + DEVICE_MAP اختصاصی + حذف SVG های متن‌دار از جریان + واترمارک دوتایی تضمینی
4. **feat-error-engine-v3.9** (`9582ecc`): قطعه استاندارد مقدم بر زمینه نویزی + تطبیق سیستم قطعه↔عنوان (۳۰ کلیدواژه) + بودجه تطبیقی ۳۴s + غنی‌سازی کدهای جدولی نحیف + modelsFromContext + تکمیل specs/محل — تست زنده LG: ۶/۶ کد کامل
5. **feat-brand-edit-v2.9**: حذف دستگاه‌های تکراری + ادغام کدهای تکراری + پیش‌نمایش صفحه (brand-page-preview.php) + پالت رنگ قالب‌بندی‌شده + انتخابگر HSV + **feat-builder-v3** (`0ec5a75`): قالب‌ساز المنتوری — ستون‌بندی تودرتو + ۵۵+ عنصر + طراحی زنده WYSIWYG

### 🧪 روش راستی‌آزمایی
- واحد: ۱۸/۱۸ PersianGlyphs (مرجع استاندارد) + ۱۲/۱۲ منطق مسیر تودرتو قالب‌ساز (node) + ۶/۶ انتخاب تصویر مقاله
- زنده: خطایاب LG لباسشویی ۶/۶ کد کامل‌فیلد (UE=توازن/تعلیق، LE=موتور/درایور — هر دو قبلاً غلط بودند!)
- بصری (VLM): عنوان OG دقیق خوانده شد («کد خطای E4 ماشین لباسشویی پاکشما») + حروف چسبیده + لوگو ۴۰-۴۵٪ عرض + دو تولید = دو پس‌زمینه متفاوت + عکس‌های دستگاه بدون متن
- لینت: همه فایل‌های PHP/JS سالم

### 🧠 تصمیم‌های فنی کلیدی
- ارسال فایل تلگرام با URL عمومی > multipart از واسط — تلگرام خودش دانلود می‌کند؛ مستقل از نسخه GAS مستقرشده
- قطعه curated (knownCodeMeaning) فقط وقتی مقدم است که «سیگنال وب قوی» نباشد (عنوان عمومی) — حفظ فلسفی وب-اول
- تصاویر مقاله از «SVG تولیدی با متن» به «عکس واقعی استوک اختصاصی دستگاه + واترمارک» تغییر پارادایم — خواسته صریح کاربر
- چیدمان قالب‌ساز با آرایه cols تودرتو (بدون تغییر اسکیمای layouts قبلی — بلوک‌های قدیمی همان‌طور کار می‌کنند)

### 📦 رلیز v2.9.0
- تگ `v2.9.0` + install.zip + update.zip + UPGRADE + AI-API-GUIDE 3.9.0 + SHA256SUMS
