# 🔄 راهنمای بروزرسانی — Sahand Service BrandMaker

نسخه هدف: **2.24.0** (موتور AI: 3.15.0) — [سابقه تغییرات](CHANGELOG.md)

---

## ⚠️ نکته مهم این نسخه (۲.۲۴.۰)

این نسخه **رفع قطعی استقرار تازه** را دارد: اگر قبلاً «استقرار» (نه بروزرسانی) با خطای
«استخراج کامل نشد — index.php در مسیر سایت یافت نشد» شکست می‌خورد، پس از بروزرسانی
دوباره «استقرار» را اجرا کنید — ریشه (پوشه‌های پیش‌ساخته مانع استخراج) برطرف شده است.
سایت‌های مستقرشده قبلی هم برای رفع «صفحات خالی» و «اعمال تم» باید یک‌بار
«بروزرسانی استقرار» شوند (فایل‌های جدید با توابع رندر و پالت زنده منتقل می‌شود).

## روش ۱ — پکیج بروزرسانی (پیشنهادی — بدون تغییر دیتابیس)

1. از پوشه فعلی سایت ساز خود **بکاپ کامل** بگیرید (فایل‌ها + دیتابیس).
2. فایل `update.zip` این رلیز را در **ریشه سایت ساز** (کنار `config.php`) آپلود کنید.
3. از فایل‌منیجر هاست (یا SSH با `unzip -o update.zip -d .`) آن را **استخراج** کنید —
   فایل‌های سیستمی جایگزین می‌شوند و `config.php` و `.htaccess` و `uploads/` و `database.sql` دست‌نخورده می‌مانند.
4. فایل `update.zip` را حذف کنید.
5. وارد پنل مدیریت شوید — همه چیز آماده است (**هیچ تغییر دیتابیسی لازم نیست**).

## روش ۲ — از سورس گیت‌هاب

```bash
git pull origin main
# اگر تغییرات محلی دارید: git stash && git pull && git stash pop
```

## فایل‌هایی که در این نسخه تغییر کردند (نسبت به 2.23.0)

| فایل | تغییر |
|------|-------|
| `core/Deployer.php` | stepFolders فقط cache + پاک‌سازی پوشه‌های تضادی + کاندید BASE_URL + پالت fallback |
| `core/ConfigGenerator.php` | require توابع رندر در config تولیدی |
| `templates/brand-core/config.php` | require includes/functions.php |
| `templates/brand-core/includes/functions.php` | گاردهای function_exists |
| `templates/brand-core/includes/header.php` | پالت زنده از API |
| `templates/brand-core/css/style.css` | متغیرهای پیش‌فرض :root |
| `admin/brand-build.ajax.php` + `admin/brand-build.php` | مرحله ۴ تک‌مقاله‌ای idempotent |
| `admin/error-codes.php` | مقاوم‌سازی خطایاب (shutdown JSON + تشخیص HTML) |
| `admin/brand-edit.php` | افزودن/حذف دستگاه در تب دستگاه‌ها |
| `engine/services/AiPhotoService.php` | ۴ سرویس رایگان جدید + کلیدواژه استوک ۴۲ دستگاه |
| `config.php` + `engine/SahandAI.php` | نسخه‌ها 2.24.0 / 3.15.0 |

## راستی‌آزمایی بعد از بروزرسانی

1. پایین پنل مدیریت → «نسخه 2.24.0» نمایش داده شود.
2. **برای هر برند مستقرشده**: دکمه «بروزرسانی استقرار» را بزنید (رفع صفحات خالی + پالت زنده منتقل می‌شود).
3. ویرایش برند ← تب دستگاه‌ها ← کارت «افزودن دستگاه جدید» ظاهر شود.
4. تنظیمات ← تولید تصویر ← ۱۸ سرویس (۷ بدون کلید) در فهرست باشد.
5. ساخت برند جدید ← مرحله ۴ (مقالات) عناوین را تک‌به‌تک زنده نمایش دهد.
6. خطایاب ← اجرا؛ اگر مهلت سرور تمام شد پیام قابل‌فهم «پایان زمان مجاز» بدهد (نه خطای JSON).

## بازگردانی (Rollback)

در صورت مشکل، بکاپ مرحله ۱ را بازگردانید. ساختار دیتابیس در این نسخه تغییر نکرده است.
