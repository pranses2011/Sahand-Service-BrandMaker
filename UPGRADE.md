# 🔄 راهنمای بروزرسانی — به نسخه ۲.۲.۰

> این راهنما نحوه ارتقای سایت ساز برند سهند سرویس به نسخه **۲.۲.۰ (موتور ۳.۲ — 🖼️ ۳ تصویر در مقاله + 📰 تحویل کامل مقاله در تلگرام + 📱 ریسپانسیو کامل)** را توضیح می‌دهد.
>
> - ارتقا از **۱.x** → دو مرحله‌ای: اول [راهنمای ارتقا به ۲.۰.۰](#--از-نسخه-1x-ابتدا-به-20) پایین همین فایل، سپس [ارتقای ۲.۰→۲.۱](#--ارتقای-20--21) و در پایان مراحل همین بخش
> - ارتقا از **۲.۰.۰** → اول [ارتقای ۲.۰→۲.۱](#--ارتقای-20--21)، سپس مراحل همین بخش
> - ارتقا از **۲.۱.۰** → فقط مراحل همین بخش

---

## ⚠️ قبل از شروع (الزامی)

| # | اقدام | توضیح |
|:-:|-------|-------|
| ۱ | 🗄️ **بکاپ کامل دیتابیس** | از phpMyAdmin → Export → SQL (کامل، با همه جدول‌ها) |
| ۲ | 📁 **بکاپ فایل‌ها** | کل پوشه سایت ساز را ZIP کنید (مخصوصاً `config.local.php` و `uploads/`) |
| ۳ | ✅ **ثبت نسخه فعلی** | پایین پنل مدیریت یا `config.php` → باید `2.1.0` باشد |

> 💡 زمان موردنیاز: حدود ۵ دقیقه | ⏸️ قطعی سرویس: لازم نیست (بدون تغییر دیتابیس)

---

## 🎯 تغییرات مهم این نسخه

- 🐛 **رفع باگ «منو در وسط صفحه باز می‌شود»** در پنل موبایل + **ریسپانسیو کامل** پنل، سایت‌های برند و صفحه نصب
- 🖼️ **۳ تصویر مرتبط برای هر مقاله** (۹ تصویر فوتورئال جدید بسته‌بندی شد + درج خودکار در جایگاه‌های استراتژیک)
- 📰 **تحویل کامل مقاله در تلگرام**: «مقاله بنویس...» یا `/article موضوع` → فایل HTML آماده انتشار برای هر سایت دیگر + نسخه Markdown + آلبوم ۳ عکس — **بدون نیاز به ورود به سایت ساز**
- 🧠 **موتور ۳.۲**: غنی‌سازی ساختاری مقاله (فهرست مطالب + سایر پرسش‌های کاربران + داده‌های تازه وب با منبع + جعبه E-E-A-T) + تحقیق وب خودکار (auto) + امتیاز هدف ۸۸
- 🌐 **جستجوی وب قوی‌تر**: ارائه‌دهنده Mojeek (مجموع ۷) + فرصت‌های کلیدواژه long-tail + تلاش مجدد خودکار
- 📰 **اخبار زنده**: `POST /api/ai/news` + فرمان «اخبار: ...» در دستیار و ربات
- 🔍 **سئو عمیق‌تر**: HowTo Schema + Speakable + تراکم کلیدواژه + زمان مطالعه + og:image
- 🔌 ۲ اندپوینت API جدید: `news`، `article-images` (مجموع ۳۹)

**💡 بدون هیچ تغییر دیتابیسی** — این نسخه کاملاً file-level است؛ جداول، seedها و داده‌ها بدون تغییر می‌مانند.

---

## 📦 روش ۱: بسته بروزرسانی (توصیه‌شده)

بسته `Sahand-Service-BrandMaker-v2.2.0-update.zip` از [صفحه رلیز](https://github.com/pranses2011/Sahand-Service-BrandMaker/releases) دانلود کنید. این بسته **فقط فایل‌های تغییرکرده** را شامل می‌شود.

### مراحل

```text
۱. محتویات ZIP بروزرسانی را استخراج کنید
۲. کل فایل‌ها را با FTP/File Manager روی هاست آپلود و جایگزین کنید
   (مسیرها داخل بسته از قبل درست است — فقط همه را روی روت سایت ساز کپی کنید)
۳. مطمئن شوید پوشه cache/engine قابل نوشتن است (مجوز 755 کافی است)
۴. پنل مدیریت را رفرش کنید — تمام! ✅
```

### فایل‌های جایگزین‌شده در بسته بروزرسانی v2.2.0

```
config.php                        ← نسخه 2.2.0
install.php                       ← ریسپانسیو مرحله‌بندی نصب
api/index.php                     ← ۲ اندپوینت جدید (news + article-images)
engine/SahandAI.php               ← موتور v3.2.0
engine/CommandAssistant.php       ← نیّت اخبار + موضوع دلخواه مقاله + no_cache
engine/generators/SmartPipeline.php        ← خط تولید ۱۱ مرحله‌ای v3.2
engine/generators/SeoGenerator.php         ← v1.1 (HowTo + تراکم + Speakable)
engine/services/WebSearchService.php       ← v1.1 (Mojeek + اخبار + opportunities)
engine/services/ArticleImageService.php    ← 🆕 سرویس ۳ تصویر مقاله
telegram/TelegramBot.php          ← 🆕 v2 — تحویل کامل مقاله + ۱۲ فرمان + callback
admin/includes/header.php         ← پس‌زمینه سایدبار + aria
assets/css/admin.css              ← 🐛 رفع باگ منو + ریسپانسیو کامل پنل
assets/js/admin.js                ← toggleSidebar هوشمند + بستن خودکار
templates/brand-core/css/style.css        ← منوی موبایل جدید + ریسپانسیو + عناصر v3.2
templates/brand-core/includes/header.php  ← منوی aria + toggleNav
templates/brand-core/js/app.js            ← toggleNav + بستن خودکار
assets/images/articles/*.jpg      ← 🆕 ۹ تصویر تخصصی مقاله (جديد)
README.md / CHANGELOG.md / UPGRADE.md / CHECKLIST.md / WORKLOG.md
docs/AI-API-GUIDE.md              ← مستندات v3.2 (اخبار + تصاویر + ربات v2)
```

> ⚠️ توجه: `config.local.php` جایگزین **نمی‌شود** (تنظیمات شما دست‌نخورده می‌ماند) — این فایل در بسته نیست.

---

## 📦 روش ۲: بسته کامل نصب (اگر تغییرات دستی زیاد دارید)

اگر فایل‌های هسته را دستی تغییر داده‌اید، بسته کامل را نصب کنید و `config.local.php` + `uploads/` + فایل‌های سفارشی خود را از بکاپ برگردانید.

---

## ✅ راستی‌آزمایی پس از بروزرسانی

| # | بررسی | انتظار |
|:-:|-------|--------|
| ۱ | پایین پنل مدیریت | نسخه **2.2.0** |
| ۲ | `GET /api/ai/engine-info` | `engine_version: "3.2.0"` |
| ۳ | 📱 پنل در موبایل | منو با ☰ از لبه صفحه باز می‌شود + پس‌زمینه تیره + با کلیک روی لینک بسته می‌شود |
| ۴ | 🖼️ تصاویر مقاله | `GET /api/ai/article-images` → ۹ تصویر با exists=true |
| ۵ | 📰 اخبار زنده | `POST /api/ai/news` با query «لوازم خانگی» |
| ۶ | 🧠 خط تولید کامل | `POST /api/ai/smart-generate` → خروجی شامل `images` (۳ عدد) و `article.toc` |
| ۷ | 🤖 ربات تلگرام | «مقاله بنویس درباره یخچال» → فایل HTML + MD + آلبوم ۳ عکس |

### تست سریع با curl (جایگزین دامنه و کلید)

```bash
# سلامت موتور
curl "https://YOUR-DOMAIN/api/ai/engine-info"

# 🖼️ فهرست تصاویر مقاله (عمومی — بدون کلید)
curl "https://YOUR-DOMAIN/api/ai/article-images"

# 📰 اخبار زنده (جدید v3.2)
curl -X POST "https://YOUR-DOMAIN/api/ai/news" \
  -H "Content-Type: application/json" -H "X-API-Key: YOUR_KEY" \
  -d '{"query": "لوازم خانگی", "limit": 3}'

# 🧠 خط تولید کامل با ۳ تصویر + تحقیق خودکار
curl -X POST "https://YOUR-DOMAIN/api/ai/smart-generate" \
  -H "Content-Type: application/json" -H "X-API-Key: YOUR_KEY" \
  -d '{"brand_id": 1, "device_key": "refrigerator", "variants": 2}'
```

### 🤖 تست تحویل کامل مقاله در تلگرام (پس از ارتقا)

۱. پنل → **🤖 ربات تلگرام** → مطمئن شوید ربات فعال و شناسه‌تان مجاز است
۲. در تلگرام به ربات بفرستید: `مقاله بنویس درباره تعمیر یخچال`
۳. انتظار: پیام «در حال ساخت...» → خلاصه → فایل `.html` → فایل `.md` → آلبوم ۳ عکس → دکمه‌های شیشه‌ای ✅

---

## 🆘 رفع اشکال

| مشکل | راه حل |
|------|--------|
| خطای نوشتن در `cache/engine` | مجوز پوشه `cache` را 755 (یا 775) کنید؛ پوشه `cache/engine` به‌صورت خودکار ساخته می‌شود |
| پنل هنوز نسخه قدیمی نشان می‌دهد | کش مرورگر (Ctrl+F5)؛ مطمئن شوید `config.php` جدید آپلود شده |
| خطای 404 در اندپوینت‌های جدید | `.htaccess` قدیم مانده — فایل `.htaccess` روت را از بسته کامل جایگزین کنید |
| تصاویر مقاله لود نمی‌شوند | پوشه `assets/images/articles/` را کامل آپلود کرده‌اید؟ (۹ فایل JPG) |
| ربات فایل نمی‌فرستد | `curl` فعال باشد + `sys_get_temp_dir()` قابل نوشتن؛ توکن ربات معتبر است |
| جستجوی وب خطا می‌دهد | مطمئن شوید cURL فعال است؛ در صورت تحریم ارائه‌دهنده، زنجیره جایگزین خودکار فعال می‌شود؛ در نهایت کلید SerpApi را در تنظیمات `websearch_settings` وارد کنید |
| وب‌هوک تلگرام set نمی‌شود | آدرس باید HTTPS باشد؛ برای تست محلی از `php telegram/poll.php` استفاده کنید |
| ربات پاسخ نمی‌دهد | شناسه چت را با `/id` بگیرید و در پنل مجاز کنید (لیست خالی = بسته) |

---

## 📚 ادامه مطالعه

- 🤖 [راهنمای کامل API هوش مصنوعی](docs/AI-API-GUIDE.md) — استفاده از موتور AI از بیرون سایت ساز (شامل راهنمای کامل ربات v2)
- 🔤 [دانلود فونت‌ها](assets/fonts/README.md) — ۳۰ فونت با لینک رسمی
- 🖼️ [دانلود پک‌های آیکون](assets/icons) — ۱۰ پک با لینک رسمی
- 📋 [سابقه تغییرات](CHANGELOG.md) — جزئیات کامل نسخه ۲.۲.۰

---

## ⬆️ از نسخه 1.x: ابتدا به 2.0

اگر هنوز نسخه ۱.x دارید، ابتدا طبق راهنمای داخل بسته `v2.0.0-update.zip` به نسخه ۲.۰.۰ ارتقا دهید (۲۷ فایل + فهرست راستی‌آزمایی همان بسته)، سپس مرحله بعدی و در پایان بخش اصلی همین فایل را اجرا کنید.

---

## ⬆️ ارتقای 2.0 → 2.1

ابتدا بسته `v2.1.0-update.zip` را نصب کنید (جستجوی آنلاین وب + ربات تلگرام + رفع باگ بحرانی نصب‌کننده دیتابیس)، سپس بخش اصلی همین فایل را اجرا کنید. جزئیات کامل تغییرات ۲.۱.۰ در [CHANGELOG](CHANGELOG.md) آمده است.
