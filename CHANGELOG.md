# 📋 سابقه تغییرات — Sahand Service BrandMaker

تمام تغییرات قابل توجه این پروژه در این فایل ثبت می‌شود.
قالب برگرفته از [Keep a Changelog](https://keepachangelog.com/fa/1.1.0/) و نسخه‌گذاری [SemVer](https://semver.org/lang/fa/) است.

---

---

## [2.11.0] — 2026-09-25 «افزونه استقرار خودکار — ساخت زیردامنه و استقرار بدون ورود به cPanel»

> 🎯 **زمینه**: درخواست کاربر «فایل (Create-Subdomain.md) را بخوان و داخل BrandMaker.md اضافه کن و درخواست داخلش را انجام بده» — پیاده‌سازی کامل پرامپت افزونه استقرار خودکار (بخش ۲۱ BrandMaker.md: پرامپت اصلی ۱۵ بخشی + چک‌لیست ۳۰ موردی + پرامپت تکمیلی ۲۰ موردی).

### 📚 ادغام سند (کامیت docs-plugin-prompt)
- **BrandMaker.md ۱۶۲۳ ← ۳۲۰۴ خط**: بخش ۲۱ «افزونه استقرار خودکار» (کل سند Create-Subdomain.md: ۱۵ بخش شامل فرآیند ۱۰ مرحله‌ای، ماژول‌ها، دیتابیس، ساختار فایل، یکپارچه‌سازی) + بخش 21-ب «پرامپت تکمیلی» (بکاپ شمسی + الگوی Document Root + ویرایش نام زیردامنه) + ردیف فهرست

### 🧩 هسته — ۱۴ کلاس جدید core/ (کامیت feat-deploy-core)
- **ShamsiDate**: تبدیل داخلی میلادی ↔ شمسی (الگوریتم ۳۳ ساله — بدون API خارجی) با ۳ فرمت: نام فایل `1404-03-25_14-30-45` / ذخیره `1404-03-25 14:30:45` / نمایش `۱۴۰۴/۰۳/۲۵ ۱۴:۳۰` + سال/ماه جاری برای الگو
- **DeployCrypto**: AES-256-CBC + HMAC-SHA256 (Encrypt-then-MAC) — کلید ۳۲ بایتی تصادفی در `cache/.deploy_secret` (chmod 600) — تشخیص دستکاری + ماسک پنل
- **SubdomainValidator**: ۸ قانون DNS (کاراکترها/طول ۲-۶۳/خط تیره ابتدا-انتها/رزرو ۳۰ کلمه/تکرار...) + ۳ پیشنهاد جایگزین (`{name}-service`، `-repair`، `-{سال شمسی}`)
- **PathResolver**: ۶ الگوی آماده + ۸ متغیر پویا + اعتبارسنجی امنیتی ۶ قاعده (مسیر مطلق/حساس/طول/کاراکتر/متغیر/یکتایی) + پیش‌نمایش + save/reset
- **CpanelAPI**: کلاینت UAPI کامل — زیردامنه (add/del/list/exists)، فایل (mkdir/upload_files/fileop extract-unlink-compress-move/list_files/save_file_content/get_file_content/chmod)، SSL (list/enable_autossl/start_scan)، AddonDomain، Quota + نرمال‌سازی مرکزی مسیرها + قرارداد PHP cURL (CURLFile)
- **FtpManager**: FTPS/Passive + mkdir بازگشتی + upload + chmod + حذف بازگشتی
- **DeploymentLogger**: دو سطح (عملیات + مراحل) + getStatus برای AJAX + برچسب/بج فارسی
- **SubdomainManager**: previewSubdomain + validateSubdomainName (DB+cPanel) + checkAvailability + confirmAndCreate + delete
- **SSLManager**: checkSSL (cPanel+HTTPS مستقیم) + installSSL (AutoSSL/LE) + waitForCertificate (polling) + checkRenewal (هشدار ۳۰ روز)
- **BackupManager**: createBackup (compress + نام شمسی) + enforceRetentionPolicy (دقیقاً ۵) + restore (بکاپ ایمنی + حذف + استخراج + flatten) + delete + weeklyScheduled
- **HealthChecker**: checkBrand (HTTP HEAD + زمان پاسخ + SSL + API) + checkAll + getDashboard + getTrend
- **ConfigGenerator**: تولید config.php (۱۱ ثابت: BRAND_ID/API_KEY/DOMAIN/SLUG/API/ASSETS/TRACKER/CACHE...) + extractPreservable (حفظ تنظیمات در بروزرسانی)
- **HtaccessGenerator**: RewriteEngine + HTTPS/www redirect + قوانین صفحات فعال برند + Gzip + کش (تصاویر ۳۰روز/CSS-JS ۷روز/فونت ۱سال/HTML ۱ساعت) + هدرهای امنیتی + 404
- **Deployer**: ارکستراتور step-based — deploy ۱۱ مرحله / update ۱۰ مرحله / delete ۵ مرحله + صف (تک‌عملیات همزمان هر برند) + rollback خودکار + buildSiteZip (همان منطق export) + runNextStep (مقاوم timeout)

### 🗄️ مهاجرت دیتابیس (اتوماتیک .schema_v211)
- ۱۱ ستون جدید `brands`: is_deployed/deployed_at/deploy_method/server_path/subdomain_name/full_domain/custom_subdomain/ssl_status/ssl_expiry/last_health_check/health_status
- ستون `can_deploy` در api_keys (کلیدهای خارجی با مجوز استقرار)
- ۶ جدول جدید: cpanel_settings (تک‌ردیفی + ۲۵ ستون) / deployments / deployment_logs / site_health / backups (با ستون‌های شمسی) / ssl_certificates
- database.sql برای نصب تازه هم‌گام شد

### 🖥️ پنل — ۷ صفحه جدید + یکپارچه‌سازی (کامیت feat-deploy-admin)
- **cpanel-settings.php**: ۴ بخش (اتصال + الگوی Document Root + FTP + تنظیمات استقرار) + تست اتصال زنده cPanel/FTP + اعتبارسنجی/پیش‌نمایش الگو + Cron Jobs آماده کپی
- **deploy.php**: لیست برندها با ستون استقرار/SSL + دیالوگ پیش‌نمایش (ویرایش نام + اعتبارسنجی زنده + پیشنهاد) + نوار پیشرفت مرحله‌ای + بروزرسانی دسته‌ای + حذف با تأیید نام برند
- **deploy-status.php**: ۷ اکشن AJAX (start/step/status/cancel/validate_subdomain/preview/batch_update)
- **subdomain-editor.php**: فرم ویرایش مستقل + پیش‌نمایش دامنه/مسیر
- **health-dashboard.php**: ۵ کارت آماری + جدول وضعیت + بررسی فوری + روند ۲۴ بررسی
- **backups.php**: تاریخ شمسی زیبا + «X از ۵» + حجم + دانلود/بازیابی/حذف + بکاپ فوری
- **deployment-logs.php**: فیلتر نوع/وضعیت + دیالوگ جزئیات مراحل
- یکپارچه‌سازی: گروه منوی جدید (۵ لینک) + ستون استقرار در brands.php + تب 🚀 استقرار در brand-edit.php + کاوت در export.php

### 🔌 API خارجی + Cron (کامیت feat-deploy-api-cron)
- ۱۳ اندپوینت: deploy (start/update/status/logs/step/list) + health (brand/check/all) + backup (list/create/restore/delete) — همه با گارد can_deploy (به‌جز health خواندنی)
- ۳ فایل cron: health-check (هر ۱۵ دقیقه + اعلان آفلاین) / ssl-check (روزانه + نصب مجدد + هشدار انقضا) / backup (هفتگی + retention) + .htaccess مسدودکننده وب

### 🧪 تضمین کیفیت (کامیت fix-deploy-integration)
- **شبیه‌ساز cPanel UAPI** (Node — deploy-mock-server.js): ۱۶ اندپوینت واقعی + فایل‌سیستم mock
- **تست جامع ۱۲ بخش / ۱۳۹ مورد / همه موفق**: مهاجرت + ShamsiDate (۴ تاریخ مرجع) + ۸ قانون زیردامنه + الگوها + AES + تولید config/htaccess (لینت PHP) + **جریان کامل استقرار ۱۱ مرحله‌ای + بروزرسانی + حذف با mock** + retention ۵ + HealthChecker + Logger
- ۶ باگ واقعی پیدا و رفع شد: multipart PHP cURL / نرمال‌سازی مسیر / openssl_x509_parse PHP 8.3 / پوشه ریشه / ستون created_at / cast بولی MySQL

### 🔐 امنیت
- 🔒 حذف cache/.deploy_secret از گیت (کلید هر نصب محلی است) + gitignore

## [2.10.0] — 2026-09-25 «دارایی‌های کامل روی سرور + نمای چاپی»

> 🎯 **زمینه**: درخواست کاربر «درخواست داخل فایل (BrandMaker.md) را انجام بده» — ممیزی کامل پرامپت در برابر پیاده‌سازی و بستن شکاف‌های واقعی. هر ۱۰ وظیفه نسخه قبل تکمیل بود؛ این نسخه شکاف‌های باقی‌مانده الزامات پرامپت را می‌بندد.

### 🔤 فونت‌های متن‌باز روی سرور (کامیت f43595b)
- **پیش‌نصب ۱۹ فونت / ۸۶ فایل / ۳.۰MB**: ۹ فونت فارسی (وزیرمتن، وزیر، صمیم، شبنم، گندم، تنها، پرستو، میخک، لاله‌زار) + ۱۰ فونت انگلیسی (Roboto، Open Sans، Montserrat، Poppins، Inter، Nunito، Raleway، Lato، Playfair Display، Source Sans Pro) — مطابق بند ۱۰ پرامپت «تمام فونت‌ها روی سرور سایت ساز ذخیره شوند» با همان چیدمان `AssetDownloader` (`fonts/{type}/{slug}/{slug}-{weight}.woff2`)
- **🐛 رفع 404 دانلود وزیر**: URL وزن regular در `sources.json` به `Vazir-Regular.woff2` اصلاح شد (قبلاً `Vazir.woff2` که در مخزن v30.1.0 وجود ندارد)
- **⬇️⬇️ دانلود گروهی**: `AssetDownloader::downloadAllFreeFonts()` + دکمه «دانلود گروهی همه فونت‌های آزاد» در پنل فونت‌ها — شمارنده «X از Y فونت آزاد کامل نصب» + دیالوگ تأیید + `ignore_user_abort` برای دانلود طولانی؛ فونت‌های تجاری (ایران‌سنس، دانا، یکان‌بخ…) همچنان فقط آپلود دستی

### 🖼️ آیکون‌های غنی از ابتدا (کامیت 1b5be5f)
- **گسترش ۱۱ پک از ۸ به ~۱۳۰ آیکون (+۱۳۹۶ فایل / جمع ۱۵۷۱)**: دانلود آرشیو رسمی هر پک (Font Awesome 6.5.2، Tabler 3.0.3، Remix 4.2.0، Lucide، Feather، Heroicons، Boxicons، Material، Phosphor) + **دو پک جدید: آیکونویر و بوت‌استرپ**
- **انتخاب گزینشی اولویت‌دار**: نام‌های پرکاربرد برای سایت تعمیرات لوازم خانگی (ابزار، لوازم خانگی، تماس، ناوبری، وضعیت‌ها) — ترجیح `-line` بر `-fill` (رمیکس) + حذف نزدیک‌های تکراری پایه یکسان
- **🔒 پاک‌سازی امن SVG**: همان قواعد `sanitizeSvg` (رد فایل‌های حاوی script/on*/javascript:/foreignObject) — اسکن تمام ۱۴۶۸ فایل جدید: صفر مورد خطرناک
- **🏷️ دیکشنری فارسی ~۲۰۰ → ~۳۲۰ مدخل**: +۱۲۰ مدخل (مکمل‌های FA/هیرو/متریال/رمیکس) + نرمال‌سازی نام (زیرخط متریال → خط تیره، حذف پسوند `-line/-fill`) + قانون عمومی `brand-x → «لوگوی x»` — پوشش فارسی برچسب‌ها **۹۴٪** (۱۴۸۸/۱۵۷۱)
- دکمه «نصب کامل پک» برای نصب هزاران آیکون بیشتر (تا سقف هر پک) همچنان فعال است

### 🖼️ رفع 404 تصاویر placeholder (کامیت b52b992)
- **سه ارجاع بدون فایل**: `assets/images/placeholders/default.svg` (fallback تصویر در پنل — helpers.php:448)، `article.svg` (تصویر شاخص مقالات بدون عکس در سایت برند — functions.php:28) و `favicon.png` (فاویکون سایت برند بدون لوگو — header.php:48) همه 404 می‌دادند! اکنون: `default.svg` + `article.svg` (SVG فارسی RTL سبک برند) + `favicon.png` + `favicon-32.png` (PIL) + `defaults/logo.svg` (لوگوی پیش‌فرض برند) + `defaults/og-default.png` (OG پیش‌فرض ۱۲۰۰×۶۳۰)
- پوشه‌های `defaults/` و `backgrounds/` طبق ساختار بند ۱۸ پرامپت ایجاد شدند

### 🖨️ فرمت چاپی درخواست + گزارش PDF آمار (کامیت b52b992)
- **`admin/request-print.php`** — برگ درخواست چاپی (بند ۱۲ پرامپت: «چاپ | فرمت چاپی»): صفحه مستقل با احراز هویت، سربرگ لوگوی برند + اطلاعات نمایندگی از `Config`، تمام فیلدها + پیوست‌ها + بخش «گزارش فنی تکنسین» خالی برای تکمیل در بازدید + محل امضای مشتری/تکنسین/مهر نمایندگی + `@media print` تمیز (A4) + شناسه چاپ — دکمه 🖨️ در جزئیات و لیست درخواست‌ها
- **`admin/analytics-report.php`** — گزارش چاپی آمار (بند ۱۱: «دانلود CSV / PDF»): همان فیلترهای داشبورد (برند/بازه) + ۴ شاخص KPI + جدول روند روزانه (۳۵ روز اخیر) با نوار سهم + تفکیک دستگاه/مرورگر/سیستم‌عامل + پراکندگی شهرها + صفحات پربازدید + سهم برندها + دکمه «🖨️ گزارش PDF» در داشبورد آمار
- PDF از طریق «چاپ مرورگر → ذخیره PDF» — بدون هیچ کتابخانه خارجی (فلسفه صفر وابستگی پرامپت)

### 🧪 راستی‌آزمایی
- توازن ساختاری ۶ فایل PHP تغییریافته: ۶/۶ OK
- اسکن امنیتی ۱۴۶۸ SVG جدید: صفر الگوی خطرناک
- تست همه URL های دانلود فونت (۱۹ فونت): ۸۶/۸۶ نصب موفق
- بازگردانی برچسب‌های اصلی پک لوازم خانگی پس از تشخیص overwrite در اسکریپت relabel — نسخه نهایی غیرمخرب (فقط برچسب غیرفارسی را جایگزین می‌کند)

## [2.9.0] — 2026-09-24 «فونت فارسی ریشه‌ای + OG پویا + تلگرام URL-اول + تصاویر واقعی + قالب‌ساز المنتوری»

### ✍️ ریشه‌یابی کامل باگ فونت فارسی OG (PersianGlyphs v2) — کامیت b78fc18
- **ریشه «حروف یک کلمه به هم چسبیده نیستند»**: `prevJoins` و `nextJoins` توابع کمکی را **جا‌به‌جا** صدا می‌زدند (سمنتیک معکوس!) — حروف بعد از ا/د/ر/ز/و فرم اشتباه می‌گرفتند («ساید» → یِ نهایی + اِ مجزا = اتصال بصری شکسته)
- **باگ زنجیره فرم**: حرف یک‌جهته بین دو طرف قابل اتصال («سلام»: ل-ا-م) به‌جای فرم نهایی، مجزا می‌شد — شرط `!nextJoinable` از شاخه نهایی حذف شد
- **نگاشت غلط «ؤ»** (FEE5/FEE6 → FE85/FE86 درست) + افزودن «ئ» (مسئله!) و «ى» به جدول
- **تست**: ۱۸/۱۸ واحد پاس — انتظارات از پیاده‌سازی مرجع استاندارد (joining classes D/R) + تأیید بصری VLM (متن دقیق خوانده شد)

### 🎨 OG v3 — پس‌زمینه پویا + لوگوی بزرگ + عنوان بدون برش
- **پس‌زمینه پویا**: ۱۰ سبک متفاوت (مش(mesh)، موج، پرتو، حباب، گرید پرسپکتیو، راه‌راه مورب، حلقه‌ها، نقطه‌موج، شفق، چندضلعی) + زاویه گرادیان تصادفی — **هر بار تولید، پس‌زمینه متفاوت** (بذر پویا؛ رنگ‌ها از پالت برند)
- **لوگوی برند بزرگ**: نشان گردگوشه ۲۰۰px (قبلاً ۱۲۲) + لوگو تا ۱۵۶px ارتفاع + رستر ۵۲۰px + سایه نرم‌تر + مونوگرام جایگزین بزرگ‌تر
- **عنوان هرگز ناقص نمی‌ماند**: شکست متوازن واژه‌محور تا ۳ خط + کاهش خودکار فونت ۵۶→۲۶ با اندازه‌گیری واقعی `imagettfbbox` تا جا شدن کامل عرض — فقط عناوین فوق‌طولانی با «…» در مرز واژه بسته می‌شوند
- **واترمارک دوتایی OG**: لوگوی برند پایین-راست + لوگوی نمایندگی پایین-چپ (شفاف)

### 🤖 ربات تلگرام — تحویل با «URL-اول» (کامیت 26ad4e7)
- **رفع قطعی «Bad Request: there is no document in the request»**: سند در مسیر عمومی موقت (uploads/temp با نام تصادفی) ذخیره و برای تلگرام «آدرس URL» ارسال می‌شود — تلگرام خودش دانلود می‌کند؛ فقط JSON از واسط عبور می‌کند و با «هر نسخه» اسکریپت واسط گوگل کار می‌کند؛ fallback: آپلود multipart → مستقیم
- **آلبوم تصاویر**: `sendMediaGroup` با URL عمومی (فقط JSON) + fallback multipart قبلی
- **پاک‌سازی خودکار** فایل‌های موقت >۶ ساعت
- **اسکریپت گوگل v3** (فایل مستقل + پنل): files_b64 + getRelayCapabilities + وب‌هوک ورودی + گزارش نسخه — فایل ریپو قبلاً فرمت v1 قدیمی بود که با فرمت PHP ناسازگار بود!

### 🖼️ تصاویر مقاله v3 — واقعی، مرتبط، بدون متن، واترمارک دوتایی
- **🐛 ریشه اصلی «تصویر لباسشویی برای مقاله یخچال»**: وقتی عنوان دلخواه بدون device_key می‌آمد، ArticleGenerator دستگاه را «تصادفی» انتخاب می‌کرد و کل مقاله (دانش/تصاویر/جدول خطا) حول دستگاه اشتباه ساخته می‌شد → حالا دستگاه از خود عنوان استنتاج می‌شود
- **CommandAssistant**: واژه‌های محاوره‌ای («یخچال» به‌جای «یخچال و فریزر») هم شناخته می‌شوند
- **بسته عکس ۹ → ۱۸**: ۹ عکس واقعی جدید (مایکروویو، فر و اجاق، هود، آبگرمکن، جاروبرقی، پکیج دیواری، لوازم کوچک، ویترین یخچال، دریچه لباسشویی) — AI-generated، بدون متن/لوگو، تأیید VLM
- **DEVICE_MAP بازنویسی**: تصویر اول هر دستگاه دقیقاً همان دستگاه (پکیج→پکیج، جارو→جارو، هود→هود...) + فیلتر فایل موجود
- **SVG های متن‌دار حذف از جریان مقاله** — تصاویر درون‌متن عکس واقعی با «هیچ متنی داخل تصویر» + واترمارک لوگوی برند (پایین-راست) و نمایندگی (پایین-چپ) شفاف

### 🚨 خطایاب AI v3.9 — کامل‌فیلد و دقیق‌تر (کامیت 9582ecc)
- **قطعه استاندارد مقدم بر زمینه**: عنوان عمومی = سیگنال وب ضعیف → قطعه curated جایگزین قطعه «پنجره زمینه» (رفع «LE → پمپ تخلیه» به‌جای موتور!)
- **تطبیق عمومی سیستم قطعه↔عنوان**: ۳۰ کلیدواژه سیستم فنی — «عدم توازن» + «سنسور دما NTC» = ناسازگاری → اصلاح خودکار
- **بودجه تطبیقی ۲۶→۳۴ ثانیه** (مطابق max_execution_time) + ۷ کوئری جستجو + ۴ صفحه جدول
- **غنی‌سازی کدهای جدول‌تأیید نحیف** (بودجه ویژه ۴ کد) + تکمیل specs/محل همه کدها از متن ذخیره‌شده
- **مدل‌های سازگار از متن** (modelsFromContext): الگوی WF-8072/JV1250/GN-H702...
- **الگوهای استخراج راه‌حل انگلیسی ×۲** + کد جدول‌تأیید هرگز رد نمی‌شود
- **تست زنده واقعی LG لباسشویی: ۶/۶ کد کامل** — UE=توازن/تعلیق، LE=موتور/درایور، DE=قفل درب، OE=تخلیه، IE=شیر ورودی، TE=سنسور دما؛ همه با دلایل ۵/راه‌حل ۷/شرح ۴۲۰+ نویسه

### 🧹 پنل برند — سه قابلیت جدید (کامیت feat-brand-edit-v2.9)
- **حذف دستگاه‌های تکراری**: دکمه با شمارش + نشان 🔁 روی ردیف‌ها؛ نگه‌داشت بهترین ردیف (توضیحدار>برجسته>فعال>قدیمی) + انتقال ویژگی‌ها + حذف بقیه
- **ادغام کدهای خطای تکراری**: فیلدهای خالی از رکوردهای دیگر پر + ادغام یکتای دلایل/راه‌حل/مدل‌ها + حذف اضافه‌ها؛ غیرتکراری‌ها دست‌نخورده
- **پیش‌نمایش هر صفحه**: مودال iframe با رندر کامل (لوگو + پالت واقعی برند + استایل RTL + سوییچ دسکتاپ/موبایل)
- **تب پالت رنگ بازطراحی**: پالت غالب لوگو بزرگ (کلیک=کپی) + گروه‌بندی فارسی متغیرها + پیش‌نمایش زنده مینی‌سایت (روشن/تاریک)
- **انتخابگر رنگ حرفه‌ای بومی**: ناحیه HSV + نوار رنگ + HEX/RGB + پیش‌فرض‌های پالت لوگو + قطره‌چشم مرورگر

### 🎭 قالب‌ساز v3 — المنتور-گونه (کامیت 0ec5a75)
- **🏛 ستون‌بندی**: بلوک «بخش چندستونی» (۲-۴ ستون زنده) + «دو بخشی نامتقارن» — چیدمان تودرتو با ناحیه رهاسازی هر ستون + جابه‌جایی بین ستون‌ها (مسیریابی امن resolveNode/resolveArray/movePathTo)
- **۵۵+ عنصر در ۱۲ دسته** (قبلاً ۳۴): هیرو شمارش معکوس، متن غنی، نقل‌قول، فهرست ویژگی، کارت دستگاه‌ها، جدول تعرفه، خبرنامه، مهارت‌ها، تب‌بندی، خط زمانی، مراحل کار، کاروسل، ویدیو، هشدار، گروه دکمه، فهرست آیکون، فوترها، نوار چسبان موبایل و...
- **⚡ طراحی زنده (WYSIWYG)**: بوم بلوک‌ها را واقعی رندر می‌کند (نه وایرفریم)؛ ویرایش متن/عنوان/تلفن با به‌روزرسانی آنی؛ نوار ابزار شناور + برچسب عنوان
- **پیش‌نمایش تودرتو**: renderLayoutLevel در template-preview + ستون خالی با placeholder
- **تست**: ۱۲/۱۲ منطق مسیر تودرتو (node) + رندر کامل بدون خطا

### 🔧 سایر
- `docs/AI-API-GUIDE.md` → ۳.۹.۰ (ردیف‌های ۳.۹.۰ و ۳.۸.۰ در تاریخچه)
- نسخه سیستم 2.9.0 + موتور 3.9.0

---

## [2.8.0] — 2026-09-24 «خطایاب صددرصد وب‌محور + تحویل سند تلگرام + bidi OG»

### 🚨 خطایاب AI v4 (ErrorCodeEngine v2.8) — فقط کدهای واقعی و راستی‌آزمایی‌شدهٔ وب
- **سیاست جدید (درخواست صریح کاربر)**: «همه کدها از جستجوی اینترنت اضافه شوند و همه دقیق و واقعی باشند» → فقط کدهای وب با شاهد اختصاصی ثبت می‌شوند؛ پایگاه دانش دیگر «کد جدید» اضافه نمی‌کند و فقط فیلدهای خالی را پر می‌کند (بدون رونویسی داده وب)؛ وب چیزی نیافت → گزارش صادقانه به‌جای کد ساختگی
- **📊 پارس ساختاریافته جدول کدها** (`parseCodeTable`): صفحات «فهرست کد سازنده» دقیق‌ترین منبع‌اند — `E4 - Water Drainage` / `OE: خطای تخلیه آب` / `UE | Unbalance` / `کد OE: خطای تخلیه` / «نمایش IE می‌دهد: خطای آبرسانی»؛ کد جدید از جدول = شاهد قوی (بدون تعمیق تک‌کد → صرفه‌جویی بودجه زمانی)
- **🐛 رفع ۲ باگ رجکس جدول**: `\s` در کلاس کاراکتر «معنا» شامل newline بود → تطبیق حریصانه `IE: Water Inlet Error` کل `UE`ی خط بعد را می‌بلعید (UE هرگز ثبت نمی‌شد!)؛ الگوی فارسی کدهای دوحرفی بدون رقم (OE/UE/IE/DE) را نمی‌دید
- **🛡 گارد نام برند**: با پشتیبانی کدهای دوحرفی، «LG خطای ...» به کد تبدیل می‌شد → LG/GE/SMEG + مخفف‌های رایج (AC/TV/PC) هرگز کد قلمداد نمی‌شوند
- **🧭 شناسایی عنوان عمومی** (`titleIsGeneric`): «خطای DE در ماشین لباسشویی» بلند است اما هیچ معنایی ندارد → معنای دقیق از جدول وب یا نقشه کدها جایگزین می‌شود (قبلاً شرط تساوی سخت‌گیرانه `=== 'خطای DE'` اجازه جایگزینی نمی‌داد)
- **🎯 قطعه از عنوان معنادار** (`partFromTitle`): ریشه «خطای قفل درب با قطعه پمپ تخلیه!» پنجره ±۴۰۰ نویسه‌ای زمینه بود که شرح کدهای دیگر صفحه را هم در خود داشت → زنجیره جدید: عنوان دقیق → وب/دانش → دلایل → دسته
- **واژه‌نامه ترجمه**: `Water Outlet` (عنوان رسمی LG برای OE!) + سنسور سطح/فشار آب (PE) + ترتیب حیاتی الگوها (`Motor Locked` → موتور نه قفل درب؛ `Child Lock` قبل از `Door`)
- **✨AI وب‌اول**: دکمه‌های بهبود فیلد (دلایل/راه‌حل/قطعه/مشخصات/محل) شواهد اختصاصی همان کد را از وب می‌گیرند + فهرست منابع در پاسخ
- **🧪 راستی‌آزمایی**: ۳۶/۳۶ تست — زنده LG لباسشویی: DE=قفل درب/قفل درب، IE=ورود آب/شیر برقی، LE=موتور/موتور، OE=تخلیه/پمپ تخلیه، UE=عدم توازن/تعلیق — عنوان و قطعه ۱۰۰٪ سازگار

### 🤖 ربات تلگرام — تحویل سند + ریشه‌یابی کامل (کامیت 8b04676)
- **رفع «there is no document»**: اسکریپت واسط گوگل قدیمی فایل base64 را پاس نمی‌داد → تشخیص خودکار این خطا + تلاش مجدد آپلود مستقیم + پیام راهنمای دقیق فارسی
- **تحویل مرحله‌مقاوم**: شکست HTML/MD/آلبوم بقیه را قطع نمی‌کند + نجات نهایی (متن کامل مقاله به‌صورت پیام بخش‌بندی‌شده وقتی هیچ فایلی نرسید)
- **آلبوم**: رستر SVG→PNG با Imagick و در نبودش ارسال به‌صورت سند

### 🖼️ OG و متن فارسی — PersianGlyphs v2 (کامیت 8b04676)
- **بازنویسی کامل `visualOrder`**: معکوس‌سازی «ترتیب توالی‌ها» (ریشه بهم‌ریختگی متن ترکیبی عدد/انگلیسی) + آینه پرانتزها + رفع باگ `prevJoins` (فاصله اتصال نمی‌شکست → حروف دو کلمه به‌هم می‌چسبیدند!)
- **اعتبارسنجی دقیق فونت**: همه فرم‌های فارسی پ چ ژ ک گ ی الزامی (فونت عربی بدون حروف فارسی رد می‌شود → مستطیل‌ها محو)
- **`compositeAlpha` دستی**: رفتار آلفای `imagecopy` بین بیلدهای GD متفاوت بود → ترکیب پیکسل‌به‌پیکسل
- **لوگوی برند روی نشان سفید گردگوشه** با سایه نرم (GD + SVG)

## [2.7.2] — 2026-09-24 «رفع تحویل مقاله ربات + OG تضمینی + خطایاب سریع + جدول کارتی موبایل»

### 🤖 رفع کرش تحویل مقاله ربات تلگرام
- **ریشه**: `CURLFile` متد `getMime()` ندارد — متد صحیح `getMimeType()` است → خطای «Call to undefined method CURLFile::getMime()» در آپلود از واسط گوگل (`apiUploadViaRelay`)
- گارد `function_exists` برای `mime_content_type` + رفع املا

### 🖼️ تصویر OG تضمینی + شفافیت واقعی لوگوها (AiImageGenerator v2.7.2)
- **ریشه‌یابی «فونت خراب»**: فونت پیش‌فرض سایت در اکثر نصب‌ها فقط woff2 است → هیچ TTFی یافت نمی‌شد → رندر به Imagick/SVG می‌افتاد که متن فارسی را بدون شکل‌دهی درست رندر نمی‌کند
- **وزیرمتن Bold+Regular بسته‌بندی‌شده در پروژه** (`assets/fonts/fa/vazirmatn/`) — مسیر GD همیشه فونت سالم دارد (سقف زنجیره: فونت پیش‌فرض → TTF محلی → WOFF تبدیلی → CDN → وزیرمتن بسته)
- **اعتبارسنجی پوشش فرم‌های نمایشی** (`fontCoversPersianForms`): پارس خالص PHP جدول cmap — فونت بدون گلیف‌های اتصال حروف (U+FB50–FEFF) رد می‌شود (پیشگیری از مربع‌های خالی)
- **رفع باگ «زمینه مشکی واترمارک»**: `imagecopymerge` آلفای تک‌پیکسل را نادیده می‌گرفت و پیکسل‌های شفاف (RGB صفر) را سیاه میکرد → کلاس جدید `ImageWatermark` با اعمال آلفای پیکسل‌به‌پیکسل + `imagecopy`
- لوگوی برند با شفافیت کامل رسم می‌شود + **مونوگرام دایره‌ای** (حرف اول برند) وقتی لوگو وجود ندارد
- هر دو لوگو با زمینه شفاف (طبق درخواست کاربر)

### 🖼️ تصاویر مقاله مطابق موضوع + واترمارک دو لوگو (ArticleImageService v2)
- **ریشه «تصویر لباسشویی برای مقاله یخچال»**: نوع `buying_guide` (و ۱۸ نوع دیگر) در نگاشت تصویر نبود → پرکننده پیش‌فرض دوم همیشه `washing_machine` بود! + کلیدهای `tv`/`vacuum`/`range_hood` نگاشت نشده بودند + نام فایل `washing-machine.jpg` با ثابت `washing_machine` نمی‌خواند
- نگاشت کامل هر ۴۲ کلید دستگاه + هر ۲۵ نوع مقاله + نام‌فایل‌های واقعی
- **استنتاج دستگاه از عنوان** (`inferDeviceKey`): ۶۰+ واژه محاوره‌ای («یخچال»، «لباسشویی»، «پکیج»...) + نام‌های رسمی دانش — طولانی‌ترین تطبیق برنده
- **واترمارک دو لوگو روی همه تصاویر**: لوگوی برند گوشه پایین-راست + لوگوی نمایندگی گوشه پایین-چپ — هر دو شفاف، نسخه کش‌شده در `uploads/articles/wm/` + همان چیدمان در تصاویر یکتای SVG

### 🚨 خطایاب AI v3.1 — سریع، دقیق، بدون فیلد خراب
- **ریشه «هیچ کدی اضافه نمی‌شود»**: جستجوی عمیق تا ۸۰+ ثانیه طول می‌کشید؛ روی هاست اشتراکی (max_execution_time=30) درخواست قبل از مرحله درج می‌مُرد → صفر رکورد! **بودجه زمانی ۲۴ ثانیه** + `set_time_limit` → تست واقعی: ۸۰s → **۲۵s**
- **نقشه کدهای شناخته‌شده** (`knownCodeMeaning`): معنای استاندارد ۴۰+ کد پرتکرار ۴ نوع دستگاه (IE=آبرسانی، OE=تخلیه، UE=عدم توازن...) + نرمال‌سازی مترادف‌ها (1E→IE، 4E→PE، UB→UE)
- **زمینه کد-محور** (`codeCentricContext`): پنجره ±۳۵۰ کاراکتر دور خود کد — ریشه قطعه‌های اشتباه (پمپ تخلیه برای IE!): متن کامل صفحه شرح همه کدها را دارد
- **رفع نشت انگلیسی در عنوان‌ها** («خطای What Does It Mean...»): پالایش عبارت‌های بی‌محتوا + ترجمه اجباری + گارد `hasEnglishGarbage` (۳+ واژه انگلیسی پشت‌سرهم؛ واژه دوزبانه مجاز)
- **KB مقدم**: برای عنوان/قطعه/دسته/شدت وقتی کد را می‌شناسد (داده curated سازنده دقیق‌تر از استخراج زمینه‌ای)

### 📋 جدول‌ها به کارت در موبایل + کادرهای راهنمای بازطراحی‌شده
- **ریشه‌یابی با مرورگر واقعی**: در دسکتاپ جدول‌ها سالم بودند؛ در موبایل (۳۶۰px) جدول ۸ستونه (مجموع min-width = ۵۷۶px) جا نمی‌شد و متن‌ها ۵-۲۰ سطر می‌شکستند
- **الگوی استاندارد «جدول→کارت»** (≤768px): هر ردیف = کارت گردگوشه، هر سلول = خط «برچسب: مقدار»
- `sahandLabelTables()` در admin.js برچسب‌ها را از thead به همه tdها تزریق می‌کند (global برای AJAX + fallback بدون JS)
- **بازطراحی کامل `.alert`** (درخواست کاربر): حذف display:flex → بلوکی + نوار رنگی سمت راست + جریان طبیعی متن + `code` با break-all

### 🗑️ حذف دسته‌جمعی کدهای خطا
- دکمه «🗑️ حذف نتایج فیلتر (تعداد)» در فهرست — دقیقاً همان رکوردهای فیلتر فعلی (برند + دستگاه + جستجو) حذف می‌شوند + دیالوگ تأیید با شمارش

### 🔐 لاگین: کپچای ستونی + بزرگ‌تر در موبایل
- کادر ورود کد امنیتی حالا **زیر تصویر** است (درخواست کاربر): تصویر تمام-عرض بالا + ورودی بزرگ زیر آن (فونت ۲۸px، موبایل ۲۶px/ارتفاع ۷۴px)
- 🐛 رفع باگ تایپی `SAHEND_VERSION` → `SAHAND_VERSION` (ثابت تعریف‌نشده = Error در PHP8 که پنل برند لاگین را می‌شکست)

---

## [2.7.1] — 2026-09-24 «رفع کرش ربات + OG برنددار + خطایاب دوفازی + مقاله موضوع‌محور + جدول‌های سالم»

### 🤖 رفع کرش کامل ربات تلگرام (باگ TypeError)
- **ریشه**: تلگرام برای متدهایی مثل `sendChatAction`، `answerCallbackQuery` و `setWebhook` به‌جای آبجکت، `result: true` برمی‌گرداند؛ متدهای `apiViaRelay`/`apiDirect`/`apiUpload`/`apiUploadViaRelay` نوع بازگشتی `array` داشتند → `TypeError` روی «همه» پیام‌ها (خطای «Return value must be of type array, true returned»)
- متد `normalizeResult` به هر ۴ مسیر API اضافه شد — true به آرایه ایمن تبدیل می‌شود

### 🖼️ تصویر OG برنددار (AiImageGenerator v2.7.1) — طبق درخواست کاربر
- **رفع باگ اصلی فونت**: `default_font_settings` نام فونت («ایران‌سنس») را ذخیره می‌کند نه اسلاگ پوشه را — glob همیشه شکست می‌خورد و فونت پیش‌فرض هیچ‌وقت استفاده نمی‌شد → نگاشت کامل نام→اسلاگ (مانیفست + دیتابیس + تطبیق نرم)
- اولویت وزن‌های سنگین TTF (bold→black→…→regular) برای رندر عنوان
- **مبدل WOFF→TTF در PHP خالص** (zlib + بازسازی ساختار SFNT، کش‌شده در `uploads/cache/fonts`)
- تلاش دانلود TTF هم‌مسیر از CDN رستیکردار با کش منفی
- جاسازی `@font-face` فونت پیش‌فرض در مسیر SVG (base64 ttf/woff2) — رندر مرورگر/Imagick با همان فونت سایت
- رستر کردن لوگوهای SVG برند/نمایندگی با Imagick در مسیر GD (قبلاً فقط رستر)
- اولویت‌بندی جدید مسیرها: **GD+فونت درست → Imagick → SVG** (قبلاً Imagick اول بود و فونت هاردکد Vazirmatn)

### 🚨 خطایاب دوفازی (ErrorCodeEngine v3.0) — دقت فیلدها چند برابر شد
- **فاز ۲ تعمیق تک‌کد**: برای ۱۰ کد برتر، جستجوی اختصاصی EN+FA حول خود کد + **خواندن متن کامل بهترین صفحه** (نه فقط اسنیپت) → دلایل/راه‌حل/مشخصات/محل از متن غنی
- کدهای بدون شاهد اختصاصی که پایگاه دانش هم نمی‌شناسد → **رد می‌شوند** (حذف کدهای توخالی/نویز)
- استخراج معنای دقیق از تیتر صفحه («E4 = Water Drainage») + واژه‌نامه ترجمه EN→FA (۱۸ عبارت)
- الگوی کد فقط-عددی سخت‌گیرانه: فقط بعد از واژه «کد/خطا/error/code» (حذف نویز «۲۱ نکته»)
- **ادغام دانش curated در رکورد وب**: وب مقدم، KB فاصله‌ها را با داده دقیق سازنده پر می‌کند (specs/location/models)
- استخراج مشخصات فنی واقعی (اهم/کیلواهم/ولت/وات/بار/سانتیگراد) و محل قطعه از متن غنی
- دکمه‌های ✨AI فیلدهای فنی حالا KB + جستجوی وب همان کد را می‌گیرند نه فالبک عمومی
- 🐛 رفع الگوی شکسته «درain» + غلط‌های املایی KB (مشلقی→مشی، خراحی→خرابی)

### ✍️ مقالات موضوع‌محور (موتور v3.3) — تمرکز کامل روی عنوان درخواستی
- **ریشه‌یابی**: موضوع کاربر هرگز به ArticleGenerator پاس نمی‌شد (بدنه از قالب تصادفی دستگاه می‌آمد!) و TitleGenerator عنوان را با فرمول کلی جایگزین می‌کرد — هر دو رفع شد
- زنجیره: CommandAssistant (موضوع→عنوان قالب‌دار + research=auto) → SmartPipeline (custom_title + research_context، حفظ عنوان موضوعی، کلیدواژه=موضوع) → ArticleGenerator
- **Outline موضوع‌محور ۷بخشی**: تعریف و اهمیت / علت‌شناسی / راه‌حل گام‌به‌گام / پرسش‌های واقعی وب با پاسخ تحلیلی H3 / هزینه و زمان / پیشگیری / اعتبار نمایندگی
- فکت‌های وب **داخل نثر** بافته می‌شوند (نه لیست خام) — مقدمه و جمع‌بندی اختصاصی موضوع
- FAQ از پرسش‌های واقعی همان موضوع + جلوگیری از تکرار بخش‌های وب در غنی‌سازی

### 📋 رفع خرابی عرض ستون‌ها در همه جدول‌ها و کادرهای راهنما
- **ریشه**: `overflow-wrap: anywhere` روی `td` عرض ذاتی سلول را به یک حرف فرو می‌ریزد → ستون‌های باریک و متن چند سطری (تا ۲۰ سطر!)
- جایگزینی با `break-word` در admin.css و قالب سایت + `table-layout: auto` + حداقل عرض ستون + فیکس کادرهای آبی/سبز (`.alert`) که همان باگ را داشتند

### 🔐 صفحه ورود دوپنلی + کپچای بزرگ
- چیدمان دوپنلی دسکتاپ: کارت فرم + پنل برند گرادیانی (لوگو، شعار، ۴ ویژگی، نشان نسخه) — موبایل تک‌کارته
- **کادر کد امنیتی چند برابر بزرگ‌تر**: فونت ورودی ۱۷→۲۶px + فاصله‌حروف ۷px + ارتفاع ۸۴px + تصویر کپچا ۲۵۰×۸۴ با حروف مقیاس‌بندی‌شده
- فیلدهای ورود بزرگ‌تر + بافت نقطه‌ای پس‌زمینه

### 🔗 سازگاری
- سازه‌های API عمومی بدون تغییر؛ خروجی `smartGenerate` همان ساختار قبلی + کلیدهای جدید اختیاری (`custom_title`, `research_context`)

---

## [2.7.0] — 2026-09-24 «سئو موضوع‌محور + خطایاب وب‌محور + نگارش v1.2 + پیش‌نمایش مقاله»

### 🎯 سئو موضوع‌محور صفحات برند (PageSeoImprover v2.1)
- هر نوع صفحه پروفایل محتوایی خودش را دارد: خانه، درباره، خدمات، تماس، ضمانت، سؤالات متداول، مقالات، کدهای خطا، تعرفه، اختصاصی
- کلیدواژه کانونی از «نوع + عنوان همان صفحه» استخراج می‌شود — نه «تعمیر + برند» برای همه صفحات
- ۲۷ بلوک متن با جمله‌های کوتاه بازنویسی شد (خوانایی ۵۳ → ۶۲+)
- افزودن خودکار: اسکیمای JSON-LD WebPage، پرسش‌های پرتکرار، EEAT عددی، کفایت تصویر، OG جایگزین
- رفع نوسان کاهش تراکم با سرفصل‌ها؛ امتیاز ۴ نوع صفحه به ۱۰۰ رسید (تست ۱۸ گانه)

### 🚨 خطایاب وب‌محور (ErrorCodeEngine v2.7)
- **جستجوی اینترنتی منبع اصلی شد** — پایگاه دانش فقط برای کدهای جا‌مانده
- اعتبارسنجی زمینه: کد فقط با حضور برند/دستگاه در متن پذیرفته می‌شود (حذف کدهای تصادفی)
- استخراج دلایل/راه‌حل از جمله‌های واقعی نتایج وب با فیلتر کیفیت
- حداقل ۵ دلیل و ۵ راه‌حل (طبق درخواست کاربر)
- زنجیره استنتاج قطعه: وب → دلایل → دسته (حذف جای‌نگه‌دار «قطعه مرتبط با کد»)
- 🐛 رفع ۲ باگ خفناک: برش ۳۰۰ کاراکتری JSON در improveField (رکورد خراب ذخیره می‌شد!) + پاکسازی UTF-8 وب

### ✒️ نگارش فارسی v1.2 (PersianGrammar)
- لایه سبک جدید: «می‌باشد»→«است» (قاعده دفتر فارسی)، فعل‌های سنگین→ساده، اداری‌زدگی، محاوره→استاندارد
- لایه حشو جدید: واژه تکراری، فاصله سه‌تایی، «اینکه که»
- ۲۵ واژه تخصصی تعمیرات (لباسشویی، جاروبرقی، سنسور، ترموستات، دیفراست و…)
- 🐛 رفع باگ گم‌شدن بلوک کد (رقم نشانگر pre/code فارسی می‌شد و هرگز بازنمی‌گشت)

### 🛡 بهبوددهنده محتوا HTML-امن (ContentImprover)
- 🐛 رفع ۴ نابودگر ساختار: fixReadability/fixCoherence/fixEngagement/fixStructure همگی strip_tags داشتند و هدینگ/لیست/بولد محتوا را پاک می‌کردند!
- پاس نهایی نگارشی همیشه اجرا می‌شود (مستقل از انتخاب ابعاد ضعیف)

### 🖼 تصاویر OG برنددار (AiImageGenerator)
- فونت پیش‌فرض سایت‌ها (تنظیمات پنل فونت‌ها → heading_fa/body_fa)
- لوگوی برند بالای تصویر (PNG-GD یا data-URI در SVG)
- واترمارک نیمه‌شفاف لوگوی نمایندگی در گوشه پایین-چپ

### 👁 پیش‌نمایش مقاله
- صفحه مستقل شبیه سایت عمومی: هدر چسبان، هیرو گرادیانی با رنگ برند، متا (تاریخ شمسی/زمان مطالعه)، تصویر شاخص + OG + تگ‌ها
- دکمه 👁 در فهرست مقالات و فرم ویرایش

### 🖼 آیکون‌ها
- جستجوی داخل کادر نمایش پک (نام/دسته فارسی و انگلیسی — بدون گردش در صفحات)
- ۲ پک جدید: آیکونویر ۱۶۷۱ آیکون (لباسشویی/یخچال/ظرفشویی/کولر) + بوت‌استرپ ۲۰۵۰ آیکون (آچار/پیچ‌گوشتی/دماسنج)

### 🤖 ربات‌ها
- 🐛 ربات بله: رفع خطای «Failed to connect to api.telegram.org» در تحویل مقاله (آپلود حالا به tapi.bale.ai می‌رود)
- 🐛 رفع پاک‌شدن متقابل تنظیمات ربات تلگرام/واسط گوگل (فرم‌ها حالا scope جدا دارند)
- آپلود فایل از طریق واسط گوگل با base64 (GAS بروزرسانی شد — تحویل مقاله در تلگرام بدون تحریم)
- 🐛 رفع کلید path در آلبوم تصاویر بله + حذف خودکار اینلاین‌کیبورد پشتیبانی‌نشده

### 🔐 ورود و تنظیمات
- 🐛 کپچا: رفع قطعی ۴۰۴/ادبلاک لپ‌تاپ — رندر درون‌خطی data-URI (صفر درخواست HTTP)
- 🎨 بازطراحی حرفه‌ای صفحه لاگین با اصول UI/UX Pro (گلس‌مورفیسم + انیمیشن + نمایش رمز)
- 🐛 رفع بهم‌ریختگی صفحه تنظیمات: ریشه = کوتیشن تودرتو در onclick که HTML را می‌شکست و فرم ایمیل‌ها بیرون layout می‌افتاد (عرض محتوا ۷۷۷→۱۱۱۶px)

---

## [2.6.0] — 2026-09-24 «خطایاب AI + یادگیری AI + تصاویر AI + ربات بله»

> 🔢 موتور AI: 3.5.0 → **3.6.0** · اندپوینت‌های API: ۴۸ → **۵۴** · فایل‌های تغییرکرده: **۴۲**

### 🚨 موتور خطایاب AI — ساختار ۱۴ فیلدی + کدهای واقعی
- پایگاه دانش `engine/knowledge/error-codes-brands.json`: ۱۵۰ کد خطای واقعی برای ۹ برند پرتقاضا (ال‌جی، سامسونگ، بوش، الکترولوکس، آریستون/ایندزیت، بکو، ویرپول، پاناسونیک، اسنوا) با زیرنوع‌ها، مدل‌های واقعی، قطعه، مشخصات فنی و محل قطعه
- `engine/generators/ErrorCodeEngine.php`: تولید همه کدهای یک برند+دستگاه — استخراج (نه ساخت!) از پایگاه دانش + جستجوی آنلاین فارسی/انگلیسی با الگوی کد و منبع‌یابی URL؛ توصیف یکتای ۳-۵ جمله‌ای سئو-پسند با بذر متفاوت برای هر رکورد
- صفحه مدیریت بازنویسی: فیلتر برند → دستگاه (AJAX) → تولید همه کدها + ویرایش کامل ۱۴ فیلدی + دکمه «✨ AI» برای بهینه‌سازی/یکتاسازی هر فیلد جداگانه
- دیتابیس: ۸ ستون جدید + severity پنج‌سطحی (بحرانی/زیاد/متوسط/کم/اطلاعاتی) — مهاجرت خودکار در بوت‌استرپ

### 🧠 موتور خودیادگیر و خودارتقادهنده
- `engine/skills/SelfLearner.php`: هر بهبود موفق (سئو/محتوا/UX/کد خطا) یک «درس» با پارامترهای موفق ثبت می‌کند؛ درس‌های مشابه تقویت می‌شوند (میانگین‌گیری تعدیلی) و در کارهای بعدی ترجیح داده می‌شوند
- لاگ حسابرسی فقط-افزودنی `logs/ai-upgrades.log` + پنل «یادگیری AI» (`admin/ai-learning.php`): آمار، فعال/غیرفعال و حذف تک‌تک درس‌ها — ارتقای اشتباه فقط همان درس را غیرفعال می‌کند
- هوک در SeoImprover (کلی و تک‌سنجه)، PageSeoImprover و ErrorCodeEngine

### 🖼️ تصاویر AI یکتا (OG + مقاله)
- `engine/services/AiImageGenerator.php`: تصویر OG مرتبط با موضوع هر مقاله/صفحه + ۲ تصویر درون‌متن یکتا (بذر از شناسه+عنوان — هیچ دو مقاله‌ای یکی نیست)
- `engine/utils/PersianGlyphs.php`: شکل‌دهی حروف فارسی (اتصال اولیه/میانی/نهایی/مجزا + نیم‌فاصله + bidi ساده‌شده) در خالص PHP برای رندر PNG با GD — سه مسیر: ImageMagick → GD+شکل‌دهنده → SVG
- هر مقاله تولیدی: OG خودکار در ستون جدید og_image + بنر شاخص از بسته واقعی + ۲ تصویر یکتای AI در متن
- ویرایش مقاله: پیش‌نمایش OG + فیلد قابل ویرایش + دکمه «🎨 تولید OG با AI» (هر کلیک = ترکیب جدید) + «🖼️ تولید مجدد تصاویر»

### 💬 ربات بله + 🌉 واسط گوگل تلگرام
- `bale/BaleBot.php` (+poll + وب‌هوک + پنل `admin/bale.php`): دستیار فارسی کامل روی بله (tapi.bale.ai) — تنظیمات مستقل، شناسه‌های مجاز، تست اتصال، وب‌هوک با راز پارامتری
- واسط Google Apps Script تلگرام: relay دوطرفه (خروجی: سایت→گوگل→تلگرام / ورودی: وب‌هوک→گوگل→سایت) — اسکریپت آماده با جای‌گذاری خودکار راز و آدرس + دکمه تست getMe

### 💬 SahandDialog — کادرهای تعاملی زیبا
- `assets/js/sahand-dialog.js`: API کامل alert/confirm/prompt/form/toast با آیکون و رنگ مرتبط با موضوع، ورودی متن/چک‌باکس/رادیو/سلکت، انیمیشن نرم، RTL — همه کادرهای پیش‌فرض پنل جایگزین شدند

### 🎯 بهبودهای سئو
- دکمه «🎯 بهبود» کنار هر سنجه در کادر آمار سئو (فقط همان سنجه اصلاح می‌شود) — `SeoImprover::improveMetric()`
- 🐛 رفع «این مقاله از قبل بهینه است» با وجود «(۱ مورد)»: سنجه تراکم کلیدواژه fixable بود اما اصلاح‌کننده نداشت — `fixKeywordDensity()` (افزایش با بخش جمع‌بندی سئو-پسند / کاهش با مترادف)
- `PageSeoImprover`: دکمه «بهبود خودکار این صفحه تا ۱۰۰» + «بهبود همه بخش‌ها و صفحات برند» (حلقه تحلیل→اصلاح→سنجش، حداکثر ۶ دور)

### 🐛 رفع باگ‌های گزارش‌شده کاربر
- **کپچا روی لب‌تاپ نمایش داده نمی‌شد**: ادبلاکرهای دسکتاپ آدرس حاوی «captcha» را بلاک می‌کنند → اندپوینت خنثی `security-code.php` + fallback AJAX با data-URI (هیچ بلاکر تصویری fetch/JSON را بلاک نمی‌کند)
- **سایدبار**: همیشه سمت راست در همه دستگاه‌ها (`right:0` فیزیکی + translateX مثبت برای مخفی‌سازی)
- **دستگاه‌های تکراری برند**: مرحله ۳ ساخت برند بدون چک وجود دوباره درج می‌کرد → چک + پاکسازی یک‌باره تکراری‌های قدیمی + متد `Database::query()`
- **نمایش آیکون‌ها کمتر از تعداد**: فقط ۱۲۰ آیکون اول مانیفست نمایش داده می‌شد → مرور کامل با دسته‌بندی پوشه‌ها + صفحه‌بندی ۹۶تایی + شمارش صحیح
- **صفحه تنظیمات**: تب‌های مقاوم `.stab-*` خوداتکا (ضدتداخل + hash + بازگشت به بالای صفحه + تمام‌عرض)
- **تولید کد خطا هیچ کدی نمی‌داد**: مسیر جدید برند→دستگاه→موتور خطایاب

### 🗑️ لغو نصب + 👁️ پیش‌نمایش + 📅 فعالیت از سال
- لغو نصب کامل فونت (حذف همه وزن‌ها) و پک آیکون (حذف بازگشتی امن `FileManager::deleteDir`)
- پیش‌نمایش زنده فونت نصب‌شده با مودال + اسلایدر اندازه
- تنظیم «سال شروع فعالیت نمایندگی» برای کارت‌های سایت


## [2.5.0] — ۱ مهر ۱۴۰۵ (2026-09-23) 🎨 کپچای تضمینی + اسکیل UI/UX Pro + بهینه PHP 8.3

> سه درخواست کاربر در این نسخه پیاده‌سازی شد: رفع نمایش کد امنیتی صفحه لاگین + حروف درشت‌تر، افزودن اسکیل UI/UX Pro به سایت‌ساز برای طراحی حرفه‌ای صفحات، و بهینه‌سازی کامل برای ارتقای PHP هاست به نسخه ۸.۳.

### 🐛 رفع شده (Fixed)

#### کد امنیتی صفحه لاگین نمایش داده نمی‌شد
- **علت ریشه‌ای**: رندر کپچا به‌طور کامل به اکستنشن GD وابسته بود — اگر GD در پیکربندی جدید PHP (مثلاً بعد از ارتقای نسخه) خاموش باشد، فراخوانی `imagecreatetruecolor()` خطای مرگبار می‌دهد → هندلر استثنا صفحه HTML خطا به تگ `<img>` برمی‌گرداند → تصویر نمایش داده نمی‌شود و ورود عملاً غیرممکن می‌شود (کد امنیتی در سشن ذخیره نمی‌شود)
- ✅ **سیستم رندر ۳ لایه‌ای تضمینی**: ۱) GD + فونت TTF — فونت‌های TTF نصب‌شده سایت‌ساز (`assets/fonts/`) یا فونت‌های رایج سیستم با اعتبارسنجی `imagettfbbox` ۲) GD فونت داخلی + بزرگ‌نمایی نرم ۲.۶× با درون‌یابی Bicubic ۳) **SVG خالص PHP** — بدون هیچ وابستگی به اکستنشن، همیشه نمایش داده می‌شود
- ✅ هر خطای GD با try/catch گرفته شده و به لایه SVG سقوط نرم می‌کند + ثبت در لاگ برای اطلاع مدیر
- ✅ پاک‌سازی کامل بافرهای خروجی قبل از ارسال هدر Content-Type (خروجی ناخواسته = تصویر خراب)

### ✨ اضافه شده (Added)

#### 🎨 اسکیل UI/UX Pro — طراح حرفه‌ای صفحات (v1.0)
- **پایگاه دانش** `engine/knowledge/uiux-patterns.json`: ۹ قانون کلاسیک UX (هیک، فیتس، یاکوب، فون رستورف، موقعیت ترتیبی، میلر، شیب هدف، اوج و پایان، زیبایی‌شناسی-قابلیت استفاده) + توکن‌های طراحی (مقیاس فاصله ۴/۸، مقیاس تایپ فارسی، رنگ ۶۰-۳۰-۱۰، گردی، سایه، بریک‌پوینت، عرض محتوا) + ۶ قاعده RTL فارسی + دسترس‌پذیری WCAG + ۷ الگوی نرخ تبدیل + **بلوپرینت ۱۷ نوع صفحه + قواعد ۳۴ بلوک + سیستم امتیازدهی ۱۳ معیاره**
- **کلاس** `engine/skills/UIUXPro.php`:
  - `designPage()` — تولید چیدمان بهینه هر نوع صفحه با دلیل طراحی هر بخش + تنظیم زمینه‌محور (نام برند، تعداد دستگاه/مقاله/نظرات)
  - `reviewLayout()` — ممیزی UX چیدمان موجود: امتیاز ۰-۱۰۰ + مشکلات اولویت‌بندی‌شده (بحرانی/مهم/متوسط/جزئی) + نقاط قوت + آمار
  - `improveLayout()` — اصلاح خودکار: حذف بلوک‌های مخفی و تکراری، تضمین هیرو در ابتدا، CTA پایانی، بردکرامب صفحات داخلی، ریتم پس‌زمینه/فاصله، مهار شلوغی
- **یکپارچه‌سازی**:
  - مرحله ۳ ساخت برند: هر ۱۵ صفحه استاندارد از ابتدا با چیدمان حرفه‌ای در `layout_json` ساخته می‌شوند (میانگین امتیاز UX بلوپرینت‌ها **۹۲/۱۰۰**)
  - قالب‌ساز درگ‌اند‌دراپ: دکمه «✨ طراحی با UI/UX Pro» + «🔍 بررسی UX» + «🛠️ اصلاح خودکار» با پنل نتایج کامل
  - داشبورد: ردیف اسکیل در کارت وضعیت موتور AI
- **۴ اندپوینت API جدید**: `POST ai/uiux-design` ، `POST ai/uiux-review` ، `POST ai/uiux-improve` ، `GET ai/uiux-skill-info` — موتور به ۳.۵.۰ ارتقا یافت

#### 🖥️ کارت «وضعیت سیستم و عملکرد» در داشبورد
- نمایش زنده نسخه PHP (با نشان وضعیت)، GD + FreeType، OPcache، mbstring، cURL، memory_limit، max_execution_time و سقف آپلود — هرکدام با نشان سبز/قرمز و راهنمای شناوری
- هشدار فعال‌سازی OPcache با راهنمای cPanel در صورت غیرفعال بودن (۲ تا ۵ برابر سرعت)

#### ⚡ بهینه‌سازی PHP 8.3
- فایل **`.user.ini`** در ریشه: OPcache + حافظه ۲۵۶M + زمان اجرا ۱۲۰s + کش مسیر ۴M + سخت‌سازی نشست (use_strict_mode) — خوانده‌شده توسط PHP-FPM/LiteSpeed/CGI هاست‌های cPanel (روی mod_php بی‌اثر و بی‌ضرر)
- **پلی‌فیل `json_validate()`**: روی PHP 8.3 از تابع بومی (سریع‌تر — بدون ساخت ساختار داده) و روی نسخه‌های قدیمی‌تر از جایگزین معادل استفاده می‌شود — قالب‌ساز به آن مهاجرت کرد
- `error_reporting` هوشمند: در حالت عملیاتی نویز `E_DEPRECATED` و `E_STRICT` لاگ نمی‌شود؛ در دیباگ همه‌چیز ثبت می‌شود
- فشرده‌سازی zlib خروجی (HTML/JSON) در صورت فعال نبودن در سطح سرور

### 🔒 سخت‌سازی (Hardening)
- بستن گپ احراز هویت هندلرهای AJAX قالب‌ساز: `save_template` و هندلرهای UI/UX حالا قبل از هر پردازش `requireLogin()` را صریحاً اجرا می‌کنند (قبلاً فقط CSRF بررسی می‌شد)
- پوشه `engine/skills/` به بارگذار خودکار کلاس‌ها اضافه شد

### 🧪 راستی‌آزمایی
- شبیه‌سازی کامل الگوریتم ممیزی/اصلاح با پورت پایتون دقیق: چیدمان خراب «services» از **۱۰ → ۸۲**، چیدمان خالی **۲۰ → ۶۴**، بلوپرینت سالم home بدون تخریب (**۹۷ → ۹۷**)
- میانگین امتیاز UX هر ۱۷ بلوپرینت: **۹۲/۱۰۰** (همه ≥ ۷۷)
- بررسی ساختاری (توازن براکت/رشته/کامنت) هر ۱۱ فایل PHP تغییریافته: موفق

---

## [2.4.1] — ۱ مهر ۱۴۰۵ (2026-09-23) 🔥 هات‌فیکس حیاتی — رفع ۵ باگ گزارش‌شده کاربر

> ⚠️ **این نسخه برای همه کاربران ۲.۴.۰ الزامی است.** بعد از آپدیت به ۲.۴.۰ سه مشکل حیاتی گزارش شد که همه در این نسخه رفع و با شبیه‌سازی کامل راستی‌آزمایی شدند: داشبورد خالی و منوی بازنشدنی، خطای «Unexpected end of JSON input» دکمه پیشنهاد عنوان سئو و خطای «Unexpected token '<'» دکمه بهبود سئو.

### 🐛 رفع شده (Fixed)

#### ۱) داشبورد خالی و منوی بازنشدنی — متد تکراری در SahandAI
- **علت ریشه‌ای**: متد `researchTopic()` به‌صورت تکراری (نسخه ۳.۱ و ۳.۳) در `engine/SahandAI.php` تعریف شده بود → خطای غیرقابل‌گرفتِ «Cannot redeclare method» به محض بارگذاری کلاس → مرگ صفحه درست بعد از هدر (بدون محتوا، بدون فوتر، بدون admin.js → منو هم کار نمی‌کرد). این باگ از v2.3.0 وجود داشت.
- رده‌های آسیب‌دیده: داشبورد، تولید مقاله، همه اندپوینت‌های `/api/ai/*`، ربات تلگرام (`/status`)
- ✅ نسخه قدیمی (۳.۱) حذف شد؛ نسخه کامل ۳.۳ (با کش موتور و گزینه‌های depth/include_news/max_sources) حفظ شد

#### ۲) خطای «Unexpected end of JSON input» در پیشنهاد عنوان سئو — خرابی UTF-8 با trim()
- **علت ریشه‌ای**: تابع `trim()` استاندارد PHP **بایت-محور** است؛ وقتی فهرست کاراکترها شامل «،» (D8 8C) یا «؛» (D8 9B) باشد، بایت نخست همه حروف فارسیِ با پیشوند D8 (ا، ب، ت، ث، ج، ح، خ، د، ذ، ر، ز، س، ش، ص، ض، ط، ظ، ع، غ و…) هم بریده می‌شد → رشته UTF-8 نامعتبر → `json_encode() === false` → `echo false` → **پاسخ کاملاً خالی**
- 🎯 نقطه خرابی: `TitleGenerator::suggestForCustom()` — یعنی هر عنوانی که با اکثر حروف الفبا شروع می‌شد خراب می‌شد
- ✅ توابع کمکی چندبایتی-امن `mb_trim()` و `mb_rtrim()` به helpers اضافه شد و **هر ۱۳ نقطه فراخوانی خطرناک** در کل پروژه اصلاح شد: TitleGenerator، SeoImprover (متا توضیحات)، PersianGrammar (۶ مورد هم‌خوانی فعل/فاعل)، KeywordAnalyzer (۵ مورد)، ContentImprover
- ✅ `json_response()` ضدخرابی شد: `JSON_INVALID_UTF8_SUBSTITUTE` + سد دفاعی نهایی — دیگر هرگز پاسخ خالی برنمی‌گردد

#### ۳) خطای «Unexpected token '<'» در دکمه بهبود سئو — تابع ناموجود mb_strtok()
- **علت ریشه‌ای**: `PersianGrammar::fixAgreement()` و `fluency()` تابع `mb_strtok()` را صدا می‌زدند که **در PHP وجود ندارد** (فقط `strtok` هست) → Error غیرقابل‌گرفت با `catch (Exception)` → هندلر سراسری HTML برمی‌گرداند در پاسخ به JSON
- ✅ با `explode(' ', $s, 2)[0]` جایگزین شد (۳ نقطه)

#### ۴) قواعد نیم‌فاصله بی‌اثر — lookbehind با طول متغیر در PCRE
- **علت ریشه‌ای**: الگوهای `(?<=[\p{L}]{2,}) +ها` و ۵ الگوی مشابه در `fixZwnj()` — PCRE از lookbehind با طول متغیر پشتیبانی نمی‌کند → `preg_replace` با هشدار شکست می‌خورد و نیم‌فاصله‌گذاری پسوندها (ها/تر/ترین/سازی/کننده/شونده) هرگز اعمال نمی‌شد
- ✅ همه با گروه گیرنده بازنویسی شدند (`([\p{L}]{2,}) +ها` → `$1‌ها`) — حالا «کتاب ها» → «کتاب‌ها» واقعاً کار می‌کند

#### ۵) باگ‌های خرد همراه
- نقطه‌گونگی دوباره پس از اصلاح فعل («می‌دهیم..») — رفع شد
- کلیدواژه‌های HTML («</p>»، «href=») در خروجی بهبود سئو — حالا `strip_tags` قبل از استخراج کلیدواژه
- کاراکتر � در متا توضیحات بهبودیافته (همان خرابی trim بایت‌محور) — رفع شد

### 🔒 سخت‌سازی (Hardening)

- **پلی‌فیل PHP ۷.۴**: توابع `str_contains` / `str_starts_with` / `str_ends_with` (فقط PHP ۸+) در config.php برای سرورهای PHP ۷.۴ تعریف شدند — TitleGenerator و AssetDownloader و CommandAssistant حالا روی PHP ۷.۴+ کار می‌کنند
- **گرفتن Throwable به‌جای Exception**: همه هندلرهای AJAX پنل (`articles.php`: تولید/پیشنهاد عنوان/بهبود سئو، `brand-build.ajax.php`)، داشبورد، هدر، `api/index.php` (۵ مورد)، rate-limit، SahandAI، SeoImprover و ArticleGenerator — دیگر هیچ Error ای به پاسخ HTML ختم نمی‌شود
- **هندلر استثنای سراسری AJAX-آگاه**: درخواست‌های دارای هدر `X-Requested-With: XMLHttpRequest` حالا پاسخ JSON (با پیام خطای واقعی) می‌گیرند نه HTML — ردهٔ «Unexpected token '<'» برای همیشه بسته شد
- **admin.js از فوتر به هدر منتقل شد** (با `defer`): حتی اگر رندر صفحه وسط کار با خطا قطع شود، منو و تعاملات پایه همچنان کار می‌کنند
- هر دو فراخوانی `fetch` دکمه‌های سئو حالا هدر `X-Requested-With` می‌فرستند

### 🧪 راستی‌آزمایی (شبیه‌سازی کامل با PHP 8.3 + پایگاه داده شبیه‌سازی‌شده)

| سناریو | قبل از فیکس | بعد از فیکس |
|--------|-------------|-------------|
| داشبورد | ~۲ کیلوبایت — قطع بعد از هدر، بدون محتوا/فوتر/JS | ۱۱,۷۸۸ بایت — رندر کامل با آمار موتور AI |
| پیشنهاد عنوان سئو (عنوان با حروف D8 مثل «آموزش…») | پاسخ خالی ۰ بایت | JSON معتبر — ۸ پیشنهاد امتیازدار |
| بهبود سئو | پاسخ HTML خطا | `success:true` — امتیاز ۳۶ → ۸۴ (+۴۸)، ۷ اصلاح |
| تولید مقاله | Fatal — متد تکراری | مقاله کامل تولید و ذخیره شد |
| هم‌خوانی فعل/فاعل | نقطه دوباره «می‌دهیم..» | «ما…می‌دهد.» → «ما…می‌دهیم.» تمیز |
| کلیدواژه‌های سئو | «</p>»، «href=» | فقط واژه‌های فارسی واقعی |

---

## [2.4.0] — ۳ مهر ۱۴۰۴ (2025-09-25) 📝 نگارش‌گر فارسی + 📰 ۲۵ نوع مقاله + 🎯 عنوان دلخواه و بهبودگر سئو + 🔎 جستجوی آنلاین هنگام نوشتن

ارتقای بزرگ فاز Q — موتور AI به **۳.۴** رسید؛ حالا یک **نگارش‌گر فارسی** کامل (نیم‌فاصله، سجاوندی، املای رایج، هم‌خوانی فعل و فاعل) در قلب خط تولید محتواست، انواع مقاله از ۹ به **۲۵ نوع** رسید، کاربر می‌تواند **عنوان دلخواه** بدهد و **بهترین عنوان سئو** را با امتیاز پیشنهاد بگیرد، زیر هر مقاله **آمار کامل سئو با دکمه بهبود خودکار** نمایش داده می‌شود، مقاله‌نویسی می‌تواند با **جستجوی اینترنت** انجام شود و **تصاویر خودکار** در متن درج می‌شوند.

### 🚀 افزودنی‌ها (Added)

#### 📝 موتور قواعد نگارشی فارسی — PersianGrammar v1.1 (۵۶۵ خط)
- کلاس جدید `engine/utils/PersianGrammar.php`: لایه کنترل کیفیت نوشتار موتور AI
  - `fix()` — اصلاح کامل HTML-امن: نیم‌فاصله (می/نمی/بی/ها/تر/ترین/ای/ی/سازی/کننده/شونده)، علائم سجاوندی (فاصله قبل/بعد، ویرگول فارسی، سه‌نقطه، گرب‌دستگاه)، املای رایج (۵۰+ واژه: تنوین، حروف چسبان، محاوره → رسمی، تکرار حروف)، ارقام فارسی، نرمال‌سازی ی/ک عربی و حذف اعراب
  - `fixAgreement()` — اصلاح پراطمینان **هم‌خوانی فعل و فاعل** (جدول ۶ ضمیر ما/شما/آنها/اینها × ۱۰ فعل رایج: «ما می‌کند» → «ما می‌کنیم»)
  - `analyze()` — امتیاز نگارش ۰-۱۰۰ + فهرست ایرادها با راهنمای رفع
  - `fluency()` — راهکارهای روان‌نویسی (شکستن جمله بلند، تنوع آغازگر، پاراگراف‌بندی)
  - رفع ۳ باگ regex در انتظار کامییت: `\b` با حروف فارسی در PCRE کار نمی‌کند → مرز یونیکد-امن `(?<!\p{L})`؛ `\d` رقم فارسی را نمی‌بست → افزودن `۰-۹`؛ `\u{200C}` در PCRE نامعتبر → `\x{200C}`

#### 📊 بُعد هشتم کیفیت «نگارش فارسی» — QualityScorer v2.1
- امتیازدهی حالا **۸ بُعدی** است (وزن نگارش: ۱۰) + گزارش کامل grammar در خروجی مقاله + استراتژی `fixGrammar` در چرخه خود-درمانی ContentImprover

#### ✍️ اصلاح نگارشی خودکار در همه خطوط تولید
- **مقالات** (ArticleGenerator v2.3): گام ۵.۶ — اصلاح نگارشی پیش از سئو
- **صفحات برند** (ContentGenerator v1.1): خروجی نهایی همیشه نگارش-اصلاح‌شده
- ۲۵ تست منطق + ۵ فایل سینتکس PASS

#### ⬇️ دانلود مستقیم فونت و آیکون روی سرور — AssetDownloader v1.0 (بخش ۱ فاز Q)
- کلاس جدید `core/AssetDownloader.php` (۷۰۹ خط): دانلود مستقیم روی **همان سرور سایت‌ساز** — بدون دانلود روی کامپیوتر و آپلود مجدد
- امنیت: وایت‌لیست میزبان‌ها (jsDelivr/npm/codeload/fonts.gstatic/raw.githubusercontent) + سقف حجم + پاک‌سازی XSS روی SVG + فقط https + پشتیبانی ZIP و TAR.GZ
- `assets/fonts/sources.json`: ۹ فونت فارسی متن‌باز + ۱۰ فونت انگلیسی — `assets/icons/sources.json`: ۹ پک آیکون (~۱۶ هزار آیکون)
- دکمه «⬇️ دانلود و نصب» در `admin/fonts.php` و «نصب کامل پک» در `admin/icons.php`

#### 📰 گسترش انواع مقاله از ۹ به ۲۵ نوع
- ۱۶ نوع جدید: ایمنی، نصب و راه‌اندازی، تعمیر شخصی/تخصصی، اشتباهات رایج، گارانتی، فناوری‌شناسی، باورغلط/واقعیت، چک‌لیست، مطالعه موردی، واژه‌نامه، تاریخچه، نکات خبرگان، علامت‌محور، آمار، محیط زیست، فرآیند نمایندگی
- `templates.json`: ۶۳ → **۱۳۶ قالب عنوان**
- سلکت گروه‌بندی‌شده ۵ دسته در پنل + تولید اولیه برند ۵ → ۸ مقاله با تنوع نوع

#### 🎯 عنوان دلخواه + پیشنهاد بهترین عنوان سئو (Q.5)
- `TitleGenerator v3.1` — `suggestForCustom()`: تحلیل عنوان کاربر (طول/عدد/پرسش/واژه قدرت/موقعیت کلیدواژه) + ساخت ۱۲+ واریانت سئو (عدد جذاب، فرم پرسشی، سال، براکت، برند) + رتبه‌بندی با نمایش gain
- ArticleGenerator: پارامتر `custom_title` — مقاله دقیقاً حول عنوان کاربر نوشته می‌شود و کلیدواژه کانونی از آن استخراج می‌شود
- پنل: فیلد «عنوان دلخواه» + دکمه «🎯 پیشنهاد بهترین عنوان سئو» + پنل تحلیل با کلیک برای جایگزینی

#### 📊 پنل آمار کامل سئو + دکمه «بهبود سئو» (Q.6)
- کلاس جدید `engine/generators/SeoImprover.php` (۵۶۷ خط):
  - `analyze()` — **۱۲ سنجه وزنی**: عنوان سئو، متا توضیحات، تراکم کلیدواژه، کلیدواژه در مقدمه، حجم، هدینگ، لینک داخلی، alt تصاویر، FAQ، TOC، نگارش فارسی، خوانایی
  - `improve()` — **۸ اصلاح خودکار**: نگارش + کلیدواژه در مقدمه + عنوان ۴۵-۶۰ + متا ۱۲۰-۱۶۰ + لینک داخلی + alt + FAQ + TOC — با گزارش قبل/بعد و ذخیره + بروزرسانی `seo_score`
- زیر ویرایشگر هر مقاله در پنل + دکمه «🎯 بهبود سئو (n مورد)» با گزارش زنده

#### 🔎 جستجوی اینترنت هنگام نوشتن مقاله (Q.7)
- گام ۱.۵ در خط تولید: `research => true` → تحقیق آنلاین روی عنوان
- ۳ بخش جدید در مقاله: «📊 داده‌های به‌روز از منابع آنلاین» + «🤔 پرسش‌های پرتکرار کاربران در وب» + «📚 منابع و مطالعه بیشتر» (لینک معتبر nofollow — E-E-A-T)
- کلیدواژه‌های ترند → تگ‌های مقاله + شکست تحقیق هرگز تولید را متوقف نمی‌کند

#### 🖼️ تصاویر خودکار در متن مقالات (Q.8)
- گام ۳.۷: ۳ تصویر (شاخص/میان‌متن/پایانی) با alt و figcaption استاندارد سئو
- ذخیره `featured_image` + `seo_score` (از SeoImprover) هنگام ذخیره مقاله — بدون تغییر دیتابیس

#### 🤖 ربات تلگرام v2.2 + دستیار فارسی (Q.9)
- ۳ فرمان جدید + معادل فارسی: `/grammar` یا `/نگارش` (امتیاز + ایرادها + متن اصلاح‌شده + روان‌نویسی)، `/suggest` یا `/پیشنهاد`، `/types` یا `/انواع`
- دستیار فارسی: «نگارش: <متن>» (ZWNJ-پذیر: غلط‌گیری/غلطگیری) + «پیشنهاد عنوان سئو: <عنوان>» + «انواع مقاله»
- رفع تداخل مسیریابی: نگارش از بهبود، عنوان سئو از پکیج سئو جدا شد — ۹ تست مسیریابی پاس

### 🔌 API
- ۳ اندپوینت جدید (مجموع **۴۴**): `POST /api/ai/suggest-article-titles` + `POST /api/ai/article-seo-stats` + `POST /api/ai/improve-article-seo`
- پارامترهای جدید `generate-article`: `custom_title` + `research` + `with_images`
- راهنمای API: ۱۷۹۴ → ۱۹۳۲ خط (بخش‌های ۵.۱۴ تا ۵.۱۷ + جدول ۳.۴ + تاریخچه)

### 🔧 تغییرات فنی
- موتور `3.3.0` → **`3.4.0`** · سیستم `2.3.0` → **`2.4.0`**
- بدون تغییر دیتابیس — کاملاً file-level (ستون‌های موجود `featured_image` و `seo_score` استفاده شدند)

---

## [2.3.0] — ۲ مهر ۱۴۰۴ (2025-09-23) 🏅 سئوی E-E-A-T + 📚 ویکی‌پدیا + ⚡ مسابقه موازی + رفع ۵ باگ گزارش‌شده کاربر

ارتقای نسخه ۲.۳ — موتور AI به ۳.۳ رسید؛ سئو با E-E-A-T و تحلیل معنایی عمیق شد، جستجوی وب موازی و با ویکی‌پدیا قوی‌تر شد و **هر ۵ باگ گزارش‌شده کاربر رفع شد**.

### 🐛 رفع اشکال (Fixed) — باگ‌های گزارش‌شده کاربر
- **صفحه ساخت برند بهم‌ریخته و متن‌ها بیرون می‌زدند** (`admin/brand-new.php`):
  - 🔍 ریشه: یک تگ بسته `?>` اضافی قبل از تنظیم عنوان صفحه — کل کد HTML بعدی به صورت **متن خام** چاپ می‌شد و هدر/استایل لود نمی‌شد
  - 🔧 رفع: حذف تگ اضافی — صفحه کاملاً سالم و ریسپانسیو
- **خطای مرحله ۱ ساخت برند (تحلیل لوگو) و توقف فرآیند**:
  - 🔍 ریشه ۱: `ImageProcessor` از **SVG پشتیبانی نمی‌کرد** (getimagesize روی SVG خطا می‌دهد) — در حالی که صفحه تنظیمات آپلود SVG را پیشنهاد می‌دهد
  - 🔍 ریشه ۲: حلقه fallback رنگ روی تصویرِ **destroy شده** اجرا می‌شد — لوگوهای تک‌رنگ/سیاه‌وسفید خطای Fatal می‌دادند
  - 🔧 رفع: پشتیبانی کامل SVG/SVGZ (استخراج رنگ از fill/stroke/stop-color/style با regex + پشتیبانی رنگ ۳/۶ رقمی و rgb()) + جابجایی imagedestroy + **fallback نرم**: تحلیل ناموفق دیگر هرگز ساخت را متوقف نمی‌کند (پالت پیش‌فرض + پیام راهنما)
- **فیلتر بالای کارت‌ها نصفه دیده می‌شد** (برندها/مقالات/درخواست‌ها/کدهای خطا):
  - 🔍 ریشه: فیلم‌های جستجو با عرض ثابت ۲۰۰/۱۵۰px داخل `.tools` بدون wrap — در موبایل سرریز و بریده می‌شدند (card دارای overflow:hidden)
  - 🔧 رفع: CSS ریسپانسیو `.tools` + عرض منعظم min/max + wrap کامل
- **صفحه تنظیمات در موبایل بهم‌ریخته بود**:
  - 🔧 رفع: ساختار جدید `tabs-bar` — تب‌ها اسکرول افقی نرم (scroll-snap) + دکمه «ذخیره همه تنظیمات» در موبایل تمام‌عرض زیر تب‌ها
- **کش مرورگر بعد از بروزرسانی** (ریشه اصلی «همه‌چیز هنوز بهم‌ریخته است» پس از ارتقا):
  - 🔧 رفع: تابع جدید `asset_ver()` — نسخه‌دار کردن CSS/JS با filemtime؛ آدرس فایل با هر تغییر عوض می‌شود و کش مرورگر خودکار می‌شکند

### 🚀 افزودنی‌ها (Added)

#### 👁️ پیش‌نمایش زنده قالب‌ها در ویرایشگر (باگ «پیش‌نمایش نشون نمی‌ده»)
- صفحه جدید `admin/template-preview.php` — رندر زنده چیدمان بلوک‌ها با محتوای نمونه فارسی (۳۵+ نوع بلوک، ریسپانسیو کامل، پشتیبانی از props پس‌زمینه/فاصله/عنوان/visible)
- دکمه «👁️ پیش‌نمایش زنده» در قالب‌ساز + سوئیچ **موبایل/تبلت/دسکتاپ** برای iframe
- وایرفریم بصری مینی هر بلوک **داخل بوم** (به جای متن خالی قبلی) + دکمه 👁 کنار هر بلوک کتابخانه و هر بلوک بوم برای پیش‌نمایش تکی

#### 🏅 سئوی E-E-A-T و تحلیل معنایی (SeoGenerator v1.2)
- **چک‌لیست E-E-A-T** هشت‌معیاری (تجربه/تخصص/اعتبار/اعتماد/ساختار/عمق/FAQ/CTA) با امتیاز و راهنمای رفع هر مورد ردشده
- **استخراج موجودیت‌ها** (Entity) از محتوا: برندها، دستگاه‌ها، شهرها و اصطلاحات فنی — پایه سئوی معنایی
- **خوشه‌بندی کلیدواژه ثانویه** (اطلاعاتی/تجاری/آموزشی) + پیشنهاد **People Also Ask**
- **تحلیل شکاف محتوایی**: عنوان‌های ۵ نتیجه اول گوگل را بدهید → موارد پوشش‌داده‌نشده گزارش می‌شود
- ۲ اندپوینت API جدید: `POST /api/ai/seo-package` (پکیج کامل + شکاف رقبا) و `POST /api/ai/eeat-check` — **مجموع ۴۱ اندپوینت AI**

#### 📰 ساختار حرفه‌ای مقالات (ArticleGenerator v2.1)
- **فهرست مطالب (TOC)** با لنگر خودکار روی سرفصل‌ها + درج بعد از مقدمه
- **زمان مطالعه** (میانگین ۲۲۰ کلمه/دقیقه فارسی) در TOC و خروجی API
- **جعبه نکته حرفه‌ای 💡** + **هشدار ایمنی ⚠️** (مقالات فنی) + **مزایا و معایب ⚖️** (مقایسه/راهنمای خرید) + **باکس آمار اعتماد 📊**
- حداقل حجم مقاله ۸۰۰ → **۱۰۰۰ کلمه**
- استایل‌های کامل عناصر جدید در سایت‌های برند (ریسپانسیو)

#### 🌐 جستجوی وب ۹ ارائه‌دهنده + مسابقه موازی (WebSearchService v1.2)
- ارائه‌دهنده **ویکی‌پدیا** (فارسی + انگلیسی با fallback خودکار) — رسمی، رایگان، پایدار، بدون کلید
- ارائه‌دهنده **Startpage** (نتایج گوگل بدون کلید)
- **مسابقه موازی (curl_multi)**: ارائه‌دهندگان رایگان همزمان شلیک می‌شوند و اولین پاسخ موفق برنده است — تأخیر جستجو تا **۵۰٪ کاهش** (+fallback ترتیبی برای سرورهای بدون curl_multi)
- هر نتیجه اکنون **تازگی** (استخراج تاریخ نسبی/میلادی/شمسی) و **اعتماد دامنه** (۰-۹۰) دارد

#### 🤖 ربات تلگرام v2.1 — ۴ فرمان جدید (مجموع ۱۶)
- `/seo [موضوع]` — پکیج سئوی پیشرفته کامل با فرمت‌دهی رنگارنگ
- `/eeat [متن]` — چک‌لیست اعتماد با راهنمای رفع
- `/plan [موضوع]` — برنامه انتشار محتوای ۸ هفته‌ای
- `/faq [موضوع]` — سوالات متداول پیشنهادی با پاسخ کوتاه
- معادل فارسی (`/سئو` `/اعتماد` `/برنامه` `/سوال`) + زبان طبیعی: «سئو بررسی کن: ...»، «اعتمادسنجی: ...»، «برنامه محتوا برای ...»، «سوالات متداول درباره ...»
- CommandAssistant: ۴ intent جدید + fallback هوشمند (اولین برند فعال برای FAQ)

### ♻️ تغییرات (Changed)
- نسخه سیستم: 2.2.0 → **2.3.0** | موتور AI: 3.2.0 → **3.3.0** — **بدون تغییر دیتابیس** (کاملاً file-level)
- CSS/JS پنل حالا با `asset_ver()` نسخه‌دار می‌شوند (شکستن کش خودکار پس از هر بروزرسانی)
- راهنمای API (docs/AI-API-GUIDE.md): ۱۷۰۰ → ۱۸۰۰+ خط — مستندات کامل ۲ اندپوینت جدید + بخش v3.3 جستجو + جدول ۱۶ فرمان ربات

## [2.2.0] — ۱ مهر ۱۴۰۴ (2025-09-23) 🖼️ ۳ تصویر در مقاله + 📰 تحویل کامل مقاله در تلگرام + 📰 اخبار زنده + ریسپانسیو کامل

ارتقای نسخه ۲.۲ — موتور AI با تصویر و اخبار غنی‌تر شد و ربات تلگرام به «کارخانه مقاله در جیب» تبدیل شد.

### 🐛 رفع اشکال (Fixed) — باگ گزارش‌شده کاربر
- **رفع باگ «منو در وسط صفحه باز می‌شود»** در پنل مدیریت موبایل:
  - 🔍 ریشه: سایدبار با `inset-inline-end:0` در RTL به لبه **چپ** چسبیده بود اما `translateX(100%)` آن را **داخل ویوپورت** (وسط صفحه) هل می‌داد
  - 🔧 رفع: `translateX(-105%)` → خروج کامل از صفحه + سایه و انیمیشن هنگام باز شدن
  - ✅ راستی‌آزمایی مرورگر هدلس در عرض ۳۹۰px: بسته = خارج از صفحه، باز = لبه + پس‌زمینه تیره + قفل اسکرول

### 🚀 افزودنی‌ها (Added)

#### 🖼️ سرویس تصاویر مقاله (ArticleImageService) + ۹ تصویر بسته‌بندی‌شده
- هر مقاله تولیدی از این پس **۳ تصویر مرتبط** دارد: بنر شاخص + ۲ تصویر درون‌متن در جایگاه‌های راهبردی (۳۵٪ و ۷۲٪ طول مقاله)
- ۹ تصویر فوتورئال تخصصی تولید و بسته‌بندی شد: یخچال، ماشین لباسشویی، ظرفشویی، کولر، تلویزیون، کارگاه، قطعات، عیب‌یابی، آشپزخانه مدرن
- انتخاب هوشمند بر اساس دستگاه/نوع مقاله + تولید خودکار alt و کپشن سئو-محور فارسی + `<figure>` استاندارد
- URLهای **مطلق** — قابل استفاده از هر سایت دیگری

#### 📰 ربات تلگرام v2 — تحویل کامل مقاله (بدون نیاز به ورود به سایت ساز)
- «مقاله بنویس...» یا `/article موضوع` → اجرای کامل خط تولید هوشمند و تحویل ۶ مرحله‌ای در تلگرام:
  خلاصه (عنوان/امتیاز/کلیدواژه/حجم/تراکم) + **فایل HTML مستقل آماده انتشار برای هر سایت** (استایل داخلی RTL + متا + OG/Twitter + اسکیمای Article/FAQ/HowTo + تصاویر مطلق) + **نسخه Markdown** + **آلبوم ۳ تصویر** + دکمه‌های اقدام بعدی
- ۱۲ فرمان: `/article` `/keywords` `/search` `/research` `/news` `/titles` `/improve` `/score` + فرمان‌های فارسی (`/مقاله` `/اخبار` ...)
- پردازش callback_query دکمه‌های شیشه‌ای + sendChatAction (typing/upload) + apiUpload با multipart برای فایل و آلبوم (sendMediaGroup)

#### 📰 اخبار زنده (Google News RSS)
- متد `news()` در WebSearchService + اندپوینت `POST /api/ai/news` + فرمان «اخبار: ...» در دستیار فارسی

#### 🌐 تقویت جستجوی آنلاین
- ارائه‌دهنده جدید **Mojeek** (رایگان، ایندکس مستقل) → ۷ ارائه‌دهنده در زنجیره جایگزینی
- **فرصت‌های کلیدواژه long-tail** (`opportunities`) در تحقیق ساختاریافته — ترکیب موضوع × ترند × سؤالات واقعی
- تلاش مجدد خودکار در خطای شبکه/خطای ۵xx

#### 🧠 SmartPipeline v3.2 — خط تولید ۱۱ مرحله‌ای
- گام‌های جدید: **غنی‌سازی ساختاری** (فهرست مطالب + «سایر پرسش‌های کاربران» PAA + بخش «داده‌های تازه از وب» با ذکر منبع + جعبه اعتبار E-E-A-T) + **درج ۳ تصویر**
- تحقیق وب حالت `"auto"`: در صورت فعال بودن سرویس خودکار اجرا می‌شود
- امتیاز هدف پیش‌فرض ۸۵ → **۸۸**

#### 🔍 SeoGenerator v1.1
- اسکیمای Article غنی‌شده: تصاویر + wordCount + dateModified + mainEntityOfPage + Speakable
- **HowTo Schema** برای مقالات راهنما/عیب‌یابی/نگهداری + تحلیل تراکم کلیدواژه + زمان مطالعه + og:image و twitter:image کامل

#### 📱 ریسپانسیو کامل همه صفحات
- پنل: پس‌زمینه تیره پشت سایدبار موبایل + قفل اسکرول + aria-expanded + بستن با Escape/کلیک لینک + تنظیمات ۶۴۰px (کارت/جدول/مودال/تب/ورود)
- سایت برند: منوی موبایل دقیقا زیر هدر با انیمیشن و اسکرول داخلی + زیرمنوی باز و تو رفته + گریدهای ضد سرریز + جدول‌های اسکرول‌دار + تنظیمات ۴۲۰px
- نصب: ریسپانسیو مرحله‌بندی و فرم‌ها زیر ۶۰۰px

#### API نسخه ۳.۲ — ۲ اندپوینت جدید (مجموع ۳۹ + وب‌هوک)
- `POST /api/ai/news` — اخبار زنده فارسی
- `GET /api/ai/article-images` — فهرست ۹ تصویر با URL مطلق (عمومی)

### 🔄 تغییرات (Changed)
- ارتقای نسخه سیستم: `2.1.0` → **`2.2.0`** | نسخه موتور: `3.1.0` → **`3.2.0`**
- `CommandAssistant`: موضوع دلخواه در دستور مقاله (پشتیبانی هر موضوعی، حتی خارج از دامنه دستگاه‌ها) + `no_cache` برای مقاله (هر درخواست = مقاله تازه)
- `docs/AI-API-GUIDE.md` — ۱۵۶۵ → ۱۷۰۰+ خط: بخش‌های ۵.۱۲ (اخبار) + ۵.۱۳ (تصاویر) + بازنویسی بخش ۱۰ (ربات v2) + تاریخچه
- استایل عناصر جدید مقاله در قالب brand-core (article-toc/figure/eeat/people-also-ask/fresh-stats)

---

## [2.1.0] — ۱ مهر ۱۴۰۴ (2025-09-23) 🌐 جستجوی آنلاین وب + 🤖 ربات تلگرام + رفع باگ بحرانی نصب

ارتقای نسخه ۲.۱ — موتور AI به اینترنت وصل شد و دستیار فارسی به تلگرام آمد.

### 🐛 رفع اشکال (Fixed) — بحرانی
- **رفع باگ نصب‌کننده دیتابیس** — خطای `SQLSTATE[42S02]: Table '....users' doesn't exist` هنگام نصب:
  - 🔍 ریشه: تقسیم regex قدیمی `install.php` هر تکه‌ای از `database.sql` که با خط کامنت `--` شروع می‌شد را «کامل» نادیده می‌گرفت → **۳۱ جدول از ۳۳ جدول هرگز ساخته نمی‌شد** و سپس ساخت کاربر مدیر با خطای 1146 شکست می‌خورد
  - 🔧 رفع: تجزیزگر ماشین-حالت امن `splitSqlStatements()` — کامنت‌های خطی (`--` و `#`) و بلوکی (`/* */`) فقط بیرون از رشته‌ها حذف، رشته‌ها با ESCAPE های `\'`، `''`، `""` دست‌نخورده، سمی‌کالن فارسی (`؛`) هرگز تفکیک نمی‌کند
  - ✅ راستی‌آزمایی حیاتی پس از نصب: بررسی `SHOW TABLES` برای ۶ جدول کلیدی با خطای شفاف در صورت نقص
  - 🔁 نصب مجدد idempotent: کاربر مدیر موجود → UPDATE؛ کلید API سیستمی فقط در صورت نبود ساخته می‌شود
  - 🧪 ۲۵ تست واحد موفق (`scripts/test-splitter.php`) + مقایسه قبل/بعد: ۴ دستور ↔ ۴۰ دستور

### 🚀 افزودنی‌ها (Added)

#### 🌐 سرویس جستجوی آنلاین وب (WebSearchService)
- **۶ ارائه‌دهنده با زنجیره جایگزین خودکار**: SerpApi / Google CSE / Bing API (کلیددار اختیاری) + DuckDuckGo HTML / DuckDuckGo Lite / Bing HTML (رایگان — بدون هیچ کلید)
- **تحقیق ساختاریافته** (`research`): ۲ جستجو هماهنگ (اصلی + قصد تجاری قیمت) + استخراج هوشمند کلیدواژه ترند (فراوانی وزنی) + سؤالات واقعی کاربران (چرا/چطور/؟) + داده‌های عددی تازه (تومان/درصد/سال) + منابع + خلاصه فارسی
- **خواندن متن صفحات** (`fetchPageText`): حذف script/style/nav + استخراج عنوان/متا/متن
- امنیت: محافظت SSRF کامل + محدودیت نرخ ۶۰/ساعت + کش ۳۰ دقیقه‌ای + چرخش User-Agent + تایم‌اوت دو مرحله‌ای

#### 🧠 یکپارچه‌سازی وب در موتور
- `SmartPipeline`: پارامتر `research` → گام تحقیق آنلاین (غنی‌سازی FAQ با ۳ سؤال واقعی + لحاظ کلیدواژه ترند در عنوان + خروجی research) — **شکست وب هرگز خط تولید را متوقف نمی‌کند**
- `CommandAssistant`: ۲ فرمان جدید — «جستجوی وب: ...» / «سرچ: ...» و «تحقیق درباره: ...»
- موتور `3.1.0` + `engineInfo` با ۳ قابلیت جدید

#### 🤖 ربات تلگرام متصل به دستیار فارسی
- **telegram/TelegramBot.php** — کلاس کامل: Bot API (getMe/setWebhook/deleteWebhook/getWebhookInfo) + sendMessage با HTML + تقسیم خودکار پیام‌های بلند (≤۴۰۹۶، مرز خط، بدون از دست رفتن محتوا)
- **مغز ربات**: احراز هویت لیست چت (خالی=بسته کامل، *=عمومی) + فرمان‌های `/start` `/help` `/status` `/id` + زبان طبیعی فارسی → CommandAssistant
- **قالب‌بندی هوشمند ۱۱ نوع عملیات**: مقاله/کلیدواژه/امتیاز/بهبود/عنوان/نیت/عیب‌یابی/کد خطا/برندها/جستجوی وب/تحقیق — با XSS ایمن
- **وب‌هوک**: `POST /api/telegram/webhook` با راستی‌آزمایی توکن مخفی `X-Telegram-Bot-Api-Secret-Token` (hash_equals)
- **long-polling**: `telegram/poll.php` برای تست محلی و هاست بدون SSL
- **پنل مدیریت**: `admin/telegram.php` — ذخیره توکن/شناسه‌ها + تست اتصال getMe + راه‌اندازی/حذف وب‌هوک (توکن مخفی خودکار) + پیام آزمایشی + وضعیت + راهنمای ۶ مرحله‌ای
- **اعلان درخواست جدید**: کانال پنجم NotificationService — درخواست‌های سایت‌های برند به چت‌های مجاز ربات ارسال می‌شود

#### API نسخه ۳.۱ — ۳ اندپوینت جدید (مجموع ۳۷)
- `POST /api/ai/web-search` — جستجوی آنلاین اینترنت
- `POST /api/ai/research` — تحقیق ساختاریافته آنلاین
- `GET /api/ai/websearch-status` — وضعیت ارائه‌دهندگان + نرخ + کش (عمومی)
- `POST /api/telegram/webhook` — وب‌هوک ربات تلگرام (عمومی + توکن مخفی)

### 🔄 تغییرات (Changed)
- ارتقای نسخه سیستم: `2.0.0` → **`2.1.0`** | نسخه موتور: `3.0.0` → **`3.1.0`**
- `database.sql` — ۲ seed جدید: `websearch_settings` + `telegram_bot_settings`
- `docs/AI-API-GUIDE.md` — ۱۳۲۷ → ۱۵۶۵ خط: بخش ۵.۸-۵.۱۱ (اندپوینت‌های وب + smart-generate با research) + بخش ۱۰ ربات تلگرام + بازشماره‌گذاری جداول
- `README.md` — نشان‌ها + جدول قابلیت‌های v3.1 + ساختار پروژه (telegram/ و services/) + مثال‌های API جدید
- اتولودر `config.php` — پوشه `engine/services/`

### 🧪 تست
- **۸۰ تست جدید** (test-engine-v31.php): تجزیه ۳ ارائه‌دهنده با فیکسچر + ۱۰ مورد SSRF + استخراج هوشمند + ربات (تقسیم پیام/قالب/احراز هویت/توکن مخفی) + فرمان‌های جدید + خط تولید research — **شامل ۳ تست زنده اینترنت موفق**
- **۲۵ تست** تجزیزگر SQL نصب‌کننده (test-splitter.php)
- رگرسیون کامل: v3 (۶۲) + v2 (۷۰) + دانش (۷۱+۲۷) + پایه (۴۳) — همه موفق
- **مجموع سیستم: ۳۷۸ تست موفق**

---

## [2.0.0] — ۱ مهر ۱۴۰۴ (2025-09-23) 🧠 موتور هوش مصنوعی نسخه ۳ «بی‌رقیب»

ارتقای بزرگ نسخه دوم سیستم — موتور AI از «ابزار تولید محتوا» به **«دستیار هوشمند کامل»** ارتقا یافت.

### 🚀 افزودنی‌ها (Added)

#### موتور هوش مصنوعی نسخه ۳
- **🧠 SmartPipeline (خط تولید هوشمند)** — یک فراخوانی = ۸ مرحله خودکار: تشخیص نیت ← انتخاب کلیدواژه کانونی ← تولید مقاله بهترین-از-N ← بهبود خودکار تا امتیاز هدف ← انتخاب عنوان بهینه CTR ← پکیج سئو کامل ← FAQ + اسکیما ← راستی‌آزمایی یکتایی — همراه با ردِیابی زمان‌بندی هر گام
- **🔧 ContentImprover (بهبوددهنده خودکار)** — چرخه خود-درمانی کیفیت: در هر دور، ۳ بُعد ضعیف شناسایی و استراتژی هدفمند اعمال می‌شود (۷ استراتژی: شکستن جمله‌های بلند، جایگزینی مترادف seed-دار، تزریق واژه‌های رابط، قلاب تعاملی/آماری، افزودن هدینگ و لیست، پاراگراف دانش‌محور، تزریق طبیعی کلیدواژه) — با **ضمانت عدم تخریب** (خروج ایمن اگر امتیاز بهبود نیابد)
- **🎙️ BrandVoiceAnalyzer (تحلیل صدای برند)** — استخراج پروفایل لحن از متن‌های برند (رسمیت، فنی‌بودن، محاوره، خوانایی، تنوع واژگان) + ۴ تیپ صدای برند (متخصص راهنما / مشاور قابل‌اعتماد / دوست صمیمی / مرجع تخصصی) + سنجش انطباق متن جدید با صدای برند + توصیه‌های تولید اختصاصی
- **🏷️ TitleGenerator (عنوان‌ساز CTR-محور)** — ۸ الگوی عنوان فارسی (آموزشی/پرسشی/عددی/فصلی/محلی/مقایسه‌ای/راه‌حلی/چک‌لیستی) + امتیازدهی ۵ عامل مؤثر بر نرخ کلیک (طول بهینه ۴۵-۶۰، عدد، سؤال، واژه‌های قدرت، موقعیت کلیدواژه)
- **💬 CommandAssistant (دستیار فرمان فارسی)** — رابط زبان طبیعی: ۹ عملیات با فرمان محاوره‌ای («مقاله بنویس برای اسنوا درباره ماشین لباسشویی»، «امتیاز این متن: ...»، «تشخیص عیب: یخچال سرد نمی‌کند») + استخراج خودکار برند و دستگاه از متن فرمان + پیشنهاد فرمان بعدی
- **⚡ EngineCache (کش موتور)** — لایه کش فایل‌محور با TTL، الگوی remember، آمار hit/miss و پاکسازی خودکار — پاسخ‌های smart-generate تکراری تا ۳۰ دقیقه آنی

#### API نسخه ۳ — ۷ اندپوینت جدید
- `POST /api/ai/smart-generate` — خط تولید هوشمند (پکیج کامل محتوا)
- `POST /api/ai/improve-content` — بهبود خودکار متن
- `POST /api/ai/brand-voice` — پروفایل صدای برند + انطباق
- `POST /api/ai/generate-titles` — عنوان‌سازی CTR-محور
- `POST /api/ai/assistant` — دستیار فرمان فارسی
- `GET /api/ai/cache-stats` — آمار کش موتور
- `POST /api/ai/cache-clear` — پاکسازی کش موتور

#### مستندات
- **🤖 docs/AI-API-GUIDE.md** — راهنمای کامل و مفصل API هوش مصنوعی برای استفاده از بیرون سایت ساز (۱۳۰۰+ خط): جدول مرجع ۳۴ اندپوینت، مستندات تفصیلی با پاسخ‌های واقعی، کلاینت آماده به PHP/Python/JavaScript/PowerShell، ۵ سناریوی واقعی، رفع اشکال و نکات امنیتی — ثبت‌شده در پنل مستندات
- **🔤 assets/fonts/README.md** — جدول دانلود ۳۰ فونت (فارسی/انگلیسی) با لینک رسمی: GitHub Releases (۹ فونت آزاد)، FontIran (۴ فونت تجاری)، Google Fonts (۱۰ فونت انگلیسی)، پورتال‌های معتبر (۷ فونت کلاسیک) + لینک مستقیم CDN + جدول وزن‌ها
- **🖼️ assets/icons/*/README.md** — ۱۰ پک آیکون با لینک رسمی (سایت + مخزن + دانلود مستقیم ZIP) — همه لینک‌ها راستی‌آزمایی‌شده

### 🐛 رفع اشکال (Fixed)
- **رفع باگ بحرانی `generateContent`** — امضای متد با روتر API ناسازگار بود (پارامتر اول رشته بود اما روتر آرایه JSON می‌داد) → اندپوینت `POST /api/ai/generate-content` از بیرون سایت ساز عملاً خطای ۵۰۰ می‌داد — اصلاح شد
- رفع تقدم روتینگ دستیار: فرمان‌های «متن‌دار با دونقطه» (امتیاز/بهبود متن) قبل از «تشخیص عیب» بررسی می‌شوند تا تداخل رخ ندهد

### 🔄 تغییرات (Changed)
- ارتقای نسخه سیستم: `1.1.0` → **`2.0.0`**
- ارتقای نسخه موتور: `2.0.0` → **`3.0.0`**
- `engineInfo()` — افزودن ۶ قابلیت جدید + لیست ژنراتورها/آنالایزرهای v3
- `README.md` اصلی — بازنویسی کامل با قابلیت‌های v3، جدول API، لینک رلیزها و مستندات
- فایل `.gitignore` — مسیر کش موتور (`cache/engine/`)

### 🧪 تست
- **۶۲ تست جدید** (test-engine-v3.php): کش، عنوان‌ساز، صدای برند، بهبوددهنده، خط تولید هوشمند، دستیار فرمان، فاساد v3، رفع باگ generateContent
- رگرسیون کامل: v2 (۷۰) + پایگاه دانش (۷۱) + E2E دانش (۲۷) + موتور پایه (۴۳) — همه موفق
- **مجموع سیستم: ۲۷۳ تست موفق**

### 📦 انتشار (Release)
- رلیز `v2.0.0` با: **بسته نصب کامل** + **بسته بروزرسانی** (از v1.x) + فایل‌های راهنما + چک‌سام SHA256

---

## [1.1.0] — ۲ مهر ۱۴۰۴ (2025-09-23) 🤖 موتور هوش مصنوعی نسخه ۲

### 🚀 افزودنی‌ها (Added)

#### موتور هوش مصنوعی نسخه ۲
- **🏆 QualityScorer** — ماژول جدید امتیازدهی کیفیت محتوا در ۷ بُعد وزنی (خوانایی، ساختار، انسجام، تمرکز بر کلیدواژه، جذابیت، کفایت حجم، تنوع جملات) با خروجی نقاط قوت/ضعف و پیشنهادهای بهبود فارسی
- **🧭 IntentClassifier** — ماژول جدید تشخیص نیت جستجو در ۴ کلاس (اطلاعاتی / تجاری / تراکنشی / ناوبری) با سطح اطمینان و پیشنهاد نوع محتوای مناسب
- **🗓️ ContentPlanner** — ماژول جدید تقویم محتوای هوشمند: برنامه انتشار N هفته‌ای با اولویت‌بندی فصلی (تقویم جلالی)، شکاف پوشش برند، رقابت‌پذیری کلیدواژه، نیت جستجو و ساعت انتشار بهینه
- **📰 ArticleGenerator بهترین-از-N** — تولید چند واریانت (۱ تا ۴) و انتخاب بهترین بر اساس امتیاز کیفیت
- **📌 جعبه «نکات کلیدی» (TL;DR)** — درج خودکار بعد از مقدمه مقالات
- **❓ بخش سوالات متداول + FAQ Schema** — هر مقاله ۳ پرسش‌وپاسخ متداول با داده ساختاریافته FAQPage در JSON-LD
- **🔗 لینک‌دهی مقالات مرتبط** — سیلوی داخلی بین مقالات همان برند
- **🍂 دو نوع مقاله جدید** — «مراقبت فصلی» (مبتنی بر تقویم فصلی زنده) و «راهنمای هزینه» (شفاف‌سازی قیمت تعمیر) — مجموع ۹ نوع مقاله
- **📏 تضمین حداقل حجم** — مقالات همیشه ≥ ۸۰۰ کلمه با گسترش خودکار هوشمند

#### ابزار پردازش زبان (TextProcessor v2)
- **🌱 ریشه‌یاب سبک فارسی** — یکسان‌سازی شکل‌های صرفی کلمه (تعمیر / تعمیرات / تعمیرها)
- **📊 سنجش خوانایی** — ۷ متریک: میانگین طول جمله، نسبت جمله‌های بلند، تنوع واژگان (TTR)، نسبت واژه‌های پیچیده و امتیاز کل ۰-۱۰۰
- **🔗 شمارش واژه‌های رابط** — ۴۵ واژه انسجام‌دهنده فارسی
- توکنایزر عمومی و تقسیم‌کننده جمله عمومی

#### سئو (SeoAnalyzer v2 + KeywordAnalyzer v2)
- ۹ بررسی سئوی جدید: کلیدواژه در سرفصل‌ها، انسجام متن، حضور پرسش، اسکیمای سازگار، کفایت تصویر، تنوع متن انکر، سیگنال‌های E-E-A-T، امتیاز خوانایی — مجموع **۲۶ بررسی وزنی**
- **🧮 TF-IDF** — وزن‌دهی کلیدواژه‌ها نسبت به مجموعه اسناد مرجع
- **🗂️ خوشه‌بندی کلیدواژه** — ساخت Topic Cluster با واژه ستون
- **⚔️ برآورد رقابت‌پذیری** کلیدواژه با تحلیل دلایل
- **🐎 پیشنهاد long-tail** — ترکیب دستگاه × شهر × اصلاح‌گر × الگوی سؤالی

#### API
- ۷ اندپوینت جدید: `GET ai/engine-info`، `POST ai/score-content`، `POST ai/classify-intent`، `POST ai/content-plan`، `POST ai/suggest-longtail`، `POST ai/cluster-keywords`، `POST ai/tfidf`
- پارامتر `variants` در `POST ai/generate-article` برای بهترین-از-N

### 🔧 بهبودها (Changed)
- ارتقای نسخه موتور AI به 2.0.0 با ثابت `SahandAI::ENGINE_VERSION`
- پارامتر `content_type` در امتیازدهی کیفیت با اهداف حجمی متمایز (مقاله/صفحه/معرفی/توضیح)

### 🧪 تست‌ها
- ۷۰ تست جدید موتور نسخه ۲ (`test-engine-v2.php`)
- مجموع تست‌های موفق سیستم: **۲۱۱** (رگرسیون کامل بدون شکست)

---

## [1.0.0] — ۱ مهر ۱۴۰۴ (2025-09-23) 🎉 انتشار اولیه

### 🚀 افزودنی‌ها
- **سایت ساز برند کامل** برای صنعت تعمیرات لوازم خانگی — ۳۳۴ فایل، ~۱۸هزار خط PHP
- **موتور AI داخلی** بدون هیچ API خارجی: ۶ ژنراتور + ۴ آنالایزر + پایگاه دانش ۱۳ فایلی
- **پایگاه دانش تقویت‌شده (فاز K)**: ۷۷ برند، ۴۲ دستگاه، ۱۲۵ کد خطا، ۴۷۵ واژه مترادف، ۲۱ سناریوی عیب‌یابی علامت‌محور، ۲۰ قطعه یدکی، تقویم فصلی ۱۲ ماه جلالی، مناطق ۱۰ شهر
- **پنل مدیریت RTL** فارسی با کپچا GD، تنظیمات ۹ بخشی، مدیریت برندها/مقالات/سئو/کدهای خطا
- **قالب‌ساز درگ‌اند‌دراپ** + ۲۵ قالب آماده + تم + منوی درختی + ۱۰۳ آیکون SVG + ۳۰ فونت
- **تحلیل لوگو K-Means** و استخراج پالت رنگ
- **مرکز سئو** ۴ تب + تولید خودکار Schema.org (LocalBusiness/Article/FAQ/Breadcrumb/Service)
- **API داخلی** ۲۵+ مسیر با احراز هویت API Key و محدودیت نرخ
- **هسته سایت برند** ۳۳ فایل + خروجی ZIP با جایگذاری متغیر
- **نصب‌کننده فارسی** مرحله‌ای + دیتابیس ۳۳ جدول
- **PWA** + مستندات فارسی ۷ سندی + README
