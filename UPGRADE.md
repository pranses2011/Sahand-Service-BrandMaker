# 🔄 راهنمای بروزرسانی — به نسخه ۲.۲۳.۰

> ⚠️ **قبل از هر چیز**: از فایل‌ها و دیتابیس خود بکاپ کامل بگیرید.

---

## 🆕 نسخه ۲.۲۳.۰ — رفع قطعی «استخراج کامل نشد» در استقرار/بروزرسانی

### 📋 چه چیزی تغییر می‌کند؟

| بخش | شرح |
|-----|-----|
| 🚨 رفع «استخراج کامل نشد — index.php یافت نشد» | دو لایه جدید: **پاک‌سازی فایل‌های تضادی** پیش از استخراج (ریشه: fileop با فایل هم‌نام موجود، استخراج را بی‌صدا رها می‌کند — clean_old دقیقاً config.php و .htaccess را حفظ می‌کرد و ZIP هر دو را دارد!) + **موتور نجات «کمکی PHP»** روی خود سایت |
| 🚑 موتور کمکی PHP (بدون FTP) | اگر استخراج cPanel ناکام بماند، فایل کمکی کوچک روی سایت نوشته و با HTTP فراخوانی می‌شود و خودش با ZipArchive بسته را استخراج می‌کند (مسطح‌سازی + مجوزدهی + گزارش JSON + خودحذف) — کاملاً مستقل از fileop و تنظیمات FTP |
| ⚙️ زنجیره سه‌موتوره | fileop ← کمکی PHP ← FTP — با راستی‌آمایی مستقل در هر مرحله + دیاگنوستیک وضعیت هر سه موتور در خطا + راهنمای اقدام |

### 🔧 روش بروزرسانی (پکیج update.zip)

1. از فایل‌ها و دیتابیس بکاپ بگیرید
2. محتویات `update.zip` را روی فایل‌های فعلی استخراج کنید (بازنویسی)
3. **فایل‌های تغییر‌یافته این نسخه** (اگر آپلود انتخابی می‌کنید):
   - `core/Deployer.php` ⚠️ حیاتی — پاک‌سازی تضادها + موتور کمکی PHP + زنجیره سه‌موتوره
   - `config.php` (نسخه سیستم 2.23.0)
   - مستندات: `CHANGELOG.md`، `WORKLOG.md`، `CHECKLIST.md`، `README.md`، `UPGRADE.md`
4. **بدون تغییر دیتابیس** — مهاجرت یا جدول جدیدی لازم نیست
5. ✅ راستی‌آزمایی: ورود به پنل ← پایین صفحه نسخه **2.23.0**

### 🚀 بعد از بروزرسانی (مهم!)

- برای هر برند مستقرشده، یک بار **«بروزرسانی استقرار»** اجرا کنید — این بار حتی اگر استخراج cPanel روی سرورتان مشکل داشته باشد، موتور کمکی PHP خودش بسته را مستقر می‌کند (بدون نیاز به تنظیمات FTP)
- پیام موفقیت اعلام می‌کند کدام موتور کار را انجام داده:
  - «فایل‌ها استخراج و راستی‌آمایی شد» = استخراج معمولی cPanel
  - «با موتور کمکی PHP مستقر شدند» = نجات بدون FTP (کاملاً سالم است)
  - «با مسیر جایگزین FTP مستقر شدند» = نجات FTP
- اگر باز هم خطا دیدید، پیام جدید وضعیت هر سه موتور را شفاف گزارش می‌کند — تنظیمات FTP را در «تنظیمات cPanel» تکمیل کنید تا موتور سوم هم فعال شود

### 🩺 بازیابی سایت‌های مستقرشده در حالت خطا

اگر برندی در میانه بروزرسانی ناموفق مانده باشد (مثلاً فایل‌های سایت پاک شده و استخراج ناموفق مانده باشد):

1. همین نسخه را نصب کنید
2. «بروزرسانی استقرار» همان برند را دوباره اجرا کنید — مرحله استخراج این بار با سه موتور تلاش می‌کند و config.php و .htaccess هم بازنویسی می‌شوند
3. در بدترین حالت، بکاپ خودکار (اگر در تنظیمات فعال بود) از «لاگ استقرار» قابل بازیابی است

### 🔍 روش تشخیص سلامت بعد از استقرار

- تست نهایی خودکار HTTP صفحه اصلی را با هر دو اسکیم بررسی می‌کند و در صورت ناسازگاری .htaccess خودکار به سطح سازگارتر تنزل می‌دهد (خوددرمانی v2.20)
- اگر پیام خطا دیدید، بخش «🩺 تشخیص» آن نمونه پاسخ سرور و انتهای error_log سایت را نشان می‌دهد — ریشه واقعی برای پشتیبانی لو می‌رود

---

## 📚 نسخه‌های قبلی

برای سابقه کامل تغییرات به `CHANGELOG.md` مراجعه کنید. نکات حیاتی نسخه‌های اخیر:

### نسخه ۲.۲۲.۰ — ریشه‌یابی HTTP 500 خط ۱۶ + خطایاب v3.14 + ۱۴ سرویس تصویر
- **مهم**: بعد از بروزرسانی به ۲.۲۲، برای هر برند مستقرشده یک بار «بروزرسانی استقرار» لازم است تا config.php جدید (با تمام توابع کمکی) نوشته شود — رفع صفحات خالی/خطای 500
- خطایاب v3.14: بودجه ۱۳۰ ثانیه + کدهای سه‌رقمی تلویزیون (101/137/324…) و دوهرفی (SE/HE/TE) و چشمک LED + KB ال‌جی مایکروویو و تلویزیون + سقف نرخ جستجو ۲۴۰
- سرویس‌های تولید تصویر ۷ → ۱۴ (شامل AI Horde رایگان بدون کلید) — انتخاب از «تنظیمات ← 🖼️ تولید تصویر»

### نسخه ۲.۲۱.۰ — قالب‌سازی همه‌جانبه
- دکمه «قالب‌ساز» و «✨ طراحی با UI/UX Pro» روی کارت هر صفحه در تب «صفحه‌ها» ویرایش برند
- کتابخانه ۱۷۰ قالب آماده (۱۰ سبک واقعاً متفاوت × ۱۷ نوع صفحه) — با باز کردن صفحه قالب‌ها فعال می‌شود
- دکمه «⏭️ رد کردن مرحله خطادار و ادامه» در ویزارد ساخت برند

### نسخه ۲.۲۰.۰ — تست نهایی هوشمند
- خوددرمانی .htaccess (سه سطح full/safe/minimal) + دوسکیم HTTP/HTTPS + دیاگنوستیک خطای نهایی (نمونه پاسخ سرور + error_log)
