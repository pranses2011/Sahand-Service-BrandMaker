# 🔄 راهنمای بروزرسانی — Sahand Service BrandMaker

نسخه هدف: **2.27.0** (موتور AI: 3.15.0) — [سابقه تغییرات](CHANGELOG.md)

---

## ⚠️ نکته مهم این نسخه (۲.۲۷.۰)

این نسخه **شش ریشه‌یابی** دارد: ① متغیرهای `{{warranty_period}}`/`{{agency_name}}`
خام در مقالات — سه لایه رفع شد (مولد + جاروی نهایی + سوئیپ هنگام خواندن؛ مقالات
قدیمی **بدون بازتولید** تمیز نمایش داده می‌شوند) ② لوگوی سایر برندها با URL مطلق
برمی‌گردد ③ پیش‌نمایش عنصر استخراجی اکنون **دقیقاً مثل سایت مبدا** است (CSS کامل
زیردرخت + مطلق‌سازی آدرس‌ها + ارتفاع خودکار) ④ خطای ۵۰۰ صفحه آمار — ستون‌های
`is_exit`/`duration` با مهاجرت خودکار اضافه می‌شوند ⑤ تنظیمات عناصر تکمیل شد
(آیتم‌های پیش‌فرض همان لحظه قابل ویرایش می‌شوند) ⑥ تغییر تم/قالب از پنل اکنون
**روی سایت‌های برند اعمال می‌شود** (بعد از حداکثر ۲ دقیقه کش). برای فعال‌شدن
رندر بلوک‌های قالب‌ساز روی سایت‌های برندِ مستقرشده، برای هر برند یک‌بار
**«بروزرسانی استقرار»** را اجرا کنید تا فایل جدید `includes/blocks.php` +
`css/blocks.css` + صفحات وصل‌شده منتقل شوند. سایت‌های بدون چیدمان به ساختار
ثابت قبلی برمی‌گردند (سازگار کامل).

## روش ۱ — پکیج بروزرسانی (پیشنهادی — تغییر دیتابیس خودکار)

1. از پوشه فعلی سایت ساز خود **بکاپ کامل** بگیرید (فایل‌ها + دیتابیس).
2. فایل `update.zip` این رلیز را در **ریشه سایت ساز** (کنار `config.php`) آپلود کنید.
3. از فایل‌منیجر هاست (یا SSH با `unzip -o update.zip -d .`) آن را **استخراج** کنید —
   فایل‌های سیستمی جایگزین می‌شوند و `config.php` و `.htaccess` و `uploads/` و `database.sql` دست‌نخورده می‌مانند.
4. فایل `update.zip` را حذف کنید.
5. وارد پنل مدیریت شوید — مهاجرت‌های خودکار (ستون‌های آمار رفتاری
   `is_exit`/`duration`/`page_title`/`viewed_at`) در اولین بازدید اجرا می‌شوند.

## روش ۲ — از سورس گیت‌هاب

```bash
git pull origin main
# اگر تغییرات محلی دارید: git stash && git pull && git stash pop
```

## 📋 پس از بروزرسانی — چک‌لیست تکمیلی

| # | اقدام | لازم/اختیاری |
|---|-------|---------------|
| ۱ | باز کردن «آمار و گزارش‌گیری» + «گزارش کامل» (رفع خطای ۵۰۰ — مهاجرت خودکار در اولین لود) | ⭐ لازم |
| ۲ | صفحه «سایر برندها» در یک سایت برند: نمایش لوگوها | ⭐ لازم (تأیید رفع) |
| ۳ | باز کردن یکی از مقالات قدیمی: نبود هیچ `{{...}}` خام در متن | ⭐ لازم (سوئیپ خودکار) |
| ۴ | برای هر برند مستقرشده: **ویرایش برند ← استقرار ← بروزرسانی استقرار** (انتقال blocks.php + blocks.css + صفحات وصل‌شده) | ⭐ لازم برای اعمال تم |
| ۵ | پنل ← تم‌ها: تغییر تم یک برند (اختصاص تکی) یا تم پیش‌فرض (عمومی) ← بعد از ≤ ۲ دقیقه سایت برند چیدمان جدید را نشان می‌دهد | ⭐ لازم (تأیید رفع) |
| ۶ | قالب‌ساز: انتخاب بلوک چک‌لیست ← پنل ویژگی‌ها ← ویرایش متن آیتم‌ها + «افزودن آیتم» | پیشنهادی (تأیید رفع) |
| ۷ | قالب‌ساز ← «🌐 استخراج از سایت» ← پیش‌نمایش عنصر: ظاهر کامل مثل سایت مبدا (نه فقط متن) | پیشنهادی (تأیید رفع) |

## 📦 فایل‌های تغییر‌یافته این نسخه

```
config.php                                   (نسخه ۲.۲۷.۰ + مهاجرت ستون‌های آمار رفتاری)
engine/utils/TextProcessor.php               (sweepPlaceholders — جاروی متغیرها)
engine/generators/ArticleGenerator.php       (۱۷ متغیر کامل + جاروی نهایی)
api/endpoints/article.php                    (سوئیپ مقالات قدیمی هنگام خواندن)
api/endpoints/template.php                   (حل چهارلایه تم + عناصر شخصی + کش)
api/index.php                                (لوگوی مطلق در GET /api/brands)
core/ElementExtractor.php                    (subtreeCss + injectRootStyle + مطلق‌سازی کامل)
core/ConfigGenerator.php                     (require blocks.php در کانفیگ تولیدی)
admin/analytics.php                           (safeQuery — مقاوم در برابر اسکمای قدیمی)
admin/analytics-report.php                   (safeQuery)
admin/template-builder.php                   (ماده‌سازی آیتم‌ها + فیلد آیکون + کش)
admin/template-preview.php                   (pvItems + ارتفاع خودکار)
admin/themes.php                              (پاک‌سازی کش برند)
templates/brand-core/includes/blocks.php     (🆕 رندرگر ۱۵۰ بلوک روی سایت برند)
templates/brand-core/css/blocks.css          (🆕 استایل بلوک‌ها زیر bb-wrap)
templates/brand-core/config.php              (require blocks.php)
templates/brand-core/includes/header.php     (بارگذاری شرطی blocks.css)
templates/brand-core/index.php               (اتصال به چیدمان API + fallback)
templates/brand-core/pages/services.php      (همان)
templates/brand-core/pages/blog.php          (همان)
templates/brand-core/pages/contact.php       (همان)
templates/brand-core/pages/about-brand.php   (همان)
docs/AI-API-GUIDE.md                         (اندپوینت template چهارلایه + لوگو مطلق)
```

## 🩺 راستی‌آزمایی

1. در پنل: «تنظیمات» ← سلامت سیستم ← نسخه باید **2.27.0** باشد.
2. «آمار و گزارش‌گیری»: بدون خطای ۵۰۰ + نوار وضعیت ردیاب + شاخص‌ها/نمودارها.
3. سایت برند ← «سایر برندها»: لوگوی هر برند (یا حرف اول به‌عنوان fallback).
4. سایت برند ← یک مقاله: متن کامل بدون هیچ `{{...}}`.
5. پنل ← تم‌ها ← تغییر تم برند ← رفرش سایت برند بعد از ≤ ۲ دقیقه: چیدمان جدید.
6. قالب‌ساز ← بلوک چک‌لیست ← پنل ویژگی‌ها: آیتم‌ها قابل ویرایش + افزودن آیتم جدید.

## 🧯 عیب‌یابی

- **آمار هنوز ۵۰۰ می‌دهد؟** مهاجرت در اولین لود هر صفحه ادمین اجرا می‌شود؛ اگر
  پرمیشن ALTER ندارید، دو ستون را دستی اضافه کنید:
  `ALTER TABLE visit_details ADD COLUMN is_exit TINYINT(1) NOT NULL DEFAULT 0, ADD COLUMN duration INT UNSIGNED NULL;`
- **تم اعمال نمی‌شود؟** مطمئن شوید «بروزرسانی استقرار» برای آن برند اجرا شده و
  کش محلی سایت برند (۱۲۰ ثانیه) منقضی شده؛ سپس Ctrl+F5.
- **مقاله قدیمی هنوز {{...}} دارد؟** صفحه را با Ctrl+F5 رفرش کنید (کش مرورگر/API تا ۲ دقیقه).
