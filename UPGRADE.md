# 🔄 راهنمای بروزرسانی — به نسخه ۲.۲۰.۰

> ⚠️ **قبل از هر چیز**: از فایل‌ها و دیتابیس خود بکاپ کامل بگیرید.

---

## 🆕 نسخه ۲.۲۰.۰ — تست نهایی هوشمند: خوددرمانی .htaccess + دوسکیم HTTP + دیاگنوستیک

### 📋 چه چیزی تغییر می‌کند؟

| بخش | شرح |
|-----|-----|
| 🚨 خطای «تست نهایی ناموفق: پاسخ HTTP: 500» | **چهار ریشه مستقل رفع شد**: ① کامنت‌های `/* */` در htaccess.template — سینتکس نامعتبر Apache → خطای 500 (برای نصب دستی ZIP **دائمی** بود!) ② «Require all denied» فقط Apache 2.4 است — روی سرورهای قدیمی cPanel «Invalid command» → 500 قطعی → بلوک‌های دوسینتکس ③ `Options -Indexes` روی هاست‌های با AllowOverride محدود → 500 ④ fallback HTTP فقط روی قطعی اتصال اجرا می‌شد — زیردامنه تازه بدون SSL → HTTPS روی vhost پیش‌فرض → 500 **کاذب** در حالی که HTTP سالم بود |
| 🩹 خوددرمانی .htaccess | در صورت 500 در تست نهایی، `.htaccess` خودکار به سطح سازگارتر تنزل و دوباره تست می‌شود: **full** (کامل) ← **safe** (بدون Options و بلوک‌های Files — ریدایرکت/کش/فشرده‌سازی حفظ) ← **minimal** (فقط DirectoryIndex + 404 + URLهای تمیز) — سطح فعال در پیام و لاگ شفاف اعلام می‌شود؛ `.htaccess` دیگر **هرگز** استقرار را متوقف نمی‌کند |
| 🔀 تست نهایی دوسکیم + چندتلاش | fallback HTTP روی «هر» پاسخ غیرقابل‌قبول HTTPS (نه فقط قطعی اتصال) + تا ۳ تلاش با فاصله ۵ ثانیه (گرم‌شدن vhost / صدور AutoSSL) + سقف ریدایرکت ۵ |
| 🩺 دیاگنوستیک شکست | پیام خطا حالا شامل: نمونه پاسخ سرور + **انتهای error_log سایت** (خوانده‌شده از cPanel API) + کد هر دو اسکیم — اگر خطایی بماند، **ریشه واقعی** (PHP فاتال؟ دایرکتیو نامعتبر؟ vhost؟) مستقیم در پیام نمایش داده می‌شود |

**بدون تغییر دیتابیس** — فقط فایل‌های زیر تغییر کرده‌اند:

| فایل | تغییر |
|------|-------|
| `core/HtaccessGenerator.php` | سه سطح سازگاری + دوسینتکس Apache 2.2/2.4 |
| `core/Deployer.php` | تست نهایی دوسکیم + retry + خوددرمانی + دیاگنوستیک + seam های تست |
| `templates/brand-core/htaccess.template` | رفع کامنت‌های نامعتبر Apache |
| `config.php` | نسخه سیستم 2.20.0 |

### 🧪 تست حیاتی بعد از بروزرسانی

بعد از بروزرسانی، یک استقرار آزمایشی (یا بروزرسانی مجدد یک برند موجود) انجام دهید — باید تا مرحله «تست نهایی» برسد و پاس کند. اگر سرور شما با دایرکتیوهای کامل سازگار نباشد، در لاگ استقرار پیام «خوددرمانی موفق: .htaccess در سطح safe/minimal فعال شد» را می‌بینید — سایت کاملاً کار می‌کند و این فقط یعنی هاردنینگ اضافی روی سرور شما پشتیبانی نمی‌شود.

### 🚀 روش بروزرسانی (پکیج update.zip)

1. **بکاپ بگیرید** (فایل‌ها + دیتابیس)
2. پکیج `update.zip` نسخه ۲.۲۰.۰ را دانلود کنید
3. محتویات را روی فایل‌های فعلی باز کنید (overwrite)
4. صفحه پنل را رفرش کنید — بج نسخه باید **2.20.0** نشان دهد
5. یک استقرار آزمایشی انجام دهید تا سلامت زنجیره تأیید شود

### 📦 روش نصب تازه (پکیج install.zip)

نصب کامل مثل قبل — از `docs/installation.html` یا `README.md` بخش نصب پیروی کنید. از این نسخه، ZIP سایت‌های برند تولیدی هم `.htaccess` سالم دارند (باگ کامنت نامعتبر رفع شد).

---

## 📚 سابقه بروزرسانی‌ها

- **2.19.1** — راستی‌آمایی استقرار: پارس چندقالبی لیست + استعلام مستقیم + آپلود overwrite
- **2.19.0** — استخراج با مکان‌یابی + بازیابی خودکار + پارس دو-قالبی UAPI
- **2.18.0** — ممیزی کامل cPanel + رفع ۸ خانواده باگ fileop
- **2.17.0** — خطایاب ریشه‌ای (پل jina) + قالب‌ساز ۱۳۰ عنصر
- **2.16.0** — ریشه‌یابی نهایی خطای mkdir استقرار
- **2.15.0** — رفع نهایی استقرار + خطایاب ۱۱ ارائه‌دهنده + ۷ سرویس تصویر + قالب‌ساز ۱۱۴ عنصر
