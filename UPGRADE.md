# 🔄 راهنمای بروزرسانی — به نسخه ۲.۲۲.۰

> ⚠️ **قبل از هر چیز**: از فایل‌ها و دیتابیس خود بکاپ کامل بگیرید.

---

## 🆕 نسخه ۲.۲۲.۰ — ریشه‌یابی قطعی خطاهای استقرار + خطایاب همه‌جانبه + ۱۴ سرویس تصویر

### 📋 چه چیزی تغییر می‌کند؟

| بخش | شرح |
|-----|-----|
| 🚨 رفع HTTP 500 خط ۱۶ + صفحات خالی | فایل `config.php` سایت‌های مستقرشده اکنون با تمام توابع کمکی تولید می‌شود — بعد از بروزرسانی، **یک بار «بروزرسانی استقرار» برای هر برند مستقرشده اجرا کنید** تا config.php جدید نوشته شود و سایت‌ها سالم شوند |
| 🚨 رفع «استخراج کامل نشد» | راستی‌آمایی حجم ZIP + نجات FTP کامل — استقرار روی سرورهای مشکل‌دار fileop هم کار می‌کند |
| 🔍 خطایاب v3.14 | بودجه ۱۳۰ث + کدهای سه‌رقمی/دوهرفی/چشمک LED + دیکشنری تلویزیون-مایکروویو + KB ال‌جی مایکروویو (SE/FE/TE/EE/PF) و تلویزیون webOS (101/105/137/142/152/324/501) |
| 📈 سقف نرخ جستجو | ۶۰ → ۲۴۰ در ساعت (خودکار با مهاجرت v222 — نیازی به کار دستی نیست) |
| 🖼️ سرویس‌های تصویر | ۷ → ۱۴ سرویس در «تنظیمات ← 🖼️ تولید تصویر» — انتخاب از لیست + تست سلامت + گزارش سرویس واقعی |

### 🔧 روش بروزرسانی (پکیج update.zip)

1. از فایل‌ها و دیتابیس بکاپ بگیرید
2. محتویات `update.zip` را روی فایل‌های فعلی استخراج کنید (بازنویسی)
3. **فایل‌های تغییر‌یافده این نسخه** (اگر آپلود انتخابی می‌کنید):
   - `config.php` (نسخه سیستم 2.22.0 + مهاجرت v222)
   - `core/ConfigGenerator.php` ⚠️ حیاتی — رفع HTTP 500
   - `core/Deployer.php` + `core/CpanelAPI.php` — استخراج ضدگلوله
   - `engine/generators/ErrorCodeEngine.php` — خطایاب v3.14
   - `engine/generators/SmartPipeline.php` — اتصال سرویس تصویر انتخابی
   - `engine/services/AiPhotoService.php` — ۱۴ سرویس
   - `engine/services/AiImageGenerator.php` + `engine/services/WebSearchService.php`
   - `engine/SahandAI.php` (نسخه موتور 3.14.0)
   - `engine/knowledge/error-codes-brands.json` — کدهای جدید KB
   - `templates/brand-core/config.php` — همگام‌سازی توابع
   - `admin/settings.php` + `admin/articles.php`
   - `database.sql` (فقط برای نصب تازه)
   - مستندات: `CHANGELOG.md`، `WORKLOG.md`، `CHECKLIST.md`، `README.md`، `UPGRADE.md`، `docs/AI-API-GUIDE.md`
4. مهاجرت v222 **خودکار** با اولین باز شدن پنل اجرا می‌شود (سقف نرخ جستجو ۶۰→۲۴۰) — بدون تغییر جدول‌ها
5. ✅ راستی‌آزمایی: ورود به پنل ← پایین صفحه نسخه **2.22.0** + «تنظیمات ← جستجوی وب» سقف ۲۴۰

### 🚀 بعد از بروزرسانی (مهم!)

برای برندهایی که قبلاً مستقر شده‌اند و سایتشان **خطای ۵۰۰ یا صفحات خالی** دارد:
1. پنل ← برند ← **«بروزرسانی استقرار»** را اجرا کنید — فایل config.php جدید (با تمام توابع) نوشته می‌شود
2. اگر استقرار قبلاً در مرحله «استخراج» شکست می‌خورد، دوباره اجرا کنید — این بار یا استخراج سالم یا نجات FTP خودکار
3. برای خطایاب: صفحه «کدهای خطا» ← تولید کدهای مایکروویو/تلویزیون ال‌جی — حالا با بودجه ۱۳۰ ثانیه و کدهای KB داخلی حداقل ۵-۷ کد پایه فوراً ثبت می‌شوند و کدهای وب هم با قدرت کامل جستجو می‌آیند

### 🧪 تست سلامت بعد از بروزرسانی

| # | بررسی | روش |
|---|-------|-----|
| 1 | نسخه سیستم | پایین پنل: `2.22.0` |
| 2 | سقف نرخ جستجو | تنظیمات ← جستجوی وب ← ۲۴۰ |
| 3 | سرویس تصویر | تنظیمات ← 🖼️ تولید تصویر ← ۱۴ سرویس در لیست + دکمه تست |
| 4 | سایت مستقرشده | باز کردن دامنه برند — صفحات محتوا دارند (نه خالی) |
| 5 | خطایاب | کدهای خطا ← تولید برای ال‌جی مایکروویو |

### 📜 سابقه نسخه‌ها

| نسخه | نکته کلیدی |
|------|-----------|
| 2.22.0 | ریشه‌یابی HTTP 500 خط ۱۶ + استخراج ضدگلوله + خطایاب v3.14 + ۱۴ سرویس تصویر |
| 2.21.0 | دکمه قالب‌ساز/UI-UX Pro روی هر صفحه + کتابخانه ۱۷۰ قالب + رد کردن مرحله |
| 2.20.0 | تست نهایی هوشمند: خوددرمانی .htaccess + دوسکیم + دیاگنوستیک |
| 2.19.x | استخراج با مکان‌یابی + پارس چندقالبی لیست + overwrite |
