# 🤖 راهنمای کامل و مفصل API هوش مصنوعی سهند — نسخه ۳.۱۱.۰ «⏱️ خطایاب مقاوم در برابر پایان زمان سرور + 🖼️ ۴ سرویس عکس رایگان جدید (مجموع ۷ بدون کلید از ۱۸)»

> 📖 **این سند، مرجع کامل استفاده از API موتور هوش مصنوعی سایت ساز برند سهند سرویس است — برای استفاده از بیرون از خود سایت ساز (رابط کاربری).**
> هر آنچه برای اتصال هر برنامه خارجی (PHP، پایتون، جاوااسکریپت، اپ موبایل، ربات تلگرام، n8n، Postman و...) به موتور AI نیاز دارید، در همین فایل است.

| 📌 مشخصات | مقدار |
|-----------|-------|
| 🏷️ نسخه موتور | **3.15.0** (خط تولید هوشمند + بهبود خودکار HTML-امن + دستیار فارسی + 🌐 جستجوی آنلاین ۱۱ ارائه‌دهنده + 🎨 اسکیل UI/UX Pro + 🚨 خطایاب v3.15 — مقاوم در برابر پایان زمان مجاز سرور: shutdown handler پاسخ JSON معتبر برمی‌گرداند + فرانت پاسخ HTML را تشخیص داده و پیام اقدام‌پذیر می‌دهد + تلاش مجدد ادامه‌دهنده (کدهای موجود رد می‌شوند) — و تمام قابلیت‌های v3.14: بودجه ۱۳۰ث + راند نجات همیشه + سرچر مشترک + تفکیک «شکست زیرساخت» از «شاهد منفی» + واریانت کد + الگوهای دوهرفی و چشمک LED + کدهای سه‌رقمی + دیکشنری/قطعات تلویزیون-مایکروویو + پنجره کد-محور + KB ال‌جی — مجموع ۱۶۸ کد) + 📸 عکس واقعی AI مرتبط با موضوع مقاله (**۱۸ سرویس انتخابی از تنظیمات — ۷ رایگان بدون کلید**: پولینیشنز Flux/Turbo + AI Horde + 🆕 لورم‌فلیکر (عکس استوک واقعی موضوعی) + 🆕 ویکیمدیا کامانز (عکس واقعی دانشنامه‌ای با فیلتر JPEG/PNG ≥۶۴۰px) + 🆕 لکسیکا (آرشیو تصاویر SD) + 🆕 پیکسام (جایگزین اضطراری)؛ کلید-رایگان: HuggingFace/DeepAI/Together/fal/Imagen/GetImg؛ اشتراکی: Stability/OpenAI×۲/Ideogram/Replicate + زنجیره fallback خودکار + گزارش سرویس به‌ازای هر تصویر + هم‌سازی ۱۶:۹ + کلیدواژه استوک اختصاصی هر ۴۲ دستگاه) |
| 🏷️ نسخه سیستم | **2.26.0** |
| 🔢 تعداد اندپوینت‌های AI | **۵۴ اندپوینت** + 🚀 **۱۳ اندپوینت استقرار خودکار** (deploy/health/backup — v2.11) + 🤖 وب‌هوک ربات تلگرام + 💬 وب‌هوک ربات بله |
| 🔐 احراز هویت | هدر `X-API-Key` |
| 📦 قالب داده | JSON (UTF-8) |
| 🌐 CORS | باز (`*`) — قابل فراخوانی از مرورگر |
| 🚦 محدودیت نرخ | ۱۲۰ درخواست در دقیقه برای هر IP |
| 📨 ثبت درخواست از سایت برند | فرم سایت برند از v2.25 ابتدا به پروکسی همان‌مبدأ (`/js/form-submit.php`) ارسال می‌شود و آن به `POST /api/brand/{id}/request` وصل است — قرارداد API تغییر نکرده |
| 📊 ردیاب بازدید (v2.26) | `POST /api/track` عمومی است (بدون کلید) و قالب درخواست تغییر نکرده — بهبود: پیام خروج (`is_exit: true` + `duration`) اکنون ردیف ورود همان صفحه را **بروزرسانی** می‌کند (نه درج تکراری) → نرخ پرش و میانگین مدت حضور واقعی ثبت می‌شوند؛ Content-Type درخواست `text/plain` (بدون preflight CORS) است و بدنه JSON خوانده می‌شود |

---

## 📑 فهرست مطالب

1. [شروع سریع — ۳۰ ثانیه‌ای](#۱-شروع-سریع--۳۰-ثانیهای)
2. [احراز هویت و دریافت کلید API](#۲-احراز-هویت-و-دریافت-کلید-api)
3. [قالب درخواست و پاسخ، خطاها و محدودیت‌ها](#۳-قالب-درخواست-و-پاسخ-خطاها-و-محدودیتها)
4. [نقشه کامل اندپوینت‌ها (جدول مرجع)](#۴-نقشه-کامل-اندپوینتها-جدول-مرجع)
5. [مستندات تفصیلی — موتور نسخه ۳ «بی‌رقیب»](#۵-مستندات-تفصیلی--موتور-نسخه-۳-بی̆رقیب)
6. [مستندات تفصیلی — تحلیلگرهای نسخه ۲](#۶-مستندات-تفصیلی--تحلیلگرهای-نسخه-۲)
7. [مستندات تفصیلی — تولیدکننده‌های پایه (نسخه ۱)](#۷-مستندات-تفصیلی--تولیدکنندههای-پایه-نسخه-۱)
8. [مستندات تفصیلی — پایگاه دانش و عیب‌یابی](#۸-مستندات-تفصیلی--پایگاه-دانش-و-عیبیابی)
9. [دستیار فرمان فارسی — راهنمای کامل](#۹-دستیار-فرمان-فارسی--راهنمای-کامل)
10. [🤖 ربات تلگرام متصل به دستیار — راه‌اندازی و استفاده](#۱۰-ربات-تلگرام-متصل-به-دستیار--راهاندازی-و-استفاده)
11. [نمونه کد کامل به زبان‌های مختلف](#۱۱-نمونه-کد-کامل-به-زبانهای-مختلف)
12. [سناریوهای واقعی استفاده](#۱۲-سناریوهای-واقعی-استفاده)
13. [کدهای خطا و رفع اشکال](#۱۳-کدهای-خطا-و-رفع-اشکال)
14. [بهترین شیوه‌ها و نکات امنیتی](#۱۴-بهترین-شیوهها-و-نکات-امنیتی)
15. [تاریخچه تغییرات API](#۱۵-تاریخچه-تغییرات-api)

---

## ۱) شروع سریع — ۳۰ ثانیه‌ای

موتور AI سهند یک سرویس **کاملاً مستقل از رابط کاربری** است. کافی است آدرس سایت ساز شما نصب شده باشد و یک کلید API داشته باشید:

```bash
# 🧪 تست سلامت موتور — بدون نیاز به کلید!
curl "https://YOUR-DOMAIN.com/api/ai/engine-info"
```

پاسخ:

```json
{
  "success": true,
  "data": {
    "engine_version": "3.0.0",
    "system_version": "2.0.0",
    "capabilities": {
      "smart_pipeline": true,
      "auto_improve": true,
      "brand_voice": true,
      "ctr_titles": true,
      "nl_assistant": true,
      "response_cache": true
    }
  }
}
```

✅ اگر این پاسخ را دیدید، موتور آماده است. حالا اولین فراخوانی **هوشمند** با کلید:

```bash
# 🚀 خط تولید هوشمند: یک فراخوانی = مقاله کامل + سئو + FAQ + تضمین کیفیت
curl -X POST "https://YOUR-DOMAIN.com/api/ai/smart-generate" \
  -H "Content-Type: application/json" \
  -H "X-API-Key: YOUR_API_KEY" \
  -d '{"brand_id": 1, "device_key": "washing_machine", "variants": 3, "target_score": 85}'
```

> ⚠️ در همه مثال‌های این سند، `https://YOUR-DOMAIN.com` را با دامنه واقعی نصب سایت ساز و `YOUR_API_KEY` را با کلید واقعی خود جایگزین کنید.

---

## ۲) احراز هویت و دریافت کلید API

### 🗝️ کلید API چیست و از کجا می‌آید؟

موتور AI با **کلید API** احراز هویت می‌شود. کلیدها در جدول `api_keys` دیتابیس ذخیره می‌شوند و سه راه برای دریافت آن وجود دارد:

| روش | توضیح |
|------|-------|
| ۱️⃣ نصب خودکار | هنگام نصب سیستم از طریق `install.php`، یک کلید اصلی به‌صورت خودکار ساخته و نمایش داده می‌شود |
| ۲️⃣ پنل مدیریت | مسیر: **پنل مدیریت → تنظیمات → بخش API** — ساخت/ابطال کلید |
| ۳️⃣ مستقیم دیتابیس | درج رکورد در جدول `api_keys` (فقط برای مدیر فنی) |

### 📤 نحوه ارسال کلید (دو روش)

**روش ۱ — هدر (توصیه‌شده):**

```http
POST /api/ai/score-content HTTP/1.1
Host: YOUR-DOMAIN.com
Content-Type: application/json
X-API-Key: sk_sahand_xxxxxxxxxxxx
```

**روش ۲ — پارامتر کوئری (برای GET ساده):**

```
GET /api/ai/knowledge/stats?api_key=sk_sahand_xxxxxxxxxxxx
```

### 📋 قواعد کلید

- قالب: حروف/اعداد انگلیسی، خط تیره و آندرلاین — بین ۱۰ تا ۷۰ کاراکتر (`^[a-zA-Z0-9_\-]{10,70}$`)
- کلید می‌تواند به یک برند خاص محدود باشد (`brand_id`) یا عمومی باشد
- کلیدهای نامعتبر با تأخیر امنیتی ۰.۵ ثانیه‌ای رد می‌شوند (ضد حمله کشف کلید)
- آمار استفاده هر کلید (تعداد درخواست، آخرین استفاده) به‌صورت خودکار ثبت می‌شود

> 🔒 **هرگز کلید API را در کد فرانت‌اند (جاواسکریپت مرورگر) قرار ندهید** — کلید همیشه باید در سرور شما بماند (بخش ۱۳ را ببینید).

---

## ۳) قالب درخواست و پاسخ، خطاها و محدودیت‌ها

### 📥 قالب درخواست

- همه اندپوینت‌های POST بدنه **JSON** می‌پذیرند: `Content-Type: application/json`
- اندپوینت‌های GET پارامتر خود را از **مسیر یا کوئری‌استرینگ** می‌گیرند
- انکودینگ اجباری: **UTF-8** (متن فارسی باید به‌درستی انکود شود)

### 📤 قالب پاسخ موفق

```json
{
  "success": true,
  "data": { ... }   // خروجی اصلی هر اندپوینت
}
```

### ❌ قالب پاسخ ناموفق

```json
{
  "success": false,
  "error": "پیام خطا به زبان فارسی"
}
```

کد HTTP هم‌زمان با خطا تغییر می‌کند (۴۰۰، ۴۰۱، ۴۰۴، ۴۲۹، ۵۰۰).

### 🚦 محدودیت نرخ (Rate Limit)

| مورد | مقدار |
|------|-------|
| سقف | **۱۲۰ درخواست در دقیقه** برای هر IP |
| پاسخ پس از عبور از سقف | HTTP `429` + هدر `Retry-After: 60` |
| بازنشانی شمارنده | هر ۶۰ ثانیه به‌صورت خودکار |

```json
{ "success": false, "error": "محدودیت نرخ درخواست — لطفاً یک دقیقه دیگر تلاش کنید" }
```

> 💡 نکته: اندپوینت‌های پرتکرار مثل `ai/smart-generate` به‌صورت داخلی **کش ۳۰ دقیقه‌ای** دارند؛ فراخوانی تکراری با پارامتر یکسان، عملاً رایگان و آنی است.

### 🌐 CORS (فراخوانی از مرورگر)

سرور این هدرها را برای همه پاسخ‌ها می‌فرستد:

```http
Access-Control-Allow-Origin: *
Access-Control-Allow-Methods: GET, POST, OPTIONS
Access-Control-Allow-Headers: Content-Type, X-API-Key
```

پس درخواست‌های AJAX از هر دامنه‌ای **از نظر فنی** ممکن است — اما به دلیل افشای کلید، توصیه نمی‌شود (بخش ۱۳).

---

## ۴) نقشه کامل اندپوینت‌ها (جدول مرجع)

### 🚀 موتور نسخه ۳ «بی‌رقیب»

| # | متد | مسیر | کار | کلید API |
|:-:|:---:|------|-----|:--------:|
| ۱ | POST | `/api/ai/smart-generate` | 🧠 خط تولید هوشمند: یک فراخوانی = مقاله + سئو + FAQ + تضمین کیفیت | ✅ |
| ۲ | POST | `/api/ai/improve-content` | 🔧 بهبود خودکار متن تا امتیاز هدف | ✅ |
| ۳ | POST | `/api/ai/brand-voice` | 🎙️ پروفایل صدای برند + انطباق متن | ✅ |
| ۴ | POST | `/api/ai/generate-titles` | 🏷️ عنوان‌سازی CTR-محور با ۸ الگو | ✅ |
| ۵ | POST | `/api/ai/assistant` | 💬 دستیار فرمان زبان طبیعی فارسی | ✅ |
| ۶ | GET | `/api/ai/cache-stats` | ⚡ آمار کش موتور | ✅ |
| ۷ | POST | `/api/ai/cache-clear` | 🧹 پاکسازی کش موتور | ✅ |

### 🌐 موتور نسخه ۳.۱ — جستجوی آنلاین وب (۹ ارائه‌دهنده: کلیددار + رایگان با جایگزینی خودکار و مسابقه موازی)

| # | متد | مسیر | کار | کلید API |
|:-:|:---:|------|-----|:--------:|
| ۸ | POST | `/api/ai/web-search` | 🔎 جستجوی آنلاین اینترنت (۹ ارائه‌دهنده + مسابقه موازی + ویکی‌پدیا — v3.3) | ✅ |
| ۹ | POST | `/api/ai/research` | 🧪 تحقیق ساختاریافته: کلیدواژه ترند + سؤالات واقعی + داده تازه + فرصت‌ها | ✅ |
| ۱۰ | GET | `/api/ai/websearch-status` | 📊 وضعیت ارائه‌دهندگان + نرخ + کش | ❌ (عمومی) |

### 🌟 موتور نسخه ۳.۲ — اخبار زنده + تصاویر مقاله

| # | متد | مسیر | کار | کلید API |
|:-:|:---:|------|-----|:--------:|
| ۳۸ | POST | `/api/ai/news` | 📰 جستجوی اخبار زنده فارسی (Google News RSS — روند/قیمت روز) | ✅ |
| ۳۹ | GET | `/api/ai/article-images` | 🖼️ فهرست ۹ تصویر تخصصی مقاله با URL مطلق | ❌ (عمومی) |

### 🏅 موتور نسخه ۳.۳ — سئوی E-E-A-T + تحلیل پیشرفته (جدید v2.3)

| # | متد | مسیر | کار | کلید API |
|:-:|:---:|------|-----|:--------:|
| ۴۰ | POST | `/api/ai/seo-package` | 🏅 پکیج کامل سئو: متا + اسکیما + E-E-A-T + موجودیت‌ها + خوشه معنایی + شکاف محتوایی | ✅ |
| ۴۱ | POST | `/api/ai/eeat-check` | ✅ چک‌لیست اعتماد E-E-A-T هشت‌معیاری با راهنمای رفع | ✅ |


### 📝 موتور نسخه ۳.۴ — نگارش فارسی + عنوان دلخواه + بهبودگر سئو (جدید v2.4 — فاز Q)

| # | متد | مسیر | کار | کلید API |
|:-:|:---:|------|-----|:--------:|
| ۴۲ | POST | `/api/ai/suggest-article-titles` | 🎯 پیشنهاد بهترین عنوان سئو برای عنوان دلخواه (تحلیل + ۸ واریانت رتبه‌بندی‌شده) | ✅ |
| ۴۳ | POST | `/api/ai/article-seo-stats` | 📊 آمار کامل سئوی مقاله — ۱۲ سنجه وزنی + امتیاز ۰-۱۰۰ | ✅ |
| ۴۴ | POST | `/api/ai/improve-article-seo` | 🎯 بهبود خودکار سئوی مقاله — ۸ اصلاح + گزارش قبل/بعد + ذخیره | ✅ |

### 🎨 موتور نسخه ۳.۵ — اسکیل UI/UX Pro (جدید v2.5)

| # | متد | مسیر | کار | کلید API |
|:-:|:---:|------|-----|:--------:|
| ۴۵ | POST | `/api/ai/uiux-design` | 🪄 طراحی چیدمان حرفه‌ای هر صفحه (۱۷ نوع صفحه — بلوک‌ها + امتیاز UX + دلیل هر بخش) | ✅ |
| ۴۶ | POST | `/api/ai/uiux-review` | 🔍 ممیزی UX چیدمان موجود — امتیاز ۰-۱۰۰ + مشکلات اولویت‌بندی‌شده + نقاط قوت | ✅ |
| ۴۷ | POST | `/api/ai/uiux-improve` | 🛠️ اصلاح خودکار چیدمان — هیرو، CTA، ریتم بصری، حذف شلوغی + امتیاز قبل/بعد | ✅ |
| ۴۸ | GET | `/api/ai/uiux-skill-info` | 📇 فراداده اسکیل (قوانین UX، توکن‌های طراحی، انواع صفحه پشتیبانی‌شده) | ✅ |

### 🚨 موتور نسخه ۳.۶ — خطایاب AI + تصاویر AI + خودیادگیر (جدید v2.6)

| # | متد | مسیر | کار | کلید API |
|:-:|:---:|------|-----|:--------:|
| ۴۹ | POST | `/api/ai/error-codes-generate` | 🚨 تولید همه کدهای خطای واقعی یک برند+دستگاه (پایگاه دانش ۱۵۰ کدی + جستجوی آنلاین فارسی/انگلیسی با منبع‌یابی) — ساختار ۱۴ فیلدی | ✅ |
| ۵۰ | POST | `/api/ai/error-code-improve-field` | ✨ بهینه‌سازی و یکتاسازی هر فیلد کد خطا با AI (عنوان/شرح/دلایل/راه‌حل/قطعه/مشخصات/محل) | ✅ |
| ۵۱ | POST | `/api/ai/generate-og-image` | 🎨 تولید تصویر OG یکتای مرتبط با محتوای صفحه/مقاله (PNG فارسی‌شکل‌یافته یا SVG) | ✅ |
| ۵۲ | GET | `/api/ai/learning/lessons` | 🧠 فهرست درس‌های آموخته‌شده موتور خودیادگیر + آمار کلی | ✅ |
| ۵۳ | POST | `/api/ai/learning/toggle` | 🔘 فعال/غیرفعال کردن یک درس خودیادگیر (غیرفعال = دیگر اعمال نمی‌شود) | ✅ |
| ۵۴ | POST | `/api/bale/webhook` | 💬 وب‌هوک ربات بله (راز مشترک پارامتری) | ❌ (عمومی) |

### 🧠 تحلیلگرهای نسخه ۲

| # | متد | مسیر | کار | کلید API |
|:-:|:---:|------|-----|:--------:|
| ۱۱ | GET | `/api/ai/engine-info` | ℹ️ نسخه و قابلیت‌های موتور | ❌ (عمومی) |
| ۱۲ | POST | `/api/ai/score-content` | 🏆 امتیاز کیفیت ۷ بُعدی | ✅ |
| ۱۳ | POST | `/api/ai/classify-intent` | 🧭 تشخیص نیت جستجو (۴ نیت) | ✅ |
| ۱۴ | POST | `/api/ai/content-plan` | 🗓️ تقویم هوشمند انتشار محتوا | ✅ |
| ۱۵ | POST | `/api/ai/suggest-longtail` | 🐎 کلیدواژه‌های long-tail + رقابت‌پذیری | ✅ |
| ۱۶ | POST | `/api/ai/cluster-keywords` | 🗂️ خوشه‌بندی کلیدواژه (Topic Cluster) | ✅ |
| ۱۷ | POST | `/api/ai/tfidf` | 🧮 وزن‌دهی TF-IDF نسبت به پایگاه دانش | ✅ |

### 📚 پایگاه دانش و عیب‌یابی (فاز K)

| # | متد | مسیر | کار | کلید API |
|:-:|:---:|------|-----|:--------:|
| ۱۸ | GET | `/api/ai/templates` | 📄 قالب‌های محتوای موجود | ✅ |
| ۱۹ | GET | `/api/ai/knowledge` | 🏷️ فهرست برندهای پایگاه دانش (۷۷ برند) | ✅ |
| ۲۰ | GET | `/api/ai/knowledge/stats` | 📊 آمار پایگاه دانش | ✅ |
| ۲۱ | GET | `/api/ai/knowledge/{brand}` | 🔎 اطلاعات دانشی یک برند | ✅ |
| ۲۲ | GET | `/api/ai/diagnostics` | 🩺 فهرست دستگاه‌های عیب‌یابی | ✅ |
| ۲۳ | GET | `/api/ai/diagnostics/{device}` | 🩺 سناریوهای عیب‌یابی یک دستگاه | ✅ |
| ۲۴ | POST | `/api/ai/diagnose` | 🎯 تشخیص سه‌سطحی علامت → علت → قطعه | ✅ |
| ۲۵ | GET | `/api/ai/seasonal` | 📅 تقویم فصلی ماه جاری | ✅ |
| ۲۶ | GET | `/api/ai/seasonal/{month}` | 📅 تقویم فصلی یک ماه مشخص | ✅ |
| ۲۷ | GET | `/api/ai/parts` | 🔩 فهرست قطعات پایگاه دانش | ✅ |
| ۲۸ | GET | `/api/ai/parts/{key}` | 🔩 مشخصات یک قطعه | ✅ |

### ⚙️ تولیدکننده‌های پایه (نسخه ۱)

| # | متد | مسیر | کار | کلید API |
|:-:|:---:|------|-----|:--------:|
| ۲۹ | POST | `/api/ai/generate-content` | ✍️ تولید محتوای صفحه (۷ نوع) | ✅ |
| ۳۰ | POST | `/api/ai/generate-article` | 📰 تولید مقاله (**۲۵ نوع**، عنوان دلخواه، جستجوی آنلاین، تصاویر خودکار — فاز Q) | ✅ |
| ۳۱ | POST | `/api/ai/generate-seo` | 🔍 تولید پکیج سئو صفحه | ✅ |
| ۳۲ | POST | `/api/ai/generate-brand-info` | 🏭 پکیج کامل برند (صفحات+FAQ+کد خطا) | ✅ |
| ۳۳ | POST | `/api/ai/generate-faq` | ❓ تولید سوالات متداول | ✅ |
| ۳۴ | POST | `/api/ai/analyze-keywords` | 🔑 استخراج کلیدواژه + چگالی | ✅ |
| ۳۵ | POST | `/api/ai/check-uniqueness` | ✅ بررسی یکتایی متن | ✅ |
| ۳۶ | POST | `/api/ai/suggest-improvements` | 💡 تحلیل جامع سئو + یکتایی | ✅ |
| ۳۷ | POST | `/api/ai/suggest-topics` | 💡 پیشنهاد موضوع مقاله | ✅ |

> 🌐 علاوه بر این‌ها، APIهای سایت برند (خارج از هوش مصنوعی) نیز موجودند: `brand/{id}`، `brand/{id}/articles`، `brand/{id}/faqs` و... — این سند فقط APIهای موتور AI را پوشش می‌دهد.

---

## ۵) مستندات تفصیلی — موتور نسخه ۳ (v3.0 تا v3.5)

### ۵.۱ 🧠 خط تولید هوشمند — `POST /api/ai/smart-generate`

**مهم‌ترین اندپوینت سیستم.** با یک فراخوانی، ۱۱ مرحله به‌صورت خودکار اجرا می‌شود (v3.2):

```
۱️⃣ تشخیص نیت جستجو  →  ۲️⃣ انتخاب کلیدواژه کانونی  →  ۳️⃣ تحقیق آنلاین وب (خودکار در صورت فعال بودن)
→  ۴️⃣ تولید مقاله (بهترین-از-N)  →  ۵️⃣ بهبود خودکار تا امتیاز هدف (پیش‌فرض ۸۸)
→  ۶️⃣ غنی‌سازی ساختاری: فهرست مطالب + سایر پرسش‌های کاربران (PAA) + آمار تازه وب + جعبه E-E-A-T
→  ۷️⃣ انتخاب و درج ۳ تصویر مرتبط  →  ۸️⃣ عنوان بهینه CTR  →  ۹️⃣ پکیج سئو کامل (+ تراکم کلیدواژه + HowTo)
→  🔟 سوالات متداول + FAQ Schema (+ سؤالات واقعی وب)  →  1️⃣1️⃣ راستی‌آزمایی یکتایی
```

#### پارامترها

| پارامتر | نوع | الزامی | پیش‌فرض | توضیح |
|---------|:---:|:------:|:-------:|-------|
| `brand_id` | int | ✅ | — | شناسه برند (از پنل مدیریت یا `GET /api/brands`) |
| `topic_type` | string | ❌ | هوشمند | نوع مقاله: اگر خالی باشد بر اساس نیت جستجو انتخاب می‌شود |
| `device_key` | string | ❌ | خودکار | کلید دستگاه: `washing_machine`، `refrigerator`، `dishwasher`، `air_conditioner` و... (لیست کامل: بخش ۸) |
| `topic` | string | ❌ | — | موضوع دلخواه (به‌جای قالب آماده)؛ برای تشخیص نیت دقیق‌تر — هر موضوعی حتی خارج از دامنه دستگاه‌ها |
| `custom_title` | string | ❌ | — | 🆕 v3.7.1: عنوان دقیق مقاله — کل بدنه دقیقاً حول همین عنوان نوشته می‌شود (outline موضوع‌محور ۷بخشی + فکت‌های وب در نثر + FAQ همان موضوع). در نبود آن و وجود `topic`، خود موتور از موضوع عنوان می‌سازد |
| `variants` | int | ❌ | `3` | تعداد واریانت تولید و انتخاب بهترین (۱ تا ۴) |
| `target_score` | int | ❌ | `88` | امتیاز کیفیت هدف (۰-۱۰۰) — موتور تا رسیدن به آن بهبود می‌دهد (v3.2: از ۸۵ به ۸۸) |
| `max_rounds` | int | ❌ | `3` | حداکثر دورهای بهبود (۱ تا ۵) |
| `research` | bool\|string | ❌ | `"auto"` | تحقیق آنلاین وب: `true` = اجبار، `false` = غیرفعال، `"auto"` = فقط اگر سرویس فعال باشد (پیش‌فرض v3.2) |
| `with_images` | bool | ❌ | `true` | درج ۳ تصویر مرتبط در مقاله (v3.2) |
| `auto_save` | bool | ❌ | `false` | ذخیره خودکار مقاله در دیتابیس برند |
| `no_cache` | bool | ❌ | `false` | `true` = دور زدن کش و تولید تازه |

#### مقدارهای معتبر `topic_type`

| مقدار | نوع مقاله |
|-------|-----------|
| `troubleshooting` | عیب‌یابی و رفع خرابی |
| `user_guide` | راهنمای استفاده |
| `maintenance` | تعمیر و نگهداری |
| `error_codes` | کدهای خطا |
| `comparison` | مقایسه و انتخاب |
| `buying_guide` | راهنمای خرید |
| `energy_saving` | صرفه‌جویی انرژی |
| `seasonal_care` | مراقبت فصلی |
| `cost_guide` | راهنمای هزینه |

#### مثال درخواست

```bash
curl -X POST "https://YOUR-DOMAIN.com/api/ai/smart-generate" \
  -H "Content-Type: application/json" \
  -H "X-API-Key: YOUR_API_KEY" \
  -d '{
    "brand_id": 1,
    "device_key": "washing_machine",
    "topic": "تعمیر ماشین لباسشویی که آب تخلیه نمی‌کند",
    "variants": 3,
    "target_score": 85,
    "max_rounds": 3
  }'
```

> 🆕 **v3.7.1 — حالت موضوع‌محور**: با ارسال `topic` (یا `custom_title`)، مقاله دقیقاً حول همان موضوع نوشته می‌شود: عنوان حفظ می‌شود (جایگزینی با فرمول کلی ندارد)، فکت‌های تحقیق وب داخل نثر می‌آیند و پرسش‌های واقعی کاربران با پاسخ تحلیلی در بدنه پاسخ می‌گیرند.

#### ساختار پاسخ (خلاصه‌شده)

```json
{
  "success": true,
  "data": {
    "article": {
      "id": null,
      "title": "آیا تعمیر ماشین لباسشویی خودتان قابل انجام است؟",
      "content": "<h2>...</h2><p>... (HTML کامل مقاله)</p>",
      "word_count": 847,
      "topic_type": "troubleshooting",
      "device_key": "washing_machine",
      "tldr": "خلاصه یک‌خطی مقاله برای جعبه TL;DR"
    },
    "seo": {
      "title": "عنوان سئو (حداکثر ۶۰ کاراکتر)",
      "meta_description": "توضیحات متا (حداکثر ۱۶۰ کاراکتر)",
      "focus_keyword": "تعمیر ماشین لباسشویی",
      "slug": "washing-machine-repair"
    },
    "faq": {
      "items": [
        { "question": "هزینه تعمیر ماشین لباسشویی چقدر است؟", "answer": "..." },
        { "question": "چطور بفهمم مشکل جدی است؟", "answer": "..." },
        { "question": "آیا تعمیر در محل انجام می‌شود؟", "answer": "..." }
      ],
      "schema": { "@type": "FAQPage", "mainEntity": [ "..." ] }
    },
    "quality": {
      "initial_score": 71,
      "final_score": 86,
      "target_score": 85,
      "target_reached": true,
      "grade": "عالی 🏆",
      "dimensions": { "readability": 82, "structure": 90, "coherence": 78, "focus": 95, "engagement": 80, "length": 88, "variety": 76 },
      "gain": 15
    },
    "intent": { "intent": "informational", "confidence": 1, "..." : "..." },
    "keyword": {
      "focus": "تعمیر ماشین لباسشویی",
      "alternatives": ["عنوان جایگزین ۱", "عنوان جایگزین ۲", "عنوان جایگزین ۳"],
      "opportunities": ["هزینه تعمیر ماشین لباسشویی", "تعمیر ماشین لباسشویی در تهران", "..."]
    },
    "images": [
      {
        "file": "washing_machine",
        "path": "assets/images/articles/washing_machine.jpg",
        "url": "https://YOUR-SITE.ir/assets/images/articles/washing_machine.jpg",
        "alt": "تعمیر ماشین لباسشویی — تعمیر تخصصی لوازم خانگی",
        "caption": "ماشین لباسشویی — مرحله بررسی تخصصی",
        "role": "featured"
      },
      { "role": "inline_mid", "...": "..." },
      { "role": "inline_end", "...": "..." }
    ],
    "uniqueness": { "uniqueness": 94, "..." : "..." },
    "research": {
      "enabled": true,
      "provider": "duckduckgo_html",
      "keywords": ["پمپ تخلیه", "تعمیرکار", "..."],
      "facts": ["قیمت پمپ تخلیه بین ۱ تا ۲ میلیون تومان..."],
      "sources": ["https://example.com/...", "..."]
    },
    "pipeline": {
      "steps": [
        { "step": "intent",   "label": "تشخیص نیت جستجو",       "ms": 3,  "intent": "informational", "confidence": 1 },
        { "step": "keywords", "label": "انتخاب کلیدواژه کانونی", "ms": 1,  "focus_keyword": "تعمیر ماشین لباسشویی" },
        { "step": "research", "label": "تحقیق آنلاین وب",        "ms": 2140, "provider": "duckduckgo_html", "questions": 5, "facts": 4 },
        { "step": "article",  "label": "تولید مقاله (بهترین-از-۳)", "ms": 184, "score": 71, "words": 812 },
        { "step": "improve",  "label": "بهبود خودکار کیفیت",      "ms": 96, "score_before": 71, "score_after": 88, "rounds": 2 },
        { "step": "enrich",   "label": "غنی‌سازی ساختاری",       "ms": 6, "toc_items": 6, "fresh_stats": 3, "people_also_ask": 4 },
        { "step": "images",   "label": "درج ۳ تصویر مرتبط در مقاله", "ms": 3, "files": ["washing_machine", "diagnostics", "spare_parts"] },
        { "step": "title",    "label": "انتخاب عنوان بهینه",      "ms": 5,  "best": "...", "score": 94 },
        { "step": "seo",      "label": "تولید پکیج سئو",          "ms": 4,  "title_len": 58, "density": 1.4 },
        { "step": "faq",      "label": "تولید سوالات متداول",     "ms": 7,  "count": 6, "from_web": 3 },
        { "step": "uniqueness","label": "راستی‌آزمایی یکتایی",    "ms": 12, "uniqueness": 94 }
      ],
      "elapsed_ms": 2497,
      "variants": 3,
      "auto_saved": false
    },
    "_cached": false
  }
}
```

#### نکات مهم

- 💰 **کش**: پاسخ یکسان برای ۳۰ دقیقه کش می‌شود؛ در پاسخ کش‌شده `"_cached": true` می‌بینید. با `"no_cache": true` تولید تازه بگیرید.
- 📊 کلید کیفیت: `data.quality.final_score` — اگر `target_reached` مقدار `false` داشت، متن را با `improve-content` (۵.۲) دوباره بهبود دهید یا `target_score` را واقع‌بینانه‌تر (۸۰) بگذارید.
- 📝 `content` HTML است (هدینگ h2/h3 + پاراگراف + لیست + figure تصاویر) و مستقیماً در CMS/سایت قابل درج است.
- 🖼️ **تصاویر (v3.2)**: آرایه `images` شامل ۳ تصویر است؛ تصویر اول (`featured`) برای بنر/og:image و دو تصویر دیگر داخل متن در ۳۵٪ و ۷۲٪ طول مقاله درج شده‌اند. URLها **مطلق** هستند و از هر سایت دیگری قابل استفاده‌اند.
- 🌐 **تحقیق (v3.2)**: با پیش‌فرض `"auto"`، اگر جستجوی وب فعال باشد خودکار انجام می‌شود؛ نتایج در `research` و آمار تازه در متن و سؤالات واقعی در FAQ تزریق می‌شود.

---

### ۵.۲ 🔧 بهبود خودکار محتوا — `POST /api/ai/improve-content`

متنی که خودتان نوشته‌اید را تحلیل می‌کند، **ابعاد ضعیف** را پیدا می‌کند و استراتژی‌های هدفمند اصلاح را اعمال می‌کند — تا وقتی امتیاز به هدف برسد (چرخه خود-درمانی).

#### پارامترها

| پارامتر | نوع | الزامی | پیش‌فرض | توضیح |
|---------|:---:|:------:|:-------:|-------|
| `content` | string | ✅ | — | متن مورد بهبود (متن ساده یا HTML) |
| `focus_keyword` | string | ❌ | — | کلیدواژه کانونی (برای بُعد «تمرکز») |
| `target_score` | int | ❌ | `85` | امتیاز هدف |
| `max_rounds` | int | ❌ | `3` | حداکثر دور بهبود (۱-۵) |
| `content_type` | string | ❌ | `null` | `article` یا `page` (برای سنجش حجم مناسب) |
| `device_key` | string | ❌ | — | اگر مرتبط با دستگاهی است، دانش آن دستگاه در بهبود حجم استفاده می‌شود |

#### مثال

```bash
curl -X POST "https://YOUR-DOMAIN.com/api/ai/improve-content" \
  -H "Content-Type: application/json" \
  -H "X-API-Key: YOUR_API_KEY" \
  -d '{
    "content": "ماشین لباسشویی دستگاه مهمی است. این دستگاه لباس را می‌شوید...",
    "focus_keyword": "تعمیر ماشین لباسشویی",
    "target_score": 85
  }'
```

#### پاسخ (خلاصه‌شده)

```json
{
  "success": true,
  "data": {
    "content": "متن بهبودیافته (جمله‌های بلند شکسته، مترادف جایگزین، واژه‌های رابط اضافه...)",
    "improved": true,
    "gain": 14,
    "score_before": 58,
    "score_after": 72,
    "grade_before": "قابل قبول 🤔",
    "grade_after": "خوب 🙂",
    "target_reached": false,
    "rounds": [
      { "round": 1, "weak_dimensions": ["variety", "coherence", "engagement"], "strategies": ["variety", "coherence", "engagement"], "score_before": 58, "score_after": 66 },
      { "round": 2, "weak_dimensions": ["structure", "length"], "strategies": ["structure", "length"], "score_before": 66, "score_after": 72 }
    ],
    "remaining_weaknesses": ["انسجام"],
    "final_suggestions": ["یک واژه رابط به هر ۳ جمله اضافه کنید"]
  }
}
```

#### استراتژی‌های بهبود (۷ مورد)

| بُعد ضعیف | استراتژی اعمالی |
|-----------|------------------|
| 📖 خوانایی | شکستن جمله‌های بلند‌تر از ۲۲ کلمه در بهترین نقطه دستوری |
| ✨ تنوع | جایگزینی مترادف seed-دار از دیکشنری ۴۷۵ واژه‌ای (تکرارپذیر) |
| 🔗 انسجام | تزریق واژه‌های رابط («در نتیجه»، «علاوه بر این»...) هر ۳ جمله |
| 💡 جذابیت | افزودن جمله تعاملی/آماری در پاراگراف پایانی |
| 🏗️ ساختار | افزودن هدینگ h2 و تقسیم پاراگراف‌های یکنواخت |
| 📏 حجم | افزودن پاراگراف دانش‌محور از پایگاه دانش دستگاه |
| 🎯 تمرکز | تزریق طبیعی کلیدواژه کانونی در پاراگراف دوم |

> 🛡️ **ضمانت عدم تخریب**: اگر در یک دور امتیاز بهبود نیابد، چرخه متوقف و آخرین نسخه خوب برگردانده می‌شود — متن شما هرگز بدتر نمی‌شود.

---

### ۵.۳ 🎙️ تحلیل صدای برند — `POST /api/ai/brand-voice`

پروفایل لحن و سبک نگارش برند را از متن‌های موجود استخراج می‌کند و می‌تواند انطباق یک متن جدید را با آن صدا بسنجد.

#### پارامترها

| پارامتر | نوع | الزامی | توضیح |
|---------|:---:|:------:|-------|
| `texts` | string \| array | ✅ (یا `content`) | متن یا آرایه‌ای از متن‌های برند (حداقل ۲۰ کلمه) |
| `content` | string | ❌ | اگر ارسال شود، علاوه بر پروفایل، **انطباق این متن** با صدای برند هم سنجیده می‌شود |

#### مثال — حالت ۱: استخراج پروفایل

```bash
curl -X POST "https://YOUR-DOMAIN.com/api/ai/brand-voice" \
  -H "Content-Type: application/json" \
  -H "X-API-Key: YOUR_API_KEY" \
  -d '{ "texts": ["متن معرفی برند شما...", "متن درباره ما..."] }'
```

#### مثال — حالت ۲: پروفایل + سنجش انطباق

```json
{
  "texts": ["متن‌های موجود برند..."],
  "content": "متن جدید که می‌خواهید انطباقش بسنجید"
}
```

#### پاسخ (خلاصه‌شده)

```json
{
  "success": true,
  "data": {
    "profile": {
      "avg_sentence_length": 14.2,
      "readability_score": 68,
      "lexical_diversity": 0.61,
      "formality": 72,
      "technical": 35,
      "conversational": 18,
      "persona": "مشاور قابل‌اعتماد 🤝",
      "persona_traits": ["رسمی و محترم", "خدمات‌محور", "خیاط‌شده برای مشتری"],
      "audience_level": "عمومی",
      "recommendations": ["جمله‌ها را با «شما» بنویسید", "..."]
    },
    "comparison": {
      "consistency": 83,
      "verdict": "کاملاً هم‌صدا ✅",
      "advice": [],
      "differences": { "formality": 6, "technical": 11 }
    }
  }
}
```

#### ۴ تیپ صدای قابل تشخیص

| تیپ | ویژگی |
|------|-------|
| 🎓 متخصص راهنما | فنی و دقیق اما آموزشی |
| 🤝 مشاور قابل‌اعتماد | رسمی، خدمات‌محور، مشتری‌محور |
| 💬 دوست صمیمی | خودمانی، ساده، روان |
| 📡 مرجع تخصصی | بسیار فنی، برای مخاطب متخصص |

---

### ۵.۴ 🏷️ عنوان‌سازی CTR-محور — `POST /api/ai/generate-titles`

برای یک موضوع، ده‌ها عنوان با ۸ الگوی اثبات‌شده تولید و بر اساس عوامل مؤثر بر نرخ کلیک رتبه‌بندی می‌کند.

#### پارامترها

| پارامتر | نوع | الزامی | پیش‌فرض | توضیح |
|---------|:---:|:------:|:-------:|-------|
| `topic` | string | ✅ | — | موضوع/کلیدواژه کانونی |
| `brand_fa` | string | ❌ | — | نام فارسی برند (برای الگوی مقایسه‌ای) |
| `device_fa` | string | ❌ | — | نام دستگاه (ترکیب هوشمند با موضوع) |
| `city` | string | ❌ | — | شهر (برای الگوی سئوی محلی) |
| `season` | string | ❌ | — | فصل (برای الگوی فصلی) |
| `count` | int | ❌ | `10` | تعداد عنوان خروجی (۱-۲۵) |

#### مثال و پاسخ واقعی

```bash
curl -X POST "https://YOUR-DOMAIN.com/api/ai/generate-titles" \
  -H "Content-Type: application/json" \
  -H "X-API-Key: YOUR_API_KEY" \
  -d '{ "topic": "تعمیر ماشین لباسشویی", "city": "تهران", "count": 3 }'
```

```json
{
  "success": true,
  "data": {
    "topic": "تعمیر ماشین لباسشویی",
    "focus": "تعمیر ماشین لباسشویی",
    "generated": 17,
    "returned": 3,
    "best": "آیا تعمیر ماشین لباسشویی خودتان قابل انجام است؟",
    "best_score": 94,
    "titles": [
      {
        "title": "آیا تعمیر ماشین لباسشویی خودتان قابل انجام است؟",
        "pattern": "question",
        "pattern_fa": "پرسشی",
        "char_count": 47,
        "score": 94,
        "has_number": true,
        "is_question": true,
        "power_words": [],
        "keyword_position": 4
      }
    ],
    "tips": [
      "عنوان ۴۵ تا ۶۰ کاراکتری در نتایج گوگل کامل نمایش داده می‌شود.",
      "وجود عدد در عنوان نرخ کلیک را تا ۱۵٪ افزایش می‌دهد.",
      "کلیدواژه کانونی را در ۳ کلمه اول عنوان بیاورید."
    ]
  }
}
```

#### ۸ الگوی عنوان

`how_to` آموزشی | `question` پرسشی | `numbered` عددی | `seasonal` فصلی | `local` محلی | `comparison` مقایسه‌ای | `solution` راه‌حلی | `checklist` چک‌لیستی

---

### ۵.۵ 💬 دستیار فرمان فارسی — `POST /api/ai/assistant`

**رابط زبان طبیعی موتور.** فرمان فارسی می‌نویسید، خودش عملیات مناسب را تشخیص می‌دهد و اجرا می‌کند. برای اپ‌ها و ربات‌ها (مثل تلگرام) بهترین نقطه شروع است.

#### پارامترها

| پارامتر | نوع | الزامی | توضیح |
|---------|:---:|:------:|-------|
| `command` | string | ✅ | فرمان زبان طبیعی فارسی |

#### مثال

```bash
curl -X POST "https://YOUR-DOMAIN.com/api/ai/assistant" \
  -H "Content-Type: application/json" \
  -H "X-API-Key: YOUR_API_KEY" \
  -d '{ "command": "عنوان برای تعمیر ماشین ظرفشویی بده" }'
```

#### پاسخ (خلاصه‌شده)

```json
{
  "success": true,
  "data": {
    "action": "titles",
    "success": true,
    "message": "۸ عنوان پیشنهادی برای «تعمیر ماشین ظرفشویی».",
    "result": { "best": "...", "titles": ["..."] },
    "suggestions": ["مقاله بنویس درباره تعمیر ماشین ظرفشویی"]
  }
}
```

> 📖 فهرست کامل فرمان‌ها و مثال‌های بیشتر: **بخش ۹**

---

### ۵.۶ ⚡ آمار کش — `GET /api/ai/cache-stats`

```bash
curl "https://YOUR-DOMAIN.com/api/ai/cache-stats" -H "X-API-Key: YOUR_API_KEY"
```

```json
{
  "success": true,
  "data": {
    "entries": 12,
    "size_bytes": 262144,
    "size_human": "256 KB",
    "hits": 45,
    "misses": 12,
    "writes": 12,
    "hit_rate": 0.789
  }
}
```

### ۵.۷ 🧹 پاکسازی کش — `POST /api/ai/cache-clear`

```bash
curl -X POST "https://YOUR-DOMAIN.com/api/ai/cache-clear" -H "X-API-Key: YOUR_API_KEY"
```

```json
{ "success": true, "data": { "cleared": 12, "message": "۱۲ ورودی کش پاک شد." } }
```

---


### ۵.۸ 🌐 جستجوی آنلاین وب — `POST /api/ai/web-search` *(جدید v3.1)*

موتور سهند حالا می‌تواند **در صورت لزوم، از اینترنت داده زنده بگیرد** — برای بهبود محتوا، سئو و مقالات.

#### پارامترها

| پارامتر | نوع | الزامی | پیش‌فرض | توضیح |
|---------|-----|:------:|:-------:|-------|
| `query` | string | ✅ | — | عبارت جستجو (فارسی یا انگلیسی، حداقل ۲ کاراکتر) |
| `limit` | int | ❌ | ۸ | حداکثر نتایج (۱ تا ۱۰) |

#### مثال

```bash
curl -X POST "https://YOUR-SITE.ir/api/ai/web-search" \
  -H "X-API-Key: smk_..." \
  -H "Content-Type: application/json" \
  -d '{"query": "تعمیر ماشین لباسشویی تهران", "limit": 3}'
```

#### پاسخ واقعی (نمونه از موتور زنده)

```json
{
  "success": true,
  "data": {
    "query": "تعمیر ماشین لباسشویی تهران",
    "provider": "duckduckgo_lite",
    "cached": false,
    "took_ms": 818,
    "results": [
      {
        "title": "تعمیرات لباسشویی در منزل ( محل ) تهران با گارانتی 180 روزه",
        "url": "https://roshaidservice.ir/services/washer-repairs",
        "snippet": "تعمیرات ( تعمیر ) ماشین لباسشویی (درب جلو-از بالا-دوقلو-سطلی-پرتابل-قدیمی-صنعتی)",
        "source": "roshaidservice.ir",
        "rank": 1
      },
      {
        "title": "مرکز تخصصی تعمیر ماشین لباسشویی در تهران با گارانتی 6 ماهه",
        "url": "https://karabama.com/...",
        "snippet": "خدمات نصب، سرویس و تعمیر ماشین لباسشویی در محل با ضمانت 6 ماهه انجام میشود.",
        "source": "karabama.com",
        "rank": 2
      }
    ]
  }
}
```

#### 🔗 زنجیره ارائه‌دهندگان (جایگزینی خودکار در صورت خطا)

| اولویت | ارائه‌دهنده | کلید لازم | توضیح |
|:------:|------------|:---------:|-------|
| ۱ | SerpApi | ✅ (اختیاری) | کیفیت بالا — کلید را در تنظیمات `websearch_settings` وارد کنید |
| ۲ | Google CSE | ✅ (اختیاری) | جستجوی سفارشی گوگل — کلید + CX |
| ۳ | Bing API | ✅ (اختیاری) | Azure Bing Search |
| ۴ | DuckDuckGo HTML | ❌ | رایگان — پیش‌فرض اول |
| ۵ | DuckDuckGo Lite | ❌ | رایگان — جایگزین سبک |
| ۶ | Bing HTML | ❌ | رایگان — آخرین جایگزین |

> 💡 بدون هیچ کلیدی کار می‌کند! کلیدها فقط کیفیت و سهمیه را بالا می‌برند.

#### ⚙️ تنظیمات (از پنل یا جدول settings — کلید `websearch_settings`)

```json
{
  "enabled": true, "timeout": 12, "max_results": 8,
  "cache_ttl": 1800, "rate_per_hour": 240,
  "providers": [],
  "serpapi_key": "", "google_cse_key": "", "google_cse_cx": "", "bing_api_key": ""
}
```

- **کش**: ۳۰ دقیقه پیش‌فرض (`cache_ttl`) — تکرار یک جستجو رایگان است
- **نرخ**: سقف ۲۴۰ جستجوی زنده در ساعت (`rate_per_hour` — حداقل مجاز ۲۴۰؛ جستجوی عمیق خطایاب هر تولید ۳۰-۴۰ کوئری می‌سوزاند)
- **امنیت**: محافظت SSRF داخلی — آدرس‌های داخلی/خصوصی مسدودند

### ۵.۸.۱ 🖼️ تنظیمات تولید تصویر مقاله — `article_photo_settings` *(گسترش‌یافته v3.15 — ۱۸ سرویس؛ ۷ رایگان بدون کلید)*

سرویس تولید «تصاویر واقعی مقاله» از جدول `settings` (کلید `article_photo_settings`) خوانده می‌شود.
**سرویس انتخابی همیشه اول تلاش می‌شود** و سرویس واقعاً استفاده‌شده در پاسخ تولید گزارش می‌شود (`service` به‌ازای هر تصویر + `notices` هشدار «کلید ثبت نشده»).

```json
{
  "service": "pollinations_flux",
  "keys": { "huggingface": "hf_...", "together": "...", "gemini": "AIza..." },
  "timeout": 45
}
```

| سرویس | کلید | توضیح |
|-------|:----:|-------|
| `pollinations_flux` | ❌ | Flux — کیفیت بالا (پیش‌فرض) |
| `pollinations_turbo` | ❌ | Turbo — سریع‌تر |
| `stablehorde` | ❌ | AI Horde — رایگان-اشتراکی، صف عمومی (۶۰-۱۲۰ث) |
| `loremflickr` | ❌ | 🆕 v3.15 — عکس استوک **واقعی موضوعی** بر اساس کلیدواژه دستگاه (سریع) |
| `wikimedia` | ❌ | 🆕 v3.15 — عکس واقعی دانشنامه‌ای ویکیمدیا کامانز (JPEG/PNG ≥۶۴۰px + بندانگشتی ۱۲۰۰px) |
| `lexica` | ❌ | 🆕 v3.15 — جستجو در آرشیو گسترده تصاویر Stable Diffusion با کلیدواژه |
| `picsum` | ❌ | 🆕 v3.15 — عکس واقعی تصادفی (آخرین جایگزین اضطراری) |
| `huggingface` | 🔑 رایگان | SDXL — توکن huggingface.co (endpoint جدید router.huggingface.co) |
| `deepai` | 🔑 رایگان | Text2Img |
| `together` | 🔑 رایگان | FLUX.1-schnell |
| `fal` | 🔑 اعتبار اولیه | fal.ai FLUX.1-schnell |
| `gemini` | 🔑 رایگان | Google Imagen 3 — کلید aistudio.google.com/apikey |
| `getimg` | 🔑 اعتبار اولیه | GetImg SDXL |
| `stability` | 🔑 اشتراکی | Stability SD3 Core |
| `openai` | 🔑 اشتراکی | DALL·E 3 |
| `openai_gptimage` | 🔑 اشتراکی | gpt-image-1 |
| `ideogram` | 🔑 اشتراکی | Ideogram v2 Turbo |
| `replicate` | 🔑 اشتراکی | Replicate FLUX.1-schnell |

> 💡 همه خروجی‌ها به‌صورت خودکار به قالب ۱۶:۹ (۱۲۰۰×۶۷۵) هم‌سازی و واترمارک دوتایی (برند + نمایندگی) می‌خورند.
> تست سلامت هر سرویس از «تنظیمات ← 🖼️ تولید تصویر ← 🧪 تست سرویس انتخابی».
> خط تولید هوشمند (SmartPipeline) هم از همان سرویس انتخابی استفاده می‌کند.
>
> 🆕 **v3.15 — سرویسهای عکس واقعی (لورم‌فلیکر/ویکیمدیا/لکسیکا) با «کلیدواژه موضوعی دستگاه» جستجو می‌کنند** نه پرامپت توصیفی؛ نگاشت اختصاصی هر ۴۲ دستگاه در `AiPhotoService::STOCK_KEYWORDS` تعریف شده و انتخاب از نتایج با بذر (seed) متنوع می‌شود. این سرویسها به‌صورت خودکار در «زنجیره جایگزین رایگان» هم شرکت می‌کنند — اگر سرویس انتخابی (حتی کلیددار بدون کلید) شکست بخورد، به‌ترتیب سرویسهای رایگان بدون کلید امتحان می‌شوند.
>
> ⏱️ **v3.15 — خطایاب مقاوم**: اگر اجرای موتور خطایاب بیش از زمان مجاز هاست طول بکشد، پاسخ معتبر JSON با پرچم `"timeout": true` و گزارش اقدام‌پذیر برمی‌گردد (نه صفحه HTML خطا). تلاش مجدد ادامه‌دهنده است — کدهای ثبت‌شده قبلی به‌صورت خودکار رد (skip) می‌شوند.

### ۵.۹ 🧪 تحقیق ساختاریافته آنلاین — `POST /api/ai/research` *(جدید v3.1)*

قوی‌ترین قابلیت وب: یک فراخوانی = کلیدواژه‌های ترند + سؤالات واقعی کاربران + داده‌های عددی تازه + منابع. **مخصوص غنی‌سازی سئو و مقالات.**

#### پارامترها

| پارامتر | نوع | الزامی | پیش‌فرض | توضیح |
|---------|-----|:------:|:-------:|-------|
| `topic` | string | ✅ | — | موضوع تحقیق (حداقل ۳ کاراکتر) |
| `fetch_pages` | bool | ❌ | false | خواندن متن کامل ۲ صفحه برتر |
| `limit` | int | ❌ | ۸ | حداکثر منابع |

#### مثال

```bash
curl -X POST "https://YOUR-SITE.ir/api/ai/research" \
  -H "X-API-Key: smk_..." \
  -H "Content-Type: application/json" \
  -d '{"topic": "تعمیر یخچال ساید بای ساید", "limit": 4}'
```

#### پاسخ واقعی (نمونه از موتور زنده)

```json
{
  "success": true,
  "data": {
    "topic": "تعمیر یخچال ساید بای ساید",
    "keywords": [
      { "keyword": "فریزر", "frequency": 7 },
      { "keyword": "تهران", "frequency": 6 },
      { "keyword": "گارانتی", "frequency": 5 }
    ],
    "questions": [],
    "facts": [
      "خدمات تعمیر یخچال فریزر و ساید بای ساید قدیمی و جدید در این مرکز با گارانتی 3 الی 12 ماه و فاکتور رسمی",
      "لیست قیمت تعمیر یخچال , خدمات نصب و تعمیر انواع برند یخچال فریزر و ساید بای ساید..."
    ],
    "sources": [
      { "title": "تعمیر یخچال در منزل و تهران با 365 روز گارانتی", "url": "https://ipemdad.com/...", "source": "ipemdad.com" }
    ],
    "summary": "تحقیق آنلاین درباره «تعمیر یخچال ساید بای ساید» با 4 منبع در 1820 میلی‌ثانیه انجام شد. کلیدواژه‌های پرتکرار وب: فریزر، تهران، گارانتی، تماس، سرویس، منزل، انجام، خدمات. 2 داده عددی تازه استخراج شد.",
    "provider": "duckduckgo_lite",
    "took_ms": 1820
  }
}
```

#### 🎯 کاربردها

- `keywords` → در متا تگ‌ها و بدنه مقاله (LSI Keywords)
- `questions` → ساختار FAQ و ستون «پرسش‌های پرتکرار»
- `facts` → آمار و قیمت‌های به‌روز در محتوا
- `sources` → بخش منابع/رفرنس مقاله

### ۵.۱۰ 📊 وضعیت جستجوی وب — `GET /api/ai/websearch-status` *(جدید v3.1)*

```bash
curl "https://YOUR-SITE.ir/api/ai/websearch-status" -H "X-API-Key: smk_..."
```

#### پاسخ واقعی (نمونه)

```json
{
  "success": true,
  "data": {
    "enabled": true,
    "providers": {
      "serpapi": false, "google_cse": false, "bing_api": false,
      "duckduckgo_html": true, "duckduckgo_lite": true, "bing_html": true
    },
    "active_chain": ["duckduckgo_html", "duckduckgo_lite", "bing_html"],
    "rate": { "used_last_hour": 4, "limit_per_hour": 60 }
  }
}
```

> 💡 این اندپوینت عمومی است (بدون کلید) — برای مانیتورینگ سرویس.

### ۵.۱۱ 🧠 خط تولید + تحقیق وب ترکیبی — `POST /api/ai/smart-generate` با `research`

اندپوینت smart-generate حالا پارامتر جدید دارد:

| پارامتر | نوع | پیش‌فرض | توضیح |
|---------|-----|:-------:|-------|
| `research` | bool\|string | `"auto"` (v3.2) | 🌐 تحقیق آنلاین: `true` = اجبار، `false` = خاموش، `"auto"` = فقط اگر سرویس فعال باشد |
| `research_fetch_pages` | bool | false | متن کامل ۲ صفحه برتر هم خوانده شود |

```bash
curl -X POST "https://YOUR-SITE.ir/api/ai/smart-generate" \
  -H "X-API-Key: smk_..." -H "Content-Type: application/json" \
  -d '{"brand_id": 1, "device_key": "washing_machine", "research": true}'
```

> 🛡️ نکته مهم: اگر اینترنت/ارائه‌دهندگان در دسترس نباشند، **خط تولید بدون خطا** با دانش داخلی ادامه می‌یابد — گام تحقیق در ردیابی با برچسب «ناموفق» ثبت می‌شود.

> 🆕 **v3.2**: خروجی تحقیق حالا شامل `opportunities` (فرصت‌های کلیدواژه long-tail) هم هست و در `data.keyword.opportunities` بازگردانده می‌شود.

---

### ۵.۱۲ 📰 جستجوی اخبار زنده — `POST /api/ai/news` *(جدید v3.2)*

اخبار تازه فارسی (Google News RSS) را برای هر موضوع جستجو می‌کند — مناسب برای روند‌های فصلی، قیمت‌های روز و به‌روزرسانی محتوای قدیمی.

| پارامتر | نوع | الزامی | پیش‌فرض | توضیح |
|---------|:---:|:------:|:-------:|-------|
| `query` | string | ✅ | — | عبارت جستجو (فارسی) |
| `limit` | int | ❌ | `8` | حداکثر خبر (۱ تا ۱۵) |

```bash
curl -X POST "https://YOUR-SITE.ir/api/ai/news" \
  -H "X-API-Key: smk_..." -H "Content-Type: application/json" \
  -d '{"query": "لوازم خانگی", "limit": 5}'
```

#### پاسخ (خلاصه‌شده)

```json
{
  "success": true,
  "data": {
    "query": "لوازم خانگی",
    "results": [
      {
        "title": "عنوان خبر تازه...",
        "url": "https://news.google.com/...",
        "snippet": "خلاصه خبر...",
        "source": "خبرگزاری...",
        "date": "2025-09-23 10:30",
        "rank": 1
      }
    ],
    "provider": "google_news_rss",
    "cached": false,
    "took_ms": 842
  }
}
```

---

### ۵.۱۳ 🖼️ فهرست تصاویر مقاله — `GET /api/ai/article-images` *(جدید v3.2)*

۹ تصویر تخصصی فوتورئال بسته‌بندی‌شده برای مقالات (یخچال، ماشین لباسشویی، ظرفشویی، کولر، تلویزیون، کارگاه، قطعات، عیب‌یابی، آشپزخانه مدرن). **بدون کلید API** — عمومی.

```bash
curl "https://YOUR-SITE.ir/api/ai/article-images"
```

#### پاسخ (خلاصه‌شده)

```json
{
  "success": true,
  "data": {
    "refrigerator":     { "url": "https://YOUR-SITE.ir/assets/images/articles/refrigerator.jpg",    "exists": true, "size": 106297 },
    "washing_machine":  { "url": "https://YOUR-SITE.ir/assets/images/articles/washing_machine.jpg", "exists": true, "size": 118616 },
    "dishwasher":       { "url": "...", "exists": true, "size": 111919 },
    "air_conditioner":  { "url": "...", "exists": true, "size": 80729 },
    "television":       { "url": "...", "exists": true, "size": 130488 },
    "workshop":         { "url": "...", "exists": true, "size": 177069 },
    "spare_parts":      { "url": "...", "exists": true, "size": 163313 },
    "diagnostics":      { "url": "...", "exists": true, "size": 87564 },
    "modern_kitchen":   { "url": "...", "exists": true, "size": 90318 }
  }
}
```

> 💡 استفاده: تصاویر smart-generate خودکار انتخاب و درج می‌شوند؛ این اندپوینت برای استفاده مستقیم در CMSهای دیگر یا افزودن به مقالات قدیمی است. همه URLها **مطلق** و به‌صورت عمومی قابل دسترس‌اند.

---

### 🏅 `POST /api/ai/seo-package` — پکیج کامل سئوی پیشرفته (v3.3)

همه ابزارهای سئو در یک فراخوانی: عنوان + متا + کلیدواژه + OG/Twitter + اسکیمای Article + **چک‌لیست E-E-A-T** + **استخراج موجودیت‌ها** + **خوشه کلیدواژه ثانویه** + **People Also Ask** + (اختیاری) **تحلیل شکاف محتوایی رقبا**.

```bash
curl -X POST https://example.com/api/ai/seo-package \
  -H "X-API-Key: bmk_..." -H "Content-Type: application/json" \
  -d '{
    "title": "تعمیر یخچال اسنوا؛ راهنمای کامل ایرادیابی",
    "content": "متن کامل مقاله... (حداقل چند پاراگراف)",
    "focus_keyword": "تعمیر یخچال اسنوا",
    "brand_id": 3,
    "competitor_titles": ["۱۰ علت یخ نزدن یخچال", "هزینه تعمیر کمپرسور یخچال"]
  }'
```

**پارامترها:**

| پارامتر | نوع | الزامی | توضیح |
|---------|-----|:------:|-------|
| `title` | string | ✅ | عنوان صفحه/مقاله |
| `content` | string | ✅ | متن کامل (HTML یا ساده) |
| `focus_keyword` | string | ❌ | کلیدواژه کانونی (برای تراکم و خوشه) |
| `brand_id` | int | ❌ | برند (نام‌ها به صورت خودکار پر می‌شوند) |
| `brand_fa` / `brand_en` | string | ❌ | نام برند (جایگزین brand_id) |
| `images[]` | array | ❌ | تصاویر برای og:image |
| `competitor_titles[]` | array | ❌ | عنوان‌های ۵ نتیجه اول گوگل → تحلیل شکاف |

**نمونه پاسخ (بخش‌های کلیدی):**

```json
{
  "success": true,
  "data": {
    "title": "تعمیر یخچال اسنوا | نمایندگی رسمی سهند سرویس",
    "description": "تعمیرات تخصصی یخچال اسنوا...",
    "keywords": "تعمیر یخچال اسنوا, یخچال فریزر اسنوا, ...",
    "og": { "og:title": "...", "og:type": "article", "og:locale": "fa_IR" },
    "schema": { "@graph": [ "Article..." ] },
    "eeat": {
      "score": 88,
      "passed": 7, "total": 8,
      "checks": [{ "key": "experience", "label": "تجربه (Experience)", "pass": true, "hint": "..." }]
    },
    "entities": [{ "name": "اسنوا", "type": "Brand" }, { "name": "یخچال فریزر", "type": "Product" }],
    "secondary_keywords": {
      "clusters": {
        "informational": ["تعمیر یخچال اسنوا چیست", "دلایل خرابی یخچال اسنوا"],
        "commercial": ["هزینه تعمیر یخچال اسنوا", "قیمت تعمیر یخچال اسنوا"],
        "howto": ["آموزش تعمیر یخچال اسنوا", "رفع عیب یخچال اسنوا"]
      },
      "people_also_ask": ["آیا تعمیر یخچال اسنوا صرفه اقتصادی دارد؟", "..."]
    },
    "content_gap": {
      "analyzed": 2,
      "gaps": [{ "title": "۱۰ علت یخ نزدن یخچال", "coverage": "35٪", "missing": true }],
      "hint": "موضوعات فهرست‌شده در محتوا پوشش داده نشده‌اند..."
    }
  }
}
```

### ✅ `POST /api/ai/eeat-check` — چک‌لیست اعتماد E-E-A-T (v3.3)

سریع‌ترین راه سنجش کیفیت محتوا از دید گوگل — ۸ معیار با راهنمای رفع برای موارد ردشده.

```bash
curl -X POST https://example.com/api/ai/eeat-check \
  -H "X-API-Key: bmk_..." -H "Content-Type: application/json" \
  -d '{ "content": "<p>متن مقاله شما...</p>" }'
```

**۸ معیار:** تجربه (Experience) | تخصص (Expertise) | اعتبار (Authoritativeness) | اعتماد و ایمنی (Trust) | ساختار عنوان‌بندی | عمق محتوا (۱۰۰۰+ کلمه) | پاسخ به سوالات رایج | فراخوان اقدام (CTA)

### 🌐 تغییرات جستجوی آنلاین در v3.3 (۹ ارائه‌دهنده + مسابقه موازی)

زنجیره ارائه‌دهندگان حالا شامل **ویکی‌پدیا فارسی/انگلیسی** (رسمی، بدون کلید، عالی برای دانش برندها و دستگاه‌ها) و **Startpage** است. ارائه‌دهندگان رایگان به صورت **موازی (curl_multi)** شلیک می‌شوند و اولین پاسخ موفق برنده است — تأخیر جستجو تا ۵۰٪ کاهش یافت. هر نتیجه اکنون دو فیلد جدید دارد:

- `freshness` — تاریخ استخراج‌شده از متن نتیجه (نسبی/میلادی/شمسی) برای سنجش تازگی
- `trust` — امتیاز اعتماد دامنه ۰ تا ۹۰ (دامنه‌های رسمی برندها، ویکی، دولت و آموزش بالا)

---


### ۵.۱۴) 🎯 پیشنهاد بهترین عنوان سئو برای عنوان دلخواه — فاز Q

```http
POST /api/ai/suggest-article-titles
```

عنوان دلخواه خود را بدهید؛ موتور آن را تحلیل کرده (طول، عدد، پرسش، واژه قدرت، موقعیت کلیدواژه)، ۱۲+ واریانت سئو-بهینه می‌سازد و همه را رتبه‌بندی می‌کند.

**پارامترها:**

| پارامتر | نوع | الزامی | توضیح |
|---------|-----|:-----:|-------|
| `custom_title` | string | ✅ | عنوان دلخواه کاربر (۵ تا ۱۲۰ نویسه) |
| `brand_id` | int | ❌ | برند (برای ترکیب نام برند در پیشنهادها) |
| `device_key` | string | ❌ | دستگاه (برای کلیدواژه دقیق‌تر) |
| `count` | int | ❌ | تعداد پیشنهاد خروجی (پیش‌فرض ۸، حداکثر ۱۵) |

**نمونه:**

```json
{
  "custom_title": "یخچال سامسونگ سرد نمی‌کند",
  "count": 5
}
```

**پاسخ (خلاصه):**

```json
{
  "success": true,
  "data": {
    "original": "یخچال سامسونگ سرد نمی‌کند",
    "original_score": 30,
    "original_analysis": {
      "char_count": 26,
      "char_verdict": "کوتاه — می‌توانید واژه‌های کلیدی بیشتری اضافه کنید",
      "has_number": false, "is_question": false,
      "power_words": [], "keyword_position": 0
    },
    "best": "یخچال سامسونگ سرد نمی‌کند؛ علل، تشخیص و راه‌حل قطعی",
    "best_score": 78,
    "best_gain": 48,
    "suggestions": [
      { "title": "...", "score": 78, "gain": 48, "is_original": false, "has_number": true, "is_question": false, "power_words": ["قطعی"] }
    ],
    "tips": ["عنوان ۴۵ تا ۶۰ کاراکتری در نتایج گوگل کامل نمایش داده می‌شود.", "..."]
  }
}
```

💡 در پنل مدیریت، همین قابلیت با دکمه «🎯 پیشنهاد بهترین عنوان سئو» زیر فیلد «عنوان دلخواه» در دسترس است.

---

### ۵.۱۵) 📊 آمار کامل سئوی مقاله — فاز Q

```http
POST /api/ai/article-seo-stats
```

۱۲ سنجه سئو با وزن‌دهی روی مقاله ذخیره‌شده اجرا و امتیاز کلی ۰-۱۰۰ برمی‌گرداند. این همان پنلی است که زیر ویرایشگر هر مقاله در پنل نمایش داده می‌شود.

**پارامترها:** `article_id` (int — ✅ الزامی)

**سنجه‌ها (۱۲ مورد):** عنوان سئو (وزن ۱۲) · متا توضیحات (۱۰) · تراکم کلیدواژه (۱۲) · کلیدواژه در مقدمه (۸) · حجم محتوا (۱۰) · ساختار هدینگ (۸) · لینک داخلی (۸) · تصاویر و alt (۶) · سوالات متداول (۸) · فهرست مطالب (۴) · **نگارش فارسی (۸ — PersianGrammar)** · خوانایی (۶)

هر سنجه: `status` (pass/warn/fail) + `detail` فارسی + `fixable` (قابل اصلاح خودکار؟).

---

### ۵.۱۶) 🎯 بهبود خودکار سئوی مقاله — فاز Q

```http
POST /api/ai/improve-article-seo
```

همه ایرادهای قابل اصلاح را به‌صورت خودکار اعمال، مقاله را ذخیره و گزارش قبل/بعد برمی‌گرداند.

**پارامترها:**

| پارامتر | نوع | الزامی | توضیح |
|---------|-----|:-----:|-------|
| `article_id` | int | ✅ | شناسه مقاله |
| `apply` | bool | ❌ | `false` = فقط پیش‌نمایش بدون ذخیره (پیش‌فرض `true`) |

**۸ اصلاح خودکار:**
۱. اصلاح نگارش فارسی (نیم‌فاصله، سجاوندی، املای رایج، هم‌خوانی فعل و فاعل)
۲. تضمین کلیدواژه کانونی در مقدمه
۳. بازنویسی عنوان سئو در بازه ۴۵-۶۰ کاراکتر
۴. بازنویسی متا توضیحات در بازه ۱۲۰-۱۶۰ کاراکتر
۵. افزودن لینک داخلی (خدمات + ثبت درخواست)
۶. افزودن alt به تصاویر بی‌alt
۷. افزودن بخش سوالات متداول (اگر نباشد)
۸. افزودن فهرست مطالب TOC (اگر نباشد)

**پاسخ:** `before.score` → `after.score` + `gain` + `applied[]` (لیست اصلاحات) + `saved`

---

### ۵.۱۷) 📰 پارامترهای جدید generate-article — فاز Q

اندپوینت `POST /api/ai/generate-article` سه پارامتر جدید گرفته است:

| پارامتر | نوع | پیش‌فرض | توضیح |
|---------|-----|---------|-------|
| `custom_title` | string | — | 🆕 عنوان دلخواه کاربر — مقاله دقیقاً حول همین عنوان نوشته می‌شود و کلیدواژه کانونی از آن استخراج می‌شود |
| `research` | bool | `false` | 🆕 جستجوی اینترنت هنگام نوشتن — ۳ بخش به مقاله اضافه می‌شود: داده‌های به‌روز + پرسش‌های واقعی کاربران + منابع (E-E-A-T) + کلیدواژه‌های ترند در تگ‌ها |
| `with_images` | bool | `true` | 🆕 تصاویر خودکار — ۳ تصویر (شاخص/میان‌متن/پایانی) با alt و figcaption در متن درج و `featured_image` در دیتابیس ذخیره می‌شود |

**تعداد انواع مقاله:** ۹ → **۲۵ نوع** (ایمنی، نصب، اشتباهات رایج، گارانتی، فناوری‌شناسی، باورغلط/واقعیت، چک‌لیست، مطالعه موردی، واژه‌نامه، تاریخچه، نکات خبرگان، علامت‌محور، آمار، محیط زیست، فرآیند نمایندگی و...).

**خروجی جدید در پاسخ:**

```json
{
  "grammar": { "score": 92, "grade": "عالی", "fixes": {"zwnj": 1, "punctuation": 1, "spelling": 1, "agreement": 0, "digits": 1, "total_fixes": 4}, "issues": [], "metrics": {} },
  "images": [ { "role": "featured", "path": "assets/images/articles/refrigerator.jpg", "url": "https://...", "alt": "...", "caption": "..." } ],
  "research": { "used": true, "provider": "...", "facts": 5, "questions": 4, "sources": 6, "keywords": [] }
}
```

---

### ۵.۱۸) 🎨 اسکیل UI/UX Pro — طراحی حرفه‌ای صفحات (v3.5)

موتور ۳.۵ با یک **اسکیل طراحی تخصصی** تجهیز شد که هنگام ساخت سایت در سایت‌ساز، چیدمان هر صفحه را بر پایه ۹ قانون کلاسیک UX (هیک، فیتس، یاکوب نیلسون، فون رستورف و...)، قواعد RTL فارسی، دسترس‌پذیری WCAG و الگوهای نرخ تبدیل تولید می‌کند. دانش اسکیل در `engine/knowledge/uiux-patterns.json` نگهداری می‌شود: توکن‌های طراحی (فاصله، تایپ، رنگ ۶۰-۳۰-۱۰، گردی، سایه، بریک‌پوینت) + بلوپرینت ۱۷ نوع صفحه + قواعد ۳۴ بلوک + سیستم امتیازدهی.

#### ۵.۱۸.۱) 🪄 طراحی چیدمان — `POST /api/ai/uiux-design`

| پارامتر | نوع | الزامی | پیش‌فرض | توضیح |
|---------|:---:|:------:|:-------:|-------|
| `page_type` | string | ✅ | `home` | نوع صفحه: `home`، `services`، `service-area`، `warranty`، `blog`، `about-agency`، `about-brand`، `contact`، `request`، `other-brands`، `error-codes`، `faq`، `sitemap-page`، `terms`، `privacy`، `about`، `custom` |
| `context` | object | ❌ | `{}` | زمینه واقعی برند: `brand_fa`، `devices_count`، `articles_count`، `has_testimonials` — چیدمان با داده‌های واقعی تنظیم می‌شود |

```bash
curl -X POST https://your-domain.com/api/ai/uiux-design   -H "X-API-Key: YOUR_KEY" -H "Content-Type: application/json"   -d '{"page_type":"services","context":{"brand_fa":"برند سهند سرویس","devices_count":12,"has_testimonials":true}}'
```

پاسخ (خلاصه‌شده):

```json
{
  "success": true,
  "data": {
    "page_type": "services",
    "page_name_fa": "صفحه خدمات",
    "goal_fa": "تبدیل بازدیدکننده به درخواست تعمیر",
    "layout": [
      {"block": "hero", "why_fa": "قانون هیک: پیشنهاد ارزش باید در ۵ ثانیه دیده شود"},
      {"block": "services_grid", "why_fa": "قانون فیتس: هدفونهای بزرگ و قابل کلیک"}
    ],
    "ux_score": 88,
    "grade_fa": "خوب — قابل انتشار",
    "rationale": ["هیرو با یک پیام و یک CTA — قانون تمرکز", "بخش اعتمادسازی بعد از خدمات"],
    "skill": "اسکیل UI/UX Pro v1.0"
  }
}
```

> 💡 نتیجه ۳۰ دقیقه در کش موتور ذخیره می‌شود.

#### ۵.۱۸.۲) 🔍 ممیزی UX — `POST /api/ai/uiux-review`

| پارامتر | نوع | الزامی | توضیح |
|---------|:---:|:------:|-------|
| `layout` | array | ✅ | آرایه بلوک‌های چیدمان موجود (حداکثر ۶۰ بلوک) |
| `page_type` | string | ❌ | نوع صفحه برای اعمال بلوپرینت مربوطه |

پاسخ: `score` (۰-۱۰۰)، `grade_fa`، `issues[]` (مشکلات اولویت‌بندی‌شده با `severity_fa`)، `strengths[]`.

#### ۵.۱۸.۳) 🛠️ اصلاح خودکار چیدمان — `POST /api/ai/uiux-improve`

| پارامتر | نوع | الزامی | توضیح |
|---------|:---:|:------:|-------|
| `layout` | array | ✅ | چیدمان فعلی (حداکثر ۶۰ بلوک) |
| `page_type` | string | ✅ | نوع صفحه (پیش‌فرض `home`) |

پاسخ: `improved_layout` (چیدمان اصلاح‌شده)، `changes[]` (فهرست تغییرات با دلیل)، `score_before` / `score_after`.

#### ۵.۱۸.۴) 📇 فراداده اسکیل — `GET /api/ai/uiux-skill-info`

```bash
curl https://your-domain.com/api/ai/uiux-skill-info -H "X-API-Key: YOUR_KEY"
```

خروجی: نسخه اسکیل، فهرست ۱۷ نوع صفحه پشتیبانی‌شده، ۹ قانون UX، توکن‌های طراحی و قواعد RTL — مناسب برای ساخت ابزارهای طراحی بیرونی که می‌خواهند با سایت‌ساز هم‌زبان باشند.

---

### ۵.۱۹) 🚨 موتور خطایاب AI + تصاویر AI + خودیادگیر (v3.6)

#### ۵.۱۹.۱) 🚨 تولید کدهای خطای واقعی — `POST /api/ai/error-codes-generate`

کدها از دو منبع «استخراج» می‌شوند (هرگز از خود موتور ساخته نمی‌شوند):

- **🌐 جستجوی آنلاین فارسی و انگلیسی** — کوئری‌های چندگانه + پارس جدول‌های کد سازنده + راستی‌آزمایی تک‌کد با خواندن صفحه کامل و منبع‌یابی URL
- **📗 پایگاه دانش تخصصی (v3.10)** — ۱۵۰+ کد curated از مستندات رسمی ۹ برند پرتقاضا (ال‌جی، سامسونگ، بوش، الکترولوکس، آریستون، بکو، ویرپول، پاناسونیک، اسنوا). کدهای معتبر KB که وب پیدا نکرد هم‌اکنون **ثبت می‌شوند** (قبلاً حذف می‌شدند) و اگر وب همان کد را با منبع ببیند، URL منبع هم الصاق می‌شود.
- **🎯 دقت تضمینی (v3.10)**: برای کدهایی که در پایگاه دانش هستند، داده curated (عنوان/قطعه/دسته/شدت/دلایل/راه‌حل‌ها) **مقدم بر** استخراج متنی وب است — پنجره متنی وب گاهی معنای کدهای مجاور را قاطی می‌کرد؛ داده KB از مستندات رسمی آمده و قطعاً درست است.

هر رکورد با ساختار کامل ۱۴ فیلدی ثبت می‌شود: برند (فا/EN)، دستگاه (فا/EN)، زیرنوع، مدل‌ها، کد، عنوان فارسی، شدت (۵ سطح بحرانی/زیاد/متوسط/کم/اطلاعاتی)، توضیح کامل ۳-۵ جمله‌ای یکتا، دلایل ۳-۷ موردی مرتب‌شده شایع→نادر، راه‌حل‌های ۳-۷ موردی با برچسب [کاربر]/[تکنسین] و مرتب‌سازی ساده→پیچیده، نوع خطا (تاکسونومی ۱۷گانه)، قطعه مربوطه، مشخصات فنی، محل قرارگیری قطعه.

| پارامتر | نوع | الزامی | توضیح |
|---------|:---:|:------:|-------|
| `brand_id` | int | ✅ | شناسه برند از جدول brands |
| `device_key` | string | ✅ | کلید دستگاه: `washing_machine`، `refrigerator`، `dishwasher`، `air_conditioner` و... |
| `use_web` | bool | ❌ | `true` (پیش‌فرض) = جستجوی آنلاین هم انجام شود |
| `overwrite` | bool | ❌ | جایگزینی کدهای قبلی همان دستگاه |

```bash
curl -X POST https://your-domain.com/api/ai/error-codes-generate \
  -H "X-API-Key: YOUR_KEY" -H "Content-Type: application/json" \
  -d '{"brand_id":5,"device_key":"washing_machine","use_web":true}'
```

پاسخ (خلاصه‌شده):

```json
{
  "inserted": 12,        // کل کدهای ثبت‌شده (وب + KB)
  "skipped": 3,          // از قبل موجود
  "total_known": 15,
  "web_found": 9,        // کدهای راستی‌آزمایی‌شده از وب
  "kb_added": 3,         // 🆕 v3.10 — کدهای معتبر تکمیلی از پایگاه دانش رسمی
  "sources": ["https://..."],
  "report": "۹ کد خطای واقعی وب ثبت شد از مجموع ۹ کد راستی‌آزمایی‌شده + ۳ کد معتبر تکمیلی از پایگاه دانش تخصصی."
}
```

اگر دستگاه ۲۰۰ کد واقعی داشته باشد، هر ۲۰۰ کد با همه فیلدها ثبت می‌شود. حتی اگر جستجوی آنلاین کاملاً قطع باشد، کدهای پایگاه دانش رسمی ثبت می‌شوند (گزارش، تفکیک شفاف وب/KB دارد).

#### ۵.۱۹.۲) ✨ بهینه‌سازی تک‌فیلد کد خطا — `POST /api/ai/error-code-improve-field`

| پارامتر | نوع | الزامی | توضیح |
|---------|:---:|:------:|-------|
| `error_id` | int | ✅ | شناسه رکورد کد خطا |
| `field` | string | ✅ | یکی از: `title` `description` `causes` `solutions` `related_part` `tech_specs` `part_location` `subtype` |

فقط همان فیلد بازنویسی/یکتاسازی می‌شود و بقیه فیلدها دست‌نخورده می‌مانند.

#### ۵.۱۹.۳) 🎨 تولید تصویر OG — `POST /api/ai/generate-og-image`

| پارامتر | نوع | الزامی | توضیح |
|---------|:---:|:------:|-------|
| `title` | string | ✅ | عنوان صفحه/مقاله (روی تصویر درج می‌شود) |
| `page_type` | string | ❌ | نوع صفحه (پیش‌فرض `home`) |
| `brand_id` | int | ❌ | برای رنگ برند (پالت اختصاصی در صورت تعریف) |
| `entity_key` | string | ❌ | کلید یکتا برای نام فایل (هر بار مقدار متفاوت = ترکیب بصری جدید) |

خروجی: `{path, url, format}` — PNG با متن فارسی شکل‌یافته (در صورت وجود فونت/GD یا ImageMagick) وگرنه SVG. هر تولید یکتاست (بذر از کلید + عنوان).

#### ۵.۱۹.۴) 🧠 خودیادگیر — `GET /api/ai/learning/lessons` + `POST /api/ai/learning/toggle`

هر بهبود موفق (سئو، محتوا، UX، کد خطا) یک «درس» با پارامترهای موفق ثبت می‌کند تا در کارهای بعدی ترجیح داده شود. تاریخچه کامل در `logs/ai-upgrades.log` (فقط-افزودنی) و درس‌ها در `engine/knowledge/ai-lessons.json` قابل فعال/غیرفعال کردن تک‌به‌تک هستند — اگر درسی اشتباه بود، فقط همان غیرفعال می‌شود و بقیه پابرجا می‌مانند. مدیریت بصری در پنل: «یادگیری AI».

#### ۵.۱۹.۵) 💬 وب‌هوک ربات بله — `POST /api/bale/webhook`

ربات بله همان دستیار فارسی ربات تلگرام را روی پیام‌رسان بله اجرا می‌کند (BaleBot — سازگار با Bot API بله در tapi.bale.ai). راه‌اندازی از پنل «ربات بله»: توکن از @BotFather بله + شناسه‌های مجاز + دکمه راه‌اندازی وب‌هوک. راستی‌آزمایی با پارامتر `?secret=…` (بله هدر مخفی ندارد).

#### ۵.۱۹.۶) 🌉 واسط Google Apps Script تلگرام

برای عبور از تحریم، کل ارتباط ربات تلگرام می‌تواند از سرورهای گوگل عبور کند: اسکریپت آماده (با جای‌گذاری خودکار راز و آدرس وب‌هوک) در پنل «ربات تلگرام ← واسط Google Apps Script» قابل کپی است — دو مسیر هم‌زمان: خروجی (سایت‌ساز → گوگل → تلگرام) و ورودی (وب‌هوک تلگرام → گوگل → سایت‌ساز).

---

## ۶) مستندات تفصیلی — تحلیلگرهای نسخه ۲

### ۶.۱ 🏆 امتیاز کیفیت محتوا — `POST /api/ai/score-content`

متن را در **۷ بُعد وزنی** می‌سنجد و امتیاز ۰-۱۰۰ + نقاط قوت/ضعف + پیشنهاد می‌دهد.

| پارامتر | نوع | الزامی | پیش‌فرض | توضیح |
|---------|:---:|:------:|:-------:|-------|
| `content` | string | ✅ | — | متن (ساده یا HTML) |
| `focus_keyword` | string | ❌ | — | کلیدواژه کانونی |
| `content_type` | string | ❌ | — | `article` یا `page` |

```bash
curl -X POST "https://YOUR-DOMAIN.com/api/ai/score-content" \
  -H "Content-Type: application/json" \
  -H "X-API-Key: YOUR_API_KEY" \
  -d '{ "content": "متن شما...", "focus_keyword": "تعمیر ماشین لباسشویی" }'
```

#### پاسخ واقعی (نمونه)

```json
{
  "success": true,
  "data": {
    "score": 62,
    "grade": "قابل قبول 🤔",
    "dimensions": {
      "readability": 71,
      "structure": 40,
      "coherence": 66,
      "focus": 100,
      "engagement": 40,
      "length": 92,
      "variety": 76
    },
    "readability": {
      "word_count": 96,
      "sentence_count": 7,
      "avg_sentence_length": 13.7,
      "long_sentence_ratio": 0,
      "lexical_diversity": 0.635,
      "complex_word_ratio": 0.208,
      "score": 71
    },
    "strengths": ["تمرکز بر کلیدواژه", "کفایت حجم"],
    "weaknesses": ["ساختار", "جذابیت"],
    "suggestions": ["یک زیرعنوان h2 یا h3 اضافه کنید"]
  }
}
```

#### مقیاس نمرات

| بازه | grade | وضعیت |
|:----:|-------|-------|
| ۹۰-۱۰۰ | عالی 🏆 | انتشار بدون تغییر |
| ۷۵-۸۹ | خوب 🙂 | قابل انتشار |
| ۵۰-۷۴ | قابل قبول 🤔 | بهبود توصیه می‌شود |
| زیر ۵۰ | ضعیف ⚠️ | بازنویسی لازم است |

### ۶.۲ 🧭 تشخیص نیت جستجو — `POST /api/ai/classify-intent`

| پارامتر | نوع | الزامی | توضیح |
|---------|:---:|:------:|-------|
| `query` | string | ✅ (یا queries) | عبارت جستجوی تکی |
| `queries` | array | ✅ (یا query) | آرایه عبارات (حالت گروهی) |

```bash
curl -X POST "https://YOUR-DOMAIN.com/api/ai/classify-intent" \
  -H "Content-Type: application/json" -H "X-API-Key: YOUR_API_KEY" \
  -d '{ "query": "بهترین ماشین لباسشویی ایرانی کدام است" }'
```

#### پاسخ واقعی (نمونه)

```json
{
  "success": true,
  "data": {
    "intent": "commercial",
    "intent_label": "🛒 تجاری/مقایسه‌ای",
    "confidence": 1,
    "scores": { "commercial": 4, "informational": 0, "transactional": 0, "navigational": 0 },
    "recommended_content": "comparison|buying_guide",
    "signals": [
      { "intent": "commercial", "match": "/بهترین/u" },
      { "intent": "commercial", "match": "/کدام/u" }
    ]
  }
}
```

| نیت | معنا | محتوای پیشنهادی |
|-----|------|------------------|
| `informational` | 📚 دنبال یادگیری | user_guide |
| `commercial` | 🛒 مقایسه/ارزیابی | comparison / buying_guide |
| `transactional` | 💳 آماده اقدام | صفحه خدمات/فرود |
| `navigational` | 🧭 دنبال برند/سایت | صفحه برند |

### ۶.۳ 🗓️ برنامه انتشار محتوا — `POST /api/ai/content-plan`

| پارامتر | نوع | الزامی | پیش‌فرض | توضیح |
|---------|:---:|:------:|:-------:|-------|
| `brand_id` | int | ✅ | — | شناسه برند |
| `weeks` | int | ❌ | `4` | افق برنامه‌ریزی (هفته) |
| `per_week` | int | ❌ | `2` | محتوا در هفته |

```bash
curl -X POST "https://YOUR-DOMAIN.com/api/ai/content-plan" \
  -H "Content-Type: application/json" -H "X-API-Key: YOUR_API_KEY" \
  -d '{ "brand_id": 1, "weeks": 6, "per_week": 2 }'
```

خروجی: تقویم هفتگی با موضوع هر روز، اولویت فصلی (تقویم جلالی)، شکاف پوشش، رقابت‌پذیری و **ساعت انتشار بهینه**.

### ۶.۴ 🐎 کلیدواژه long-tail — `POST /api/ai/suggest-longtail`

| پارامتر | نوع | الزامی | پیش‌فرض |
|---------|:---:|:------:|:-------:|
| `keyword` | string | ✅ | — |
| `count` | int | ❌ | `12` |

```bash
curl -X POST "https://YOUR-DOMAIN.com/api/ai/suggest-longtail" \
  -H "Content-Type: application/json" -H "X-API-Key: YOUR_API_KEY" \
  -d '{ "keyword": "تعمیر یخچال", "count": 5 }'
```

#### پاسخ واقعی (نمونه)

```json
{
  "success": true,
  "data": {
    "seed_keyword": "تعمیر یخچال",
    "suggestions": [
      { "keyword": "چرا تعمیر یخچال نیاز دارم؟", "competition": "کم 🟢", "competition_score": 16 },
      { "keyword": "چه زمانی تعمیر یخچال ضروری است؟", "competition": "کم 🟢", "competition_score": 16 },
      { "keyword": "هزینه تعمیر یخچال چقدر است؟", "competition": "کم 🟢", "competition_score": 16 }
    ]
  }
}
```

### ۶.۵ 🗂️ خوشه‌بندی کلیدواژه — `POST /api/ai/cluster-keywords`

| پارامتر | نوع | الزامی | توضیح |
|---------|:---:|:------:|-------|
| `keywords` | array | ✅ | آرایه‌ای از کلیدواژه‌ها |

```bash
curl -X POST "https://YOUR-DOMAIN.com/api/ai/cluster-keywords" \
  -H "Content-Type: application/json" -H "X-API-Key: YOUR_API_KEY" \
  -d '{ "keywords": ["تعمیر یخچال", "یخچال سرد نمی کند", "نشت گاز یخچال", "تعمیر لباسشویی", "لباسشویی لرزش دارد"] }'
```

خروجی: خوشه‌های موضوعی (Topic Clusters) با ریشه‌های مشترک — برای معماری سیلو محتوایی.

### ۶.۶ 🧮 TF-IDF — `POST /api/ai/tfidf`

| پارامتر | نوع | الزامی | پیش‌فرض |
|---------|:---:|:------:|:-------:|
| `text` | string | ✅ | — |
| `top` | int | ❌ | `15` |

وزن‌دهی آماری واژه‌های متن نسبت به **پایگاه دانش ۷۷ برند** — پیدا کردن واژه‌های متمایزکننده.

---

## ۷) مستندات تفصیلی — تولیدکننده‌های پایه (نسخه ۱)

### ۷.۱ ✍️ تولید محتوای صفحه — `POST /api/ai/generate-content`

| پارامتر | نوع | الزامی | توضیح |
|---------|:---:|:------:|-------|
| `type` | string | ✅ | نوع محتوا (جدول زیر) |
| `brand_id` | int | ✅ | شناسه برند |
| `device_key` | string | ❌ | برای نوع `device_desc` الزامی است |

| مقدار `type` | خروجی |
|--------------|-------|
| `brand_intro` | معرفی برند |
| `brand_history` | تاریخچه برند |
| `agency_about` | درباره نمایندگی |
| `warranty` | شرایط گارانتی |
| `service_area` | محدوده خدمات |
| `services_intro` | معرفی خدمات |
| `device_desc` | توضیح یک دستگاه (نیازمند `device_key`) |

> ⚠️ در نسخه ۲.۰.۰ سیستم، این اندپوینت اصلاح شد: پارامتر `type` داخل بدنه JSON ارسال می‌شود (قبلاً باگ ناسازگاری امضا وجود داشت).

### ۷.۲ 📰 تولید مقاله — `POST /api/ai/generate-article`

| پارامتر | نوع | الزامی | پیش‌فرض | توضیح |
|---------|:---:|:------:|:-------:|-------|
| `brand_id` | int | ✅ | — | شناسه برند |
| `topic_type` | string | ❌ | `troubleshooting` | یکی از ۹ نوع (جدول بخش ۵.۱) |
| `device_key` | string | ❌ | تصادفی | دستگاه هدف |
| `variants` | int | ❌ | `2` | بهترین-از-N (۱-۴) |
| `auto_save` | bool | ❌ | `false` | ذخیره در دیتابیس |

خروجی شامل: `title`، `content` (HTML)، `word_count` (تضمین حداقل ۸۰۰ کلمه)، `tldr`، `faq`، `quality` و `related_articles`.

### ۷.۳ 🔍 پکیج سئو — `POST /api/ai/generate-seo`

| پارامتر | نوع | الزامی | پیش‌فرض |
|---------|:---:|:------:|:-------:|
| `content` | string | ✅ | — |
| `page_type` | string | ❌ | `home` |
| `brand_id` | int | ❌ | — |

خروجی: `title`، `meta_description`، `slug`، `schemas` (LocalBusiness / Article / Breadcrumb) و `head` آماده درج.

### ۷.۴ 🏭 پکیج کامل برند — `POST /api/ai/generate-brand-info`

| پارامتر | نوع | الزامی |
|---------|:---:|:------:|
| `brand_id` | int | ✅ |

تولید یکجای: معرفی + تاریخچه + توضیح همه دستگاه‌ها + FAQ + کدهای خطای رایج. **پرسبک‌ترین عملیات** — برای برند تازه ثبت‌شده.

### ۷.۵ ❓ تولید FAQ — `POST /api/ai/generate-faq`

| پارامتر | نوع | الزامی | پیش‌فرض |
|---------|:---:|:------:|:-------:|
| `brand_id` | int | ✅ | — |
| `count` | int | ❌ | `10` |

### ۷.۶ 🔑 تحلیل کلیدواژه — `POST /api/ai/analyze-keywords`

| پارامتر | نوع | الزامی | توضیح |
|---------|:---:|:------:|-------|
| `text` | string | ✅ | متن تحلیل‌شونده |
| `top` | int | ❌ | تعداد کلیدواژه برتر (۲۰) |
| `focus_keyword` | string | ❌ | فعال‌سازی تحلیل چگالی |
| `brand_id` | int | ❌ | فعال‌سازی پیشنهاد برند-محور |

### ۷.۷ ✅ یکتایی — `POST /api/ai/check-uniqueness`

| پارامتر | نوع | الزامی |
|---------|:---:|:------:|
| `content` | string | ✅ |
| `brand_id` | int | ❌ |

### ۷.۸ 💡 تحلیل جامع بهبود — `POST /api/ai/suggest-improvements`

| پارامتر | نوع | الزامی |
|---------|:---:|:------:|
| `content` | string | ✅ |
| `focus_keyword` | string | ❌ |
| `brand_id` | int | ❌ |

ترکیب ۲۶ بررسی سئو + یکتایی + کلیدواژه در یک فراخوانی.

### ۷.۹ 💡 پیشنهاد موضوع — `POST /api/ai/suggest-topics`

| پارامتر | نوع | الزامی | پیش‌فرض |
|---------|:---:|:------:|:-------:|
| `brand_id` | int | ✅ | — |
| `count` | int | ❌ | `5` |

---

## ۸) مستندات تفصیلی — پایگاه دانش و عیب‌یابی

### ۸.۱ کلیدهای دستگاه معتبر (`device_key`)

`refrigerator` یخچال | `washing_machine` ماشین لباسشویی | `dishwasher` ماشین ظرفشویی | `air_conditioner` کولر گازی | `package` پکیج | `microwave` مایکروویو | `tv` تلویزیون | `water_heater` آبگرمکن | `stove` اجاق گاز | `dryer` خشک‌کن | `oven` فر | `cooktop` هاب | `range_hood` هود | `vacuum` جاروبرقی | `freezer` فریزر | `air_fryer` سرخ‌کن بدون روغن | `cooler` کولر آبی | `heater` بخاری | `fan_coil` فن‌کویل | `steam_cleaner` بخارشوی | `coffee_maker` قهوه‌ساز | `kettle` کتری برقی | `blender` مخلوط‌کن | `juicer` آبمیوه‌گیری | `mixer` غذاکوب | `rice_cooker` آب‌پز | `toaster` توستر | `iron` اتو | `hair_dryer` سشوار | و ۱۲ دستگاه دیگر (مجموع ۴۲)

### ۸.۲ 🎯 تشخیص سه‌سطحی — `POST /api/ai/diagnose`

| پارامتر | نوع | الزامی | توضیح |
|---------|:---:|:------:|-------|
| `device` | string | ✅ | کلید دستگاه (بخش ۸.۱) |
| `symptom` | string | ✅ | علامت به زبان طبیعی |

```bash
curl -X POST "https://YOUR-DOMAIN.com/api/ai/diagnose" \
  -H "Content-Type: application/json" -H "X-API-Key: YOUR_API_KEY" \
  -d '{ "device": "refrigerator", "symptom": "یخچال خنک نمی‌کند اما روشن است" }'
```

خروجی: سناریوی منطبق + **علت‌های وزن‌دار با احتمال** + بررسی خانگی هر علت + اقدام فوری + قطعات مرتبط.

تطبیق سه‌سطحی: (۱) علامت کامل ← (۲) شامل بودن ← (۳) شباهت هم‌پوشانی واژه‌ها (فازی).

### ۸.۳ سایر اندپوینت‌های دانش

```
GET /api/ai/templates                     → همه قالب‌های محتوایی
GET /api/ai/knowledge                     → فهرست ۷۷ برند دانشی
GET /api/ai/knowledge/stats               → آمار پایگاه دانش
GET /api/ai/knowledge/{brand}             → دانش یک برند (مثال: /ai/knowledge/snowa)
GET /api/ai/diagnostics                   → دستگاه‌های دارای سناریو
GET /api/ai/diagnostics/{device}          → سناریوهای یک دستگاه
GET /api/ai/seasonal                      → تقویم فصلی ماه جاری
GET /api/ai/seasonal/{month}              → تقویم ماه خاص (مثال: /ai/seasonal/7 = مهر)
GET /api/ai/parts                         → فهرست قطعات
GET /api/ai/parts/{key}                   → قطعه خاص (مثال: /ai/parts/compressor)
```

---

## ۹) دستیار فرمان فارسی — راهنمای کامل

دستیار (`POST /api/ai/assistant`) فرمان طبیعی فارسی می‌فهمد و خودش اندپوینت مناسب را اجرا می‌کند. **برای ساخت ربات/اتوماسیون، فقط همین یک اندپوینت کافی است.**

### 📋 جدول کامل فرمان‌ها

| فرمان نمونه | عمل (`action`) | توضیح |
|-------------|----------------|-------|
| `راهنما` | `help` | فهرست قابلیت‌ها |
| `مقاله بنویس برای اسنوا درباره ماشین لباسشویی` | `article` | خط تولید هوشمند کامل |
| `مقاله آموزشی درباره یخچال بنویس` | `article` | نوع مقاله از فرمان استخراج می‌شود |
| `مقاله مقایسه‌ای درباره جاروبرقی بساز` | `article` | → topic_type=comparison |
| `مقاله درباره هزینه تعمیر پکیج بنویس` | `article` | → topic_type=cost_guide |
| `کلمات کلیدی ماشین لباسشویی` | `keywords` | تحلیل + رقابت‌پذیری + long-tail |
| `امتیاز این متن: <متن>` | `score` | امتیاز ۷ بُعدی (متن بعد از دونقطه) |
| `این متن را بهبود بده: <متن>` | `improve` | بهبود خودکار |
| `عنوان برای تعمیر ماشین ظرفشویی بده` | `titles` | ۸ عنوان رتبه‌بندی‌شده |
| `نیت جستجوی خرید یخچال ساید بای ساید چیست؟` | `intent` | تشخیص نیت |
| `تشخیص عیب: یخچال سرد نمی‌کند` | `diagnose` | تشخیص سه‌سطحی |
| `کد خطا E24 ماشین ظرفشویی` | `error-codes` | جستجوی پایگاه ۱۲۵ کدی |
| `برندهای پایگاه دانش` | `brands` | فهرست ۷۷ برند |
| `جستجوی وب: قیمت موتور ماشین لباسشویی` | `web_search` | 🌐 جستجوی آنلاین اینترنت (v3.1) |
| `سرچ: یخچال ساید بای ساید` | `web_search` | مترادف «جستجوی وب» |
| `تحقیق درباره: ماشین لباسشویی دوو` | `web_research` | 🧪 تحقیق ساختاریافته آنلاین (v3.1) |

### 🧠 هوشمندی‌های استخراج

- **برند**: نام برند (فارسی/انگلیسی/مستعار) از متن فرمان پیدا و به برند ثبت‌شده دیتابیس نگاشت می‌شود
- **دستگاه**: از ۴۲ دستگاه دانشی تشخیص داده می‌شود
- **نوع مقاله**: از کلیدواژه‌های فرمان (آموزشی/نگهداری/مقایسه/هزینه/فصلی) استنتاج می‌شود

### 🔁 جریان مکالمه‌ای (suggestions)

هر پاسخ کلید `suggestions` دارد — فرمان‌های پیشنهادی منطقی بعدی. برای ربات چت: این موارد را به‌صورت دکمه نمایش دهید.

---

## ۱۰) 🤖 ربات تلگرام متصل به دستیار — راه‌اندازی و استفاده (v2.1)

نسخه ۲.۲ یک **ربات تلگرام کامل متصل به دستیار فارسی موتور** دارد: کاربر در تلگرام فارسی می‌نویسد، همان موتور AI پاسخ می‌دهد. ربات مستقل از این API کار می‌کند (به کلید API نیاز ندارد).

### 📰 قابلیت ستاره نسخه ۲ — تحویل کامل مقاله با ۳ عکس (بدون نیاز به ورود به سایت ساز)

کافی است در تلگرام بنویسید «مقاله بنویس درباره ...» یا `/article موضوع` بفرستید؛ ربات خط تولید هوشمند کامل را اجرا و نتیجه را **درون تلگرام** تحویل می‌دهد:

1. ✅ پیام خلاصه (عنوان + امتیاز کیفیت + کلیدواژه + تعداد کلمه + تراکم کلیدواژه)
2. 📄 فایل **HTML مستقل** — آماده انتشار برای هر سایت دیگری (استایل داخلی RTL + متا سئو + OG/Twitter + اسکیمای Article/FAQ/HowTo + تصاویر با لینک مطلق)
3. 📝 فایل **Markdown** — مناسب وردپرس/انجمن/ابزارهای محتوا
4. 🖼️ **آلبوم ۳ تصویر** مرتبط با موضوع
5. 🔘 دکمه‌های شیشه‌ای اقدام بعدی (مقاله دیگر / کلیدواژه / تحقیق / اخبار)

### ⚙️ راه‌اندازی گام‌به‌گام (۵ دقیقه)

1. **ساخت ربات**: در تلگرام به [@BotFather](https://t.me/BotFather) → `/newbot` → نام و یوزرنیم انتخاب کنید → توکن را کپی کنید
2. **ذخیره توکن**: پنل مدیریت سایت ساز → **🤖 ربات تلگرام** → توکن را وارد و ذخیره کنید
3. **تست اتصال**: دکمه «🔌 تست اتصال» را بزنید (باید @نام‌ربات را نشان دهد)
4. **مجاز کردن خودتان**: در تلگرام به ربات خود `/start` و سپس `/id` بفرستید → شناسه عددی را در فیلد «شناسه‌های مجاز» پنل وارد کنید
5. **فعال‌سازی وب‌هوک**: دکمه «🪝 راه‌اندازی وب‌هوک» را بزنید (به HTTPS نیاز دارد — توکن مخفی امنیتی خودکار ساخته می‌شود)

> 🖥️ **هاست بدون SSL؟** به جای وب‌هوک از حالت polling استفاده کنید: `php telegram/poll.php` از خط فرمان (برای تست).

### 💬 چه کارهایی می‌فهمد؟

همه ۱۶ فرمان (v2.1) — مقاله، کلیدواژه، امتیاز، بهبود، عنوان، نیت، عیب‌یابی، کد خطا، برندها، اخبار + 🌐 جستجوی وب + 🧪 تحقیق آنلاین + 🏅 سئو + ✅ E-E-A-T + 🗓️ برنامه محتوا + ❓ FAQ + فرمان‌های مدیریتی:

| فرمان تلگرام | کار |
|--------------|-----|
| `/start` | خوش‌آمدگویی + کیبورد میان‌بر |
| `/help` | راهنمای کامل قابلیت‌ها |
| `/status` | وضعیت موتور (نسخه، دانش، جستجوی وب، تصاویر) |
| `/id` | 🆔 نمایش شناسه چت (برای مجازسازی) |
| `/article تعمیر یخچال اسنوا` | 📰 **مقاله کامل: فایل HTML + MD + آلبوم ۳ عکس** |
| `/search قیمت موتور ماشین لباسشویی` | 🌐 جستجوی آنلاین |
| `/research یخچال ساید بای ساید` | 🧪 تحقیق ساختاریافته + فرصت‌های کلیدواژه |
| `/news لوازم خانگی` | 📰 اخبار زنده (جدید) |
| `/keywords ماشین ظرفشویی` | 🔑 کلیدواژه + long-tail |
| `/titles تعمیر کولر گازی` | 🏷️ پیشنهاد عنوان CTR-محور |
| `/improve متن...` | 🔧 بهبود متن تا امتیاز هدف |
| `/score متن...` | 🏆 امتیاز ۷ بُعدی متن |
| `/seo تعمیر یخچال اسنوا` | 🏅 **پکیج سئوی پیشرفته** (جدید v2.1): عنوان/متا/کلیدواژه + E-E-A-T + موجودیت‌ها + خوشه معنایی + سوالات پرتکرار |
| `/eeat متن مقاله...` | ✅ **چک‌لیست اعتماد E-E-A-T** (جدید v2.1): ۸ معیار با راهنمای رفع |
| `/plan یخچال پاکشما` | 🗓️ **برنامه انتشار محتوا** (جدید v2.1): تقویم ۸ هفته‌ای |
| `/faq ماشین لباسشویی دوو` | ❓ **سوالات متداول پیشنهادی** (جدید v2.1) |
| `مقاله بنویس برای پاکشما درباره ماشین لباسشویی` | 📰 همان تحویل کامل مقاله با زبان طبیعی |
| `اخبار: لوازم خانگی` | 📰 اخبار زنده با زبان طبیعی |
| `سئو بررسی کن: تعمیر یخچال اسنوا` | 🏅 پکیج سئو با زبان طبیعی (جدید v2.1) |
| `اعتمادسنجی: متن مقاله` | ✅ چک‌لیست E-E-A-T با زبان طبیعی (جدید v2.1) |
| `برنامه محتوا برای یخچال` | 🗓️ برنامه محتوا با زبان طبیعی (جدید v2.1) |
| `سوالات متداول درباره ماشین ظرفشویی` | ❓ FAQ با زبان طبیعی (جدید v2.1) |

> 🎴 فرمان‌های فارسی هم کار می‌کنند: `/مقاله`، `/اخبار`، `/جستجو`، `/تحقیق`، `/کلمات`، `/عنوان`، `/بهبود`، `/امتیاز`، و جدید: `/سئو`، `/اعتماد`، `/برنامه`، `/سوال`

### 🔐 امنیت

- فقط **شناسه‌های چت مجاز** پاسخ می‌گیرند (لیست خالی = هیچ‌کس — کاملاً بسته)
- وب‌هوک با هدر مخفی `X-Telegram-Bot-Api-Secret-Token` راستی‌آزمایی می‌شود (hash_equals)
- پیام‌های بلند خودکار به قطعات ≤۴۰۹۶ تقسیم می‌شوند
- اعلان «درخواست خدمات جدید» سایت‌های برند هم به چت‌های مجاز ارسال می‌شود (قابل فعال‌سازی از پنل)

### 📨 اندپوینت وب‌هوک (مرجع)

```
POST https://YOUR-SITE.ir/api/telegram/webhook
```

- عمومی (بدون X-API-Key) — تلگرام نمی‌تواند هدر سفارشی بفرستد
- امنیت با توکن مخفی که هنگام setWebhook تنظیم شده
- 🔒 این اندپوینت فقط توسط سرورهای تلگرام فراخوانی می‌شود — فراخوانی دستی بدون توکن مخفی 403 می‌گیرد

---

## ۱۱) نمونه کد کامل به زبان‌های مختلف

### 🟦 PHP (cURL)

```php
<?php
/**
 * 🤖 کلاینت API موتور هوش مصنوعی سهند — PHP
 */
class SahandAIClient
{
    private string $baseUrl;
    private string $apiKey;

    public function __construct(string $baseUrl, string $apiKey)
    {
        $this->baseUrl = rtrim($baseUrl, '/');   // مثال: https://sahand.example.com
        $this->apiKey = $apiKey;
    }

    /** 🚀 فراخوانی عمومی */
    public function post(string $endpoint, array $data): array
    {
        $ch = curl_init($this->baseUrl . '/api/' . $endpoint);
        curl_setopt_array($ch, [
            CURLOPT_RETURNTRANSFER => true,
            CURLOPT_POST           => true,
            CURLOPT_TIMEOUT        => 120,
            CURLOPT_HTTPHEADER     => [
                'Content-Type: application/json',
                'X-API-Key: ' . $this->apiKey,
            ],
            CURLOPT_POSTFIELDS     => json_encode($data, JSON_UNESCAPED_UNICODE),
        ]);
        $body   = curl_exec($ch);
        $status = curl_getinfo($ch, CURLINFO_HTTP_CODE);
        curl_close($ch);

        $json = json_decode((string)$body, true) ?: [];
        if ($status >= 400 || empty($json['success'])) {
            throw new RuntimeException('خطای API (' . $status . '): ' . ($json['error'] ?? $body));
        }
        return $json['data'];
    }

    /** 📖 فراخوانی GET */
    public function get(string $endpoint): array
    {
        $ch = curl_init($this->baseUrl . '/api/' . $endpoint);
        curl_setopt_array($ch, [
            CURLOPT_RETURNTRANSFER => true,
            CURLOPT_TIMEOUT        => 30,
            CURLOPT_HTTPHEADER     => ['X-API-Key: ' . $this->apiKey],
        ]);
        $body   = curl_exec($ch);
        $status = curl_getinfo($ch, CURLINFO_HTTP_CODE);
        curl_close($ch);

        $json = json_decode((string)$body, true) ?: [];
        if ($status >= 400 || empty($json['success'])) {
            throw new RuntimeException('خطای API (' . $status . '): ' . ($json['error'] ?? $body));
        }
        return $json['data'];
    }
}

/* ---------- استفاده ---------- */
$ai = new SahandAIClient('https://YOUR-DOMAIN.com', 'YOUR_API_KEY');

// ۱) خط تولید هوشمند
$package = $ai->post('ai/smart-generate', [
    'brand_id'     => 1,
    'device_key'   => 'washing_machine',
    'variants'     => 3,
    'target_score' => 85,
]);
echo "عنوان: {$package['article']['title']}\n";
echo "امتیاز نهایی: {$package['quality']['final_score']}\n";
echo "کلمات: {$package['article']['word_count']}\n";

// ۲) بهبود متن خودتان
$improved = $ai->post('ai/improve-content', [
    'content'       => 'متن شما اینجا...',
    'focus_keyword' => 'تعمیر یخچال',
]);
echo "امتیاز: {$improved['score_before']} ← {$improved['score_after']}\n";

// ۳) دستیار فارسی
$reply = $ai->post('ai/assistant', ['command' => 'کلمات کلیدی ماشین ظرفشویی']);
echo $reply['message'] . "\n";
```

### 🟩 Python (requests)

```python
"""
🤖 کلاینت API موتور هوش مصنوعی سهند — Python
نیازمند: pip install requests
"""
import requests


class SahandAI:
    def __init__(self, base_url: str, api_key: str):
        self.base = base_url.rstrip('/')          # مثال: https://sahand.example.com
        self.headers = {
            'Content-Type': 'application/json',
            'X-API-Key': api_key,
        }

    def post(self, endpoint: str, data: dict) -> dict:
        r = requests.post(f'{self.base}/api/{endpoint}',
                          json=data, headers=self.headers, timeout=120)
        payload = r.json()
        if r.status_code >= 400 or not payload.get('success'):
            raise RuntimeError(f"خطای API ({r.status_code}): {payload.get('error')}")
        return payload['data']

    def get(self, endpoint: str) -> dict:
        r = requests.get(f'{self.base}/api/{endpoint}',
                         headers=self.headers, timeout=30)
        payload = r.json()
        if r.status_code >= 400 or not payload.get('success'):
            raise RuntimeError(f"خطای API ({r.status_code}): {payload.get('error')}")
        return payload['data']


# ---------- استفاده ----------
ai = SahandAI('https://YOUR-DOMAIN.com', 'YOUR_API_KEY')

# ۱) خط تولید هوشمند — یک فراخوانی، پکیج کامل
pkg = ai.post('ai/smart-generate', {
    'brand_id': 1,
    'device_key': 'refrigerator',
    'variants': 3,
    'target_score': 85,
})
print('عنوان:', pkg['article']['title'])
print('امتیاز:', pkg['quality']['final_score'])
print('سئو:', pkg['seo']['title'])

# ۲) امتیازدهی متن
score = ai.post('ai/score-content', {'content': 'متن شما...', 'focus_keyword': 'تعمیر یخچال'})
print(f"امتیاز: {score['score']}/100 ({score['grade']})")
print('نقاط ضعف:', score['weaknesses'])

# ۳) دستیار فارسی
reply = ai.post('ai/assistant', {'command': 'تشخیص عیب: یخچال خنک نمی‌کند'})
print(reply['message'])
for cause in reply['result'].get('causes', []):
    print(f"  • {cause['cause']} ({cause['probability']*100:.0f}%)")

# ۴) عنوان‌سازی
titles = ai.post('ai/generate-titles', {'topic': 'سرویس کولر گازی', 'city': 'تهران', 'count': 5})
print('بهترین عنوان:', titles['best'], f"({titles['best_score']} امتیاز)")
```

### 🟨 JavaScript / Node.js (fetch)

```javascript
/**
 * 🤖 کلاینت API موتور هوش مصنوعی سهند — Node.js 18+
 */
class SahandAIClient {
  constructor(baseUrl, apiKey) {
    this.base = baseUrl.replace(/\/+$/, '');   // مثال: https://sahand.example.com
    this.apiKey = apiKey;
  }

  async post(endpoint, data) {
    const res = await fetch(`${this.base}/api/${endpoint}`, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'X-API-Key': this.apiKey,
      },
      body: JSON.stringify(data),
    });
    const payload = await res.json();
    if (!res.ok || !payload.success) {
      throw new Error(`خطای API (${res.status}): ${payload.error}`);
    }
    return payload.data;
  }

  async get(endpoint) {
    const res = await fetch(`${this.base}/api/${endpoint}`, {
      headers: { 'X-API-Key': this.apiKey },
    });
    const payload = await res.json();
    if (!res.ok || !payload.success) {
      throw new Error(`خطای API (${res.status}): ${payload.error}`);
    }
    return payload.data;
  }
}

/* ---------- استفاده ---------- */
const ai = new SahandAIClient('https://YOUR-DOMAIN.com', 'YOUR_API_KEY');

// ۱) خط تولید هوشمند
const pkg = await ai.post('ai/smart-generate', {
  brand_id: 1,
  device_key: 'dishwasher',
  variants: 3,
  target_score: 85,
});
console.log('عنوان:', pkg.article.title);
console.log('امتیاز:', pkg.quality.final_score);
console.log('FAQ:', pkg.faq.items.length, 'سوال');

// ۲) برنامه محتوایی
const plan = await ai.post('ai/content-plan', { brand_id: 1, weeks: 4, per_week: 2 });
console.log('تعداد گام‌های برنامه:', plan.schedule?.length ?? plan.plan?.length ?? 0);

// ۳) دستیار
const reply = await ai.post('ai/assistant', { command: 'مقاله بنویس برای پاکشما درباره یخچال' });
console.log(reply.message);
```

### 🐧 Windows PowerShell

```powershell
# 🚀 خط تولید هوشمند در پاورشل
$body = @{
    brand_id     = 1
    device_key   = "washing_machine"
    variants     = 3
    target_score = 85
} | ConvertTo-Json

Invoke-RestMethod -Uri "https://YOUR-DOMAIN.com/api/ai/smart-generate" `
    -Method Post -ContentType "application/json; charset=utf-8" `
    -Headers @{ "X-API-Key" = "YOUR_API_KEY" } -Body $body
```

---

## ۱۲) سناریوهای واقعی استفاده

### سناریو ۱ — سایت/اپ خارجی که مقاله سفارشی می‌سازد

```
کاربر موضوع می‌دهد
   ↓
POST /api/ai/classify-intent        ← نیت جستجو
   ↓
POST /api/ai/smart-generate         ← پکیج کامل (مقاله+سئو+FAQ)
   ↓
quality.target_reached == false ?
   ├─ بله → POST /api/ai/improve-content (دور دیگر)
   └─ خیر ✓
   ↓
نمایش مقاله + ذخیره در سیستم خودتان
```

### سناریو ۲ — ربات تلگرام فارسی (فقط با ۱ اندپوینت)

```python
# هر پیام کاربر → دستیار
reply = ai.post('ai/assistant', {'command': user_text})
bot.reply(reply['message'])              # پیام اصلی
bot.show_buttons(reply['suggestions'])   # دکمه‌های فرمان بعدی
```

### سناریو ۳ — پایش کیفیت محتوای سایت موجود

```
برای هر صفحه سایت شما:
   ↓
POST /api/ai/score-content  ← امتیاز ۷ بُعدی
   ↓
score < 70 ?
   ├─ بله → POST /api/ai/improve-content ← بهبود خودکار
   └─ خیر ✓
```

### سناریو ۴ — تقویم محتوایی فصلی

```
GET  /api/ai/seasonal/7               ← مهر: آماده‌سازی وسط پاییز
   ↓
POST /api/ai/content-plan             ← برنامه ۴ هفته‌ای
   ↓
هر هفته: POST /api/ai/smart-generate با موضوع برنامه
```

### سناریو ۵ — پشتیبانی فنی هوشمند

```
کاربر: «یخچالم صدای عجیب می‌دهد و سرد نمی‌کند»
   ↓
POST /api/ai/diagnose {device: "refrigerator", symptom: "..."}
   ↓
علت‌های وزن‌دار + بررسی خانگی + قطعات مرتبط
   ↓
در صورت نیاز: POST /api/ai/assistant {command: "مقاله درباره همین مشکل بنویس"}
```

---

## ۱۳) کدهای خطا و رفع اشکال

| کد HTTP | معنا | راه حل |
|:-------:|------|--------|
| `200` | ✅ موفق | — |
| `400` | ❌ پارامتر نامعتبر/ناکافی | پیام `error` فارسی را بخوانید؛ پارامترهای الزامی را چک کنید |
| `401` | 🔑 کلید API نامعتبر/ارسال‌نشده | هدر `X-API-Key` را با کلید معتبر ارسال کنید |
| `404` | 🔍 منبع یافت نشد | مسیر/کلید (برند، دستگاه، قطعه، ماه) را بررسی کنید |
| `429` | 🚦 عبور از محدودیت نرخ | ۶۰ ثانیه صبر کنید (هدر `Retry-After`) یا درخواست‌ها را صف کنید |
| `500` | 💥 خطای داخلی سرور | لاگ سرور را بررسی کنید؛ اگر متن خیلی کوتاه است حداقل ۳۰ کلمه بفرستید |

### ❓ رفع اشکال‌های رایج

| مشکل | علت | راه حل |
|------|-----|--------|
| «برند یافت نشد» | `brand_id` اشتباه یا برند غیرفعال | با `GET /api/brands` لیست معتبر را بگیرید |
| «متن کافی برای تحلیل نیست» | متن زیر حداقل | حداقل ۲۰-۳۰ کلمه ارسال کنید |
| «علامت در پایگاه دانش یافت نشد» | علامت خیلی خاص | علامت را با کلمات کلیدی‌تر بنویسید («خنک نمی‌کند» به‌جای «مشکل دارد») |
| پاسخ‌های تکراری | کش فعال است | `"no_cache": true` بفرستید |
| متن فارسی به‌هم‌ریخته | انکودینگ | حتماً `Content-Type: application/json; charset=utf-8` |

---

## ۱۴) بهترین شیوه‌ها و نکات امنیتی

### 🔐 امنیت

1. **کلید فقط در سرور** — هرگز در جاوااسکریپت مرورگر/اپ موبایل کامپایل‌نشده قرار نگیرد؛ یک لایه پروکسی سرور بسازید
2. **کلید جدا برای هر اپ** — برای هر سرویس مصرف‌کننده، کلید مستقل بسازید تا بتوانید هر یک را مستقل ابطال کنید
3. **HTTPS اجباری** — کلید در هدر به‌صورت متن ساده حرکت می‌کند؛ فقط روی TLS
4. **محدودسازی برند** — اگر اپ شما فقط برای یک برند است، کلید brand-محور بسازید

### ⚡ کارایی

1. از **کش داخلی** بهره ببرید — فراخوانی تکراری `smart-generate` با پارامتر یکسان رایگان است
2. `variants: 2` برای پیش‌نمایش سریع و `variants: 3-4` برای انتشار نهایی
3. برای متن‌های بلند (بالای ۵۰۰۰ کلمه) `timeout` کلاینت را حداقل ۱۲۰ ثانیه بگذارید
4. در صورت خطای `429`، retry با backoff نمایی (۵s → ۱۰s → ۲۰s) پیاده کنید

### 📊 کیفیت خروجی

1. `target_score: 85` نقطه شیرین کیفیت/زمان است؛ `90+` زمان تولید را چند برابر می‌کند
2. `focus_keyword` را همیشه ارسال کنید — بُعد «تمرکز» بدون آن خنثی می‌شود
3. برای مقالات سایت عمومی، `content_type: "article"` را در `improve-content` ارسال کنید

---

## ۱۴-ب) 🚀 API استقرار خودکار *(جدید v2.11)*

> 💡 از این بخش می‌توانید **از بیرون از سایت‌ساز** استقرار، بروزرسانی، حذف، بکاپ و مانیتورینگ سلامت سایت‌های برند را کنترل کنید — مناسب اتوماسیون، n8n، اسکریپت سرور و پنل‌های اختصاصی.

### 🔐 پیش‌نیاز: کلید با مجوز استقرار (can_deploy)

اندپوینت‌های deploy و backup **از کلید معمولی برند استفاده نمی‌کنند** — چون کلیدهای برند فقط برای دریافت محتوای خودشان مجازند:

> ✅ **روش پیشنهادی (v2.12) — از پنل، بدون SQL:**
> منوی **«سیستم ← 🔑 کلیدهای API»** ← «➕ ساخت کلید API جدید»: برچسب دلخواه بنویسید، برند را روی «کلید سیستمی» بگذارید، ✅ «مجوز استقرار (can_deploy)» را فعال کنید و کلید بسازید — سپس با دکمه 📋 کپی کنید.

<details>
<summary>روش قدیمی — مستقیم با SQL (برای آشنایی)</summary>

```sql
INSERT INTO api_keys (brand_id, api_key, label, is_active, can_deploy)
VALUES (NULL, 'bmk_deploy_YOUR_SECRET_HERE', 'کلید استقرار خارجی', 1, 1);
```
</details>

کلید را در هدر `X-API-Key` بفرستید (همان روش همیشگی).

> ⚠️ کلید استقرار بسیار قدرتمند است (دسترسی کامل فایل‌های هاست) — فقط روی سرور امن نگه دارید.

### 🚀 ۱. شروع استقرار برند — `POST /api/deploy/{brandId}`

```bash
curl -X POST "https://brandmaker.example.com/api/deploy/12" \
  -H "X-API-Key: bmk_deploy_..." \
  -H "Content-Type: application/json" \
  -d '{"subdomain": "samsung", "ssl_install": true}'
```

| پارامتر | نوع | پیش‌فرض | توضیح |
|---------|-----|---------|-------|
| `subdomain` | string | نام پیشنهادی از برند | نام زیردامنه — اعتبارسنجی ۸ قانون DNS خودکار |
| `ssl_install` | bool | `true` | نصب SSL خودکار |
| `domain_type` | string | `"subdomain"` | 🆕 v2.12: `"subdomain"` یا `"addon"` (دامنه الحاقی مستقل) |
| `addon_domain` | string | — | 🆕 v2.12: دامنه کامل الحاقی — **الزامی وقتی `domain_type=addon`** (مثلاً `mybrand.ir`)؛ باید قبل از استقرار DNS آن به هاست اشاره کند |

**مثال استقرار روی دامنه الحاقی (Addon) — 🆕 v2.12:**
```bash
curl -X POST "https://brandmaker.example.com/api/deploy/12" \
  -H "X-API-Key: bmk_deploy_..." \
  -H "Content-Type: application/json" \
  -d '{"domain_type": "addon", "addon_domain": "samsung-repair.ir", "ssl_install": true}'
```

**پاسخ موفق:**
```json
{
  "success": true,
  "deployment_id": 42,
  "message": "استقرار در صف قرار گرفت.",
  "steps": [
    {"key": "prerequisites", "title": "بررسی پیش‌نیازها و تولید بسته سایت"},
    {"key": "subdomain_check", "title": "بررسی وجود زیردامنه"},
    {"key": "subdomain_create", "title": "ساخت زیردامنه در cPanel"},
    "...": "۱۱ مرحله"
  ]
}
```

**خطای نام تکراری** (۴۲۲) با پیشنهاد جایگزین:
```json
{
  "success": false,
  "error": "زیردامنه «samsung» در cPanel موجود است و متعلق به این برند نیست — نام دیگری انتخاب کنید.",
  "suggestions": ["samsung-service", "samsung-repair", "samsung-1404"]
}
```

### ⚡ ۲. اجرای مرحله بعدی — `POST /api/deploy/step/{deploymentId}`

استقرار **مرحله‌به‌مرحله** اجرا می‌شود (مقاوم به timeout — هر فراخوانی یک مرحله):

```bash
curl -X POST "https://brandmaker.example.com/api/deploy/step/42" -H "X-API-Key: bmk_deploy_..."
```

```json
{
  "success": true, "done": false,
  "step": "upload", "title": "آپلود فایل‌ها",
  "message": "بسته سایت آپلود شد (2.1 MB — روش API)",
  "progress": 50
}
```

حلقه کامل در PHP:
```php
do {
    $r = callApi('POST', "/api/deploy/step/{$deploymentId}");
    echo "{$r['progress']}% — {$r['message']}\n";
    usleep(900000);
} while (!$r['done']);
echo $r['success'] ? '✅ استقرار کامل شد' : '❌ ' . ($r['error'] ?? '');
```

### 🔄 ۳. بروزرسانی بدون از دست رفتن تنظیمات — `POST /api/deploy/{brandId}/update`

بکاپ خودکار ← حفظ config ← حذف قدیمی ← آپلود جدید ← بازنویسی config ← تست — همان حلقه `step` را اجرا کنید.

### 📊 ۴. وضعیت آخرین عملیات برند — `GET /api/deploy/{brandId}/status`

```json
{
  "success": true,
  "deployment": {"id": 42, "action": "deploy", "status": "success", "domain": "samsung.example.com", "duration": 47},
  "steps": [{"step": "prerequisites", "status": "success", "message": "پیش‌نیازها تأیید شد", "ms": 812}]
}
```

### 📜 ۵. لاگ یک عملیات — `GET /api/deploy/{deploymentId}/logs`
### 📋 ۶. لیست عملیات اخیر — `GET /api/deploy/list?limit=20`

### 🩺 ۷. سلامت برند — `GET /api/health/{brandId}` (کلید معمولی کافی است)

```json
{
  "success": true,
  "data": {
    "brand": "سامسونگ", "domain": "samsung.example.com",
    "status": "online", "checked_at": "2026-09-25 14:30:00",
    "ssl": {"status": "active", "expiry": "2026-12-24"},
    "last_check": {"http_status": 200, "response_time": 342, "api_ok": true}
  }
}
```

### 🔍 ۸. بررسی فوری زنده — `POST /api/health/{brandId}/check`
### 📊 ۹. همه سایت‌ها — `GET /api/health/all`

```json
{
  "success": true,
  "data": [
    {"brand": "سامسونگ", "status": "online", "http_status": 200, "response_time": 342, "ssl": "active"}
  ],
  "summary": {"total": 3, "online": 2, "offline": 1}
}
```

### 💾 ۱۰-۱۳. بکاپ‌ها

| اندپوینت | متد | عملکرد |
|----------|-----|--------|
| `/api/backup/{brandId}` | GET | لیست ۵ بکاپ اخیر + آمار (با تاریخ شمسی) |
| `/api/backup/{brandId}/create` | POST | بکاپ فوری — نام `{slug}_1404-03-25_14-30-45.zip` |
| `/api/backup/{backupId}/restore` | POST | بازیابی (ابتدا بکاپ ایمنی گرفته می‌شود) |
| `/api/backup/{backupId}/delete` | POST | حذف بکاپ |

### 🚦 خطاها

| کد | شرح |
|-----|-----|
| 403 | کلید مجوز `can_deploy` ندارد |
| 404 | برند/عملیات/بکاپ یافت نشد |
| 422 | نام زیردامنه نامعتبر (با `suggestions`) |
| 400 | عملیات دیگر در حال اجرا / تنظیمات ناقص |

## ۱۵) تاریخچه تغییرات API

| نسخه | تاریخ | تغییرات |
|------|-------|---------|
| **۳.۱۴.۰** | مهر ۱۴۰۵ | 🚨 خطایاب همه‌جانبه — ریشه‌یابی نهایی «مایکروویو/تلویزیون ال‌جی هیچ کدی پیدا نکرد»: بودجه ۴۴→۱۳۰ث + راند نجات همیشه با بودجه تازه ۴۵ث + سرچر مشترک بین راندها (cooldown جینا حفظ) + تفکیک «شکست زیرساخت» از «شاهد منفی» (هاست بلاک دیگر باعث رد همه کدها نمی‌شود) + واریانت کد (E-01↔E01↔E 01) + کدهای دوهرفی کامل (SE/HE/TE/BE/...) + کدهای چشمک LED + کدهای سه‌رقمی در پارس جدول + دیکشنری و قطعات تلویزیون/مایکروویو (بک‌لایت/T-Con/webOS/مگنترون/فیوز حرارتی/...) + پنجره کد-محور برای دلایل/راه‌حل (رفع آلودگی زمینه SERP) + کدهای خطا دیگر «مدل سازگار» حساب نمی‌شوند + کوئری‌های تخصصی webOS/مایکروویو + KB: ال‌جی مایکروویو (SE/FE/TE/EE/PF) و تلویزیون webOS (101/105/137/142/152/324/501) + سقف نرخ ۶۰→۲۴۰ (کلمپ + مهاجرت) | 📸 AiPhotoService v3: رجیستری ۷→۱۴ سرویس (+AI Horde رایگان + fal + Imagen 3 + gpt-image-1 + Ideogram + GetImg + Replicate) + رفع تست سلامت (تصویر ۶۴۰×۳۶۰ + متد GET صحیح) + گزارش سرویس به‌ازای هر تصویر + اعلان «کلید ثبت نشده» + هم‌سازی همه خروجی‌ها به ۱۶:۹ + endpoint جدید HuggingFace router + SmartPipeline از سرویس انتخابی استفاده می‌کند |
| **۳.۱۰.۰** | مهر ۱۴۰۵ | 🚨 خطایاب دو-منظوره v3.10 — ریشه‌یابی «فقط ۳ کد برای ظرفشویی ال‌جی»: سیاست KB-fallback (کدهای معتبر پایگاه دانش رسمی که وب نیافت ثبت می‌شوند — قبلاً سیاست فقط-وب حذفشان می‌کرد) + فیلد جدید `kb_added` در پاسخ + اولویت داده curated KB بر استخراج متنی وب (رفع فیلدهای اشتباه) + گسترش KB ظرفشویی ال‌جی به ۱۴ کد + الگوی استخراج 1E/PE/CE/AE/bE | 🖼️ تصاویر: حذف هوشمند پس‌زمینه سفید لوگوهای JPG (flood-fill + feather) در OG و واترمارک‌ها + واترمارک‌های بزرگ‌تر (برند ۳۰٪ / نمایندگی ۲۵٪) + figure() با ابعاد واقعی و استایل ضدکشیدگی درون‌خطی + استنتاج دستگاه از متن کامل مقاله |
| **۳.۹.۱** | مهر ۱۴۰۵ | 🚀 بخش ۱۴-ب: ۱۳ اندپوینت استقرار خودکار (deploy/health/backup) — شروع استقرار با اعتبارسنجی زیردامنه + پیشنهاد جایگزین، اجرای مرحله‌به‌مرحله مقاوم timeout، بروزرسانی با حفظ config، بکاپ شمسی ۵نسخه، بازیابی، سلامت زنده |
| **۳.۹.۰** | مهر ۱۴۰۵ | 🚨 خطایاب کامل‌فیلد v3.9: قطعه استاندارد مقدم بر زمینه (رفع «LE → پمپ تخلیه») + تطبیق عمومی سیستم قطعه↔عنوان (۳۰ کلیدواژه) + بودجه تطبیقی ۳۴s + ۷ کوئری + غنی‌سازی کدهای جدولی نحیف + مدل‌های سازگار از متن (modelsFromContext) + تکمیل specs/محل همه کدها — تست زنده ۶/۶ کامل | 🖼️ تصاویر مقاله v3: ۱۸ عکس واقعی اختصاصی دستگاه (AI-generated بدون متن) + ریشه‌یابی «تصویر لباسشویی برای مقاله یخچال» (استنتاج دستگاه از عنوان دلخواه — قبلاً تصادفی!) + واترمارک دوتایی تضمینی | ✍️ PersianGlyphs v2: رفع ریشه‌ای «به‌هم‌ریختگی حروف» (سواپ prevJoins/nextJoins + باگ زنجیره فرم + ؤ/ئ/ى) — ۱۸/۱۸ تست | 🎨 OG v3: پس‌زمینه پویا ۱۰ سبک (هر تولید متفاوت) + لوگوی بزرگ ۲۰۰px + عنوان بدون برش (خودجا‌شو ۳ خط) | 🤖 تلگرام: تحویل سند/آلبوم با «URL-اول» (سازگار با هر نسخه واسط — رفع قطعی there is no document) + اسکریپت گوگل v3 |
| **۳.۸.۰** | مهر ۱۴۰۵ | 🚨 خطایاب صددرصد وب‌محور: سیاست فقط-وب + پارس جدول کدها (parseCodeTable) + گارد نام برند + گارد سازگاری قطعه↔عنوان + بهبود تک‌فیلد وب-اول | 🤖 تحویل سند تلگرام مقاوم + GAS v2 + PersianGlyphs bidi بازنویسی |
| **۳.۷.۰** | مهر ۱۴۰۴ | 🎯 سئو موضوع‌محور صفحات برند (PageSeoImprover v2.1 — کلیدواژه از نوع+عنوان هر صفحه، نه «تعمیر+برند» برای همه؛ پروفایل محتوایی ۱۰ نوع صفحه؛ اسکیمای WebPage؛ پرسش‌های پرتکرار؛ EEAT عددی؛ خوانایی جمله‌های کوتاه؛ امتیاز تا ۱۰۰) + 🚨 خطایاب وب‌محور (جستجوی اینترنتی منبع اصلی؛ اعتبارسنجی زمینه؛ استخراج دلایل/راه‌حل از جمله‌های واقعی وب با فیلتر کیفیت؛ حداقل ۵ دلیل/۵ راه‌حل؛ زنجیره استنتاج قطعه؛ رفع باگ برش ۳۰۰ کاراکتری JSON) + ✒️ نگارش فارسی v1.2 (لایه سبک: می‌باشد→است و…؛ لایه حشو؛ ۲۵ واژه تعمیرات؛ رفع باگ گم‌شدن بلوک کد) + 🛡 بهبوددهنده محتوا HTML-امن (رفع ۴ نابودگر ساختار) + 🖼 تصاویر OG با فونت پیش‌فرض سایت + لوگوی برند + واترمارک نمایندگی + 👁 پیش‌نمایش مقاله (articles.php?preview=ID) |
| **۳.۶.۰** | مهر ۱۴۰۴ | 🚨 موتور خطایاب AI: ۲ اندپوینت (`error-codes-generate` تولید همه کدهای واقعی برند+دستگاه با ساختار ۱۴ فیلدی از پایگاه دانش ۱۵۰ کدی + جستجوی آنلاین فارسی/انگلیسی، `error-code-improve-field` بهینه‌سازی تک‌فیلد) + 🎨 `generate-og-image` تصویر OG یکتای مرتبط با محتوا (PNG فارسی‌شکل‌یافته با شکل‌دهنده حروف خالص PHP) + ۲ تصویر یکتای AI درون هر مقاله + 🧠 خودیادگیر SelfLearner (`learning/lessons`، `learning/toggle` — لاگ ارتقاها + فعال/غیرفعال تک‌درس) + 💬 وب‌هوک ربات بله + 🌉 واسط Google Apps Script تلگرام (relay دوطرفه) + 🔘 دکمه بهبود تک‌سنجه سئو + 🚀 بهبود خودکار صفحات برند تا ۱۰۰
| **۳.۵.۰** | مهر ۱۴۰۴ | 🎨 اسکیل UI/UX Pro برای طراحی حرفه‌ای صفحات سایت‌ساز: ۴ اندپوینت جدید (`uiux-design` طراحی چیدمان ۱۷ نوع صفحه، `uiux-review` ممیزی UX با امتیاز ۰-۱۰۰، `uiux-improve` اصلاح خودکار چیدمان، `uiux-skill-info` فراداده اسکیل) + پایگاه دانش uiux-patterns.json (۹ قانون UX + توکن‌های طراحی + قواعد RTL فارسی + WCAG + بلوپرینت ۱۷ صفحه + ۳۴ بلوک) + بهینه‌سازی کامل PHP 8.3 (OPcache + zlib + json_validate)
| **۳.۴.۰** | مهر ۱۴۰۴ | 📝 فاز Q: موتور نگارش فارسی PersianGrammar (بُعد هشتم کیفیت «نگارش») + عنوان دلخواه با پیشنهاد بهترین عنوان سئو (`suggest-article-titles`) + آمار کامل ۱۲ سنجه سئو (`article-seo-stats`) + بهبودگر خودکار سئو (`improve-article-seo`) + ۲۵ نوع مقاله + جستجوی آنلاین در مقاله‌نویسی (`research`) + تصاویر خودکار (`with_images`) + ربات تلگرام v2.2 (/grammar و /suggest و /types) + دستیار فارسی: «نگارش:» و «پیشنهاد عنوان سئو:» و «انواع مقاله»

| نسخه سیستم | نسخه موتور | تغییرات اندپوینت |
|:----------:|:----------:|------------------|
| **2.26.0** | 3.15.0 | بدون تغییر قرارداد اندپوینت — بهبود داخلی: 🐛 `POST /api/track` (عمومی): پیام خروج (`is_exit`+`duration`) ردیف ورود همان صفحه را بروزرسانی می‌کند (نرخ پرش/مدت حضور واقعی) + پنل آمار ادمین: نوار وضعیت زنده ردیاب (فعال/غیرفعال + بازدید امروز/کل + آخرین بازدید + راهنمای بروزرسانی استقرار) + قالب‌ساز: ظواهر متعدد برای همه ۱۵۰ عنصر (۱۲ ظاهر بدنه + ۸ استایل دکمه + ۵ افکت هاور — props جدید variant/btnStyle/hoverFx در layout_json، سازگار با چیدمان‌های قدیمی) + استخراج عناصر از سایت خارجی (کلاس ElementExtractor + جدول personal_elements + هندلرهای AJAX ادمین extract_elements/save_personal_element/delete_personal_element — خارج از API عمومی) |
| 1.0.0 | 1.0.0 | ۱۳ اندپوینت پایه (تولیدکننده‌ها + دانش) |
| 1.1.0 | 2.0.0 | +۷ اندپوینت تحلیلی (score، intent، plan، longtail، cluster، tfidf، engine-info) |
| 2.0.0 | 3.0.0 | +۷ اندپوینت بی‌رقیب (smart-generate، improve-content، brand-voice، generate-titles، assistant، cache-stats، cache-clear)؛ 🐛 اصلاح امضای `generate-content` برای فراخوانی خارجی صحیح |
| 2.1.0 | 3.1.0 | +۳ اندپوینت جستجوی آنلاین وب (`web-search`، `research`، `websearch-status`)؛ پارامتر `research` در smart-generate؛ ۲ فرمان جدید دستیار (`جستجوی وب:` و `تحقیق درباره:`)؛ 🤖 وب‌هوک ربات تلگرام `telegram/webhook`؛ 🐛 رفع باگ بحرانی نصب‌کننده دیتابیس |
| 2.2.0 | 3.2.0 | +۲ اندپوینت جدید (`news` اخبار زنده، `article-images` فهرست تصاویر)؛ smart-generate: ۱۱ مرحله (فهرست مطالب + PAA + آمار تازه + E-E-A-T + ۳ تصویر + HowTo Schema + تراکم کلیدواژه + زمان مطالعه)، `research: "auto"`، `with_images`، پیش‌فرض امتیاز هدف ۸۸؛ research: خروجی `opportunities`؛ ارائه‌دهنده جستجو Mojeek؛ 🤖 ربات تلگرام v2: تحویل کامل مقاله (فایل HTML/MD + آلبوم ۳ عکس) + ۱۲ فرمان + دکمه شیشه‌ای؛ 🐛 رفع باگ منوی وسط صفحه + ریسپانسیو کامل |
| **2.21.0** | 3.13.0 | بدون تغییر اندپوینت — لایه قالب‌سازی و UX: دکمه «🎭 قالب‌ساز» + «✨ طراحی با UI/UX Pro» روی کارت هر صفحه برند (هندلر AJAX uiux_design_page داخلی admin) + کتابخانه TemplateLibrary ۱۷۰ قالب (۱۰ سبک × ۱۷ نوع صفحه — سید غیرمخبر از صفحه قالب‌ها، بدون تغییر DB) + قالب‌ساز همه‌انواع (۱۷ گزینه) + ویزارد ساخت: دکمه «⏭️ رد کردن مرحله و ادامه» + رفع بلوک مرده pagination و کد مرده نام برند در هیرو |
| **2.20.0** | 3.13.0 | بدون تغییر اندپوینت — زیرساخت استقرار: 🩹 رفع خطای «تست نهایی ناموفق: پاسخ HTTP: 500» (چهار ریشه: کامنت نامعتبر Apache در htaccess.template + Require فقط 2.4 + Options/AllowOverride + fallback HTTP فقط روی قطعی) + HtaccessGenerator سه‌سطحی (full/safe/minimal) + خوددرمانی تنزل خودکار + تست نهایی دوسکیم با ۳ تلاش + دیاگنوستیک error_log |
| **2.19.1** | 3.13.0 | بدون تغییر اندپوینت — زیرساخت استقرار: 🐛 رفع خطای «محتوای مسیر سایت: خالی» (listFiles تک‌قالبی بود — پاسخ قدیمی/ناشناخته بی‌صدا به پوشه خالی ترجمه می‌شد و راستی‌آمایی شکست کاذب می‌خورد → چندقالبی با fallback سه‌لایه UAPI→API2 + تفکیک «خالیِ قطعی» از «ناشناخته») + entryExists دو-لایه (استعلام مستقیم get_file_information + fallback لیست) + آپلود overwrite=1 و راستی‌آزمایی succeeded/failed (پیش‌فرض cPanel فایل تکراری را رد می‌کند!) + دیاگنوستیک سه‌موتوره (وجود ZIP + وضعیت لیست + نمونه پاسخ خام) + کش لیست با ابطال جهش‌محور |
| **2.19.0** | 3.13.0 | بدون تغییر اندپوینت — زیرساخت استقرار: 🐛 رفع خطای «استخراج کامل نشد — index.php یافت نشد» (destfiles طبق مستندات فقط برای copy/move/rename است؛ cPanel قدیمی داخل «پوشه هم‌نام آرشیو» استخراج می‌کند → مکان‌یابی + انتقال خودکار به ریشه + راستی‌آمایی) + پارس دو-قالبی پاسخ UAPI (تخت قدیمی + result تو-در-تو جدید) + ادغام هوشمند پوشه‌های هم‌نام + دیاگنوستیک محتوای مسیر سایت در خطا |
| **2.18.0** | 3.13.0 | بدون تغییر اندپوینت — استقرار خودکار: 🐛 ریشه‌یابی نهایی خطای «استخراج ZIP ناموفق (function fileop not found in Fileman)» + ممیزی کامل همه فراخوانی‌های cPanel با مستندات رسمی و رفع ۸ خانواده باگ: fileop فقط در API2 (استخراج/حذف/فشرده‌سازی/جابجایی از UAPI صدا زده می‌شدند!) + پارامترهای صحیح save_file_content/get_file_content + chmod از API2 (UAPI ندارد) + ادغام files+dirs در list_files + SSL با توابع واقعی UAPI (list_ssl_items/start_autossl_check) + راستی‌آمایی index.php پس از استخراج |
| **2.17.0** | 3.13.0 | بدون تغییر اندپوینت — `error-codes-generate`: 🐛 رفع سه ریشه «هیچ کدی پیدا نشد» (باگ تاریخی httpGet از v2.2 + بلاک IP + کدهای سه‌رقمی تلویزیون webOS) + **پل jina** (جستجوی DDG و خواندن صفحات 403/JS از سرورهای r.jina.ai — برو‌رفتنی از هر هاست، بدون کلید) + رفع regex «too large» فیلدها. قالب‌ساز: ۱۳۰ عنصر (۱۶ جدید) + همه تنظیمات اثرگذار |
| **2.16.0** | 3.12.0 | بدون تغییر اندپوینت — استقرار خودکار: 🐛 ریشه‌یابی نهایی خطای «ساخت پوشه‌های حیاتی ناموفق (function mkdir not found in Fileman)» — تابع Fileman::mkdir در UAPI هرگز وجود نداشته (مستندات رسمی cPanel) → ساخت پوشه حالا با **API2 رسمی** (path + name + permissions) + ساخت بازگشتی تودرتو + راستی‌آزمایی وجود + writeFile دولایه (UAPI save_file_content → API2 savefile) + Deployer سه‌لایه (API2 → FTP → استخراج ZIP) |
| **2.15.0** | 3.12.0 | بدون اندپوینت جدید — `error-codes-generate`: **۱۱ ارائه‌دهنده جستجو** (۵ جدید: Google HTML / Yandex / Ecosia / Brave / SearXNG-JSON — ریشه‌یابی «هیچ کدی پیدا نشد»: ارائه‌دهنده‌های قبلی از بسیاری هاست‌ها بلاک بودند) + **راند نجات** با نام‌های مترادف دستگاه (مایکروویو → Microwave/OTR، تلویزیون → TV/LED/OLED) + ۱۲ کوئری کشف + سقف نرخ ۶۰ → ۲۴۰/ساعت + بودجه ۴۴s + متن صفحات ۱۴k. تصاویر مقاله: فیلد جدید `service` در پاسخ داخلی (سرویسی که واقعاً عکس ساخت) + `photo_source` + **۷ سرویس رایگان تولید تصویر** از تنظیمات پنل (Pollinations Flux/Turbo بدون کلید + HuggingFace/DeepAI/Together با کلید رایگان + Stability/OpenAI) با زنجیره «انتخابی همیشه اول» |
| **2.14.0** | 3.11.0 | بدون اندپوینت جدید — `error-codes-generate`: پشتیبانی کدهای خط‌تیره‌دار (E-01/F-13 — رفع «مایکروویو ال‌جی هیچ کدی پیدا نکرد») + استخراج ساختاریافته بخش‌های Possible Causes/Troubleshooting + ترجمه فارسی فیلدها (دیگر انگلیسی خام ذخیره نمی‌شود) + فیلد `photo_source` در تولید تصاویر مقاله (`ai_photo` = عکس واقعی Pollinations / `package` = بسته آماده) + تصویر شاخص هم عکس واقعی AI + اعداد فارسی در تصاویر OG |
| **2.13.0** | 3.10.0 | بدون اندپوینت جدید — `error-codes-generate`: فیلد `kb_added` + ثبت کدهای پایگاه دانش حتی با قطعی جستجوی وب + دقت curated-اول؛ تصاویر OG/مقاله: پس‌زمینه شفاف لوگوهای JPG + ضدکشیدگی + واترمارک بزرگ‌تر |
| **2.12.0** | 3.9.2 | 🌐 پارامترهای `domain_type` و `addon_domain` در `POST /api/deploy/{brandId}` (استقرار روی دامنه الحاقی — Addon Domain مستقل)؛ 🔑 ساخت کلید can_deploy از پنل (سیستم ← کلیدهای API) به‌جای SQL؛ 🔖 فیلد `favicon` در پاسخ `GET /api/brand/{brandId}` (پیش‌فرض = لوگوی برند)؛ 🖼 تصاویر OG بدون کادر با پس‌زمینه کنتراست‌دار (تحلیل روشنایی لوگو)؛ 🪧 واترمارک تصاویر مقاله بزرگ‌تر (برند ۲۲٪ / نمایندگی ۱۷٪)؛ 🐛 رفع پارامتر `use_web` خطایاب (مقدار on چک‌باکس پذیرفته می‌شود) |
| **2.11.0** | 3.9.1 | +**۱۳ اندپوینت استقرار خودکار**: deploy (start/update/status/logs/step/list) + health (brand/check/all) + backup (list/create/restore/delete) — با گارد مجوز `can_deploy` روی کلیدها؛ سایت‌ساز از بیرون قابل کنترل کامل: استقرار زیردامنه cPanel + بروزرسانی + بکاپ شمسی + مانیتورینگ سلامت |
| **2.7.1** | **3.7.1** | بدون اندپوینت جدید — پارامتر جدید `custom_title` در smart-generate (مقاله دقیقاً حول عنوان درخواستی: outline موضوع‌محور ۷بخشی + فکت‌های وب در نثر + پاسخ تحلیلی پرسش‌های واقعی + FAQ همان موضوع)؛ خطایاب دوفازی (جستجوی اختصاصی تک‌کد + خواندن متن کامل صفحه + رد کدهای بی‌شاهد + ادغام داده سازنده + استخراج specs/محل واقعی)؛ تصویر OG: رفع باگ نام→اسلاگ فونت + مبدل WOFF→TTF + @font-face جاسازی + رستر SVG لوگوها؛ 🐛 رفع کرش کامل ربات تلگرام (TypeError نتیجه true)؛ 🐛 رفع خرابی عرض ستون همه جدول‌ها (overflow-wrap:anywhere)؛ لاگین دوپنلی + کپچای بزرگ‌تر |
| **2.7.2** | **3.7.2** | بدون اندپوینت جدید — خطایاب v3.1 (بودجه زمانی ۲۴s برای ثبت واقعی کدها روی هاست اشتراکی + نقشه ۴۰+ کد شناخته‌شده IE/OE/UE + زمینه کد-محور + رفع نشت انگلیسی عنوان‌ها)؛ تصاویر مقاله مطابق موضوع (استنتاج دستگاه از عنوان + نگاشت ۴۲ دستگاه × ۲۵ نوع) + واترمارک دو لوگو شفاف؛ OG تضمینی (وزیرمتن بسته‌بندی + اعتبارسنجی پوشش فرم‌ها)؛ 🐛 رفع کرش تحویل مقاله ربات (CURLFile::getMime) |
| **2.7.0** | **3.7.0** | بدون اندپوینت جدید — ارتقای عمیق کیفیت: سئو صفحات برند موضوع‌محور (۱۰ پروفایل نوع صفحه)؛ خطایاب: جستجوی وب منبع اصلی + ۵ دلیل/۵ راه‌حل + استخراج از منابع واقعی؛ نگارش فارسی v1.2 (سبک + حشو + ۲۵ واژه تعمیرات)؛ بهبوددهنده محتوا HTML-امن (رفع نابودی ساختار)؛ تصاویر OG: فونت پیش‌فرض سایت + لوگوی برند + واترمارک نمایندگی؛ پیش‌نمایش مقاله؛ جستجوی داخل پک آیکون + ۲ پک جدید (آیکونویر ۱۶۷۱ / بوت‌استرپ ۲۰۵۰)؛ ربات بله: رفع خطای آپلود به api.telegram.org؛ رفع تداخل تنظیمات ربات/واسط گوگل؛ آپلود فایل از واسط گوگل (base64)؛ کپچای درون‌خطی لاگین (رفع ۴۰۴/ادبلاک)؛ رفع بهم‌ریختگی صفحه تنظیمات (ریشه: کوتیشن تودرتو)؛ بازطراحی حرفه‌ای لاگین (UI/UX Pro) |
| **2.6.0** | **3.6.0** | +۶ اندپوینت (خطایاب AI ×۲، تصویر OG، خودیادگیر ×۲، وب‌هوک بله)؛ کپچای ضد-ادبلاک (اندپوینت خنثی + fallback data-URI)؛ سایدبار همیشه راست؛ تب‌های مقاوم تنظیمات؛ رفع دستگاه‌های تکراری برند؛ رفع «بهینه است با وجود ۱ مورد» (اصلاح‌کننده تراکم کلیدواژه)؛ مرور آیکون‌ها با دسته‌بندی پوشه + صفحه‌بندی + لغو نصب پک؛ پیش‌نمایش فونت + لغو نصب فونت؛ SahandDialog (کادرهای تعاملی زیبا — جایگزین کامل alert/confirm)؛ سال شروع فعالیت نمایندگی؛ مهاجرت خودکار دیتابیس (og_image + ۸ ستون کد خطا + severity پنج‌سطحی)
| **2.5.0** | **3.5.0** | +۴ اندپوینت اسکیل UI/UX (`uiux-design`، `uiux-review`، `uiux-improve`، `uiux-skill-info`)؛ کپچای ورود با رندر ۳ لایه‌ای تضمینی + حروف ۲.۵ برابر درشت‌تر؛ بهینه PHP 8.3: فایل `.user.ini` (OPcache/JIT + حافظه ۲۵۶M) + `json_validate` بومی + فشرده‌سازی zlib + کارت «وضعیت سیستم» در داشبورد
| **2.4.1** | **3.4.1** | 🚑 هات‌فیکس: رفع داشبورد خالی (متد تکراری researchTopic) + رفع خطای خالی پاسخ پیشنهاد عنوان (باگ mb تریم) + رفع خطای HTML در بهبود سئو (mb_strtok ناموجود) + پلی‌فیل PHP 7.4 + هندلر استثنای سراسری AJAX-آگاه
| **2.3.0** | **3.3.0** | +۲ اندپوینت جدید (`seo-package` پکیج سئوی پیشرفته، `eeat-check` چک‌لیست E-E-A-T)؛ 🌐 جستجو: ۹ ارائه‌دهنده (+ویکی‌پدیا fa/en + Startpage) + مسابقه موازی curl_multi + تازگی و اعتماد دامنه؛ 📰 مقاله: TOC + زمان مطالعه + جعبه نکته/هشدار + مزایا-معایب + حداقل ۱۰۰۰ کلمه؛ 🤖 ربات v2.1: ۴ فرمان جدید (/seo /eeat /plan /faq) + زبان طبیعی؛ 🐛 رفع خطای مرحله ۱ تحلیل لوگو (SVG + باگ imagedestroy) + باگ صفحه ساخت برند + کش CSS |

> 📚 مستندات تکمیلی: [README اصلی ریپو](../README.md) | [راهنمای نصب](installation.html) | [راهنمای کاربری پنل](user-guide.html)
